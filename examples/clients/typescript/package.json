{
  "name": "maverick-mcp-ts-example",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "Runnable example + CI smoke: drive Lightwork over MCP from TypeScript.",
  "scripts": {
    "check": "npm run test:environment && npm run typecheck && tsx client.ts",
    "test:environment": "node --import tsx --test environment.test.ts",
    "typecheck": "tsc --noEmit --strict --target ES2022 --module NodeNext --moduleResolution NodeNext client.ts environment.ts environment.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "overrides": {
    "@hono/node-server": "2.0.12",
    "body-parser": "2.3.0",
    "fast-uri": "3.1.5"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
