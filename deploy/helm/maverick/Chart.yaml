apiVersion: v2
name: maverick
description: >-
  Maverick — governed, self-improving AI workforce. Runs the dashboard
  (web UI + API + webhooks) with persistent state. The control plane is
  single-replica; Postgres adds managed world-model HA and tenant isolation.
type: application
# Chart version is independent of the app; bump on template changes.
version: 0.1.0
# Tracks the maverick package cohort.
appVersion: "0.1.7"
home: https://github.com/Daybreak-AI-Labs/Law_Firm
sources:
  - https://github.com/Daybreak-AI-Labs/Law_Firm
maintainers:
  - name: Day AI Labs
keywords:
  - ai-agent
  - dashboard
  - enterprise
