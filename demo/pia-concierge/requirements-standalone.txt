# PIA Concierge — STANDALONE agent dependencies.
# This is the complete third-party surface of the standalone SKU: it imports
# nothing from the Lightwork platform (`maverick`). Pin-tested floors; upgrade
# deliberately, not automatically.
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
jinja2>=3.1
python-multipart>=0.0.32
cryptography>=42.0
h11>=0.16.0
idna>=3.18
