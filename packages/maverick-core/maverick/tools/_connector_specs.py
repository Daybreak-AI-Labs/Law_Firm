"""Connector spec data for :mod:`enterprise_connectors`.

Pure ``dict`` literals extracted verbatim from ``enterprise_connectors.py`` so the
logic module (env-filling, read-variant derivation, tool building, catalog) stays
readable. No imports, no logic; appending a connector is still a one-line edit
here. ``_fill_env`` in the parent module normalises env-var names at import time.
"""
from __future__ import annotations

# name, description, **auth-overrides (base_url_env/token_env default to
# <NAME>_BASE_URL / <NAME>_TOKEN -- spell them out only when they differ).
_SPECS: list[dict] = [
    # --- Support / CX ---
    dict(name="zendesk",
         description="Zendesk Support REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /api/v2/tickets.json, /api/v2/search.json?query=. "
         "Auth: ZENDESK_BASE_URL (https://{sub}.zendesk.com) + ZENDESK_TOKEN."),
    dict(name="freshdesk",
         basic=True, description="Freshdesk REST. ops get/post/put/delete (writes "
         "need confirm). e.g. /api/v2/tickets. Auth: FRESHDESK_BASE_URL "
         "(https://{sub}.freshdesk.com) + FRESHDESK_TOKEN (API key)."),
    dict(name="freshservice", basic=True,
         description="Freshservice ITSM REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /api/v2/tickets. Auth: FRESHSERVICE_BASE_URL + "
         "FRESHSERVICE_TOKEN (API key)."),
    dict(name="intercom",
         description="Intercom REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /contacts, /conversations. Auth: INTERCOM_BASE_URL "
         "(https://api.intercom.io) + INTERCOM_TOKEN."),
    # --- Identity ---
    dict(name="okta",
         scheme="SSWS", description="Okta management REST. ops get/post/put/delete "
         "(writes need confirm). e.g. /api/v1/users, /api/v1/groups. Auth: "
         "OKTA_BASE_URL (https://{org}.okta.com) + OKTA_TOKEN (SSWS API token)."),
    # --- Content / storage / e-sign ---
    dict(name="box",
         description="Box content REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /folders/0/items, /search?query=. Auth: BOX_BASE_URL "
         "(https://api.box.com/2.0) + BOX_TOKEN."),
    dict(name="docusign",
         description="DocuSign eSignature REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /restapi/v2.1/accounts/{acct}/envelopes. Auth: "
         "DOCUSIGN_BASE_URL + DOCUSIGN_TOKEN."),
    # --- Procurement / spend ---
    dict(name="coupa",
         description="Coupa spend/procurement REST. ops get/post/put/delete (writes "
         "need confirm). e.g. /api/requisitions, /api/purchase_orders. Auth: "
         "COUPA_BASE_URL + COUPA_TOKEN."),
    dict(name="ariba",
         description="SAP Ariba REST. ops get/post/put/delete (writes need confirm). "
         "Auth: ARIBA_BASE_URL + ARIBA_TOKEN."),
    # --- Work management ---
    dict(name="smartsheet",
         description="Smartsheet REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /sheets, /sheets/{id}/rows. Auth: SMARTSHEET_BASE_URL "
         "(https://api.smartsheet.com/2.0) + SMARTSHEET_TOKEN."),
    dict(name="wrike",
         description="Wrike work-management REST. ops get/post/put/delete (writes "
         "need confirm). e.g. /tasks, /folders. Auth: WRIKE_BASE_URL "
         "(https://www.wrike.com/api/v4) + WRIKE_TOKEN."),
    # --- HR / recruiting ---
    dict(name="bamboohr",
         basic=True, description="BambooHR REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /v1/employees/directory. Auth: BAMBOOHR_BASE_URL "
         "(https://api.bamboohr.com/api/gateway.php/{company}) + BAMBOOHR_TOKEN (API key)."),
    dict(name="greenhouse",
         basic=True, description="Greenhouse Harvest REST. ops get/post/put/delete "
         "(writes need confirm). e.g. /v1/candidates, /v1/jobs. Auth: "
         "GREENHOUSE_BASE_URL (https://harvest.greenhouse.io) + GREENHOUSE_TOKEN (API key)."),
    dict(name="lever",
         basic=True, description="Lever recruiting REST. ops get/post/put/delete "
         "(writes need confirm). e.g. /v1/opportunities, /v1/postings. Auth: "
         "LEVER_BASE_URL (https://api.lever.co) + LEVER_TOKEN (API key)."),
    # --- BI / analytics ---
    dict(name="tableau",
         token_header="X-Tableau-Auth", scheme="",
         description="Tableau Server/Cloud REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /api/3.21/sites/{id}/workbooks. Auth: TABLEAU_BASE_URL + "
         "TABLEAU_TOKEN (signed-in X-Tableau-Auth token)."),
    dict(name="powerbi",
         description="Microsoft Power BI REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /groups, /datasets, /reports. Auth: POWERBI_BASE_URL "
         "(https://api.powerbi.com/v1.0/myorg) + POWERBI_TOKEN."),
    dict(name="looker",
         scheme="token", description="Looker REST. ops get/post/put/delete (writes "
         "need confirm). e.g. /api/4.0/looks, /api/4.0/dashboards. Auth: "
         "LOOKER_BASE_URL + LOOKER_TOKEN (Authorization: token <access_token>)."),
    # --- Observability ---
    dict(name="newrelic",
         token_header="Api-Key", scheme="",
         description="New Relic REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v2/applications.json. Auth: NEWRELIC_BASE_URL "
         "(https://api.newrelic.com) + NEWRELIC_TOKEN (Api-Key header)."),
    dict(name="dynatrace",
         scheme="Api-Token", description="Dynatrace REST. ops get/post/put/delete "
         "(writes need confirm). e.g. /api/v2/problems, /api/v2/metrics/query. Auth: "
         "DYNATRACE_BASE_URL + DYNATRACE_TOKEN (Authorization: Api-Token <tok>)."),
    dict(name="grafana",
         description="Grafana REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /api/dashboards/uid/{uid}, /api/datasources. Auth: GRAFANA_BASE_URL "
         "+ GRAFANA_TOKEN (service-account token)."),

    # --- CRM / sales engagement ---
    dict(name="zoho",
         scheme="Zoho-oauthtoken", description="Zoho CRM REST. ops "
         "get/post/put/patch/delete (writes need confirm). e.g. /crm/v5/Leads. "
         "Auth: ZOHO_BASE_URL + ZOHO_TOKEN (Zoho-oauthtoken)."),
    dict(name="pipedrive",
         description="Pipedrive REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v1/deals, /v1/persons. Auth: PIPEDRIVE_BASE_URL + PIPEDRIVE_TOKEN."),
    dict(name="salesloft",
         description="Salesloft REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v2/people, /v2/cadences. Auth: SALESLOFT_BASE_URL + SALESLOFT_TOKEN."),
    dict(name="outreach",
         description="Outreach REST. ops get/post/patch/delete (writes need confirm). "
         "e.g. /api/v2/prospects, /api/v2/sequences. Auth: OUTREACH_BASE_URL + OUTREACH_TOKEN."),
    dict(name="gong", basic=True,
         description="Gong revenue-intelligence REST. ops get/post (writes need "
         "confirm). e.g. /v2/calls, /v2/users. Auth: GONG_BASE_URL + GONG_TOKEN "
         "(accessKey:secret, basic)."),
    dict(name="clari",
         token_header="apikey", scheme="", description="Clari REST. ops get/post "
         "(writes need confirm). Auth: CLARI_BASE_URL + CLARI_TOKEN (apikey header)."),
    dict(name="creatio",
         description="Creatio (bpm'online) OData REST. ops get/post/patch/delete "
         "(writes need confirm). Auth: CREATIO_BASE_URL + CREATIO_TOKEN."),
    dict(name="pega",
         description="Pega REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /prweb/api/v1/cases. Auth: PEGA_BASE_URL + PEGA_TOKEN."),

    # --- ERP / finance ---
    dict(name="sage_intacct", description="Sage Intacct REST. ops "
         "get/post/put/delete (writes need confirm). Auth: SAGE_INTACCT_BASE_URL + "
         "SAGE_INTACCT_TOKEN."),
    dict(name="epicor",
         basic=True, description="Epicor ERP REST. ops get/post/patch/delete (writes "
         "need confirm). Auth: EPICOR_BASE_URL + EPICOR_TOKEN (user:pass, basic)."),
    dict(name="ifs",
         description="IFS Cloud OData REST. ops get/post/patch/delete (writes need "
         "confirm). Auth: IFS_BASE_URL + IFS_TOKEN."),
    dict(name="unit4",
         description="Unit4 ERP REST. ops get/post/put/delete (writes need confirm). "
         "Auth: UNIT4_BASE_URL + UNIT4_TOKEN."),
    dict(name="acumatica",
         description="Acumatica REST. ops get/put/delete (writes need confirm). "
         "e.g. /entity/Default/.../SalesOrder. Auth: ACUMATICA_BASE_URL + ACUMATICA_TOKEN."),
    dict(name="quickbooks",
         description="QuickBooks Online REST. ops get/post (writes need confirm). "
         "e.g. /v3/company/{id}/query. Auth: QUICKBOOKS_BASE_URL + QUICKBOOKS_TOKEN."),
    dict(name="xero",
         description="Xero accounting REST. ops get/post/put (writes need confirm). "
         "e.g. /api.xro/2.0/Invoices. Auth: XERO_BASE_URL + XERO_TOKEN."),
    dict(name="billdotcom",
         description="Bill.com REST. ops get/post (writes need confirm). Auth: "
         "BILLDOTCOM_BASE_URL + BILLDOTCOM_TOKEN."),

    # --- Support / CX / contact center ---
    dict(name="genesys",
         description="Genesys Cloud REST. ops get/post/put/patch/delete (writes need "
         "confirm). e.g. /api/v2/conversations. Auth: GENESYS_BASE_URL + GENESYS_TOKEN."),
    dict(name="nice_cxone",
         description="NICE CXone REST. ops get/post/put/delete (writes need confirm). "
         "Auth: NICE_CXONE_BASE_URL + NICE_CXONE_TOKEN."),
    dict(name="five9", basic=True,
         description="Five9 REST. ops get/post/put/delete (writes need confirm). "
         "Auth: FIVE9_BASE_URL + FIVE9_TOKEN (user:pass, basic)."),
    dict(name="talkdesk",
         description="Talkdesk REST. ops get/post/put/delete (writes need confirm). "
         "Auth: TALKDESK_BASE_URL + TALKDESK_TOKEN."),
    dict(name="helpscout",
         description="Help Scout REST. ops get/post/put/patch/delete (writes need "
         "confirm). e.g. /v2/conversations. Auth: HELPSCOUT_BASE_URL + HELPSCOUT_TOKEN."),
    dict(name="kustomer",
         description="Kustomer REST. ops get/post/put/delete (writes need confirm). "
         "Auth: KUSTOMER_BASE_URL + KUSTOMER_TOKEN."),
    dict(name="sprinklr",
         description="Sprinklr REST. ops get/post/put/delete (writes need confirm). "
         "Auth: SPRINKLR_BASE_URL + SPRINKLR_TOKEN."),

    # --- ITSM / alerting ---
    dict(name="bmc_helix",
         scheme="AR-JWT", description="BMC Helix / Remedy REST. ops get/post/put/delete "
         "(writes need confirm). Auth: BMC_HELIX_BASE_URL + BMC_HELIX_TOKEN (AR-JWT)."),
    dict(name="ivanti",
         description="Ivanti Neurons / ITSM REST. ops get/post/put/delete (writes need "
         "confirm). Auth: IVANTI_BASE_URL + IVANTI_TOKEN."),
    dict(name="solarwinds",
         description="SolarWinds Service Desk REST. ops get/post/put/delete (writes "
         "need confirm). Auth: SOLARWINDS_BASE_URL + SOLARWINDS_TOKEN."),
    dict(name="manageengine", scheme="Zoho-oauthtoken",
         description="ManageEngine ServiceDesk Plus REST. ops get/post/put/delete "
         "(writes need confirm). Auth: MANAGEENGINE_BASE_URL + MANAGEENGINE_TOKEN."),
    dict(name="xmatters",
         description="xMatters REST. ops get/post/delete (writes need confirm). "
         "e.g. /api/xm/1/events. Auth: XMATTERS_BASE_URL + XMATTERS_TOKEN."),

    # --- HR ---
    dict(name="rippling",
         description="Rippling REST. ops get/post (writes need confirm). e.g. "
         "/platform/api/employees. Auth: RIPPLING_BASE_URL + RIPPLING_TOKEN."),
    dict(name="gusto",
         description="Gusto REST. ops get/post/put (writes need confirm). e.g. "
         "/v1/companies/{id}/employees. Auth: GUSTO_BASE_URL + GUSTO_TOKEN."),

    # --- Cloud / infra ---
    dict(name="fastly",
         token_header="Fastly-Key", scheme="", description="Fastly REST. ops "
         "get/post/put/delete (writes need confirm). Auth: FASTLY_BASE_URL + "
         "FASTLY_TOKEN (Fastly-Key)."),
    dict(name="akamai",
         description="Akamai REST. ops get/post/put/delete (writes need confirm). "
         "Auth: AKAMAI_BASE_URL + AKAMAI_TOKEN."),
    dict(name="digitalocean", description="DigitalOcean REST. ops "
         "get/post/put/delete (writes need confirm). e.g. /v2/droplets. Auth: "
         "DIGITALOCEAN_BASE_URL (https://api.digitalocean.com) + DIGITALOCEAN_TOKEN."),
    dict(name="terraform",
         description="Terraform Cloud/Enterprise REST. ops get/post/patch/delete "
         "(writes need confirm). e.g. /api/v2/organizations/{org}/workspaces. Auth: "
         "TERRAFORM_BASE_URL + TERRAFORM_TOKEN."),
    dict(name="vault",
         token_header="X-Vault-Token", scheme="", description="HashiCorp Vault REST. "
         "ops get/post/delete (writes need confirm). e.g. /v1/secret/data/{path}. "
         "Auth: VAULT_BASE_URL + VAULT_TOKEN (X-Vault-Token)."),

    # --- Identity / IAM ---
    dict(name="pingone",
         description="Ping Identity (PingOne) REST. ops get/post/put/delete (writes "
         "need confirm). Auth: PINGONE_BASE_URL + PINGONE_TOKEN."),
    dict(name="cyberark",
         description="CyberArk REST. ops get/post/put/delete (writes need confirm). "
         "Auth: CYBERARK_BASE_URL + CYBERARK_TOKEN."),
    dict(name="sailpoint",
         description="SailPoint IdentityNow REST. ops get/post/patch/delete (writes "
         "need confirm). e.g. /v3/accounts. Auth: SAILPOINT_BASE_URL + SAILPOINT_TOKEN."),
    dict(name="onelogin",
         description="OneLogin REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /api/2/users. Auth: ONELOGIN_BASE_URL + ONELOGIN_TOKEN."),
    dict(name="auth0",
         description="Auth0 Management REST. ops get/post/patch/delete (writes need "
         "confirm). e.g. /api/v2/users. Auth: AUTH0_BASE_URL + AUTH0_TOKEN."),
    dict(name="duo", basic=True,
         description="Cisco Duo Admin REST. ops get/post (writes need confirm). "
         "Auth: DUO_BASE_URL + DUO_TOKEN (ikey:skey, basic)."),

    # --- Security / SIEM / endpoint ---
    dict(name="crowdstrike", description="CrowdStrike Falcon REST. ops "
         "get/post/patch/delete (writes need confirm). e.g. /detects/queries/detects/v1. "
         "Auth: CROWDSTRIKE_BASE_URL + CROWDSTRIKE_TOKEN."),
    dict(name="splunk",
         description="Splunk REST. ops get/post/delete (writes need confirm). e.g. "
         "/services/search/jobs. Auth: SPLUNK_BASE_URL + SPLUNK_TOKEN."),
    dict(name="zscaler",
         description="Zscaler REST. ops get/post/put/delete (writes need confirm). "
         "Auth: ZSCALER_BASE_URL + ZSCALER_TOKEN."),
    dict(name="tenable",
         token_header="X-ApiKeys", scheme="", description="Tenable.io REST. ops "
         "get/post/put/delete (writes need confirm). Auth: TENABLE_BASE_URL + "
         "TENABLE_TOKEN ('accessKey=...;secretKey=...')."),
    dict(name="qualys",
         basic=True, description="Qualys REST. ops get/post (writes need confirm). "
         "Auth: QUALYS_BASE_URL + QUALYS_TOKEN (user:pass, basic)."),
    dict(name="rapid7",
         token_header="X-Api-Key", scheme="", description="Rapid7 InsightVM/IDR REST. "
         "ops get/post/put/delete (writes need confirm). Auth: RAPID7_BASE_URL + "
         "RAPID7_TOKEN (X-Api-Key)."),
    dict(name="sentinelone", scheme="ApiToken", description="SentinelOne "
         "REST. ops get/post/delete (writes need confirm). e.g. /web/api/v2.1/agents. "
         "Auth: SENTINELONE_BASE_URL + SENTINELONE_TOKEN (ApiToken)."),
    dict(name="proofpoint", basic=True, description="Proofpoint REST. ops "
         "get/post (writes need confirm). Auth: PROOFPOINT_BASE_URL + PROOFPOINT_TOKEN "
         "(principal:secret, basic)."),
    dict(name="snyk",
         scheme="token", description="Snyk REST. ops get/post/delete (writes need "
         "confirm). e.g. /rest/orgs. Auth: SNYK_BASE_URL + SNYK_TOKEN (token <key>)."),
    dict(name="fortinet",
         description="Fortinet FortiGate/FortiManager REST. ops get/post/put/delete "
         "(writes need confirm). Auth: FORTINET_BASE_URL + FORTINET_TOKEN."),

    # --- BI / analytics ---
    dict(name="qlik",
         description="Qlik Cloud REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /api/v1/apps. Auth: QLIK_BASE_URL + QLIK_TOKEN."),
    dict(name="thoughtspot", description="ThoughtSpot REST. ops get/post "
         "(writes need confirm). e.g. /api/rest/2.0/metadata/search. Auth: "
         "THOUGHTSPOT_BASE_URL + THOUGHTSPOT_TOKEN."),
    dict(name="sisense",
         description="Sisense REST. ops get/post/put/delete (writes need confirm). "
         "Auth: SISENSE_BASE_URL + SISENSE_TOKEN."),
    dict(name="domo",
         description="Domo REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v1/datasets. Auth: DOMO_BASE_URL + DOMO_TOKEN."),
    dict(name="mode", basic=True,
         description="Mode Analytics REST. ops get/post (writes need confirm). Auth: "
         "MODE_BASE_URL + MODE_TOKEN (token:secret, basic)."),
    dict(name="metabase",
         token_header="X-API-Key", scheme="", description="Metabase REST. ops "
         "get/post/put/delete (writes need confirm). e.g. /api/card. Auth: "
         "METABASE_BASE_URL + METABASE_TOKEN (X-API-Key)."),

    # --- DevOps / CI ---
    dict(name="jenkins",
         basic=True, description="Jenkins REST. ops get/post (writes need confirm). "
         "e.g. /api/json, /job/{name}/build. Auth: JENKINS_BASE_URL + JENKINS_TOKEN "
         "(user:apitoken, basic)."),
    dict(name="circleci",
         token_header="Circle-Token", scheme="", description="CircleCI REST. ops "
         "get/post (writes need confirm). e.g. /api/v2/project/{slug}/pipeline. Auth: "
         "CIRCLECI_BASE_URL + CIRCLECI_TOKEN (Circle-Token)."),
    dict(name="jfrog",
         description="JFrog Artifactory REST. ops get/post/put/delete (writes need "
         "confirm). e.g. /artifactory/api/repositories. Auth: JFROG_BASE_URL + JFROG_TOKEN."),
    dict(name="sonarqube",
         basic=True, description="SonarQube REST. ops get/post (writes need confirm). "
         "e.g. /api/issues/search, /api/projects/search. Auth: SONARQUBE_BASE_URL + "
         "SONARQUBE_TOKEN (token:, basic)."),
    dict(name="azure_devops", basic=True, description="Azure DevOps REST. "
         "ops get/post/patch/put (writes need confirm). e.g. /{org}/{proj}/_apis/wit/"
         "workitems. Auth: AZURE_DEVOPS_BASE_URL + AZURE_DEVOPS_TOKEN (:PAT, basic)."),

    # --- Marketing / commerce ---
    dict(name="mailchimp",
         basic=True, description="Mailchimp Marketing REST. ops get/post/put/patch/"
         "delete (writes need confirm). e.g. /3.0/lists. Auth: MAILCHIMP_BASE_URL + "
         "MAILCHIMP_TOKEN (anystring:apikey, basic)."),
    dict(name="klaviyo",
         scheme="Klaviyo-API-Key", description="Klaviyo REST. ops get/post/patch/delete "
         "(writes need confirm). Auth: KLAVIYO_BASE_URL + KLAVIYO_TOKEN (Klaviyo-API-Key)."),
    dict(name="braze",
         description="Braze REST. ops get/post (writes need confirm). e.g. "
         "/users/export/ids, /messages/send. Auth: BRAZE_BASE_URL + BRAZE_TOKEN."),
    dict(name="marketo",
         description="Adobe Marketo Engage REST. ops get/post (writes need confirm). "
         "e.g. /rest/v1/leads.json. Auth: MARKETO_BASE_URL + MARKETO_TOKEN."),
    dict(name="sfmc",
         description="Salesforce Marketing Cloud REST. ops get/post/put/delete (writes "
         "need confirm). Auth: SFMC_BASE_URL + SFMC_TOKEN."),
    dict(name="segment",
         description="Twilio Segment Public API. ops get/post/patch/delete (writes "
         "need confirm). e.g. /workspaces, /sources. Auth: SEGMENT_BASE_URL + SEGMENT_TOKEN."),
    dict(name="adobe_analytics", description="Adobe Analytics 2.0 REST. ops "
         "get/post (writes need confirm). e.g. /reports. Auth: ADOBE_ANALYTICS_BASE_URL "
         "+ ADOBE_ANALYTICS_TOKEN."),
    dict(name="aem", basic=True,
         description="Adobe Experience Manager REST. ops get/post/put/delete (writes "
         "need confirm). Auth: AEM_BASE_URL + AEM_TOKEN (user:pass, basic)."),
    dict(name="bigcommerce", token_header="X-Auth-Token", scheme="",
         description="BigCommerce REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v3/catalog/products. Auth: BIGCOMMERCE_BASE_URL + BIGCOMMERCE_TOKEN "
         "(X-Auth-Token)."),
    dict(name="sendgrid",
         description="Twilio SendGrid REST. ops get/post/put/patch/delete (writes need "
         "confirm). e.g. /v3/mail/send, /v3/marketing/contacts. Auth: SENDGRID_BASE_URL "
         "(https://api.sendgrid.com) + SENDGRID_TOKEN."),

    # --- Data / ETL / streaming ---
    dict(name="fivetran",
         basic=True, description="Fivetran REST. ops get/post/patch/delete (writes need "
         "confirm). e.g. /v1/connectors. Auth: FIVETRAN_BASE_URL + FIVETRAN_TOKEN "
         "(apikey:apisecret, basic)."),
    dict(name="dbt", scheme="Token",
         description="dbt Cloud REST. ops get/post (writes need confirm). e.g. "
         "/api/v2/accounts/{id}/jobs. Auth: DBT_BASE_URL + DBT_TOKEN (Token <key>)."),
    dict(name="airflow",
         basic=True, description="Apache Airflow REST. ops get/post/patch/delete "
         "(writes need confirm). e.g. /api/v1/dags. Auth: AIRFLOW_BASE_URL + "
         "AIRFLOW_TOKEN (user:pass, basic)."),
    dict(name="confluent",
         basic=True, description="Confluent Cloud REST. ops get/post/patch/delete "
         "(writes need confirm). e.g. /kafka/v3/clusters. Auth: CONFLUENT_BASE_URL + "
         "CONFLUENT_TOKEN (key:secret, basic)."),
    dict(name="informatica", description="Informatica IICS REST. ops "
         "get/post (writes need confirm). Auth: INFORMATICA_BASE_URL + INFORMATICA_TOKEN."),
    dict(name="talend",
         description="Talend Cloud REST. ops get/post/put/delete (writes need confirm). "
         "Auth: TALEND_BASE_URL + TALEND_TOKEN."),
    dict(name="matillion",
         basic=True, description="Matillion REST. ops get/post (writes need confirm). "
         "Auth: MATILLION_BASE_URL + MATILLION_TOKEN (user:pass, basic)."),
    dict(name="cloudera",
         basic=True, description="Cloudera Manager REST. ops get/post/put/delete "
         "(writes need confirm). Auth: CLOUDERA_BASE_URL + CLOUDERA_TOKEN (user:pass, basic)."),

    # --- Collaboration / PM ---
    dict(name="miro",
         description="Miro REST. ops get/post/patch/delete (writes need confirm). "
         "e.g. /v2/boards. Auth: MIRO_BASE_URL (https://api.miro.com) + MIRO_TOKEN."),
    dict(name="coda",
         description="Coda REST. ops get/post/put/delete (writes need confirm). e.g. "
         "/v1/docs. Auth: CODA_BASE_URL (https://coda.io/apis/v1) + CODA_TOKEN."),
    dict(name="basecamp",
         description="Basecamp REST. ops get/post/put (writes need confirm). Auth: "
         "BASECAMP_BASE_URL + BASECAMP_TOKEN."),
    dict(name="planview",
         description="Planview REST. ops get/post/put/delete (writes need confirm). "
         "Auth: PLANVIEW_BASE_URL + PLANVIEW_TOKEN."),

    # --- ERP / HR (additional) ---
    dict(name="infor",
         description="Infor CloudSuite (ION) REST. ops get/post/put/delete (writes "
         "need confirm). Auth: INFOR_BASE_URL + INFOR_TOKEN."),
    dict(name="netsuite",
         description="Oracle NetSuite SuiteTalk REST. ops get/post/patch/delete (writes "
         "need confirm). e.g. /services/rest/record/v1/salesOrder. Auth: "
         "NETSUITE_BASE_URL + NETSUITE_TOKEN."),
    dict(name="adp",
         description="ADP Workforce Now REST. ops get/post (writes need confirm). e.g. "
         "/hr/v2/workers. Auth: ADP_BASE_URL + ADP_TOKEN."),
    dict(name="ukg",
         description="UKG (Ultimate Kronos) REST. ops get/post/put/delete (writes need "
         "confirm). Auth: UKG_BASE_URL + UKG_TOKEN."),

    # --- iPaaS / integration ---
    dict(name="mulesoft",
         description="MuleSoft Anypoint REST. ops get/post/put/delete (writes need "
         "confirm). Auth: MULESOFT_BASE_URL + MULESOFT_TOKEN."),
    dict(name="boomi", basic=True,
         description="Boomi AtomSphere REST. ops get/post (writes need confirm). Auth: "
         "BOOMI_BASE_URL + BOOMI_TOKEN (user:token, basic)."),
    dict(name="workato",
         description="Workato REST. ops get/post/put/delete (writes need confirm). "
         "Auth: WORKATO_BASE_URL + WORKATO_TOKEN."),
    dict(name="zapier",
         description="Zapier NLA / REST. ops get/post (writes need confirm). Auth: "
         "ZAPIER_BASE_URL + ZAPIER_TOKEN."),
    dict(name="n8n", token_header="X-N8N-API-KEY", scheme="",
         description="n8n workflow-automation REST. ops get/post/put/delete (writes "
         "need confirm). e.g. GET /api/v1/workflows, POST /api/v1/workflows/{id}/"
         "activate, POST /webhook/{path} to fire a workflow. Auth: N8N_BASE_URL "
         "(your n8n instance, e.g. https://{host}/api/v1) + N8N_TOKEN (X-N8N-API-KEY)."),
    dict(name="power_automate",
         description="Microsoft Power Automate management REST. ops get/post/patch/"
         "delete (writes need confirm). e.g. GET /providers/Microsoft.ProcessSimple/"
         "environments/{env}/flows, POST {env}/flows/{flow}/triggers/manual/run to "
         "fire a flow. Auth: POWER_AUTOMATE_BASE_URL (https://api.flow.microsoft.com) "
         "+ POWER_AUTOMATE_TOKEN (Entra ID OAuth bearer)."),
    dict(name="make",
         description="Make (Integromat) REST. ops get/post (writes need confirm). e.g. "
         "GET /api/v2/scenarios, POST /api/v2/scenarios/{id}/run. Auth: MAKE_BASE_URL "
         "(https://{zone}.make.com) + MAKE_TOKEN."),

    # --- Cloud platforms / orchestration (control-plane REST) ---
    dict(name="kubernetes",
         description="Kubernetes API server REST. ops get/post/patch/delete (writes "
         "need confirm). e.g. /api/v1/namespaces/{ns}/pods. Auth: KUBERNETES_BASE_URL "
         "+ KUBERNETES_TOKEN (service-account bearer)."),
    dict(name="openshift",
         description="Red Hat OpenShift API REST. ops get/post/patch/delete (writes "
         "need confirm). Auth: OPENSHIFT_BASE_URL + OPENSHIFT_TOKEN (bearer)."),
    dict(name="vsphere",
         token_header="vmware-api-session-id", scheme="", description="VMware vSphere "
         "REST. ops get/post/patch/delete (writes need confirm). e.g. /api/vcenter/vm. "
         "Auth: VSPHERE_BASE_URL + VSPHERE_TOKEN (session id)."),
    dict(name="azure",
         description="Microsoft Azure Resource Manager REST. ops get/put/post/patch/"
         "delete (writes need confirm). e.g. /subscriptions/{id}/resourcegroups. Auth: "
         "AZURE_BASE_URL (https://management.azure.com) + AZURE_TOKEN."),
    dict(name="gcp",
         description="Google Cloud REST (Compute/Resource Manager/...). ops get/post/"
         "patch/delete (writes need confirm). Auth: GCP_BASE_URL + GCP_TOKEN (OAuth "
         "bearer, e.g. gcloud auth print-access-token)."),
    dict(name="ibm_cloud",
         description="IBM Cloud REST. ops get/post/put/delete (writes need confirm). "
         "Auth: IBM_CLOUD_BASE_URL + IBM_CLOUD_TOKEN (IAM bearer)."),
    dict(name="alibaba_cloud", description="Alibaba Cloud REST. ops get/post "
         "(writes need confirm). Auth: ALIBABA_CLOUD_BASE_URL + ALIBABA_CLOUD_TOKEN."),

    # --- Security (cloud-native / SIEM) ---
    dict(name="sentinel",
         description="Microsoft Sentinel REST (Azure). ops get/put/post/delete (writes "
         "need confirm). e.g. .../providers/Microsoft.SecurityInsights/incidents. Auth: "
         "SENTINEL_BASE_URL + SENTINEL_TOKEN."),
    dict(name="defender",
         description="Microsoft Defender REST. ops get/post (writes need confirm). e.g. "
         "/api/alerts, /api/machines. Auth: DEFENDER_BASE_URL + DEFENDER_TOKEN."),
    dict(name="qradar",
         token_header="SEC", scheme="", description="IBM QRadar REST. ops get/post "
         "(writes need confirm). e.g. /api/siem/offenses. Auth: QRADAR_BASE_URL + "
         "QRADAR_TOKEN (SEC header)."),
    dict(name="palo_alto",
         description="Palo Alto Networks (Cortex/Prisma) REST. ops get/post (writes "
         "need confirm). Auth: PALO_ALTO_BASE_URL + PALO_ALTO_TOKEN."),
    dict(name="jamf",
         description="Jamf Pro REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /api/v1/computers-inventory. Auth: JAMF_BASE_URL + JAMF_TOKEN."),

    # --- Comms ---
    dict(name="ringcentral", description="RingCentral REST. ops get/post/"
         "put/delete (writes need confirm). e.g. /restapi/v1.0/account/~/extension. "
         "Auth: RINGCENTRAL_BASE_URL + RINGCENTRAL_TOKEN."),
    dict(name="vonage",
         description="Vonage REST. ops get/post (writes need confirm). Auth: "
         "VONAGE_BASE_URL + VONAGE_TOKEN."),
    dict(name="webex",
         description="Cisco Webex REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /v1/messages, /v1/rooms. Auth: WEBEX_BASE_URL (https://webexapis.com) + "
         "WEBEX_TOKEN."),
    dict(name="slack_api",
         description="Slack Web API. ops get/post (writes need confirm). e.g. POST "
         "/api/chat.postMessage, GET /api/conversations.list, GET /api/users.list. "
         "Auth: SLACK_API_BASE_URL (https://slack.com) + SLACK_API_TOKEN (bot/user "
         "OAuth bearer, xoxb-.../xoxp-...)."),
    dict(name="google_calendar",
         description="Google Calendar REST v3. ops get/post/put/delete (writes need "
         "confirm). e.g. /calendars/primary/events, /calendars/primary/events/quickAdd. "
         "Auth: GOOGLE_CALENDAR_BASE_URL (https://www.googleapis.com/calendar/v3) + "
         "GOOGLE_CALENDAR_TOKEN (OAuth2 bearer, refreshed externally)."),

    # --- Databases / BI over REST ---
    dict(name="neo4j", basic=True,
         description="Neo4j HTTP API (Cypher). op post (Cypher via /db/neo4j/tx/commit; "
         "needs confirm). Auth: NEO4J_BASE_URL + NEO4J_TOKEN (user:pass, basic)."),
    dict(name="teradata",
         basic=True, description="Teradata REST (Vantage). ops get/post (writes need "
         "confirm). Auth: TERADATA_BASE_URL + TERADATA_TOKEN (user:pass, basic)."),
    dict(name="microstrategy", token_header="X-MSTR-AuthToken", scheme="",
         description="MicroStrategy REST. ops get/post/put/delete (writes need confirm). "
         "Auth: MICROSTRATEGY_BASE_URL + MICROSTRATEGY_TOKEN (X-MSTR-AuthToken)."),
    dict(name="cognos",
         description="IBM Cognos Analytics REST. ops get/post (writes need confirm). "
         "Auth: COGNOS_BASE_URL + COGNOS_TOKEN."),

    # --- Finance / ERP / HR / CS (additional) ---
    dict(name="concur",
         description="SAP Concur REST. ops get/post/put/delete (writes need confirm). "
         "e.g. /expensereports/v4/reports. Auth: CONCUR_BASE_URL + CONCUR_TOKEN."),
    dict(name="anaplan",
         description="Anaplan REST. ops get/post/put (writes need confirm). Auth: "
         "ANAPLAN_BASE_URL + ANAPLAN_TOKEN."),
    dict(name="smartrecruiters", token_header="X-SmartToken", scheme="",
         description="SmartRecruiters REST. ops get/post/put/delete (writes need "
         "confirm). Auth: SMARTRECRUITERS_BASE_URL + SMARTRECRUITERS_TOKEN (X-SmartToken)."),
    dict(name="gainsight",
         token_header="accesskey", scheme="", description="Gainsight REST. ops get/post "
         "(writes need confirm). Auth: GAINSIGHT_BASE_URL + GAINSIGHT_TOKEN (accesskey)."),
    dict(name="amplitude",
         basic=True, description="Amplitude Analytics REST. ops get/post (writes need "
         "confirm). Auth: AMPLITUDE_BASE_URL + AMPLITUDE_TOKEN (apikey:secret, basic)."),

    # --- Payments / spend / financial close ---
    dict(name="square",
         description="Square REST. ops get/post/put/delete (writes need confirm). Auth: "
         "SQUARE_BASE_URL (https://connect.squareup.com) + SQUARE_TOKEN (bearer)."),
    dict(name="paypal",
         description="PayPal REST. ops get/post/put/delete (writes need confirm). Auth: "
         "PAYPAL_BASE_URL (https://api-m.paypal.com) + PAYPAL_TOKEN (OAuth bearer)."),
    dict(name="adyen",
         token_header="X-API-Key", scheme="", description="Adyen REST. ops get/post "
         "(writes need confirm). Auth: ADYEN_BASE_URL + ADYEN_TOKEN (X-API-Key)."),
    dict(name="ramp",
         description="Ramp REST (spend). ops get/post/put/delete (writes need confirm). "
         "Auth: RAMP_BASE_URL (https://api.ramp.com) + RAMP_TOKEN (bearer)."),
    dict(name="brex",
         description="Brex REST (spend). ops get/post/put/delete (writes need confirm). "
         "Auth: BREX_BASE_URL (https://platform.brexapis.com) + BREX_TOKEN (bearer)."),
    dict(name="blackline",
         description="BlackLine REST (financial close). ops get/post (writes need "
         "confirm). Auth: BLACKLINE_BASE_URL + BLACKLINE_TOKEN (bearer)."),
    dict(name="workiva",
         description="Workiva (Wdesk) REST. ops get/post/put/delete (writes need "
         "confirm). Auth: WORKIVA_BASE_URL + WORKIVA_TOKEN (bearer)."),

    # --- HR / HCM / payroll / recruiting (additional) ---
    dict(name="successfactors", basic=True, description="SAP SuccessFactors "
         "OData REST. ops get/post (writes need confirm). Auth: SUCCESSFACTORS_BASE_URL + "
         "SUCCESSFACTORS_TOKEN (user@company:pass, basic)."),
    dict(name="cornerstone", description="Cornerstone OnDemand REST. ops "
         "get/post (writes need confirm). Auth: CORNERSTONE_BASE_URL + CORNERSTONE_TOKEN (bearer)."),
    dict(name="icims", basic=True,
         description="iCIMS REST (recruiting). ops get/post (writes need confirm). Auth: "
         "ICIMS_BASE_URL + ICIMS_TOKEN (user:pass, basic)."),
    dict(name="paylocity",
         description="Paylocity REST. ops get/post/put (writes need confirm). Auth: "
         "PAYLOCITY_BASE_URL + PAYLOCITY_TOKEN (OAuth bearer)."),
    dict(name="workable",
         description="Workable REST (recruiting). ops get/post (writes need confirm). "
         "Auth: WORKABLE_BASE_URL + WORKABLE_TOKEN (bearer)."),
    dict(name="deel",
         description="Deel REST (global payroll/EOR). ops get/post/put/delete (writes "
         "need confirm). Auth: DEEL_BASE_URL + DEEL_TOKEN (bearer)."),

    # --- Commerce platforms ---
    dict(name="magento",
         description="Adobe Commerce (Magento) REST. ops get/post/put/delete (writes "
         "need confirm). e.g. /rest/V1/products. Auth: MAGENTO_BASE_URL + MAGENTO_TOKEN (bearer)."),
    dict(name="salesforce_commerce", base_url_env="SFCC_BASE_URL", token_env="SFCC_TOKEN",
         description="Salesforce Commerce Cloud (SCAPI/OCAPI) REST. ops get/post/put/"
         "delete (writes need confirm). Auth: SFCC_BASE_URL + SFCC_TOKEN (bearer)."),
    dict(name="sap_commerce", description="SAP Commerce Cloud (Hybris) OCC "
         "REST. ops get/post/put/delete (writes need confirm). Auth: SAP_COMMERCE_BASE_URL "
         "+ SAP_COMMERCE_TOKEN (OAuth bearer)."),

    # --- Observability / APM (additional) ---
    dict(name="appdynamics", description="AppDynamics REST. ops get/post "
         "(writes need confirm). Auth: APPDYNAMICS_BASE_URL + APPDYNAMICS_TOKEN (OAuth bearer)."),
    dict(name="sumologic",
         basic=True, description="Sumo Logic REST. ops get/post/put/delete (writes need "
         "confirm). Auth: SUMOLOGIC_BASE_URL + SUMOLOGIC_TOKEN (accessId:accessKey, basic)."),
    dict(name="logicmonitor", description="LogicMonitor REST. ops get/post "
         "(writes need confirm). Auth: LOGICMONITOR_BASE_URL + LOGICMONITOR_TOKEN (Bearer API token)."),

    # --- Security / GRC (additional) ---
    dict(name="netskope",
         token_header="Netskope-Api-Token", scheme="", description="Netskope REST. ops "
         "get/post (writes need confirm). Auth: NETSKOPE_BASE_URL + NETSKOPE_TOKEN (Netskope-Api-Token)."),
    dict(name="cisco_umbrella", base_url_env="UMBRELLA_BASE_URL", token_env="UMBRELLA_TOKEN",
         description="Cisco Umbrella REST. ops get/post/put/delete (writes need confirm). "
         "Auth: UMBRELLA_BASE_URL + UMBRELLA_TOKEN (OAuth bearer)."),
    dict(name="vanta",
         description="Vanta REST (GRC/compliance). ops get/post (writes need confirm). "
         "Auth: VANTA_BASE_URL + VANTA_TOKEN (OAuth bearer)."),
    dict(name="drata",
         description="Drata REST (GRC/compliance). ops get/post (writes need confirm). "
         "Auth: DRATA_BASE_URL + DRATA_TOKEN (bearer)."),
    dict(name="logicgate",
         description="LogicGate Risk Cloud REST. ops get/post/put/delete (writes need "
         "confirm). Auth: LOGICGATE_BASE_URL + LOGICGATE_TOKEN (bearer)."),

    # --- Sales intelligence ---
    dict(name="apollo",
         token_header="X-Api-Key", scheme="", description="Apollo.io REST. ops get/post "
         "(writes need confirm). Auth: APOLLO_BASE_URL + APOLLO_TOKEN (X-Api-Key)."),
    dict(name="zoominfo",
         description="ZoomInfo REST. ops get/post (writes need confirm). Auth: "
         "ZOOMINFO_BASE_URL + ZOOMINFO_TOKEN (JWT bearer)."),
    dict(name="clearbit",
         description="Clearbit REST. ops get/post (writes need confirm). Auth: "
         "CLEARBIT_BASE_URL + CLEARBIT_TOKEN (bearer)."),

    # --- DevOps / CD / registries (additional) ---
    dict(name="argocd",
         description="Argo CD REST. ops get/post/put/delete (writes need confirm). Auth: "
         "ARGOCD_BASE_URL + ARGOCD_TOKEN (bearer)."),
    dict(name="harness",
         token_header="x-api-key", scheme="", description="Harness REST. ops get/post/put/"
         "delete (writes need confirm). Auth: HARNESS_BASE_URL + HARNESS_TOKEN (x-api-key)."),
    dict(name="octopus_deploy", base_url_env="OCTOPUS_BASE_URL", token_env="OCTOPUS_TOKEN",
         token_header="X-Octopus-ApiKey", scheme="", description="Octopus Deploy REST. ops "
         "get/post/put/delete (writes need confirm). Auth: OCTOPUS_BASE_URL + OCTOPUS_TOKEN (X-Octopus-ApiKey)."),
    dict(name="dockerhub",
         description="Docker Hub REST. ops get/post/put/delete (writes need confirm). "
         "Auth: DOCKERHUB_BASE_URL (https://hub.docker.com) + DOCKERHUB_TOKEN (JWT bearer)."),

    # --- Marketing / product analytics (additional) ---
    dict(name="iterable",
         token_header="Api-Key", scheme="", description="Iterable REST. ops get/post "
         "(writes need confirm). Auth: ITERABLE_BASE_URL + ITERABLE_TOKEN (Api-Key)."),
    dict(name="pendo",
         token_header="x-pendo-integration-key", scheme="", description="Pendo REST. ops "
         "get/post (writes need confirm). Auth: PENDO_BASE_URL + PENDO_TOKEN (x-pendo-integration-key)."),

    # --- Contact center / CX (additional) ---
    dict(name="dialpad",
         description="Dialpad REST. ops get/post/put/delete (writes need confirm). Auth: "
         "DIALPAD_BASE_URL + DIALPAD_TOKEN (bearer)."),
    dict(name="aircall",
         basic=True, description="Aircall REST. ops get/post/put/delete (writes need "
         "confirm). Auth: AIRCALL_BASE_URL + AIRCALL_TOKEN (api_id:api_token, basic)."),
    dict(name="front",
         description="Front REST (shared inbox). ops get/post/put/delete (writes need "
         "confirm). Auth: FRONT_BASE_URL + FRONT_TOKEN (bearer)."),
    dict(name="gladly",
         basic=True, description="Gladly REST. ops get/post/put (writes need confirm). "
         "Auth: GLADLY_BASE_URL + GLADLY_TOKEN (email:apitoken, basic)."),

    # --- Design / diagramming / events ---
    dict(name="figma",
         token_header="X-Figma-Token", scheme="", description="Figma REST. ops get/post "
         "(writes need confirm). Auth: FIGMA_BASE_URL (https://api.figma.com) + FIGMA_TOKEN (X-Figma-Token)."),
    dict(name="lucid",
         description="Lucid (Lucidchart) REST. ops get/post/put/delete (writes need "
         "confirm). Auth: LUCID_BASE_URL + LUCID_TOKEN (bearer)."),
    dict(name="eventbrite",
         description="Eventbrite REST. ops get/post/put/delete (writes need confirm). "
         "Auth: EVENTBRITE_BASE_URL + EVENTBRITE_TOKEN (bearer)."),
    dict(name="cvent",
         description="Cvent REST (events). ops get/post/put/delete (writes need confirm). "
         "Auth: CVENT_BASE_URL + CVENT_TOKEN (OAuth bearer)."),

    # --- Final checklist closers (CRM / payroll / incident mgmt) ---
    dict(name="sugarcrm",
         description="SugarCRM REST (v11+). ops get/post/put/delete (writes need confirm). "
         "Auth: SUGARCRM_BASE_URL + SUGARCRM_TOKEN (OAuth bearer)."),
    dict(name="paychex",
         description="Paychex Flex REST. ops get/post (writes need confirm). Auth: "
         "PAYCHEX_BASE_URL + PAYCHEX_TOKEN (OAuth bearer)."),
    dict(name="opsgenie",
         scheme="GenieKey", description="Opsgenie REST (incident mgmt). ops get/post/put/"
         "delete (writes need confirm). Auth: OPSGENIE_BASE_URL + OPSGENIE_TOKEN (GenieKey)."),

    # --- Finance long-pole: banking / treasury / payments ---
    dict(name="modern_treasury", basic=True,
         description="Modern Treasury REST (payments/ledgers/reconciliation). ops get/post/"
         "put/delete (writes need confirm). e.g. /api/payment_orders, /api/counterparties, "
         "/api/internal_accounts. Auth: MODERN_TREASURY_BASE_URL "
         "(https://app.moderntreasury.com) + MODERN_TREASURY_TOKEN (Basic org_id:api_key)."),
    dict(name="mercury",
         description="Mercury banking REST (read balances/transactions; payments are "
         "confirm-gated). e.g. /accounts, /account/{id}/transactions. Auth: MERCURY_BASE_URL "
         "(https://api.mercury.com/api/v1) + MERCURY_TOKEN (bearer)."),
    dict(name="wise",
         description="Wise (TransferWise) REST (multi-currency balances/transfers; writes "
         "need confirm). e.g. /v1/profiles, /v1/borderless-accounts. Auth: WISE_BASE_URL "
         "(https://api.wise.com) + WISE_TOKEN (bearer)."),

    # --- Finance long-pole: AP / spend / travel & expense ---
    dict(name="airbase",
         description="Airbase spend/AP REST. ops get/post/put/delete (writes need confirm). "
         "Auth: AIRBASE_BASE_URL (https://api.airbase.io) + AIRBASE_TOKEN (bearer)."),
    dict(name="navan",
         description="Navan (TripActions) travel & expense REST. ops get/post (writes need "
         "confirm). Auth: NAVAN_BASE_URL (https://api.navan.com) + NAVAN_TOKEN (OAuth bearer)."),
    dict(name="pleo",
         description="Pleo spend-management REST. ops get/post (writes need confirm). Auth: "
         "PLEO_BASE_URL (https://external.pleo.io) + PLEO_TOKEN (bearer)."),

    # --- Finance long-pole: tax ---
    dict(name="avalara",
         basic=True, description="Avalara AvaTax REST (sales/use tax). ops get/post (writes "
         "need confirm). e.g. /api/v2/transactions/create, /api/v2/companies. Auth: "
         "AVALARA_BASE_URL (https://rest.avatax.com) + AVALARA_TOKEN (Basic account#:licensekey)."),
    dict(name="vertex_tax",
         description="Vertex O Series tax REST (calculation/returns; distinct from the "
         "Vertex AI tool). ops get/post (writes need confirm). Auth: VERTEX_TAX_BASE_URL "
         "(your Vertex endpoint) + VERTEX_TAX_TOKEN (OAuth bearer)."),
    dict(name="cch_axcess",
         extra_headers_env={"Ocp-Apim-Subscription-Key": "CCH_AXCESS_SUBSCRIPTION_KEY"},
         description="Wolters Kluwer CCH Axcess Open Integration Platform REST "
         "(Tax / Document / Workstream). ops get/post/put/delete (writes need "
         "confirm). e.g. /api/TaxService/v1.0/..., /api/DocumentService/v1.0/.... "
         "Auth: CCH_AXCESS_BASE_URL (https://api.cchaxcess.com) + CCH_AXCESS_TOKEN "
         "(OAuth bearer) + CCH_AXCESS_SUBSCRIPTION_KEY (Ocp-Apim-Subscription-Key)."),
    dict(name="gosystem_tax",
         description="Thomson Reuters GoSystem Tax RS REST (returns, e-file status, "
         "locators). ops get/post (writes need confirm). Auth: GOSYSTEM_TAX_BASE_URL "
         "(your GoSystem Tax API endpoint) + GOSYSTEM_TAX_TOKEN (OAuth bearer)."),

    # --- Finance long-pole: close / EPM-FP&A / equity ---
    dict(name="floqast",
         description="FloQast close & reconciliation REST. ops get/post (writes need "
         "confirm). Auth: FLOQAST_BASE_URL (https://api.floqast.com) + FLOQAST_TOKEN (bearer)."),
    dict(name="pigment",
         description="Pigment EPM / planning REST. ops get/post (writes need confirm). Auth: "
         "PIGMENT_BASE_URL (your Pigment API base) + PIGMENT_TOKEN (bearer)."),
    dict(name="planful",
         description="Planful EPM REST. ops get/post (writes need confirm). Auth: "
         "PLANFUL_BASE_URL (your Planful tenant) + PLANFUL_TOKEN (bearer)."),
    dict(name="carta",
         description="Carta cap-table / equity REST. ops get/post (writes need confirm). "
         "Auth: CARTA_BASE_URL (https://api.carta.com) + CARTA_TOKEN (OAuth bearer)."),

    # --- Legal: contract lifecycle / practice management ---
    dict(name="ironclad",
         description="Ironclad CLM REST. paths /public/api/v1/... (workflows, records). "
         "ops get/post/put/delete (writes need confirm). Auth: IRONCLAD_BASE_URL "
         "(https://ironcladapp.com) + IRONCLAD_TOKEN (bearer)."),
    dict(name="contractbook", description="Contractbook CLM REST. ops get/post/put/delete (writes need confirm). "
         "Auth: CONTRACTBOOK_BASE_URL (https://api.contractbook.com) + CONTRACTBOOK_TOKEN (bearer)."),
    dict(name="clio",
         description="Clio legal practice-management REST (v4). ops get/post/put/delete "
         "(writes need confirm). Auth: CLIO_BASE_URL (https://app.clio.com) + CLIO_TOKEN "
         "(OAuth bearer)."),

    # --- HR / People ---
    dict(name="hibob", basic=True,
         description="HiBob (bob) HRIS REST. ops get/post/put (writes need confirm). e.g. "
         "/v1/people, /v1/people/search. Auth: HIBOB_BASE_URL (https://api.hibob.com) + "
         "HIBOB_TOKEN (Basic service-user-id:token)."),
    dict(name="lattice",
         description="Lattice performance/engagement REST. ops get/post (writes need "
         "confirm). Auth: LATTICE_BASE_URL (https://api.latticehq.com) + LATTICE_TOKEN (bearer)."),

    # --- Product / Engineering: feature management ---
    dict(name="launchdarkly", scheme="",
         description="LaunchDarkly feature-flag REST (v2). ops get/post/patch/delete (writes "
         "need confirm). e.g. /api/v2/flags/{proj}. Auth: LAUNCHDARKLY_BASE_URL "
         "(https://app.launchdarkly.com) + LAUNCHDARKLY_TOKEN (raw Authorization)."),
    dict(name="split",
         description="Split feature-flag / experimentation REST. ops get/post/patch (writes "
         "need confirm). Auth: SPLIT_BASE_URL (https://api.split.io) + SPLIT_TOKEN (bearer)."),

    # --- Operations / supply chain / logistics ---
    dict(name="samsara",
         description="Samsara fleet / IoT REST. ops get/post (writes need confirm). e.g. "
         "/fleet/vehicles, /fleet/vehicles/locations. Auth: SAMSARA_BASE_URL "
         "(https://api.samsara.com) + SAMSARA_TOKEN (bearer)."),
    dict(name="easypost",
         basic=True, description="EasyPost shipping REST. ops get/post (writes need confirm). "
         "e.g. /v2/shipments, /v2/trackers. Auth: EASYPOST_BASE_URL (https://api.easypost.com) "
         "+ EASYPOST_TOKEN (Basic; API key as username)."),
    dict(name="flexport",
         description="Flexport freight / logistics REST. ops get/post (writes need confirm). "
         "Auth: FLEXPORT_BASE_URL (https://api.flexport.com) + FLEXPORT_TOKEN (bearer)."),
    dict(name="shippo",
         scheme="ShippoToken", description="Shippo multi-carrier shipping REST. ops get/post "
         "(writes need confirm). Auth: SHIPPO_BASE_URL (https://api.goshippo.com) + "
         "SHIPPO_TOKEN (ShippoToken scheme)."),

    # --- Strategy / market intelligence; IT-GRC ---
    dict(name="crunchbase",
         token_header="X-cb-user-key", scheme="",
         description="Crunchbase company / funding data REST (v4). op get (read-only). e.g. "
         "/api/v4/entities/organizations/{id}, /api/v4/searches/organizations. Auth: "
         "CRUNCHBASE_BASE_URL (https://api.crunchbase.com) + CRUNCHBASE_TOKEN (X-cb-user-key)."),
    dict(name="secureframe", description="Secureframe compliance-automation REST. ops get/post (writes need "
         "confirm). Auth: SECUREFRAME_BASE_URL (https://api.secureframe.com) + "
         "SECUREFRAME_TOKEN (bearer)."),

    # --- Finance long-pole: subscription billing / revenue ---
    dict(name="zuora",
         description="Zuora billing / revenue REST. ops get/post/put (writes need confirm). "
         "e.g. /v1/subscriptions, /v1/accounts, /v1/invoices. Auth: ZUORA_BASE_URL "
         "(https://rest.zuora.com) + ZUORA_TOKEN (OAuth bearer)."),
    dict(name="chargebee",
         basic=True, description="Chargebee subscription-billing REST (v2). ops get/post "
         "(writes need confirm). e.g. /api/v2/subscriptions, /api/v2/invoices. Auth: "
         "CHARGEBEE_BASE_URL (https://{site}.chargebee.com) + CHARGEBEE_TOKEN (Basic; API "
         "key as username)."),
    dict(name="recurly",
         basic=True, description="Recurly subscription-billing REST (v3). ops get/post/put "
         "(writes need confirm). e.g. /accounts, /subscriptions, /invoices. Auth: "
         "RECURLY_BASE_URL (https://v3.recurly.com) + RECURLY_TOKEN (Basic; API key as username)."),
    dict(name="gocardless",
         description="GoCardless bank-debit REST. ops get/post (writes need confirm; set "
         "GoCardless-Version via the path/headers per their docs). e.g. /payments, /mandates, "
         "/customers. Auth: GOCARDLESS_BASE_URL (https://api.gocardless.com) + GOCARDLESS_TOKEN "
         "(bearer)."),
    dict(name="freshbooks",
         description="FreshBooks accounting REST. ops get/post/put (writes need confirm). "
         "e.g. /accounting/account/{id}/invoices/invoices. Auth: FRESHBOOKS_BASE_URL "
         "(https://api.freshbooks.com) + FRESHBOOKS_TOKEN (OAuth bearer)."),

    # --- Long-tail SaaS (e-commerce, vertical industries, collaboration, etc.) ---
    dict(name="google_meet", description="Google Meet REST (via Google Workspace APIs). ops get/post/put/delete (writes need confirm). Auth: GOOGLE_MEET_BASE_URL (https://meet.googleapis.com) + GOOGLE_MEET_TOKEN (OAuth2 bearer)."),
    dict(name="gotomeeting", description="GoTo Meeting REST. ops get/post/put/delete (writes need confirm). Auth: GOTOMEETING_BASE_URL (https://api.getgo.com) + GOTOMEETING_TOKEN (OAuth2 bearer)."),
    dict(name="gotowebinar", description="GoTo Webinar REST. ops get/post/put/delete (writes need confirm). Auth: GOTOWEBINAR_BASE_URL (https://api.getgo.com) + GOTOWEBINAR_TOKEN (OAuth2 bearer)."),
    dict(name="bluejeans", description="BlueJeans Meetings REST. ops get/post/put/delete (writes need confirm). Auth: BLUEJEANS_BASE_URL (https://bluejeans.com/seam/api) + BLUEJEANS_TOKEN (OAuth2 bearer)."),
    dict(name="whereby", description="Whereby video meetings REST. ops get/post/put/delete (writes need confirm). Auth: WHEREBY_BASE_URL (https://api.whereby.dev) + WHEREBY_TOKEN (API key)."),
    dict(name="jitsi", description="Jitsi (JaaS) meetings REST. ops get/post/put/delete (writes need confirm). Auth: JITSI_BASE_URL (https://8x8.vc) + JITSI_TOKEN (JWT app key)."),
    dict(name="chime", description="Amazon Chime REST. ops get/post/put/delete (writes need confirm). Auth: CHIME_BASE_URL (https://service.chime.aws.amazon.com) + CHIME_TOKEN (AWS SigV4/bearer)."),
    dict(name="skype", description="Skype (Microsoft Bot/Graph) REST. ops get/post/put/delete (writes need confirm). Auth: SKYPE_BASE_URL (https://graph.skype.com) + SKYPE_TOKEN (OAuth2 bearer)."),
    dict(name="rocketchat", description="Rocket.Chat REST. ops get/post/put/delete (writes need confirm). Auth: ROCKETCHAT_BASE_URL (https://{host}) + ROCKETCHAT_TOKEN (personal access token)."),
    dict(name="mattermost", description="Mattermost REST. ops get/post/put/delete (writes need confirm). Auth: MATTERMOST_BASE_URL (https://{host}) + MATTERMOST_TOKEN (personal access token)."),
    dict(name="chanty", description="Chanty team chat REST. ops get/post/put/delete (writes need confirm). Auth: CHANTY_BASE_URL (https://api.chanty.com) + CHANTY_TOKEN (API key)."),
    dict(name="flock", description="Flock team chat REST. ops get/post/put/delete (writes need confirm). Auth: FLOCK_BASE_URL (https://api.flock.com) + FLOCK_TOKEN (OAuth2 bearer)."),
    dict(name="twist", description="Twist (Doist) team messaging REST. ops get/post/put/delete (writes need confirm). Auth: TWIST_BASE_URL (https://api.twist.com) + TWIST_TOKEN (OAuth2 bearer)."),
    dict(name="chatwork", description="Chatwork REST. ops get/post/put/delete (writes need confirm). Auth: CHATWORK_BASE_URL (https://api.chatwork.com) + CHATWORK_TOKEN (API token)."),
    dict(name="zulip", basic=True, description="Zulip REST. ops get/post/put/delete (writes need confirm). Auth: ZULIP_BASE_URL (https://{org}.zulipchat.com) + ZULIP_TOKEN (bot email:api-key basic auth)."),
    dict(name="symphony", description="Symphony messaging REST. ops get/post/put/delete (writes need confirm). Auth: SYMPHONY_BASE_URL (https://{pod}.symphony.com) + SYMPHONY_TOKEN (session/key auth bearer)."),
    dict(name="wire_app", description="Wire secure messaging REST. ops get/post/put/delete (writes need confirm). Auth: WIRE_APP_BASE_URL (https://prod-nginz-https.wire.com) + WIRE_APP_TOKEN (OAuth2 bearer)."),
    dict(name="element_im", description="Element/Matrix client-server REST. ops get/post/put/delete (writes need confirm). Auth: ELEMENT_IM_BASE_URL (https://matrix.org) + ELEMENT_IM_TOKEN (Matrix access token)."),
    dict(name="guilded", description="Guilded REST. ops get/post/put/delete (writes need confirm). Auth: GUILDED_BASE_URL (https://www.guilded.gg/api) + GUILDED_TOKEN (bot token)."),
    dict(name="groupme", description="GroupMe REST. ops get/post/put/delete (writes need confirm). Auth: GROUPME_BASE_URL (https://api.groupme.com) + GROUPME_TOKEN (access token)."),
    dict(name="google_chat", description="Google Chat REST. ops get/post/put/delete (writes need confirm). Auth: GOOGLE_CHAT_BASE_URL (https://chat.googleapis.com) + GOOGLE_CHAT_TOKEN (OAuth2 bearer or webhook token)."),
    dict(name="livestorm", description="Livestorm webinar REST. ops get/post/put/delete (writes need confirm). Auth: LIVESTORM_BASE_URL (https://api.livestorm.co) + LIVESTORM_TOKEN (API token)."),
    dict(name="demio", description="Demio webinar REST. ops get/post/put/delete (writes need confirm). Auth: DEMIO_BASE_URL (https://my.demio.com/api) + DEMIO_TOKEN (API key)."),
    dict(name="on24", description="ON24 webcast REST. ops get/post/put/delete (writes need confirm). Auth: ON24_BASE_URL (https://api.on24.com) + ON24_TOKEN (API key)."),
    dict(name="teamviewer", description="TeamViewer REST. ops get/post/put/delete (writes need confirm). Auth: TEAMVIEWER_BASE_URL (https://webapi.teamviewer.com) + TEAMVIEWER_TOKEN (API token)."),
    dict(name="anydesk", description="AnyDesk for Business REST. ops get/post/put/delete (writes need confirm). Auth: ANYDESK_BASE_URL (https://api.anydesk.com) + ANYDESK_TOKEN (API key)."),
    dict(name="loom", description="Loom video messaging REST. ops get/post/put/delete (writes need confirm). Auth: LOOM_BASE_URL (https://www.loom.com/api) + LOOM_TOKEN (API key)."),
    dict(name="vidyard", description="Vidyard video REST. ops get/post/put/delete (writes need confirm). Auth: VIDYARD_BASE_URL (https://api.vidyard.com) + VIDYARD_TOKEN (API key)."),
    dict(name="screencastify", description="Screencastify REST. ops get/post/put/delete (writes need confirm). Auth: SCREENCASTIFY_BASE_URL (https://api.screencastify.com) + SCREENCASTIFY_TOKEN (API key)."),
    dict(name="fireflies", description="Fireflies.ai meeting-notes REST/GraphQL. ops get/post/put/delete (writes need confirm). Auth: FIREFLIES_BASE_URL (https://api.fireflies.ai) + FIREFLIES_TOKEN (API key)."),
    dict(name="otter_ai", description="Otter.ai transcription REST. ops get/post/put/delete (writes need confirm). Auth: OTTER_AI_BASE_URL (https://otter.ai/forward) + OTTER_AI_TOKEN (API token)."),
    dict(name="grain", description="Grain meeting recording REST. ops get/post/put/delete (writes need confirm). Auth: GRAIN_BASE_URL (https://api.grain.com) + GRAIN_TOKEN (API token)."),
    dict(name="tldv", description="tl;dv meeting recorder REST. ops get/post/put/delete (writes need confirm). Auth: TLDV_BASE_URL (https://pasta.tldv.io) + TLDV_TOKEN (API key)."),
    dict(name="fathom_video", description="Fathom video meeting REST. ops get/post/put/delete (writes need confirm). Auth: FATHOM_VIDEO_BASE_URL (https://api.fathom.video) + FATHOM_VIDEO_TOKEN (API key)."),
    dict(name="fellow_app", description="Fellow.app meeting notes REST. ops get/post/put/delete (writes need confirm). Auth: FELLOW_APP_BASE_URL (https://api.fellow.app) + FELLOW_APP_TOKEN (API key)."),
    dict(name="doodle", description="Doodle scheduling REST. ops get/post/put/delete (writes need confirm). Auth: DOODLE_BASE_URL (https://api.doodle.com) + DOODLE_TOKEN (OAuth2 bearer)."),
    dict(name="acuity_scheduling", basic=True, description="Acuity Scheduling REST. ops get/post/put/delete (writes need confirm). Auth: ACUITY_SCHEDULING_BASE_URL (https://acuityscheduling.com/api/v1) + ACUITY_SCHEDULING_TOKEN (user-id:api-key basic auth)."),
    dict(name="chili_piper", description="Chili Piper scheduling REST. ops get/post/put/delete (writes need confirm). Auth: CHILI_PIPER_BASE_URL (https://api.chilipiper.com) + CHILI_PIPER_TOKEN (API key)."),
    dict(name="savvycal", description="SavvyCal scheduling REST. ops get/post/put/delete (writes need confirm). Auth: SAVVYCAL_BASE_URL (https://api.savvycal.com) + SAVVYCAL_TOKEN (API key)."),
    dict(name="cal_com", description="Cal.com scheduling REST. ops get/post/put/delete (writes need confirm). Auth: CAL_COM_BASE_URL (https://api.cal.com) + CAL_COM_TOKEN (API key)."),
    dict(name="evernote", description="Evernote REST. ops get/post/put/delete (writes need confirm). Auth: EVERNOTE_BASE_URL (https://api.evernote.com) + EVERNOTE_TOKEN (OAuth1/dev token)."),
    dict(name="onenote", description="Microsoft OneNote REST (Graph). ops get/post/put/delete (writes need confirm). Auth: ONENOTE_BASE_URL (https://graph.microsoft.com) + ONENOTE_TOKEN (OAuth2 bearer)."),
    dict(name="bear_notes", description="Bear notes X-callback/REST (via Bear API). ops get/post/put/delete (writes need confirm). Auth: BEAR_NOTES_BASE_URL (https://api.bear.app) + BEAR_NOTES_TOKEN (API token)."),
    dict(name="simplenote", description="Simplenote REST. ops get/post/put/delete (writes need confirm). Auth: SIMPLENOTE_BASE_URL (https://api2.simperium.com) + SIMPLENOTE_TOKEN (auth token)."),
    dict(name="standardnotes", description="Standard Notes sync REST. ops get/post/put/delete (writes need confirm). Auth: STANDARDNOTES_BASE_URL (https://api.standardnotes.com) + STANDARDNOTES_TOKEN (OAuth2 bearer)."),
    dict(name="joplin", description="Joplin (self-hosted sync server) REST. ops get/post/put/delete (writes need confirm). Auth: JOPLIN_BASE_URL (http://localhost:41184) + JOPLIN_TOKEN (API token)."),
    dict(name="dropbox_paper", description="Dropbox Paper REST. ops get/post/put/delete (writes need confirm). Auth: DROPBOX_PAPER_BASE_URL (https://api.dropboxapi.com) + DROPBOX_PAPER_TOKEN (OAuth2 bearer)."),
    dict(name="quip", description="Quip (Salesforce) REST. ops get/post/put/delete (writes need confirm). Auth: QUIP_BASE_URL (https://platform.quip.com) + QUIP_TOKEN (API token)."),
    dict(name="craft_docs", description="Craft docs REST. ops get/post/put/delete (writes need confirm). Auth: CRAFT_DOCS_BASE_URL (https://api.craft.do) + CRAFT_DOCS_TOKEN (API token)."),
    dict(name="roam_research", description="Roam Research REST (Backend API). ops get/post/put/delete (writes need confirm). Auth: ROAM_RESEARCH_BASE_URL (https://api.roamresearch.com) + ROAM_RESEARCH_TOKEN (API token)."),
    dict(name="logseq", description="Logseq local HTTP API. ops get/post/put/delete (writes need confirm). Auth: LOGSEQ_BASE_URL (http://localhost:12315) + LOGSEQ_TOKEN (API token)."),
    dict(name="remnote", description="RemNote REST. ops get/post/put/delete (writes need confirm). Auth: REMNOTE_BASE_URL (https://api.remnote.io) + REMNOTE_TOKEN (API key)."),
    dict(name="workflowy", description="WorkFlowy REST. ops get/post/put/delete (writes need confirm). Auth: WORKFLOWY_BASE_URL (https://workflowy.com/api) + WORKFLOWY_TOKEN (API key)."),
    dict(name="dynalist", description="Dynalist REST. ops get/post/put/delete (writes need confirm). Auth: DYNALIST_BASE_URL (https://dynalist.io/api/v1) + DYNALIST_TOKEN (API token)."),
    dict(name="nuclino", description="Nuclino REST. ops get/post/put/delete (writes need confirm). Auth: NUCLINO_BASE_URL (https://api.nuclino.com) + NUCLINO_TOKEN (API key)."),
    dict(name="tettra", description="Tettra wiki REST. ops get/post/put/delete (writes need confirm). Auth: TETTRA_BASE_URL (https://api.tettra.co) + TETTRA_TOKEN (API key)."),
    dict(name="guru", description="Guru knowledge REST. ops get/post/put/delete (writes need confirm). Auth: GURU_BASE_URL (https://api.getguru.com) + GURU_TOKEN (API user + token)."),
    dict(name="slite", description="Slite wiki REST. ops get/post/put/delete (writes need confirm). Auth: SLITE_BASE_URL (https://api.slite.com) + SLITE_TOKEN (API key)."),
    dict(name="almanac_hq", description="Almanac docs REST. ops get/post/put/delete (writes need confirm). Auth: ALMANAC_HQ_BASE_URL (https://api.almanac.io) + ALMANAC_HQ_TOKEN (API key)."),
    dict(name="google_docs", description="Google Docs REST. ops get/post/put/delete (writes need confirm). Auth: GOOGLE_DOCS_BASE_URL (https://docs.googleapis.com) + GOOGLE_DOCS_TOKEN (OAuth2 bearer)."),
    dict(name="google_sheets", description="Google Sheets REST. ops get/post/put/delete (writes need confirm). Auth: GOOGLE_SHEETS_BASE_URL (https://sheets.googleapis.com) + GOOGLE_SHEETS_TOKEN (OAuth2 bearer)."),
    dict(name="google_slides", description="Google Slides REST. ops get/post/put/delete (writes need confirm). Auth: GOOGLE_SLIDES_BASE_URL (https://slides.googleapis.com) + GOOGLE_SLIDES_TOKEN (OAuth2 bearer)."),
    dict(name="onedrive", description="Microsoft OneDrive REST (Graph). ops get/post/put/delete (writes need confirm). Auth: ONEDRIVE_BASE_URL (https://graph.microsoft.com) + ONEDRIVE_TOKEN (OAuth2 bearer)."),
    dict(name="teamwork", basic=True, description="Teamwork Projects REST. ops get/post/put/delete (writes need confirm). Auth: TEAMWORK_BASE_URL (https://{site}.teamwork.com) + TEAMWORK_TOKEN (API key as basic-auth username)."),
    dict(name="redmine", token_header="X-Redmine-API-Key", scheme="", description="Redmine REST. ops get/post/put/delete (writes need confirm). Auth: REDMINE_BASE_URL (https://{host}) + REDMINE_TOKEN (API key)."),
    dict(name="taiga", description="Taiga project management REST. ops get/post/put/delete (writes need confirm). Auth: TAIGA_BASE_URL (https://api.taiga.io) + TAIGA_TOKEN (auth token)."),
    dict(name="podio", description="Podio REST. ops get/post/put/delete (writes need confirm). Auth: PODIO_BASE_URL (https://api.podio.com) + PODIO_TOKEN (OAuth2 bearer)."),
    dict(name="zoho_projects", description="Zoho Projects REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_PROJECTS_BASE_URL (https://projectsapi.zoho.com) + ZOHO_PROJECTS_TOKEN (OAuth2 bearer)."),
    dict(name="liquidplanner", description="LiquidPlanner REST. ops get/post/put/delete (writes need confirm). Auth: LIQUIDPLANNER_BASE_URL (https://app.liquidplanner.com/api) + LIQUIDPLANNER_TOKEN (OAuth2 bearer)."),
    dict(name="workfront", description="Adobe Workfront REST. ops get/post/put/delete (writes need confirm). Auth: WORKFRONT_BASE_URL (https://{sub}.my.workfront.com) + WORKFRONT_TOKEN (API key)."),
    dict(name="meistertask", description="MeisterTask REST. ops get/post/put/delete (writes need confirm). Auth: MEISTERTASK_BASE_URL (https://www.meistertask.com/api) + MEISTERTASK_TOKEN (OAuth2 bearer)."),
    dict(name="freedcamp", description="Freedcamp REST. ops get/post/put/delete (writes need confirm). Auth: FREEDCAMP_BASE_URL (https://freedcamp.com/api/v1) + FREEDCAMP_TOKEN (API key)."),
    dict(name="proofhub", description="ProofHub REST. ops get/post/put/delete (writes need confirm). Auth: PROOFHUB_BASE_URL (https://{sub}.proofhub.com/api) + PROOFHUB_TOKEN (API token)."),
    dict(name="hive_app", description="Hive project management REST. ops get/post/put/delete (writes need confirm). Auth: HIVE_APP_BASE_URL (https://app.hive.com/api) + HIVE_APP_TOKEN (API key)."),
    dict(name="height_app", description="Height task management REST. ops get/post/put/delete (writes need confirm). Auth: HEIGHT_APP_BASE_URL (https://api.height.app) + HEIGHT_APP_TOKEN (OAuth2 bearer)."),
    dict(name="shortcut", description="Shortcut (formerly Clubhouse) REST. ops get/post/put/delete (writes need confirm). Auth: SHORTCUT_BASE_URL (https://api.app.shortcut.com) + SHORTCUT_TOKEN (API token)."),
    dict(name="pivotal_tracker", description="Pivotal Tracker REST. ops get/post/put/delete (writes need confirm). Auth: PIVOTAL_TRACKER_BASE_URL (https://www.pivotaltracker.com/services/v5) + PIVOTAL_TRACKER_TOKEN (API token)."),
    dict(name="targetprocess", description="Targetprocess REST. ops get/post/put/delete (writes need confirm). Auth: TARGETPROCESS_BASE_URL (https://{sub}.tpondemand.com) + TARGETPROCESS_TOKEN (API token)."),
    dict(name="productboard", description="Productboard REST. ops get/post/put/delete (writes need confirm). Auth: PRODUCTBOARD_BASE_URL (https://api.productboard.com) + PRODUCTBOARD_TOKEN (API token)."),
    dict(name="aha_io", description="Aha! roadmapping REST. ops get/post/put/delete (writes need confirm). Auth: AHA_IO_BASE_URL (https://{sub}.aha.io/api) + AHA_IO_TOKEN (API key)."),
    dict(name="roadmunk", description="Roadmunk REST. ops get/post/put/delete (writes need confirm). Auth: ROADMUNK_BASE_URL (https://roadmunk.com/api) + ROADMUNK_TOKEN (API key)."),
    dict(name="favro", description="Favro REST. ops get/post/put/delete (writes need confirm). Auth: FAVRO_BASE_URL (https://favro.com/api) + FAVRO_TOKEN (API token)."),
    dict(name="kanbanize", description="Kanbanize (Businessmap) REST. ops get/post/put/delete (writes need confirm). Auth: KANBANIZE_BASE_URL (https://{sub}.kanbanize.com/api) + KANBANIZE_TOKEN (API key)."),
    dict(name="nulab_backlog", description="Backlog (Nulab) REST. ops get/post/put/delete (writes need confirm). Auth: NULAB_BACKLOG_BASE_URL (https://{sub}.backlog.com/api/v2) + NULAB_BACKLOG_TOKEN (API key)."),
    dict(name="yougile", description="YouGile REST. ops get/post/put/delete (writes need confirm). Auth: YOUGILE_BASE_URL (https://ru.yougile.com/api-v2) + YOUGILE_TOKEN (API key)."),
    dict(name="taskade", description="Taskade REST. ops get/post/put/delete (writes need confirm). Auth: TASKADE_BASE_URL (https://www.taskade.com/api) + TASKADE_TOKEN (API key)."),
    dict(name="ntask", description="nTask project management REST. ops get/post/put/delete (writes need confirm). Auth: NTASK_BASE_URL (https://api.ntaskmanager.com) + NTASK_TOKEN (API token)."),
    dict(name="zenkit", description="Zenkit REST. ops get/post/put/delete (writes need confirm). Auth: ZENKIT_BASE_URL (https://zenkit.com/api/v1) + ZENKIT_TOKEN (API token)."),
    dict(name="quire", description="Quire task management REST. ops get/post/put/delete (writes need confirm). Auth: QUIRE_BASE_URL (https://api.quire.io) + QUIRE_TOKEN (OAuth2 bearer)."),
    dict(name="todoist", description="Todoist REST. ops get/post/put/delete (writes need confirm). Auth: TODOIST_BASE_URL (https://api.todoist.com) + TODOIST_TOKEN (API token)."),
    dict(name="mural", description="Mural whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: MURAL_BASE_URL (https://api.mural.co) + MURAL_TOKEN (OAuth2 bearer)."),
    dict(name="invision_freehand", description="InVision Freehand REST. ops get/post/put/delete (writes need confirm). Auth: INVISION_FREEHAND_BASE_URL (https://api.invisionapp.com) + INVISION_FREEHAND_TOKEN (API token)."),
    dict(name="conceptboard", description="Conceptboard whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: CONCEPTBOARD_BASE_URL (https://app.conceptboard.com/api) + CONCEPTBOARD_TOKEN (API key)."),
    dict(name="stormboard", description="Stormboard whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: STORMBOARD_BASE_URL (https://api.stormboard.com) + STORMBOARD_TOKEN (API key)."),
    dict(name="limnu", description="Limnu whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: LIMNU_BASE_URL (https://api.limnu.com) + LIMNU_TOKEN (API key)."),
    dict(name="ziteboard", description="Ziteboard whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: ZITEBOARD_BASE_URL (https://ziteboard.com/api) + ZITEBOARD_TOKEN (API key)."),
    dict(name="yammer", description="Yammer (Microsoft Viva Engage) REST. ops get/post/put/delete (writes need confirm). Auth: YAMMER_BASE_URL (https://www.yammer.com/api/v1) + YAMMER_TOKEN (OAuth2 bearer)."),
    dict(name="workplace_meta", description="Workplace by Meta REST (Graph API). ops get/post/put/delete (writes need confirm). Auth: WORKPLACE_META_BASE_URL (https://graph.facebook.com) + WORKPLACE_META_TOKEN (OAuth2 bearer)."),
    dict(name="beekeeper", description="Beekeeper employee comms REST. ops get/post/put/delete (writes need confirm). Auth: BEEKEEPER_BASE_URL (https://{sub}.beekeeper.io/api) + BEEKEEPER_TOKEN (API token)."),
    dict(name="staffbase", description="Staffbase intranet REST. ops get/post/put/delete (writes need confirm). Auth: STAFFBASE_BASE_URL (https://{sub}.staffbase.com/api) + STAFFBASE_TOKEN (API token)."),
    dict(name="simpplr", description="Simpplr intranet REST. ops get/post/put/delete (writes need confirm). Auth: SIMPPLR_BASE_URL (https://{sub}.simpplr.com/api) + SIMPPLR_TOKEN (OAuth2 bearer)."),
    dict(name="happeo", description="Happeo intranet REST. ops get/post/put/delete (writes need confirm). Auth: HAPPEO_BASE_URL (https://api.happeo.com) + HAPPEO_TOKEN (API token)."),
    dict(name="microsoft_planner", description="Microsoft Planner REST (Graph). ops get/post/put/delete (writes need confirm). Auth: MICROSOFT_PLANNER_BASE_URL (https://graph.microsoft.com) + MICROSOFT_PLANNER_TOKEN (OAuth2 bearer)."),
    dict(name="google_keep", description="Google Keep REST (via Google Workspace Keep API). ops get/post/put/delete (writes need confirm). Auth: GOOGLE_KEEP_BASE_URL (https://keep.googleapis.com) + GOOGLE_KEEP_TOKEN (OAuth2 bearer)."),
    dict(name="skype_for_business", description="Skype for Business REST (Graph/legacy UCWA). ops get/post/put/delete (writes need confirm). Auth: SKYPE_FOR_BUSINESS_BASE_URL (https://graph.microsoft.com) + SKYPE_FOR_BUSINESS_TOKEN (OAuth2 bearer)."),
    dict(name="viber", description="Viber messaging REST. ops get/post/put/delete (writes need confirm). Auth: VIBER_BASE_URL (https://chatapi.viber.com) + VIBER_TOKEN (bot auth token)."),
    dict(name="telegram", description="Telegram Bot REST. ops get/post/put/delete (writes need confirm). Auth: TELEGRAM_BASE_URL (https://api.telegram.org) + TELEGRAM_TOKEN (bot token)."),
    dict(name="signal", description="Signal messaging REST (signal-cli REST API). ops get/post/put/delete (writes need confirm). Auth: SIGNAL_BASE_URL (http://localhost:8080) + SIGNAL_TOKEN (local API token)."),
    dict(name="wechat_work", description="WeChat Work (WeCom) REST. ops get/post/put/delete (writes need confirm). Auth: WECHAT_WORK_BASE_URL (https://qyapi.weixin.qq.com) + WECHAT_WORK_TOKEN (access token)."),
    dict(name="line_works", description="LINE WORKS REST. ops get/post/put/delete (writes need confirm). Auth: LINE_WORKS_BASE_URL (https://www.worksapis.com) + LINE_WORKS_TOKEN (OAuth2 bearer)."),
    dict(name="threema", description="Threema Work messaging REST. ops get/post/put/delete (writes need confirm). Auth: THREEMA_BASE_URL (https://msgapi.threema.ch) + THREEMA_TOKEN (API secret)."),
    dict(name="band_app", description="Band (Naver) group REST. ops get/post/put/delete (writes need confirm). Auth: BAND_APP_BASE_URL (https://openapi.band.us) + BAND_APP_TOKEN (OAuth2 bearer)."),
    dict(name="remind_app", description="Remind (school comms) REST. ops get/post/put/delete (writes need confirm). Auth: REMIND_APP_BASE_URL (https://api.remind.com) + REMIND_APP_TOKEN (OAuth2 bearer)."),
    dict(name="zoho_cliq", description="Zoho Cliq REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_CLIQ_BASE_URL (https://cliq.zoho.com/api) + ZOHO_CLIQ_TOKEN (OAuth2 bearer)."),
    dict(name="speakap", description="Speakap employee comms REST. ops get/post/put/delete (writes need confirm). Auth: SPEAKAP_BASE_URL (https://api.speakap.io) + SPEAKAP_TOKEN (OAuth2 bearer)."),
    dict(name="jostle", description="Jostle intranet REST. ops get/post/put/delete (writes need confirm). Auth: JOSTLE_BASE_URL (https://{sub}.jostle.me/api) + JOSTLE_TOKEN (API token)."),
    dict(name="unily", description="Unily intranet REST. ops get/post/put/delete (writes need confirm). Auth: UNILY_BASE_URL (https://{sub}.unily.com/api) + UNILY_TOKEN (OAuth2 bearer)."),
    dict(name="igloo_software", description="Igloo Software intranet REST. ops get/post/put/delete (writes need confirm). Auth: IGLOO_SOFTWARE_BASE_URL (https://{sub}.igloosoftware.com/api) + IGLOO_SOFTWARE_TOKEN (API key)."),
    dict(name="interact_intranet", description="Interact intranet REST. ops get/post/put/delete (writes need confirm). Auth: INTERACT_INTRANET_BASE_URL (https://{sub}.interactsoftware.com/api) + INTERACT_INTRANET_TOKEN (API key)."),
    dict(name="workzone", description="Workzone project management REST. ops get/post/put/delete (writes need confirm). Auth: WORKZONE_BASE_URL (https://www.workzone.com/api) + WORKZONE_TOKEN (API key)."),
    dict(name="clarizen", description="Clarizen (Planview) project REST. ops get/post/put/delete (writes need confirm). Auth: CLARIZEN_BASE_URL (https://api.clarizen.com) + CLARIZEN_TOKEN (OAuth2 bearer)."),
    dict(name="easyprojects", description="Easy Projects REST. ops get/post/put/delete (writes need confirm). Auth: EASYPROJECTS_BASE_URL (https://{sub}.easyprojects.net/api) + EASYPROJECTS_TOKEN (API key)."),
    dict(name="mavenlink", description="Mavenlink (Kantata) REST. ops get/post/put/delete (writes need confirm). Auth: MAVENLINK_BASE_URL (https://api.mavenlink.com) + MAVENLINK_TOKEN (OAuth2 bearer)."),
    dict(name="celoxis", description="Celoxis project management REST. ops get/post/put/delete (writes need confirm). Auth: CELOXIS_BASE_URL (https://{sub}.celoxis.com/api) + CELOXIS_TOKEN (API key)."),
    dict(name="paymo", description="Paymo REST. ops get/post/put/delete (writes need confirm). Auth: PAYMO_BASE_URL (https://app.paymoapp.com/api) + PAYMO_TOKEN (API key)."),
    dict(name="scoro", description="Scoro REST. ops get/post/put/delete (writes need confirm). Auth: SCORO_BASE_URL (https://{sub}.scoro.com/api) + SCORO_TOKEN (API key)."),
    dict(name="avaza", description="Avaza REST. ops get/post/put/delete (writes need confirm). Auth: AVAZA_BASE_URL (https://api.avaza.com) + AVAZA_TOKEN (OAuth2 bearer)."),
    dict(name="hubstaff_tasks", description="Hubstaff Tasks REST. ops get/post/put/delete (writes need confirm). Auth: HUBSTAFF_TASKS_BASE_URL (https://api.hubstaff.com) + HUBSTAFF_TASKS_TOKEN (OAuth2 bearer)."),
    dict(name="glasscubes", description="Glasscubes collaboration REST. ops get/post/put/delete (writes need confirm). Auth: GLASSCUBES_BASE_URL (https://api.glasscubes.com) + GLASSCUBES_TOKEN (API key)."),
    dict(name="bitrix24", description="Bitrix24 REST. ops get/post/put/delete (writes need confirm). Auth: BITRIX24_BASE_URL (https://{sub}.bitrix24.com/rest) + BITRIX24_TOKEN (webhook/OAuth2 bearer)."),
    dict(name="flowlu", description="Flowlu REST. ops get/post/put/delete (writes need confirm). Auth: FLOWLU_BASE_URL (https://{sub}.flowlu.com/api) + FLOWLU_TOKEN (API key)."),
    dict(name="accelo", description="Accelo REST. ops get/post/put/delete (writes need confirm). Auth: ACCELO_BASE_URL (https://{sub}.api.accelo.com) + ACCELO_TOKEN (OAuth2 bearer)."),
    dict(name="workamajig", description="Workamajig REST. ops get/post/put/delete (writes need confirm). Auth: WORKAMAJIG_BASE_URL (https://{sub}.workamajig.com/api) + WORKAMAJIG_TOKEN (API token)."),
    dict(name="ganttpro", description="GanttPRO REST. ops get/post/put/delete (writes need confirm). Auth: GANTTPRO_BASE_URL (https://ganttpro.com/api) + GANTTPRO_TOKEN (API key)."),
    dict(name="teamgantt", description="TeamGantt REST. ops get/post/put/delete (writes need confirm). Auth: TEAMGANTT_BASE_URL (https://api.teamgantt.com) + TEAMGANTT_TOKEN (API key)."),
    dict(name="instagantt", description="Instagantt REST. ops get/post/put/delete (writes need confirm). Auth: INSTAGANTT_BASE_URL (https://api.instagantt.com) + INSTAGANTT_TOKEN (API key)."),
    dict(name="smartdraw", description="SmartDraw diagramming REST. ops get/post/put/delete (writes need confirm). Auth: SMARTDRAW_BASE_URL (https://cloud.smartdraw.com/api) + SMARTDRAW_TOKEN (API key)."),
    dict(name="creately", description="Creately diagramming/whiteboard REST. ops get/post/put/delete (writes need confirm). Auth: CREATELY_BASE_URL (https://api.creately.com) + CREATELY_TOKEN (API key)."),
    dict(name="gliffy", description="Gliffy diagramming REST. ops get/post/put/delete (writes need confirm). Auth: GLIFFY_BASE_URL (https://api.gliffy.com) + GLIFFY_TOKEN (API key)."),
    dict(name="drawio", description="diagrams.net (draw.io) REST (self-hosted/Confluence app). ops get/post/put/delete (writes need confirm). Auth: DRAWIO_BASE_URL (https://{host}) + DRAWIO_TOKEN (API token)."),
    dict(name="whimsical", description="Whimsical whiteboard/diagram REST. ops get/post/put/delete (writes need confirm). Auth: WHIMSICAL_BASE_URL (https://api.whimsical.com) + WHIMSICAL_TOKEN (API key)."),
    dict(name="nimbus_note", description="Nimbus Note REST. ops get/post/put/delete (writes need confirm). Auth: NIMBUS_NOTE_BASE_URL (https://api.nimbusweb.me) + NIMBUS_NOTE_TOKEN (OAuth2 bearer)."),
    dict(name="zoho_connect", description="Zoho Connect REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_CONNECT_BASE_URL (https://connect.zoho.com/api) + ZOHO_CONNECT_TOKEN (OAuth2 bearer)."),
    dict(name="wimi", description="Wimi collaboration REST. ops get/post/put/delete (writes need confirm). Auth: WIMI_BASE_URL (https://api.wimi-teamwork.com) + WIMI_TOKEN (OAuth2 bearer)."),
    dict(name="toggl_plan", description="Toggl Plan REST. ops get/post/put/delete (writes need confirm). Auth: TOGGL_PLAN_BASE_URL (https://api.plan.toggl.com) + TOGGL_PLAN_TOKEN (API token)."),
    dict(name="copper", description="Copper CRM REST. ops get/post/put/delete (writes need confirm). e.g. /developer_api/v1/leads/search. Auth: COPPER_BASE_URL (https://api.copper.com) + COPPER_TOKEN (API key, requires user email)."),
    dict(name="insightly", basic=True, description="Insightly CRM REST. ops get/post/put/delete (writes need confirm). e.g. /v3.1/Contacts. Auth: INSIGHTLY_BASE_URL (https://api.insightly.com) + INSIGHTLY_TOKEN (API key as basic user)."),
    dict(name="nimble", description="Nimble CRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/contacts. Auth: NIMBLE_BASE_URL (https://api.nimble.com) + NIMBLE_TOKEN (OAuth bearer token)."),
    dict(name="keap", description="Keap (Infusionsoft) CRM REST. ops get/post/put/delete (writes need confirm). e.g. /crm/rest/v1/contacts. Auth: KEAP_BASE_URL (https://api.infusionsoft.com) + KEAP_TOKEN (OAuth bearer token)."),
    dict(name="nutshell", description="Nutshell CRM REST/JSON-RPC. ops get/post/put/delete (writes need confirm). e.g. /rest/contacts. Auth: NUTSHELL_BASE_URL (https://app.nutshell.com/api) + NUTSHELL_TOKEN (API key)."),
    dict(name="close", description="Close CRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/lead/. Auth: CLOSE_BASE_URL (https://api.close.com) + CLOSE_TOKEN (API key, bearer)."),
    dict(name="agilecrm", basic=True, description="Agile CRM REST. ops get/post/put/delete (writes need confirm). e.g. /dev/api/contacts. Auth: AGILECRM_BASE_URL (https://{domain}.agilecrm.com) + AGILECRM_TOKEN (email:API key)."),
    dict(name="capsulecrm", basic=True, description="Capsule CRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/parties. Auth: CAPSULECRM_BASE_URL (https://api.capsulecrm.com) + CAPSULECRM_TOKEN (API key as basic user)."),
    dict(name="vtiger", basic=True, description="Vtiger CRM webservice REST. ops get/post/put/delete (writes need confirm). e.g. /webservice.php. Auth: VTIGER_BASE_URL (https://{instance}.vtiger.com) + VTIGER_TOKEN (username + access key)."),
    dict(name="streak", description="Streak CRM REST (Gmail add-on). ops get/post/put/delete (writes need confirm). e.g. /api/v1/pipelines. Auth: STREAK_BASE_URL (https://api.streak.com) + STREAK_TOKEN (API key)."),
    dict(name="salesflare", description="Salesflare CRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/accounts. Auth: SALESFLARE_BASE_URL (https://api.salesflare.com) + SALESFLARE_TOKEN (API key)."),
    dict(name="membrain", description="Membrain CRM REST. ops get/post/put/delete (writes need confirm). Auth: MEMBRAIN_BASE_URL (https://api.membrain.com) + MEMBRAIN_TOKEN (API key, bearer)."),
    dict(name="highrise", basic=True, description="Highrise CRM REST/XML API. ops get/post/put/delete (writes need confirm). e.g. /contacts.xml. Auth: HIGHRISE_BASE_URL (https://{account}.highrisehq.com) + HIGHRISE_TOKEN (API key as basic user)."),
    dict(name="maximizer", description="Maximizer CRM REST. ops get/post/put/delete (writes need confirm). Auth: MAXIMIZER_BASE_URL (https://api.maximizer.com) + MAXIMIZER_TOKEN (OAuth bearer token)."),
    dict(name="amocrm", description="Kommo (amoCRM) REST. ops get/post/put/delete (writes need confirm). e.g. /api/v4/leads. Auth: AMOCRM_BASE_URL (https://{subdomain}.amocrm.com) + AMOCRM_TOKEN (OAuth bearer token)."),
    dict(name="salesmate", description="Salesmate CRM REST. ops get/post/put/delete (writes need confirm). e.g. /v3/contacts. Auth: SALESMATE_BASE_URL (https://{domain}.salesmate.io) + SALESMATE_TOKEN (API key)."),
    dict(name="nethunt", description="NetHunt CRM REST. ops get/post/put/delete (writes need confirm). Auth: NETHUNT_BASE_URL (https://nethunt.com/api/v1) + NETHUNT_TOKEN (API key)."),
    dict(name="pipeliner", description="Pipeliner CRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/v100/rest/spaces. Auth: PIPELINER_BASE_URL (https://{space}.pipelinersales.com) + PIPELINER_TOKEN (OAuth bearer token)."),
    dict(name="workbooks", description="Workbooks CRM REST/JSON API. ops get/post/put/delete (writes need confirm). Auth: WORKBOOKS_BASE_URL (https://{instance}.workbooks.com) + WORKBOOKS_TOKEN (API key)."),
    dict(name="suitecrm", description="SuiteCRM REST (v8 JSON:API, self-hosted). ops get/post/put/delete (writes need confirm). e.g. /Api/V8/module/Contacts. Auth: SUITECRM_BASE_URL (https://{host}/legacy) + SUITECRM_TOKEN (OAuth2 bearer token)."),
    dict(name="espocrm", token_header="X-Api-Key", scheme="", description="EspoCRM REST (self-hosted). ops get/post/put/delete (writes need confirm). e.g. /api/v1/Contact. Auth: ESPOCRM_BASE_URL (https://{host}) + ESPOCRM_TOKEN (API key header)."),
    dict(name="odoo", description="Odoo CRM REST/JSON-RPC (self-hosted). ops get/post/put/delete (writes need confirm). e.g. /web/dataset/call_kw. Auth: ODOO_BASE_URL (https://{instance}.odoo.com) + ODOO_TOKEN (API key)."),
    dict(name="sharpspring", description="SharpSpring CRM/marketing REST (JSON-RPC). ops get/post/put/delete (writes need confirm). e.g. /pubapi/v1/. Auth: SHARPSPRING_BASE_URL (https://api.sharpspring.com) + SHARPSPRING_TOKEN (API key + secret)."),
    dict(name="activecampaign", token_header="Api-Token", scheme="", description="ActiveCampaign CRM/marketing REST. ops get/post/put/delete (writes need confirm). e.g. /api/3/contacts. Auth: ACTIVECAMPAIGN_BASE_URL (https://{account}.api-us1.com) + ACTIVECAMPAIGN_TOKEN (API key)."),
    dict(name="ontraport", description="Ontraport CRM REST. ops get/post/put/delete (writes need confirm). e.g. /1/Contacts. Auth: ONTRAPORT_BASE_URL (https://api.ontraport.com) + ONTRAPORT_TOKEN (API key + App ID)."),
    dict(name="methodcrm", description="Method:CRM REST. ops get/post/put/delete (writes need confirm). Auth: METHODCRM_BASE_URL (https://rest.method.me) + METHODCRM_TOKEN (API key, bearer)."),
    dict(name="apptivo", description="Apptivo CRM REST. ops get/post/put/delete (writes need confirm). e.g. /app/dao/v6/contacts. Auth: APPTIVO_BASE_URL (https://api.apptivo.com) + APPTIVO_TOKEN (API key + access key)."),
    dict(name="onepagecrm", basic=True, description="OnePageCRM REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/contacts. Auth: ONEPAGECRM_BASE_URL (https://app.onepagecrm.com/api/v3) + ONEPAGECRM_TOKEN (API key as basic user)."),
    dict(name="reallysimplesystems", description="Really Simple Systems CRM REST. ops get/post/put/delete (writes need confirm). Auth: REALLYSIMPLESYSTEMS_BASE_URL (https://api.reallysimplesystems.com) + REALLYSIMPLESYSTEMS_TOKEN (OAuth bearer token)."),
    dict(name="teamgate", description="Teamgate CRM REST. ops get/post/put/delete (writes need confirm). e.g. /crm/api/v1/leads. Auth: TEAMGATE_BASE_URL (https://api.teamgate.com) + TEAMGATE_TOKEN (OAuth bearer token)."),
    dict(name="followupboss", basic=True, description="Follow Up Boss (real estate CRM) REST. ops get/post/put/delete (writes need confirm). e.g. /v1/people. Auth: FOLLOWUPBOSS_BASE_URL (https://api.followupboss.com) + FOLLOWUPBOSS_TOKEN (API key as basic user)."),
    dict(name="liondesk", description="LionDesk (real estate CRM) REST. ops get/post/put/delete (writes need confirm). Auth: LIONDESK_BASE_URL (https://api.liondesk.com) + LIONDESK_TOKEN (API key, bearer)."),
    dict(name="contactually", description="Contactually CRM REST. ops get/post/put/delete (writes need confirm). Auth: CONTACTUALLY_BASE_URL (https://api.contactually.com) + CONTACTUALLY_TOKEN (OAuth bearer token)."),
    dict(name="pipelinecrm", description="Pipeline CRM (formerly PipelineDeals) REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/deals. Auth: PIPELINECRM_BASE_URL (https://api.pipelinedeals.com) + PIPELINECRM_TOKEN (API key)."),
    dict(name="salesrabbit", description="SalesRabbit (field sales CRM) REST. ops get/post/put/delete (writes need confirm). Auth: SALESRABBIT_BASE_URL (https://api.salesrabbit.com) + SALESRABBIT_TOKEN (OAuth bearer token)."),
    dict(name="spotio", description="SPOTIO (field sales CRM) REST. ops get/post/put/delete (writes need confirm). Auth: SPOTIO_BASE_URL (https://api.spotio.com) + SPOTIO_TOKEN (API key, bearer)."),
    dict(name="reply_io", description="Reply.io sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /v1/people. Auth: REPLY_IO_BASE_URL (https://api.reply.io) + REPLY_IO_TOKEN (API key)."),
    dict(name="yesware", description="Yesware sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: YESWARE_BASE_URL (https://api.yesware.com) + YESWARE_TOKEN (API key, bearer)."),
    dict(name="mixmax", description="Mixmax sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /api/sequences. Auth: MIXMAX_BASE_URL (https://api.mixmax.com) + MIXMAX_TOKEN (API key, bearer)."),
    dict(name="outplay", description="Outplay sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: OUTPLAY_BASE_URL (https://api.outplayhq.com) + OUTPLAY_TOKEN (API key, bearer)."),
    dict(name="klenty", description="Klenty sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: KLENTY_BASE_URL (https://api.klenty.com) + KLENTY_TOKEN (API key, bearer)."),
    dict(name="veloxy", description="Veloxy sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: VELOXY_BASE_URL (https://api.veloxy.io) + VELOXY_TOKEN (API key, bearer)."),
    dict(name="salesken", description="Salesken conversation intelligence REST. ops get/post/put/delete (writes need confirm). Auth: SALESKEN_BASE_URL (https://api.salesken.ai) + SALESKEN_TOKEN (API key, bearer)."),
    dict(name="momentum", description="Momentum.io revenue workflow REST. ops get/post/put/delete (writes need confirm). Auth: MOMENTUM_BASE_URL (https://api.momentum.io) + MOMENTUM_TOKEN (API key, bearer)."),
    dict(name="revenueio", description="Revenue.io sales engagement/call intelligence REST. ops get/post/put/delete (writes need confirm). Auth: REVENUEIO_BASE_URL (https://api.revenue.io) + REVENUEIO_TOKEN (API key, bearer)."),
    dict(name="groove", description="Groove.co (Clari) sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /v2/flows. Auth: GROOVE_BASE_URL (https://api.groove.co) + GROOVE_TOKEN (API key, bearer)."),
    dict(name="cirrusinsight", description="Cirrus Insight sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: CIRRUSINSIGHT_BASE_URL (https://api.cirrusinsight.com) + CIRRUSINSIGHT_TOKEN (API key, bearer)."),
    dict(name="persistiq", description="PersistIQ sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /v1/campaigns. Auth: PERSISTIQ_BASE_URL (https://api.persistiq.com) + PERSISTIQ_TOKEN (API key, bearer)."),
    dict(name="autoklose", description="Autoklose sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: AUTOKLOSE_BASE_URL (https://api.autoklose.com) + AUTOKLOSE_TOKEN (API key, bearer)."),
    dict(name="woodpecker", basic=True, description="Woodpecker.co cold-email sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /rest/v1/campaign_list. Auth: WOODPECKER_BASE_URL (https://api.woodpecker.co) + WOODPECKER_TOKEN (API key as basic user)."),
    dict(name="lemlist", basic=True, description="lemlist sales engagement REST. ops get/post/put/delete (writes need confirm). e.g. /api/campaigns/{id}/leads. Auth: LEMLIST_BASE_URL (https://api.lemlist.com) + LEMLIST_TOKEN (API key as basic user)."),
    dict(name="instantly", description="Instantly.ai cold-email sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: INSTANTLY_BASE_URL (https://api.instantly.ai) + INSTANTLY_TOKEN (API key, bearer)."),
    dict(name="smartlead", description="Smartlead cold-email sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: SMARTLEAD_BASE_URL (https://server.smartlead.ai) + SMARTLEAD_TOKEN (API key)."),
    dict(name="overloop", description="Overloop (Prospect.io) sales engagement REST. ops get/post/put/delete (writes need confirm). Auth: OVERLOOP_BASE_URL (https://api.overloop.com) + OVERLOOP_TOKEN (API key, bearer)."),
    dict(name="lusha", description="Lusha contact/prospecting REST. ops get/post/put/delete (writes need confirm). e.g. /v2/person. Auth: LUSHA_BASE_URL (https://api.lusha.com) + LUSHA_TOKEN (API key)."),
    dict(name="seamlessai", description="Seamless.AI prospecting REST. ops get/post/put/delete (writes need confirm). Auth: SEAMLESSAI_BASE_URL (https://api.seamless.ai) + SEAMLESSAI_TOKEN (API key, bearer)."),
    dict(name="cognism", description="Cognism prospecting/sales intelligence REST. ops get/post/put/delete (writes need confirm). Auth: COGNISM_BASE_URL (https://app.cognism.com/api) + COGNISM_TOKEN (API key, bearer)."),
    dict(name="rocketreach", description="RocketReach prospecting REST. ops get/post/put/delete (writes need confirm). e.g. /v2/api/search. Auth: ROCKETREACH_BASE_URL (https://api.rocketreach.co) + ROCKETREACH_TOKEN (API key)."),
    dict(name="uplead", description="UpLead prospecting REST. ops get/post/put/delete (writes need confirm). Auth: UPLEAD_BASE_URL (https://api.uplead.com) + UPLEAD_TOKEN (API key)."),
    dict(name="hunter", description="Hunter.io email-finder REST. ops get/post/put/delete (writes need confirm). e.g. /v2/domain-search. Auth: HUNTER_BASE_URL (https://api.hunter.io) + HUNTER_TOKEN (API key)."),
    dict(name="leadiq", description="LeadIQ prospecting REST. ops get/post/put/delete (writes need confirm). Auth: LEADIQ_BASE_URL (https://api.leadiq.com) + LEADIQ_TOKEN (API key, bearer)."),
    dict(name="salesintel", description="SalesIntel sales intelligence REST. ops get/post/put/delete (writes need confirm). Auth: SALESINTEL_BASE_URL (https://api.salesintel.io) + SALESINTEL_TOKEN (API key, bearer)."),
    dict(name="highspot", description="Highspot sales enablement REST. ops get/post/put/delete (writes need confirm). e.g. /v1.0/items. Auth: HIGHSPOT_BASE_URL (https://api.highspot.com) + HIGHSPOT_TOKEN (API key + secret)."),
    dict(name="seismic", description="Seismic sales enablement REST. ops get/post/put/delete (writes need confirm). Auth: SEISMIC_BASE_URL (https://api.seismic.com) + SEISMIC_TOKEN (OAuth bearer token)."),
    dict(name="showpad", description="Showpad sales enablement REST. ops get/post/put/delete (writes need confirm). Auth: SHOWPAD_BASE_URL (https://api.showpad.com) + SHOWPAD_TOKEN (OAuth bearer token)."),
    dict(name="mediafly", description="Mediafly sales enablement REST. ops get/post/put/delete (writes need confirm). Auth: MEDIAFLY_BASE_URL (https://api.mediafly.com) + MEDIAFLY_TOKEN (API key, bearer)."),
    dict(name="clearslide", description="ClearSlide sales enablement REST. ops get/post/put/delete (writes need confirm). Auth: CLEARSLIDE_BASE_URL (https://api.clearslide.com) + CLEARSLIDE_TOKEN (OAuth bearer token)."),
    dict(name="livechat", description="LiveChat REST. ops get/post/put/delete (writes need confirm). e.g. /v3.5/agent/action/list_chats. Auth: LIVECHAT_BASE_URL (https://api.livechatinc.com) + LIVECHAT_TOKEN (OAuth bearer token)."),
    dict(name="tawkto", description="tawk.to chat REST. ops get/post/put/delete (writes need confirm). Auth: TAWKTO_BASE_URL (https://api.tawk.to) + TAWKTO_TOKEN (API key, bearer)."),
    dict(name="userlike", description="Userlike chat REST. ops get/post/put/delete (writes need confirm). e.g. /api/chat/v1/conversations. Auth: USERLIKE_BASE_URL (https://api.userlike.com) + USERLIKE_TOKEN (OAuth bearer token)."),
    dict(name="chatra", description="Chatra live chat REST. ops get/post/put/delete (writes need confirm). Auth: CHATRA_BASE_URL (https://app.chatra.io/api) + CHATRA_TOKEN (API key, bearer)."),
    dict(name="olark", basic=True, description="Olark live chat REST. ops get/post/put/delete (writes need confirm). e.g. /api/chats. Auth: OLARK_BASE_URL (https://api.olark.com) + OLARK_TOKEN (API key as basic user)."),
    dict(name="helpcrunch", description="HelpCrunch REST. ops get/post/put/delete (writes need confirm). Auth: HELPCRUNCH_BASE_URL (https://api.helpcrunch.com) + HELPCRUNCH_TOKEN (API key, bearer)."),
    dict(name="drift", description="Drift conversational marketing/chat REST. ops get/post/put/delete (writes need confirm). e.g. /conversations/{id}. Auth: DRIFT_BASE_URL (https://driftapi.com) + DRIFT_TOKEN (OAuth bearer token)."),
    dict(name="crisp", description="Crisp chat/support REST. ops get/post/put/delete (writes need confirm). e.g. /v1/website/{id}/conversations. Auth: CRISP_BASE_URL (https://api.crisp.chat) + CRISP_TOKEN (API identifier + key)."),
    dict(name="kayako", basic=True, description="Kayako helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/cases. Auth: KAYAKO_BASE_URL (https://{sub}.kayako.com) + KAYAKO_TOKEN (email:password basic)."),
    dict(name="deskpro", description="Deskpro helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/tickets. Auth: DESKPRO_BASE_URL (https://{instance}.deskpro.com) + DESKPRO_TOKEN (API key, bearer)."),
    dict(name="happyfox", basic=True, description="HappyFox helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/1.1/json/tickets/. Auth: HAPPYFOX_BASE_URL (https://{sub}.happyfox.com) + HAPPYFOX_TOKEN (API key + auth code, basic)."),
    dict(name="teamsupport", basic=True, description="TeamSupport helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/tickets. Auth: TEAMSUPPORT_BASE_URL (https://app.teamsupport.com/api) + TEAMSUPPORT_TOKEN (API key as basic user)."),
    dict(name="visionhelpdesk", description="Vision Helpdesk REST. ops get/post/put/delete (writes need confirm). Auth: VISIONHELPDESK_BASE_URL (https://{sub}.visionhelpdesk.com) + VISIONHELPDESK_TOKEN (API key)."),
    dict(name="liveagent", token_header="apikey", scheme="", description="LiveAgent helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/tickets. Auth: LIVEAGENT_BASE_URL (https://{sub}.ladesk.com) + LIVEAGENT_TOKEN (API key header)."),
    dict(name="uservoice", description="UserVoice REST. ops get/post/put/delete (writes need confirm). Auth: USERVOICE_BASE_URL (https://{sub}.uservoice.com) + USERVOICE_TOKEN (OAuth bearer token)."),
    dict(name="supportpal", description="SupportPal helpdesk REST (self-hosted). ops get/post/put/delete (writes need confirm). e.g. /api/index.php/ticket. Auth: SUPPORTPAL_BASE_URL (https://{host}) + SUPPORTPAL_TOKEN (API key, bearer)."),
    dict(name="gorgias", basic=True, description="Gorgias helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/tickets. Auth: GORGIAS_BASE_URL (https://{sub}.gorgias.com) + GORGIAS_TOKEN (email:API key basic)."),
    dict(name="reamaze", basic=True, description="Re:amaze helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/conversations. Auth: REAMAZE_BASE_URL (https://{sub}.reamaze.io) + REAMAZE_TOKEN (email:API token basic)."),
    dict(name="richpanel", description="Richpanel helpdesk REST. ops get/post/put/delete (writes need confirm). Auth: RICHPANEL_BASE_URL (https://api.richpanel.com) + RICHPANEL_TOKEN (API key, bearer)."),
    dict(name="jitbit", basic=True, description="Jitbit Helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /helpdesk/api/tickets. Auth: JITBIT_BASE_URL (https://{sub}.jitbit.com) + JITBIT_TOKEN (API key as basic user)."),
    dict(name="osticket", token_header="X-API-Key", scheme="", description="osTicket REST (self-hosted). ops get/post/put/delete (writes need confirm). e.g. /api/tickets.json. Auth: OSTICKET_BASE_URL (https://{host}) + OSTICKET_TOKEN (API key header)."),
    dict(name="supportbee", basic=True, description="SupportBee helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /tickets.json. Auth: SUPPORTBEE_BASE_URL (https://{company}.supportbee.com) + SUPPORTBEE_TOKEN (API token as basic user)."),
    dict(name="spiceworks", description="Spiceworks Cloud Help Desk REST. ops get/post/put/delete (writes need confirm). Auth: SPICEWORKS_BASE_URL (https://api.spiceworks.com) + SPICEWORKS_TOKEN (OAuth bearer token)."),
    dict(name="zendesk_qa", description="Zendesk QA (Klaus) conversation review REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/reviews. Auth: ZENDESK_QA_BASE_URL (https://api.zendesk.com/qa) + ZENDESK_QA_TOKEN (API key, bearer)."),
    dict(name="maestroqa", description="MaestroQA contact-center QA REST. ops get/post/put/delete (writes need confirm). Auth: MAESTROQA_BASE_URL (https://api.maestroqa.com) + MAESTROQA_TOKEN (API key, bearer)."),
    dict(name="loris", description="Loris customer conversation intelligence REST. ops get/post/put/delete (writes need confirm). Auth: LORIS_BASE_URL (https://api.loris.ai) + LORIS_TOKEN (API key, bearer)."),
    dict(name="forethought", description="Forethought AI customer support REST. ops get/post/put/delete (writes need confirm). Auth: FORETHOUGHT_BASE_URL (https://api.forethought.ai) + FORETHOUGHT_TOKEN (API key, bearer)."),
    dict(name="ada", description="Ada support chatbot REST. ops get/post/put/delete (writes need confirm). Auth: ADA_BASE_URL (https://{handle}.ada.support) + ADA_TOKEN (API key, bearer)."),
    dict(name="solvvy", description="Solvvy (Zoom Virtual Agent) support automation REST. ops get/post/put/delete (writes need confirm). Auth: SOLVVY_BASE_URL (https://api.solvvy.com) + SOLVVY_TOKEN (API key, bearer)."),
    dict(name="aisera", description="Aisera AI service desk REST. ops get/post/put/delete (writes need confirm). Auth: AISERA_BASE_URL (https://api.aisera.com) + AISERA_TOKEN (OAuth bearer token)."),
    dict(name="moveworks", description="Moveworks AI support/service desk REST. ops get/post/put/delete (writes need confirm). Auth: MOVEWORKS_BASE_URL (https://api.moveworks.ai) + MOVEWORKS_TOKEN (OAuth bearer token)."),
    dict(name="khoros", description="Khoros social/community customer engagement REST. ops get/post/put/delete (writes need confirm). Auth: KHOROS_BASE_URL (https://api.khoros.com) + KHOROS_TOKEN (OAuth bearer token)."),
    dict(name="yellowai", description="Yellow.ai conversational support REST. ops get/post/put/delete (writes need confirm). Auth: YELLOWAI_BASE_URL (https://api.yellow.ai) + YELLOWAI_TOKEN (API key, bearer)."),
    dict(name="totango", description="Totango customer success REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/activities. Auth: TOTANGO_BASE_URL (https://api.totango.com) + TOTANGO_TOKEN (API key, bearer)."),
    dict(name="churnzero", description="ChurnZero customer success REST. ops get/post/put/delete (writes need confirm). e.g. /api/accounts. Auth: CHURNZERO_BASE_URL (https://analytics.churnzero.net) + CHURNZERO_TOKEN (API key)."),
    dict(name="planhat", description="Planhat customer success REST. ops get/post/put/delete (writes need confirm). e.g. /companies. Auth: PLANHAT_BASE_URL (https://api.planhat.com) + PLANHAT_TOKEN (API key, bearer)."),
    dict(name="vitally", description="Vitally customer success REST. ops get/post/put/delete (writes need confirm). e.g. /resources/accounts. Auth: VITALLY_BASE_URL (https://{org}.rest.vitally.io) + VITALLY_TOKEN (API key, bearer)."),
    dict(name="custify", description="Custify customer success REST. ops get/post/put/delete (writes need confirm). Auth: CUSTIFY_BASE_URL (https://api.custify.com) + CUSTIFY_TOKEN (API key, bearer)."),
    dict(name="clientsuccess", description="ClientSuccess customer success REST. ops get/post/put/delete (writes need confirm). Auth: CLIENTSUCCESS_BASE_URL (https://api.clientsuccess.com) + CLIENTSUCCESS_TOKEN (API key, bearer)."),
    dict(name="akita", description="Akita customer success REST. ops get/post/put/delete (writes need confirm). Auth: AKITA_BASE_URL (https://api.akitaapp.com) + AKITA_TOKEN (API key, bearer)."),
    dict(name="catalyst", description="Catalyst customer success REST. ops get/post/put/delete (writes need confirm). Auth: CATALYST_BASE_URL (https://api.catalyst.io) + CATALYST_TOKEN (API key, bearer)."),
    dict(name="freshsales", basic=True, description="Freshsales (Freshworks CRM) REST. ops get/post/put/delete (writes need confirm). e.g. /api/contacts. Auth: FRESHSALES_BASE_URL (https://{sub}.myfreshworks.com/crm/sales) + FRESHSALES_TOKEN (API key)."),
    dict(name="freshcaller", description="Freshcaller (Freshworks contact center) REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/calls. Auth: FRESHCALLER_BASE_URL (https://{sub}.freshcaller.com) + FRESHCALLER_TOKEN (API key)."),
    dict(name="zohodesk", description="Zoho Desk helpdesk REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/tickets. Auth: ZOHODESK_BASE_URL (https://desk.zoho.com) + ZOHODESK_TOKEN (OAuth bearer token)."),
    dict(name="cloudtalk", basic=True, description="CloudTalk contact center REST. ops get/post/put/delete (writes need confirm). e.g. /api/calls.json. Auth: CLOUDTALK_BASE_URL (https://my.cloudtalk.io/api) + CLOUDTALK_TOKEN (API key + secret, basic)."),
    dict(name="ozonetel", description="Ozonetel contact center REST. ops get/post/put/delete (writes need confirm). Auth: OZONETEL_BASE_URL (https://api.ozonetel.com) + OZONETEL_TOKEN (API key, bearer)."),
    dict(name="knowlarity", description="Knowlarity contact center REST. ops get/post/put/delete (writes need confirm). Auth: KNOWLARITY_BASE_URL (https://kpi.knowlarity.com) + KNOWLARITY_TOKEN (API key, bearer)."),
    dict(name="exotel", description="Exotel contact center REST. ops get/post/put/delete (writes need confirm). e.g. /v1/Accounts/{sid}/Calls. Auth: EXOTEL_BASE_URL (https://api.exotel.com) + EXOTEL_TOKEN (API key + token, basic)."),
    dict(name="ameyo", description="Ameyo contact center REST. ops get/post/put/delete (writes need confirm). Auth: AMEYO_BASE_URL (https://{instance}.ameyo.com) + AMEYO_TOKEN (OAuth bearer token)."),
    dict(name="eightx8", description="8x8 contact center/CPaaS REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/calls. Auth: EIGHTX8_BASE_URL (https://api.8x8.com) + EIGHTX8_TOKEN (OAuth bearer token)."),
    dict(name="kixie", description="Kixie contact center/dialer REST. ops get/post/put/delete (writes need confirm). e.g. /v1/calls. Auth: KIXIE_BASE_URL (https://apig.kixie.com) + KIXIE_TOKEN (API key + business ID)."),
    dict(name="justcall", description="JustCall contact center REST. ops get/post/put/delete (writes need confirm). e.g. /v1/calls. Auth: JUSTCALL_BASE_URL (https://api.justcall.io) + JUSTCALL_TOKEN (API key + secret)."),
    dict(name="openphone", description="OpenPhone business phone REST. ops get/post/put/delete (writes need confirm). e.g. /v1/calls. Auth: OPENPHONE_BASE_URL (https://api.openphone.com) + OPENPHONE_TOKEN (API key)."),
    dict(name="grasshopper", description="Grasshopper business phone REST. ops get/post/put/delete (writes need confirm). Auth: GRASSHOPPER_BASE_URL (https://api.grasshopper.com) + GRASSHOPPER_TOKEN (OAuth bearer token)."),
    dict(name="callrail", basic=True, description="CallRail call tracking REST. ops get/post/put/delete (writes need confirm). e.g. /v3/a/{account_id}/calls.json. Auth: CALLRAIL_BASE_URL (https://api.callrail.com) + CALLRAIL_TOKEN (API key as basic user)."),
    dict(name="playvox", description="Playvox contact-center QA/workforce REST. ops get/post/put/delete (writes need confirm). Auth: PLAYVOX_BASE_URL (https://api.playvox.com) + PLAYVOX_TOKEN (OAuth bearer token)."),
    dict(name="assembled", description="Assembled contact-center workforce management REST. ops get/post/put/delete (writes need confirm). Auth: ASSEMBLED_BASE_URL (https://api.assembled.com) + ASSEMBLED_TOKEN (API key, bearer)."),
    dict(name="verint", description="Verint workforce engagement/contact center REST. ops get/post/put/delete (writes need confirm). Auth: VERINT_BASE_URL (https://api.verint.com) + VERINT_TOKEN (OAuth bearer token)."),
    dict(name="calabrio", description="Calabrio workforce management (contact center) REST. ops get/post/put/delete (writes need confirm). Auth: CALABRIO_BASE_URL (https://api.calabrio.com) + CALABRIO_TOKEN (OAuth bearer token)."),
    dict(name="contentguru", description="Content Guru (storm) contact center REST. ops get/post/put/delete (writes need confirm). Auth: CONTENTGURU_BASE_URL (https://api.puzzel.com) + CONTENTGURU_TOKEN (API key, bearer)."),
    dict(name="ujet", description="UJET contact center REST. ops get/post/put/delete (writes need confirm). Auth: UJET_BASE_URL (https://api.ujet.co) + UJET_TOKEN (OAuth bearer token)."),
    dict(name="zappix", description="Zappix visual IVR/contact center REST. ops get/post/put/delete (writes need confirm). Auth: ZAPPIX_BASE_URL (https://api.zappix.com) + ZAPPIX_TOKEN (API key, bearer)."),
    dict(name="replicant", description="Replicant AI contact center REST. ops get/post/put/delete (writes need confirm). Auth: REPLICANT_BASE_URL (https://api.replicant.ai) + REPLICANT_TOKEN (API key, bearer)."),
    dict(name="cresta", description="Cresta contact-center AI coaching REST. ops get/post/put/delete (writes need confirm). Auth: CRESTA_BASE_URL (https://api.cresta.com) + CRESTA_TOKEN (OAuth bearer token)."),
    dict(name="tethr", description="Tethr conversation analytics REST. ops get/post/put/delete (writes need confirm). Auth: TETHR_BASE_URL (https://api.tethr.com) + TETHR_TOKEN (API key, bearer)."),
    dict(name="invoca", description="Invoca call tracking/revenue attribution REST. ops get/post/put/delete (writes need confirm). e.g. /v1/networks/{id}/calls. Auth: INVOCA_BASE_URL (https://api.invoca.net) + INVOCA_TOKEN (OAuth bearer token)."),
    dict(name="marchex", description="Marchex call analytics REST. ops get/post/put/delete (writes need confirm). Auth: MARCHEX_BASE_URL (https://api.marchex.com) + MARCHEX_TOKEN (API key, bearer)."),
    dict(name="threeclogic", description="3CLogic contact center (ServiceNow-integrated) REST. ops get/post/put/delete (writes need confirm). Auth: THREECLOGIC_BASE_URL (https://api.3clogic.com) + THREECLOGIC_TOKEN (OAuth bearer token)."),
    dict(name="revenuegrid", description="Revenue Grid revenue-intelligence REST. ops get/post/put/delete (writes need confirm). Auth: REVENUEGRID_BASE_URL (https://api.revenuegrid.com) + REVENUEGRID_TOKEN (OAuth bearer token)."),
    dict(name="aviso", description="Aviso revenue intelligence/forecasting REST. ops get/post/put/delete (writes need confirm). Auth: AVISO_BASE_URL (https://api.aviso.com) + AVISO_TOKEN (API key, bearer)."),
    dict(name="boostup", description="BoostUp revenue intelligence REST. ops get/post/put/delete (writes need confirm). Auth: BOOSTUP_BASE_URL (https://api.boostup.ai) + BOOSTUP_TOKEN (API key, bearer)."),
    dict(name="peopleai", description="People.ai revenue intelligence REST. ops get/post/put/delete (writes need confirm). Auth: PEOPLEAI_BASE_URL (https://api.people.ai) + PEOPLEAI_TOKEN (OAuth bearer token)."),
    dict(name="insightsquared", description="InsightSquared (Mediafly) revenue analytics REST. ops get/post/put/delete (writes need confirm). Auth: INSIGHTSQUARED_BASE_URL (https://api.insightsquared.com) + INSIGHTSQUARED_TOKEN (API key, bearer)."),
    dict(name="chorus", description="Chorus.ai (ZoomInfo) conversation intelligence REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/calls. Auth: CHORUS_BASE_URL (https://api.chorus.ai) + CHORUS_TOKEN (API key, bearer)."),
    dict(name="avoma", description="Avoma meeting/revenue intelligence REST. ops get/post/put/delete (writes need confirm). e.g. /v1/meetings. Auth: AVOMA_BASE_URL (https://api.avoma.com) + AVOMA_TOKEN (API key)."),
    dict(name="jiminny", description="Jiminny conversation intelligence REST. ops get/post/put/delete (writes need confirm). Auth: JIMINNY_BASE_URL (https://api.jiminny.com) + JIMINNY_TOKEN (API key, bearer)."),
    dict(name="observeai", description="Observe.AI contact-center conversation intelligence REST. ops get/post/put/delete (writes need confirm). Auth: OBSERVEAI_BASE_URL (https://api.observe.ai) + OBSERVEAI_TOKEN (OAuth bearer token)."),
    dict(name="callminer", description="CallMiner conversation analytics REST. ops get/post/put/delete (writes need confirm). Auth: CALLMINER_BASE_URL (https://api.callminer.net) + CALLMINER_TOKEN (OAuth bearer token)."),
    dict(name="balto", description="Balto real-time call guidance REST. ops get/post/put/delete (writes need confirm). Auth: BALTO_BASE_URL (https://api.balto.ai) + BALTO_TOKEN (API key, bearer)."),
    dict(name="execvision", description="ExecVision conversation intelligence REST. ops get/post/put/delete (writes need confirm). Auth: EXECVISION_BASE_URL (https://api.execvision.io) + EXECVISION_TOKEN (API key, bearer)."),
    dict(name="zoho_analytics", description="Zoho Analytics REST. ops get/post/put/delete (writes need confirm). Reports/dashboards/data API. Auth: ZOHO_ANALYTICS_BASE_URL (https://analyticsapi.zoho.com) + ZOHO_ANALYTICS_TOKEN (OAuth bearer)."),
    dict(name="yellowfin", description="Yellowfin BI REST API. ops get/post/put/delete (writes need confirm). Reports/dashboards. Auth: YELLOWFIN_BASE_URL (self-hosted or cloud instance URL) + YELLOWFIN_TOKEN."),
    dict(name="pentaho", description="Pentaho BI/BA Server REST. ops get/post/put/delete (writes need confirm). Auth: PENTAHO_BASE_URL (server URL) + PENTAHO_TOKEN."),
    dict(name="birst", description="Birst (Infor) BI REST API. ops get/post/put/delete (writes need confirm). Spaces/queries/data upload. Auth: BIRST_BASE_URL (https://login.bws.birst.com) + BIRST_TOKEN (OAuth bearer)."),
    dict(name="gooddata", description="GoodData Cloud REST/GraphQL. ops get/post/put/delete (writes need confirm). Workspaces/insights. Auth: GOODDATA_BASE_URL (https://{org}.cloud.gooddata.com) + GOODDATA_TOKEN (API token)."),
    dict(name="redash", description="Redash REST API. ops get/post/put/delete (writes need confirm). Queries/dashboards. Auth: REDASH_BASE_URL (instance URL) + REDASH_TOKEN (API key)."),
    dict(name="superset", description="Apache Superset REST API. ops get/post/put/delete (writes need confirm). Charts/dashboards/datasets. Auth: SUPERSET_BASE_URL (instance URL) + SUPERSET_TOKEN (JWT bearer)."),
    dict(name="klipfolio", description="Klipfolio REST API. ops get/post/put/delete (writes need confirm). Klips/data sources. Auth: KLIPFOLIO_BASE_URL (https://app.klipfolio.com) + KLIPFOLIO_TOKEN (API key)."),
    dict(name="databox", description="Databox REST API. ops get/post/put/delete (writes need confirm). Metrics/datablocks. Auth: DATABOX_BASE_URL (https://api.databox.com) + DATABOX_TOKEN (push token / OAuth bearer)."),
    dict(name="geckoboard", description="Geckoboard REST API. ops get/post/put/delete (writes need confirm). Datasets/widgets. Auth: GECKOBOARD_BASE_URL (https://api.geckoboard.com) + GECKOBOARD_TOKEN (API key)."),
    dict(name="looker_studio", description="Google Looker Studio (Data Studio) REST/reporting API. ops get/post/put/delete (writes need confirm). Auth: LOOKER_STUDIO_BASE_URL (https://datastudio.googleapis.com) + LOOKER_STUDIO_TOKEN (OAuth bearer)."),
    dict(name="sap_businessobjects", description="SAP BusinessObjects BI Platform REST API. ops get/post/put/delete (writes need confirm). Reports/folders. Auth: SAP_BUSINESSOBJECTS_BASE_URL (BOE server URL) + SAP_BUSINESSOBJECTS_TOKEN."),
    dict(name="oracle_analytics", description="Oracle Analytics Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: ORACLE_ANALYTICS_BASE_URL (instance URL) + ORACLE_ANALYTICS_TOKEN (OAuth bearer)."),
    dict(name="alteryx", description="Alteryx Server/Analytics Cloud REST API. ops get/post/put/delete (writes need confirm). Workflows/jobs. Auth: ALTERYX_BASE_URL (server or cloud URL) + ALTERYX_TOKEN."),
    dict(name="knime", description="KNIME Business Hub REST API. ops get/post/put/delete (writes need confirm). Workflows/jobs. Auth: KNIME_BASE_URL (hub URL) + KNIME_TOKEN."),
    dict(name="rapidminer", description="RapidMiner (Altair AI Hub) REST API. ops get/post/put/delete (writes need confirm). Processes/jobs. Auth: RAPIDMINER_BASE_URL (hub URL) + RAPIDMINER_TOKEN."),
    dict(name="sigma_computing", description="Sigma Computing REST API. ops get/post/put/delete (writes need confirm). Workbooks/datasets. Auth: SIGMA_COMPUTING_BASE_URL (https://api.sigmacomputing.com) + SIGMA_COMPUTING_TOKEN (OAuth bearer)."),
    dict(name="preset", description="Preset (hosted Superset) REST API. ops get/post/put/delete (writes need confirm). Charts/dashboards. Auth: PRESET_BASE_URL (https://api.app.preset.io) + PRESET_TOKEN (API token)."),
    dict(name="hex", description="Hex REST API. ops get/post/put/delete (writes need confirm). Projects/runs. Auth: HEX_BASE_URL (https://app.hex.tech) + HEX_TOKEN (API token)."),
    dict(name="deepnote", description="Deepnote REST API. ops get/post/put/delete (writes need confirm). Projects/notebooks. Auth: DEEPNOTE_BASE_URL (https://api.deepnote.com) + DEEPNOTE_TOKEN."),
    dict(name="observable", description="Observable (Framework/Cloud) REST API. ops get/post/put/delete (writes need confirm). Notebooks/data. Auth: OBSERVABLE_BASE_URL (https://api.observablehq.com) + OBSERVABLE_TOKEN."),
    dict(name="quicksight", description="Amazon QuickSight REST API. ops get/post/put/delete (writes need confirm). Dashboards/datasets/analyses. Auth: QUICKSIGHT_BASE_URL (AWS regional endpoint) + QUICKSIGHT_TOKEN (SigV4/OAuth bearer)."),
    dict(name="spotfire", description="TIBCO Spotfire REST API. ops get/post/put/delete (writes need confirm). Analysis files/library. Auth: SPOTFIRE_BASE_URL (server URL) + SPOTFIRE_TOKEN."),
    dict(name="pyramid_analytics", description="Pyramid Analytics REST API. ops get/post/put/delete (writes need confirm). Auth: PYRAMID_ANALYTICS_BASE_URL (instance URL) + PYRAMID_ANALYTICS_TOKEN (OAuth bearer)."),
    dict(name="grow", description="Grow.com BI REST API. ops get/post/put/delete (writes need confirm). Metrics/data sources. Auth: GROW_BASE_URL (https://app.grow.com) + GROW_TOKEN (API key)."),
    dict(name="count_co", description="Count (count.co) REST/GraphQL API. ops get/post/put/delete (writes need confirm). Canvases/queries. Auth: COUNT_CO_BASE_URL (https://count.co) + COUNT_CO_TOKEN."),
    dict(name="boardintl", description="Board International BI/CPM REST API. ops get/post/put/delete (writes need confirm). Auth: BOARDINTL_BASE_URL (server URL) + BOARDINTL_TOKEN."),
    dict(name="stitchdata", description="Stitch (Talend) data pipeline REST API. ops get/post/put/delete (writes need confirm). Sources/destinations. Auth: STITCHDATA_BASE_URL (https://api.stitchdata.com) + STITCHDATA_TOKEN (API key)."),
    dict(name="hevo", description="Hevo Data pipeline REST API. ops get/post/put/delete (writes need confirm). Pipelines/sources. Auth: HEVO_BASE_URL (https://us.hevodata.com/api) + HEVO_TOKEN."),
    dict(name="airbyte", description="Airbyte REST API. ops get/post/put/delete (writes need confirm). Connections/sources/destinations/syncs. Auth: AIRBYTE_BASE_URL (https://api.airbyte.com) + AIRBYTE_TOKEN (OAuth bearer)."),
    dict(name="meltano", description="Meltano REST API. ops get/post/put/delete (writes need confirm). Projects/pipelines. Auth: MELTANO_BASE_URL (instance URL) + MELTANO_TOKEN."),
    dict(name="rivery", description="Rivery data pipeline REST API. ops get/post/put/delete (writes need confirm). Rivers/runs. Auth: RIVERY_BASE_URL (https://console.rivery.io/api) + RIVERY_TOKEN."),
    dict(name="portable", description="Portable.io ETL REST API. ops get/post/put/delete (writes need confirm). Connectors/runs. Auth: PORTABLE_BASE_URL (https://api.portable.io) + PORTABLE_TOKEN."),
    dict(name="estuary", description="Estuary Flow REST API. ops get/post/put/delete (writes need confirm). Captures/materializations. Auth: ESTUARY_BASE_URL (https://api.estuary.dev) + ESTUARY_TOKEN."),
    dict(name="streamsets", description="StreamSets (DataOps) REST API. ops get/post/put/delete (writes need confirm). Pipelines/jobs. Auth: STREAMSETS_BASE_URL (control hub URL) + STREAMSETS_TOKEN."),
    dict(name="nifi", description="Apache NiFi REST API. ops get/post/put/delete (writes need confirm). Flows/processors. Auth: NIFI_BASE_URL (instance URL) + NIFI_TOKEN (bearer, via NiFi Registry auth)."),
    dict(name="singer", description="Singer.io tap/target orchestration REST API (e.g. via Meltano hub). ops get/post/put/delete (writes need confirm). Auth: SINGER_BASE_URL (orchestrator URL) + SINGER_TOKEN."),
    dict(name="luigi", description="Luigi task pipeline visualizer/central scheduler REST API. ops get/post/put/delete (writes need confirm). Auth: LUIGI_BASE_URL (scheduler URL) + LUIGI_TOKEN."),
    dict(name="prefect", description="Prefect Cloud/Server REST API. ops get/post/put/delete (writes need confirm). Flows/deployments/runs. Auth: PREFECT_BASE_URL (https://api.prefect.cloud) + PREFECT_TOKEN (API key)."),
    dict(name="dagster", description="Dagster Cloud GraphQL API. ops get/post/put/delete (writes need confirm). Jobs/runs/assets. Auth: DAGSTER_BASE_URL (https://{org}.dagster.cloud) + DAGSTER_TOKEN (user token)."),
    dict(name="keboola", description="Keboola Connection REST API. ops get/post/put/delete (writes need confirm). Buckets/tables/orchestrations. Auth: KEBOOLA_BASE_URL (https://connection.keboola.com) + KEBOOLA_TOKEN (storage API token)."),
    dict(name="panoply", description="Panoply (SQream) data platform REST API. ops get/post/put/delete (writes need confirm). Auth: PANOPLY_BASE_URL (https://app.panoply.io/api) + PANOPLY_TOKEN."),
    dict(name="etleap", description="Etleap ETL REST API. ops get/post/put/delete (writes need confirm). Pipelines/sources. Auth: ETLEAP_BASE_URL (instance URL) + ETLEAP_TOKEN."),
    dict(name="cloverdx", description="CloverDX (CloverETL) REST API. ops get/post/put/delete (writes need confirm). Jobs/tasks. Auth: CLOVERDX_BASE_URL (server URL) + CLOVERDX_TOKEN."),
    dict(name="snaplogic", description="SnapLogic REST API. ops get/post/put/delete (writes need confirm). Pipelines/tasks. Auth: SNAPLOGIC_BASE_URL (https://elastic.snaplogic.com) + SNAPLOGIC_TOKEN (bearer)."),
    dict(name="jitterbit", description="Jitterbit Harmony REST API. ops get/post/put/delete (writes need confirm). Operations/environments. Auth: JITTERBIT_BASE_URL (https://api.jitterbit.com) + JITTERBIT_TOKEN (OAuth bearer)."),
    dict(name="trayio", description="Tray.io (Tray Platform) REST API. ops get/post/put/delete (writes need confirm). Workflows. Auth: TRAYIO_BASE_URL (https://api.tray.io) + TRAYIO_TOKEN."),
    dict(name="celigo", description="Celigo Integrator.io REST API. ops get/post/put/delete (writes need confirm). Flows/connections. Auth: CELIGO_BASE_URL (https://api.integrator.io) + CELIGO_TOKEN."),
    dict(name="skyvia", description="Skyvia data integration REST API. ops get/post/put/delete (writes need confirm). Packages/connections. Auth: SKYVIA_BASE_URL (https://app.skyvia.com/api) + SKYVIA_TOKEN."),
    dict(name="hightouch", description="Hightouch (reverse ETL) REST API. ops get/post/put/delete (writes need confirm). Syncs/models/sources. Auth: HIGHTOUCH_BASE_URL (https://api.hightouch.com) + HIGHTOUCH_TOKEN (API key)."),
    dict(name="rudderstack", description="RudderStack REST/HTTP source API. ops get/post/put/delete (writes need confirm). Events/sources/destinations. Auth: RUDDERSTACK_BASE_URL (workspace or self-hosted URL) + RUDDERSTACK_TOKEN (write key)."),
    dict(name="mparticle", description="mParticle CDP REST API. ops get/post/put/delete (writes need confirm). Events/feeds/audiences. Auth: MPARTICLE_BASE_URL (https://api.mparticle.com) + MPARTICLE_TOKEN (API key/secret)."),
    dict(name="polytomic", description="Polytomic (reverse ETL / sync) REST API. ops get/post/put/delete (writes need confirm). Syncs/connections. Auth: POLYTOMIC_BASE_URL (https://app.polytomic.com/api) + POLYTOMIC_TOKEN."),
    dict(name="snowplow", description="Snowplow Analytics REST/collector API. ops get/post/put/delete (writes need confirm). Auth: SNOWPLOW_BASE_URL (collector endpoint) + SNOWPLOW_TOKEN."),
    dict(name="apache_hop", description="Apache Hop orchestration REST API. ops get/post/put/delete (writes need confirm). Pipelines/workflows. Auth: APACHE_HOP_BASE_URL (server URL) + APACHE_HOP_TOKEN."),
    dict(name="wordpress", basic=True, description="WordPress REST API. ops get/post/put/delete (writes need confirm). e.g. /wp-json/wp/v2/posts. Auth: WORDPRESS_BASE_URL (https://{site}) + WORDPRESS_TOKEN (application password, basic auth)."),
    dict(name="drupal", description="Drupal JSON:API. ops get/post/put/delete (writes need confirm). e.g. /jsonapi/node/article. Auth: DRUPAL_BASE_URL (site URL) + DRUPAL_TOKEN (OAuth bearer)."),
    dict(name="joomla", description="Joomla REST API (com_api). ops get/post/put/delete (writes need confirm). Auth: JOOMLA_BASE_URL (site URL) + JOOMLA_TOKEN."),
    dict(name="squarespace", description="Squarespace Commerce/Content REST API. ops get/post/put/delete (writes need confirm). Auth: SQUARESPACE_BASE_URL (https://api.squarespace.com) + SQUARESPACE_TOKEN (API key)."),
    dict(name="webflow", description="Webflow REST API v2. ops get/post/put/delete (writes need confirm). Sites/collections/items. Auth: WEBFLOW_BASE_URL (https://api.webflow.com) + WEBFLOW_TOKEN (OAuth bearer)."),
    dict(name="contentful", description="Contentful Content Delivery/Management REST API. ops get/post/put/delete (writes need confirm). Spaces/entries/assets. Auth: CONTENTFUL_BASE_URL (https://api.contentful.com) + CONTENTFUL_TOKEN (CMA/CDA token)."),
    dict(name="sanity", description="Sanity.io Content API. ops get/post/put/delete (writes need confirm). Datasets/documents. Auth: SANITY_BASE_URL (https://{project}.api.sanity.io) + SANITY_TOKEN."),
    dict(name="strapi", description="Strapi headless CMS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/{content-type}. Auth: STRAPI_BASE_URL (instance URL) + STRAPI_TOKEN (API token)."),
    dict(name="prismic", description="Prismic headless CMS REST API. ops get/post/put/delete (writes need confirm). Documents/releases. Auth: PRISMIC_BASE_URL (https://{repo}.cdn.prismic.io) + PRISMIC_TOKEN (access token)."),
    dict(name="contentstack", description="Contentstack Content Management REST API. ops get/post/put/delete (writes need confirm). Auth: CONTENTSTACK_BASE_URL (https://api.contentstack.io) + CONTENTSTACK_TOKEN (management token)."),
    dict(name="storyblok", description="Storyblok headless CMS REST API. ops get/post/put/delete (writes need confirm). Stories/components. Auth: STORYBLOK_BASE_URL (https://mapi.storyblok.com) + STORYBLOK_TOKEN (OAuth token)."),
    dict(name="kentico", description="Kentico Xperience REST API. ops get/post/put/delete (writes need confirm). Auth: KENTICO_BASE_URL (instance URL) + KENTICO_TOKEN (OAuth bearer)."),
    dict(name="umbraco", description="Umbraco Content Delivery/Management REST API. ops get/post/put/delete (writes need confirm). Auth: UMBRACO_BASE_URL (site URL) + UMBRACO_TOKEN."),
    dict(name="sitecore", description="Sitecore Content Hub / Experience Management REST API. ops get/post/put/delete (writes need confirm). Auth: SITECORE_BASE_URL (instance URL) + SITECORE_TOKEN (OAuth bearer)."),
    dict(name="ghost", description="Ghost CMS Admin/Content REST API. ops get/post/put/delete (writes need confirm). Posts/pages/members. Auth: GHOST_BASE_URL (https://{site}/ghost/api) + GHOST_TOKEN (Admin API key, JWT signed)."),
    dict(name="buttercms", description="ButterCMS REST API. ops get/post/put/delete (writes need confirm). Pages/collections/posts. Auth: BUTTERCMS_BASE_URL (https://api.buttercms.com) + BUTTERCMS_TOKEN (API token)."),
    dict(name="datocms", description="DatoCMS Content Management/Delivery API (REST + GraphQL). ops get/post/put/delete (writes need confirm). Auth: DATOCMS_BASE_URL (https://site-api.datocms.com) + DATOCMS_TOKEN (API token)."),
    dict(name="hygraph", description="Hygraph (GraphCMS) GraphQL/Management API. ops get/post/put/delete (writes need confirm). Auth: HYGRAPH_BASE_URL (project management endpoint) + HYGRAPH_TOKEN (permanent auth token)."),
    dict(name="craftcms", description="Craft CMS REST/GraphQL API. ops get/post/put/delete (writes need confirm). Auth: CRAFTCMS_BASE_URL (site URL) + CRAFTCMS_TOKEN."),
    dict(name="directus", description="Directus headless CMS REST API. ops get/post/put/delete (writes need confirm). Collections/items. Auth: DIRECTUS_BASE_URL (instance URL) + DIRECTUS_TOKEN (static/OAuth token)."),
    dict(name="cockpitcms", description="Cockpit CMS REST API. ops get/post/put/delete (writes need confirm). Collections/content. Auth: COCKPITCMS_BASE_URL (instance URL) + COCKPITCMS_TOKEN (API key)."),
    dict(name="agilitycms", description="Agility CMS REST API. ops get/post/put/delete (writes need confirm). Content items/pages. Auth: AGILITYCMS_BASE_URL (https://api.agilitycms.com) + AGILITYCMS_TOKEN."),
    dict(name="kontent", description="Kontent.ai (Kentico Cloud) Content Management API. ops get/post/put/delete (writes need confirm). Auth: KONTENT_BASE_URL (https://manage.kontent.ai) + KONTENT_TOKEN."),
    dict(name="bloomreach", description="Bloomreach Content/Discovery REST API. ops get/post/put/delete (writes need confirm). Auth: BLOOMREACH_BASE_URL (instance URL) + BLOOMREACH_TOKEN (API key)."),
    dict(name="optimizely", description="Optimizely Content Cloud / Experimentation REST API. ops get/post/put/delete (writes need confirm). Auth: OPTIMIZELY_BASE_URL (https://api.optimizely.com) + OPTIMIZELY_TOKEN."),
    dict(name="acquia", description="Acquia Cloud Platform REST API. ops get/post/put/delete (writes need confirm). Sites/environments/deployments. Auth: ACQUIA_BASE_URL (https://cloud.acquia.com/api) + ACQUIA_TOKEN (OAuth bearer)."),
    dict(name="decapcms", description="Decap CMS (Netlify CMS) git-backed content API. ops get/post/put/delete (writes need confirm). Auth: DECAPCMS_BASE_URL (git provider API URL) + DECAPCMS_TOKEN."),
    dict(name="webnode", description="Webnode website builder REST API. ops get/post/put/delete (writes need confirm). Auth: WEBNODE_BASE_URL (account URL) + WEBNODE_TOKEN."),
    dict(name="weebly", description="Weebly (Square Online) REST API. ops get/post/put/delete (writes need confirm). Sites/pages. Auth: WEEBLY_BASE_URL (https://api.weebly.com) + WEEBLY_TOKEN (OAuth bearer)."),
    dict(name="jimdo", description="Jimdo website builder REST API. ops get/post/put/delete (writes need confirm). Auth: JIMDO_BASE_URL (account URL) + JIMDO_TOKEN."),
    dict(name="godaddy_builder", description="GoDaddy Websites + Marketing REST API. ops get/post/put/delete (writes need confirm). Auth: GODADDY_BUILDER_BASE_URL (https://api.godaddy.com) + GODADDY_BUILDER_TOKEN (API key/secret)."),
    dict(name="typo3", description="TYPO3 CMS REST API (extension-based). ops get/post/put/delete (writes need confirm). Auth: TYPO3_BASE_URL (site URL) + TYPO3_TOKEN."),
    dict(name="concretecms", description="Concrete CMS REST API. ops get/post/put/delete (writes need confirm). Pages/files. Auth: CONCRETECMS_BASE_URL (site URL) + CONCRETECMS_TOKEN (OAuth bearer)."),
    dict(name="wagtail", description="Wagtail CMS REST API (wagtail.api.v2). ops get/post/put/delete (writes need confirm). Pages/documents/images. Auth: WAGTAIL_BASE_URL (site URL) + WAGTAIL_TOKEN."),
    dict(name="pimcore", description="Pimcore PIM/DXP REST API. ops get/post/put/delete (writes need confirm). Objects/assets/documents. Auth: PIMCORE_BASE_URL (instance URL) + PIMCORE_TOKEN (API key)."),
    dict(name="wix", description="Wix REST API. ops get/post/put/delete (writes need confirm). Sites/data collections/stores. Auth: WIX_BASE_URL (https://www.wixapis.com) + WIX_TOKEN (OAuth bearer)."),
    dict(name="webiny", description="Webiny headless CMS/serverless REST/GraphQL API. ops get/post/put/delete (writes need confirm). Auth: WEBINY_BASE_URL (instance URL) + WEBINY_TOKEN."),
    dict(name="builderio", description="Builder.io headless CMS REST API. ops get/post/put/delete (writes need confirm). Content/models. Auth: BUILDERIO_BASE_URL (https://builder.io/api) + BUILDERIO_TOKEN (private API key)."),
    dict(name="tinacms", description="TinaCMS content API (GraphQL). ops get/post/put/delete (writes need confirm). Auth: TINACMS_BASE_URL (https://content.tinajs.io) + TINACMS_TOKEN."),
    dict(name="duda", description="Duda website builder REST API. ops get/post/put/delete (writes need confirm). Sites/pages. Auth: DUDA_BASE_URL (https://api.duda.co) + DUDA_TOKEN (API key/secret)."),
    dict(name="redshift", description="Amazon Redshift Data API. ops get/post/put/delete (writes need confirm). Statements/queries. Auth: REDSHIFT_BASE_URL (AWS regional endpoint) + REDSHIFT_TOKEN (SigV4/IAM or bearer)."),
    dict(name="vertica", description="Vertica REST API (management/query). ops get/post/put/delete (writes need confirm). Auth: VERTICA_BASE_URL (cluster URL) + VERTICA_TOKEN."),
    dict(name="clickhouse", description="ClickHouse HTTP/REST interface. ops get/post/put/delete (writes need confirm). Queries via HTTP. Auth: CLICKHOUSE_BASE_URL (instance URL) + CLICKHOUSE_TOKEN."),
    dict(name="singlestore", description="SingleStore (MemSQL) Data API. ops get/post/put/delete (writes need confirm). Auth: SINGLESTORE_BASE_URL (instance URL) + SINGLESTORE_TOKEN."),
    dict(name="exasol", description="Exasol REST/WebSocket API. ops get/post/put/delete (writes need confirm). Auth: EXASOL_BASE_URL (cluster URL) + EXASOL_TOKEN."),
    dict(name="yellowbrick", description="Yellowbrick Data Warehouse REST API. ops get/post/put/delete (writes need confirm). Auth: YELLOWBRICK_BASE_URL (instance URL) + YELLOWBRICK_TOKEN."),
    dict(name="presto", description="Presto (PrestoDB) REST API. ops get/post/put/delete (writes need confirm). Statements/queries. Auth: PRESTO_BASE_URL (coordinator URL) + PRESTO_TOKEN."),
    dict(name="trino", description="Trino REST API. ops get/post/put/delete (writes need confirm). Statements/queries. Auth: TRINO_BASE_URL (coordinator URL) + TRINO_TOKEN."),
    dict(name="dremio", description="Dremio REST API. ops get/post/put/delete (writes need confirm). Datasets/jobs/sql. Auth: DREMIO_BASE_URL (instance URL) + DREMIO_TOKEN."),
    dict(name="druid", description="Apache Druid REST API. ops get/post/put/delete (writes need confirm). Datasources/queries/ingestion. Auth: DRUID_BASE_URL (coordinator/router URL) + DRUID_TOKEN."),
    dict(name="rockset", description="Rockset REST API. ops get/post/put/delete (writes need confirm). Collections/queries. Auth: ROCKSET_BASE_URL (https://api.rockset.com) + ROCKSET_TOKEN (API key)."),
    dict(name="motherduck", description="MotherDuck (cloud DuckDB) REST API. ops get/post/put/delete (writes need confirm). Auth: MOTHERDUCK_BASE_URL (https://api.motherduck.com) + MOTHERDUCK_TOKEN."),
    dict(name="doris", description="Apache Doris REST API. ops get/post/put/delete (writes need confirm). Load/query stream API. Auth: DORIS_BASE_URL (FE node URL) + DORIS_TOKEN."),
    dict(name="firebolt", description="Firebolt cloud data warehouse REST API. ops get/post/put/delete (writes need confirm). Auth: FIREBOLT_BASE_URL (https://api.app.firebolt.io) + FIREBOLT_TOKEN (OAuth bearer)."),
    dict(name="starburst", description="Starburst (Trino enterprise) REST API. ops get/post/put/delete (writes need confirm). Auth: STARBURST_BASE_URL (cluster URL) + STARBURST_TOKEN."),
    dict(name="greenplum", description="Greenplum Database REST/management API. ops get/post/put/delete (writes need confirm). Auth: GREENPLUM_BASE_URL (cluster URL) + GREENPLUM_TOKEN."),
    dict(name="kinetica", description="Kinetica GPU database REST API. ops get/post/put/delete (writes need confirm). Auth: KINETICA_BASE_URL (instance URL) + KINETICA_TOKEN."),
    dict(name="timescaledb", description="TimescaleDB (Timescale Cloud) REST/management API. ops get/post/put/delete (writes need confirm). Auth: TIMESCALEDB_BASE_URL (https://console.cloud.timescale.com/api) + TIMESCALEDB_TOKEN."),
    dict(name="influxdb", description="InfluxDB REST API. ops get/post/put/delete (writes need confirm). Buckets/write/query. Auth: INFLUXDB_BASE_URL (instance URL) + INFLUXDB_TOKEN (API token)."),
    dict(name="jaspersoft", description="TIBCO JasperReports Server REST API. ops get/post/put/delete (writes need confirm). Reports/repository. Auth: JASPERSOFT_BASE_URL (server URL) + JASPERSOFT_TOKEN."),
    dict(name="logianalytics", description="Logi Analytics (insightsoftware) REST API. ops get/post/put/delete (writes need confirm). Auth: LOGIANALYTICS_BASE_URL (instance URL) + LOGIANALYTICS_TOKEN."),
    dict(name="phocas", description="Phocas Software BI REST API. ops get/post/put/delete (writes need confirm). Auth: PHOCAS_BASE_URL (instance URL) + PHOCAS_TOKEN."),
    dict(name="supermetrics", description="Supermetrics REST API. ops get/post/put/delete (writes need confirm). Data source queries. Auth: SUPERMETRICS_BASE_URL (https://api.supermetrics.com) + SUPERMETRICS_TOKEN (API key)."),
    dict(name="funnelio", description="Funnel.io marketing data REST API. ops get/post/put/delete (writes need confirm). Auth: FUNNELIO_BASE_URL (https://api.funnel.io) + FUNNELIO_TOKEN."),
    dict(name="adverity", description="Adverity data integration REST API. ops get/post/put/delete (writes need confirm). Datastreams/workflows. Auth: ADVERITY_BASE_URL (instance URL) + ADVERITY_TOKEN."),
    dict(name="improvado", description="Improvado marketing data pipeline REST API. ops get/post/put/delete (writes need confirm). Auth: IMPROVADO_BASE_URL (instance URL) + IMPROVADO_TOKEN."),
    dict(name="windsorai", description="Windsor.ai data connector REST API. ops get/post/put/delete (writes need confirm). Auth: WINDSORAI_BASE_URL (https://connectors.windsor.ai) + WINDSORAI_TOKEN (API key)."),
    dict(name="chartmogul", description="ChartMogul SaaS analytics REST API. ops get/post/put/delete (writes need confirm). Customers/subscriptions/metrics. Auth: CHARTMOGUL_BASE_URL (https://api.chartmogul.com) + CHARTMOGUL_TOKEN (API key)."),
    dict(name="baremetrics", basic=True, description="Baremetrics REST API. ops get/post/put/delete (writes need confirm). Metrics/customers. Auth: BAREMETRICS_BASE_URL (https://api.baremetrics.com) + BAREMETRICS_TOKEN (access token, basic auth)."),
    dict(name="heap", description="Heap Analytics REST API. ops get/post/put/delete (writes need confirm). Events/definitions. Auth: HEAP_BASE_URL (https://heapanalytics.com/api) + HEAP_TOKEN (API key)."),
    dict(name="countly", description="Countly product analytics REST API. ops get/post/put/delete (writes need confirm). Apps/events. Auth: COUNTLY_BASE_URL (instance URL) + COUNTLY_TOKEN (API key)."),
    dict(name="matomo", description="Matomo (Piwik) Analytics REST API. ops get/post/put/delete (writes need confirm). e.g. /index.php?module=API. Auth: MATOMO_BASE_URL (instance URL) + MATOMO_TOKEN (token_auth)."),
    dict(name="woopra", description="Woopra analytics REST API. ops get/post/put/delete (writes need confirm). Events/profiles. Auth: WOOPRA_BASE_URL (https://api.woopra.com) + WOOPRA_TOKEN."),
    dict(name="kissmetrics", description="Kissmetrics REST API. ops get/post/put/delete (writes need confirm). Events/properties. Auth: KISSMETRICS_BASE_URL (https://trk.kissmetrics.io) + KISSMETRICS_TOKEN (API key)."),
    dict(name="crazy_egg", description="Crazy Egg REST API. ops get/post/put/delete (writes need confirm). Snapshots/heatmaps. Auth: CRAZY_EGG_BASE_URL (https://api.crazyegg.com) + CRAZY_EGG_TOKEN."),
    dict(name="hotjar", description="Hotjar REST API. ops get/post/put/delete (writes need confirm). Sites/heatmaps/surveys. Auth: HOTJAR_BASE_URL (https://api.hotjar.com) + HOTJAR_TOKEN (API key)."),
    dict(name="fullstory", description="FullStory REST API. ops get/post/put/delete (writes need confirm). Sessions/events/users. Auth: FULLSTORY_BASE_URL (https://api.fullstory.com) + FULLSTORY_TOKEN (API key)."),
    dict(name="luckyorange", description="Lucky Orange REST API. ops get/post/put/delete (writes need confirm). Recordings/heatmaps. Auth: LUCKYORANGE_BASE_URL (instance URL) + LUCKYORANGE_TOKEN."),
    dict(name="contentsquare", description="Contentsquare REST API. ops get/post/put/delete (writes need confirm). Analytics/sessions. Auth: CONTENTSQUARE_BASE_URL (https://api.contentsquare.com) + CONTENTSQUARE_TOKEN (OAuth bearer)."),
    dict(name="quantum_metric", description="Quantum Metric REST API. ops get/post/put/delete (writes need confirm). Sessions/insights. Auth: QUANTUM_METRIC_BASE_URL (instance URL) + QUANTUM_METRIC_TOKEN."),
    dict(name="teamcity", description="JetBrains TeamCity REST API. ops get/post/put/delete (writes need confirm). e.g. /app/rest/builds, /app/rest/projects. Auth: TEAMCITY_BASE_URL (https://{server}) + TEAMCITY_TOKEN (access token)."),
    dict(name="bamboo", description="Atlassian Bamboo REST API. ops get/post/put/delete (writes need confirm). e.g. /rest/api/latest/plan, /rest/api/latest/result. Auth: BAMBOO_BASE_URL (https://{server}) + BAMBOO_TOKEN (PAT)."),
    dict(name="travisci", description="Travis CI REST API. ops get/post/put/delete (writes need confirm). e.g. /repo/{repo}/builds, /repo/{repo}/requests. Auth: TRAVISCI_BASE_URL (https://api.travis-ci.com) + TRAVISCI_TOKEN."),
    dict(name="buildkite", description="Buildkite REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/organizations/{org}/pipelines, /v2/builds. Auth: BUILDKITE_BASE_URL (https://api.buildkite.com) + BUILDKITE_TOKEN."),
    dict(name="drone", description="Drone CI REST API. ops get/post/put/delete (writes need confirm). e.g. /api/repos, /api/repos/{owner}/{repo}/builds. Auth: DRONE_BASE_URL (https://{server}) + DRONE_TOKEN."),
    dict(name="gocd", description="GoCD REST API. ops get/post/put/delete (writes need confirm). e.g. /go/api/pipelines, /go/api/agents. Auth: GOCD_BASE_URL (https://{server}:8154) + GOCD_TOKEN (access token)."),
    dict(name="appveyor", description="AppVeyor REST API. ops get/post/put/delete (writes need confirm). e.g. /api/projects, /api/builds. Auth: APPVEYOR_BASE_URL (https://ci.appveyor.com) + APPVEYOR_TOKEN."),
    dict(name="codeship", description="Codeship (CloudBees) REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/organizations/{org}/projects. Auth: CODESHIP_BASE_URL (https://api.codeship.com) + CODESHIP_TOKEN."),
    dict(name="semaphoreci", description="Semaphore CI REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1alpha/projects, /api/v1alpha/pipelines. Auth: SEMAPHORECI_BASE_URL (https://{org}.semaphoreci.com) + SEMAPHORECI_TOKEN."),
    dict(name="codefresh", description="Codefresh REST API. ops get/post/put/delete (writes need confirm). e.g. /api/pipelines, /api/builds. Auth: CODEFRESH_BASE_URL (https://g.codefresh.io) + CODEFRESH_TOKEN."),
    dict(name="spinnaker", description="Spinnaker (Gate) REST API. ops get/post/put/delete (writes need confirm). e.g. /applications, /pipelines. Auth: SPINNAKER_BASE_URL (https://{gate-host}) + SPINNAKER_TOKEN."),
    dict(name="concourse", description="Concourse CI REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/teams/{team}/pipelines, /api/v1/builds. Auth: CONCOURSE_BASE_URL (https://{server}) + CONCOURSE_TOKEN."),
    dict(name="gitea", description="Gitea REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/repos, /api/v1/issues. Auth: GITEA_BASE_URL (https://{server}) + GITEA_TOKEN."),
    dict(name="sonatype_nexus", description="Sonatype Nexus Repository REST API. ops get/post/put/delete (writes need confirm). e.g. /service/rest/v1/repositories, /service/rest/v1/search. Auth: SONATYPE_NEXUS_BASE_URL (https://{server}) + SONATYPE_NEXUS_TOKEN.", basic=True),
    dict(name="packagecloud", description="packagecloud.io REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/repos, /api/v1/repos/{repo}/packages.json. Auth: PACKAGECLOUD_BASE_URL (https://packagecloud.io) + PACKAGECLOUD_TOKEN.", basic=True),
    dict(name="cloudsmith", description="Cloudsmith package registry REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/packages/{owner}/{repo}/, /v1/repos/{owner}/. Auth: CLOUDSMITH_BASE_URL (https://api.cloudsmith.io) + CLOUDSMITH_TOKEN."),
    dict(name="npm_registry", description="npm registry REST API. ops get/post/put/delete (writes need confirm). e.g. /-/org/{org}/package, /{package}. Auth: NPM_REGISTRY_BASE_URL (https://registry.npmjs.org) + NPM_REGISTRY_TOKEN."),
    dict(name="pypi", description="PyPI/Warehouse REST + upload API. ops get/post/put/delete (writes need confirm). e.g. /pypi/{project}/json, /legacy/. Auth: PYPI_BASE_URL (https://pypi.org) + PYPI_TOKEN (API token)."),
    dict(name="anaconda_cloud", description="Anaconda.org REST API. ops get/post/put/delete (writes need confirm). e.g. /api/package/{owner}/{package}, /api/channels. Auth: ANACONDA_CLOUD_BASE_URL (https://api.anaconda.org) + ANACONDA_CLOUD_TOKEN."),
    dict(name="gemfury", description="Gemfury package registry REST API. ops get/post/put/delete (writes need confirm). e.g. /packages, /versions. Auth: GEMFURY_BASE_URL (https://api.fury.io) + GEMFURY_TOKEN.", basic=True),
    dict(name="honeycomb", description="Honeycomb.io observability REST API. ops get/post/put/delete (writes need confirm). e.g. /1/datasets, /1/triggers/{dataset}. Auth: HONEYCOMB_BASE_URL (https://api.honeycomb.io) + HONEYCOMB_TOKEN.", token_header="X-Honeycomb-Team", scheme=""),
    dict(name="lightstep", description="Lightstep (ServiceNow Cloud Observability) REST API. ops get/post/put/delete (writes need confirm). e.g. /public/v0.2/{org}/projects. Auth: LIGHTSTEP_BASE_URL (https://api.lightstep.com) + LIGHTSTEP_TOKEN."),
    dict(name="instana", description="Instana observability REST API. ops get/post/put/delete (writes need confirm). e.g. /api/events, /api/application-monitoring/metrics. Auth: INSTANA_BASE_URL (https://{tenant}.instana.io) + INSTANA_TOKEN (apiToken).", token_header="Authorization", scheme="apiToken"),
    dict(name="site24x7", description="Site24x7 monitoring REST API. ops get/post/put/delete (writes need confirm). e.g. /api/monitors, /api/current_status. Auth: SITE24X7_BASE_URL (https://www.site24x7.com/api) + SITE24X7_TOKEN (Zoho OAuth token)."),
    dict(name="uptimerobot", description="UptimeRobot REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/getMonitors, /v2/newMonitor. Auth: UPTIMEROBOT_BASE_URL (https://api.uptimerobot.com) + UPTIMEROBOT_TOKEN (API key)."),
    dict(name="pingdom", description="Pingdom REST API. ops get/post/put/delete (writes need confirm). e.g. /api/3.1/checks, /api/3.1/results/{id}. Auth: PINGDOM_BASE_URL (https://api.pingdom.com) + PINGDOM_TOKEN."),
    dict(name="statuscake", description="StatusCake REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/uptime, /v1/ssl. Auth: STATUSCAKE_BASE_URL (https://api.statuscake.com) + STATUSCAKE_TOKEN."),
    dict(name="betteruptime", description="Better Stack (Better Uptime) REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/monitors, /api/v2/incidents. Auth: BETTERUPTIME_BASE_URL (https://uptime.betterstack.com) + BETTERUPTIME_TOKEN."),
    dict(name="checkly", description="Checkly monitoring REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/checks, /v1/check-results. Auth: CHECKLY_BASE_URL (https://api.checklyhq.com) + CHECKLY_TOKEN."),
    dict(name="catchpoint", description="Catchpoint REST API. ops get/post/put/delete (writes need confirm). e.g. /ui/api/v2/tests, /ui/api/v2/nodes. Auth: CATCHPOINT_BASE_URL (https://io.catchpoint.com) + CATCHPOINT_TOKEN (OAuth bearer)."),
    dict(name="wavefront", description="VMware Aria Operations for Applications (Wavefront) REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/alert, /api/v2/dashboard. Auth: WAVEFRONT_BASE_URL (https://{cluster}.wavefront.com) + WAVEFRONT_TOKEN."),
    dict(name="librato", description="Librato (SolarWinds) metrics REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/metrics, /v1/alerts. Auth: LIBRATO_BASE_URL (https://metrics-api.librato.com) + LIBRATO_TOKEN.", basic=True),
    dict(name="victorops", description="Splunk On-Call (VictorOps) REST API. ops get/post/put/delete (writes need confirm). e.g. /api-public/v1/incidents, /api-public/v1/teams. Auth: VICTOROPS_BASE_URL (https://api.victorops.com) + VICTOROPS_TOKEN."),
    dict(name="bugsnag", description="Bugsnag (SmartBear Insight Hub) REST API. ops get/post/put/delete (writes need confirm). e.g. /organizations/{org}/errors, /projects/{id}/events. Auth: BUGSNAG_BASE_URL (https://api.bugsnag.com) + BUGSNAG_TOKEN."),
    dict(name="rollbar", description="Rollbar REST API. ops get/post/put/delete (writes need confirm). e.g. /api/1/items, /api/1/item/{id}. Auth: ROLLBAR_BASE_URL (https://api.rollbar.com) + ROLLBAR_TOKEN (project access token).", token_header="X-Rollbar-Access-Token", scheme=""),
    dict(name="airbrake", description="Airbrake error tracking REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v4/projects/{id}/groups, /api/v4/projects/{id}/notices. Auth: AIRBRAKE_BASE_URL (https://api.airbrake.io) + AIRBRAKE_TOKEN."),
    dict(name="honeybadger", description="Honeybadger REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/projects/{id}/faults, /v2/projects/{id}/notices. Auth: HONEYBADGER_BASE_URL (https://app.honeybadger.io) + HONEYBADGER_TOKEN (personal auth token).", basic=True),
    dict(name="raygun", description="Raygun error/crash reporting REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/applications, /v3/errors-grouped. Auth: RAYGUN_BASE_URL (https://api.raygun.com) + RAYGUN_TOKEN.", basic=True),
    dict(name="trackjs", description="TrackJS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/telemetry, /api/v1/errors. Auth: TRACKJS_BASE_URL (https://api.trackjs.com) + TRACKJS_TOKEN.", basic=True),
    dict(name="logrocket", description="LogRocket REST API. ops get/post/put/delete (writes need confirm). e.g. /orgs/{org}/apps/{app}/sessions, /issues. Auth: LOGROCKET_BASE_URL (https://api.logrocket.com) + LOGROCKET_TOKEN."),
    dict(name="statsig", description="Statsig feature-flag/experimentation REST API. ops get/post/put/delete (writes need confirm). e.g. /console/v1/gates, /console/v1/experiments. Auth: STATSIG_BASE_URL (https://statsigapi.net) + STATSIG_TOKEN (console API key)."),
    dict(name="unleash", description="Unleash feature-flag REST API. ops get/post/put/delete (writes need confirm). e.g. /api/admin/projects, /api/admin/features. Auth: UNLEASH_BASE_URL (https://{server}) + UNLEASH_TOKEN.", token_header="Authorization", scheme=""),
    dict(name="flagsmith", description="Flagsmith feature-flag REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/projects, /api/v1/features. Auth: FLAGSMITH_BASE_URL (https://api.flagsmith.com) + FLAGSMITH_TOKEN.", scheme="Token"),
    dict(name="configcat", description="ConfigCat feature-flag management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/products, /v1/settings. Auth: CONFIGCAT_BASE_URL (https://api.configcat.com) + CONFIGCAT_TOKEN.", basic=True),
    dict(name="linearb", description="LinearB engineering-metrics REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/teams, /api/v1/metrics. Auth: LINEARB_BASE_URL (https://public-api.linearb.io) + LINEARB_TOKEN."),
    dict(name="codacy", description="Codacy code-quality REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/organizations/{org}/repositories, /api/v3/analysis. Auth: CODACY_BASE_URL (https://app.codacy.com) + CODACY_TOKEN.", token_header="api-token", scheme=""),
    dict(name="deepsource", description="DeepSource code-quality REST/GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /graphql/, /api/checks. Auth: DEEPSOURCE_BASE_URL (https://api.deepsource.com) + DEEPSOURCE_TOKEN."),
    dict(name="semgrep", description="Semgrep AppSec Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/deployments/{id}/findings, /api/v1/deployments/{id}/scans. Auth: SEMGREP_BASE_URL (https://semgrep.dev/api) + SEMGREP_TOKEN."),
    dict(name="sysdig", description="Sysdig Secure/Monitor REST API. ops get/post/put/delete (writes need confirm). e.g. /api/scanning/v1/anchore, /api/v1/alerts. Auth: SYSDIG_BASE_URL (https://{region}.app.sysdig.com) + SYSDIG_TOKEN."),
    dict(name="aqua_security", description="Aqua Security (CSPM/scanning) REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/images, /api/v2/risks. Auth: AQUA_SECURITY_BASE_URL (https://{server}) + AQUA_SECURITY_TOKEN."),
    dict(name="lacework", description="Lacework cloud-security REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/Alerts, /api/v2/Entities/Machines. Auth: LACEWORK_BASE_URL (https://{account}.lacework.net) + LACEWORK_TOKEN."),
    dict(name="orca_security", description="Orca Security REST API. ops get/post/put/delete (writes need confirm). e.g. /api/alerts, /api/assets. Auth: ORCA_SECURITY_BASE_URL (https://api.orcasecurity.io) + ORCA_SECURITY_TOKEN."),
    dict(name="prisma_cloud", description="Palo Alto Networks Prisma Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /alert, /cloud/{cloudType}/resource. Auth: PRISMA_CLOUD_BASE_URL (https://api.prismacloud.io) + PRISMA_CLOUD_TOKEN."),
    dict(name="teleport", description="Teleport access-plane REST/gRPC-gateway API. ops get/post/put/delete (writes need confirm). e.g. /v1/webapi/sites, /v1/webapi/users. Auth: TELEPORT_BASE_URL (https://{cluster}.teleport.sh) + TELEPORT_TOKEN."),
    dict(name="boundary", description="HashiCorp Boundary REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/targets, /v1/sessions. Auth: BOUNDARY_BASE_URL (https://{server}) + BOUNDARY_TOKEN."),
    dict(name="doppler", description="Doppler secrets-management REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/projects, /v3/configs/config/secrets. Auth: DOPPLER_BASE_URL (https://api.doppler.com) + DOPPLER_TOKEN.", basic=True),
    dict(name="infisical", description="Infisical secrets-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/secrets, /api/v2/workspace. Auth: INFISICAL_BASE_URL (https://app.infisical.com) + INFISICAL_TOKEN."),
    dict(name="akeyless", description="Akeyless vaultless secrets REST API. ops get/post/put/delete (writes need confirm). e.g. /get-secret-value, /list-items. Auth: AKEYLESS_BASE_URL (https://api.akeyless.io) + AKEYLESS_TOKEN."),
    dict(name="keeper", description="Keeper Secrets Manager REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/records, /api/v1/folders. Auth: KEEPER_BASE_URL (https://keepersecurity.com/api) + KEEPER_TOKEN."),
    dict(name="delinea", description="Delinea (Thycotic) Secret Server REST API. ops get/post/put/delete (writes need confirm). e.g. /SecretServer/api/v1/secrets, /api/v1/folders. Auth: DELINEA_BASE_URL (https://{tenant}.secretservercloud.com) + DELINEA_TOKEN."),
    dict(name="rabbitmq", description="RabbitMQ management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/queues, /api/exchanges. Auth: RABBITMQ_BASE_URL (https://{server}:15672) + RABBITMQ_TOKEN.", basic=True),
    dict(name="temporal", description="Temporal Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/namespaces, /api/v1/workflows. Auth: TEMPORAL_BASE_URL (https://cloud.temporal.io) + TEMPORAL_TOKEN."),
    dict(name="camunda", description="Camunda 8 (Zeebe/Operate) REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/process-definitions, /v1/process-instances. Auth: CAMUNDA_BASE_URL (https://{cluster}.camunda.io) + CAMUNDA_TOKEN."),
    dict(name="tinybird", description="Tinybird real-time analytics REST API. ops get/post/put/delete (writes need confirm). e.g. /v0/pipes, /v0/datasources. Auth: TINYBIRD_BASE_URL (https://api.tinybird.co) + TINYBIRD_TOKEN."),
    dict(name="cockroachlabs", description="CockroachDB Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/clusters, /api/v1/databases. Auth: COCKROACHLABS_BASE_URL (https://cockroachlabs.cloud) + COCKROACHLABS_TOKEN."),
    dict(name="planetscale", description="PlanetScale REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/organizations/{org}/databases, /v1/branches. Auth: PLANETSCALE_BASE_URL (https://api.planetscale.com) + PLANETSCALE_TOKEN."),
    dict(name="supabase", description="Supabase management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/projects, /v1/projects/{ref}/database/query. Auth: SUPABASE_BASE_URL (https://api.supabase.com) + SUPABASE_TOKEN."),
    dict(name="neon", description="Neon Postgres management REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/projects, /v2/projects/{id}/branches. Auth: NEON_BASE_URL (https://console.neon.tech/api) + NEON_TOKEN."),
    dict(name="fauna", description="Fauna database REST/FQL API. ops get/post/put/delete (writes need confirm). e.g. /query/1, /import. Auth: FAUNA_BASE_URL (https://db.fauna.com) + FAUNA_TOKEN."),
    dict(name="upstash", description="Upstash (Redis/Kafka/QStash) REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/redis/database, /v2/kafka/cluster. Auth: UPSTASH_BASE_URL (https://api.upstash.com) + UPSTASH_TOKEN."),
    dict(name="bunny_net", description="bunny.net CDN REST API. ops get/post/put/delete (writes need confirm). e.g. /pullzone, /storagezone. Auth: BUNNY_NET_BASE_URL (https://api.bunny.net) + BUNNY_NET_TOKEN.", token_header="AccessKey", scheme=""),
    dict(name="keycdn", description="KeyCDN REST API. ops get/post/put/delete (writes need confirm). e.g. /zones.json, /zones/purge/{id}.json. Auth: KEYCDN_BASE_URL (https://api.keycdn.com) + KEYCDN_TOKEN.", basic=True),
    dict(name="imgix", description="imgix image-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/sources, /api/v1/purge. Auth: IMGIX_BASE_URL (https://api.imgix.com) + IMGIX_TOKEN."),
    dict(name="cloudinary", description="Cloudinary media-management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1_1/{cloud_name}/resources, /v1_1/{cloud_name}/image/upload. Auth: CLOUDINARY_BASE_URL (https://api.cloudinary.com) + CLOUDINARY_TOKEN.", basic=True),
    dict(name="statuspage", description="Atlassian Statuspage REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/pages/{id}/incidents, /v1/pages/{id}/components. Auth: STATUSPAGE_BASE_URL (https://api.statuspage.io) + STATUSPAGE_TOKEN.", scheme="OAuth"),
    dict(name="incident_io", description="incident.io REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/incidents, /v2/severities. Auth: INCIDENT_IO_BASE_URL (https://api.incident.io) + INCIDENT_IO_TOKEN."),
    dict(name="firehydrant", description="FireHydrant incident-management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/incidents, /v1/services. Auth: FIREHYDRANT_BASE_URL (https://api.firehydrant.io) + FIREHYDRANT_TOKEN."),
    dict(name="rootly", description="Rootly incident-management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/incidents, /v1/services. Auth: ROOTLY_BASE_URL (https://api.rootly.com) + ROOTLY_TOKEN."),
    dict(name="blameless", description="Blameless incident-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/incidents, /api/v1/postmortems. Auth: BLAMELESS_BASE_URL (https://{org}.blameless.com) + BLAMELESS_TOKEN."),
    dict(name="squadcast", description="Squadcast incident-response REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/incidents, /v3/services. Auth: SQUADCAST_BASE_URL (https://api.squadcast.com) + SQUADCAST_TOKEN."),
    dict(name="ilert", description="ilert incident-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/incidents, /api/alerts. Auth: ILERT_BASE_URL (https://api.ilert.com) + ILERT_TOKEN.", basic=True),
    dict(name="algolia", description="Algolia search REST API. ops get/post/put/delete (writes need confirm). e.g. /1/indexes/{index}, /1/indexes/{index}/query. Auth: ALGOLIA_BASE_URL (https://{app_id}.algolia.net) + ALGOLIA_TOKEN (Admin API key).", token_header="X-Algolia-API-Key", scheme=""),
    dict(name="meilisearch", description="Meilisearch REST API. ops get/post/put/delete (writes need confirm). e.g. /indexes, /indexes/{index}/search. Auth: MEILISEARCH_BASE_URL (https://{server}) + MEILISEARCH_TOKEN."),
    dict(name="typesense", description="Typesense REST API. ops get/post/put/delete (writes need confirm). e.g. /collections, /collections/{name}/documents/search. Auth: TYPESENSE_BASE_URL (https://{cluster}.typesense.net) + TYPESENSE_TOKEN.", token_header="X-TYPESENSE-API-KEY", scheme=""),
    dict(name="portainer", description="Portainer container-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/endpoints, /api/stacks. Auth: PORTAINER_BASE_URL (https://{server}:9443) + PORTAINER_TOKEN."),
    dict(name="rancher", description="Rancher REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/clusters, /v3/projects. Auth: RANCHER_BASE_URL (https://{server}) + RANCHER_TOKEN."),
    dict(name="packer", description="HCP Packer REST API. ops get/post/put/delete (writes need confirm). e.g. /packer/2023-01-01/organizations/{org}/buckets, /images. Auth: PACKER_BASE_URL (https://api.cloud.hashicorp.com) + PACKER_TOKEN."),
    dict(name="ansible_tower", description="Ansible Automation Platform (Tower/AWX) REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/job_templates, /api/v2/jobs. Auth: ANSIBLE_TOWER_BASE_URL (https://{server}) + ANSIBLE_TOWER_TOKEN."),
    dict(name="puppet", description="Puppet Enterprise REST API. ops get/post/put/delete (writes need confirm). e.g. /puppetdb/v4/nodes, /orchestrator/v1/jobs. Auth: PUPPET_BASE_URL (https://{server}:8143) + PUPPET_TOKEN."),
    dict(name="chef", description="Chef Automate REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v0/cfgmgmt/nodes, /api/v0/events. Auth: CHEF_BASE_URL (https://{server}) + CHEF_TOKEN.", token_header="api-token", scheme=""),
    dict(name="browserstack", description="BrowserStack REST API. ops get/post/put/delete (writes need confirm). e.g. /automate/builds.json, /app-automate/builds.json. Auth: BROWSERSTACK_BASE_URL (https://api.browserstack.com) + BROWSERSTACK_TOKEN.", basic=True),
    dict(name="saucelabs", description="Sauce Labs REST API. ops get/post/put/delete (writes need confirm). e.g. /rest/v1/{user}/jobs, /rest/v1/{user}/builds. Auth: SAUCELABS_BASE_URL (https://api.us-west-1.saucelabs.com) + SAUCELABS_TOKEN.", basic=True),
    dict(name="lambdatest", description="LambdaTest REST API. ops get/post/put/delete (writes need confirm). e.g. /automation/api/v1/builds, /automation/api/v1/sessions. Auth: LAMBDATEST_BASE_URL (https://api.lambdatest.com) + LAMBDATEST_TOKEN.", basic=True),
    dict(name="applitools", description="Applitools Eyes REST API. ops get/post/put/delete (writes need confirm). e.g. /api/results, /api/sessions/batches. Auth: APPLITOOLS_BASE_URL (https://eyesapi.applitools.com) + APPLITOOLS_TOKEN."),
    dict(name="percy", description="Percy (BrowserStack) visual-testing REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/builds, /api/v1/snapshots. Auth: PERCY_BASE_URL (https://percy.io/api/v1) + PERCY_TOKEN."),
    dict(name="chromatic", description="Chromatic (Storybook) REST API. ops get/post/put/delete (writes need confirm). e.g. /openapi/builds, /openapi/snapshots. Auth: CHROMATIC_BASE_URL (https://index.chromatic.com) + CHROMATIC_TOKEN."),
    dict(name="testrail", description="TestRail REST API. ops get/post/put/delete (writes need confirm). e.g. /index.php?/api/v2/get_cases, /add_run. Auth: TESTRAIL_BASE_URL (https://{org}.testrail.io) + TESTRAIL_TOKEN.", basic=True),
    dict(name="zephyr", description="Zephyr Scale (SmartBear) REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/testcases, /v2/testexecutions. Auth: ZEPHYR_BASE_URL (https://api.zephyrscale.smartbear.com) + ZEPHYR_TOKEN."),
    dict(name="qase", description="Qase test-management REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/project, /v1/case/{code}. Auth: QASE_BASE_URL (https://api.qase.io) + QASE_TOKEN.", token_header="Token", scheme=""),
    dict(name="mabl", description="mabl test-automation REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/plans, /v1/runs. Auth: MABL_BASE_URL (https://api.mabl.com) + MABL_TOKEN."),
    dict(name="scaleway", description="Scaleway Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /instance/v1/zones/{zone}/servers, /account/v2/projects. Auth: SCALEWAY_BASE_URL (https://api.scaleway.com) + SCALEWAY_TOKEN.", token_header="X-Auth-Token", scheme=""),
    dict(name="hetzner", description="Hetzner Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/servers, /v1/volumes. Auth: HETZNER_BASE_URL (https://api.hetzner.cloud) + HETZNER_TOKEN."),
    dict(name="equinix_metal", description="Equinix Metal (bare-metal cloud) REST API. ops get/post/put/delete (writes need confirm). e.g. /metal/v1/devices, /metal/v1/projects. Auth: EQUINIX_METAL_BASE_URL (https://api.equinix.com) + EQUINIX_METAL_TOKEN.", token_header="X-Auth-Token", scheme=""),
    dict(name="zabbix", description="Zabbix JSON-RPC monitoring API. ops get/post/put/delete (writes need confirm). e.g. /api_jsonrpc.php host.get, item.get. Auth: ZABBIX_BASE_URL (https://{server}/zabbix) + ZABBIX_TOKEN (API token)."),
    dict(name="papertrail", description="SolarWinds Papertrail log-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/events/search.json, /api/v1/systems.json. Auth: PAPERTRAIL_BASE_URL (https://papertrailapp.com) + PAPERTRAIL_TOKEN.", token_header="X-Papertrail-Token", scheme=""),
    dict(name="loggly", description="SolarWinds Loggly REST API. ops get/post/put/delete (writes need confirm). e.g. /apiv2/search, /apiv2/customer. Auth: LOGGLY_BASE_URL (https://{subdomain}.loggly.com) + LOGGLY_TOKEN.", basic=True),
    dict(name="coralogix", description="Coralogix observability REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/logs, /api/v1/alerts. Auth: CORALOGIX_BASE_URL (https://api.coralogix.com) + CORALOGIX_TOKEN."),
    dict(name="logz_io", description="Logz.io REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/search, /v2/alerts. Auth: LOGZ_IO_BASE_URL (https://api.logz.io) + LOGZ_IO_TOKEN.", token_header="X-API-TOKEN", scheme=""),
    dict(name="sourcegraph", description="Sourcegraph GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /.api/graphql, /.api/search/stream. Auth: SOURCEGRAPH_BASE_URL (https://{server}) + SOURCEGRAPH_TOKEN.", scheme="token"),
    dict(name="gitpod", description="Gitpod REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/workspaces, /v1/projects. Auth: GITPOD_BASE_URL (https://api.gitpod.io) + GITPOD_TOKEN."),
    dict(name="codesandbox", description="CodeSandbox REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/sandboxes, /api/v1/sandboxes/{id}. Auth: CODESANDBOX_BASE_URL (https://codesandbox.io/api) + CODESANDBOX_TOKEN."),
    dict(name="replit", description="Replit REST/GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /graphql, /data/repls. Auth: REPLIT_BASE_URL (https://replit.com) + REPLIT_TOKEN."),
    dict(name="jumpcloud", description="JumpCloud directory REST API. ops get/post/put/delete (writes need confirm). e.g. /api/systems, /api/systemusers. Auth: JUMPCLOUD_BASE_URL (https://console.jumpcloud.com/api) + JUMPCLOUD_TOKEN.", token_header="x-api-key", scheme=""),
    dict(name="codeclimate", description="Code Climate REST API. ops get/post/put/delete (writes need confirm). e.g. /repos, /repos/{id}/snapshots. Auth: CODECLIMATE_BASE_URL (https://api.codeclimate.com) + CODECLIMATE_TOKEN.", token_header="Authorization", scheme="Token token="),
    dict(name="coveralls", description="Coveralls REST API. ops get/post/put/delete (writes need confirm). e.g. /api/repos, /v1/repos/{repo}/badge. Auth: COVERALLS_BASE_URL (https://coveralls.io/api) + COVERALLS_TOKEN (repo_token)."),
    dict(name="codecov", description="Codecov REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/{service}/{owner}/repos, /commits. Auth: CODECOV_BASE_URL (https://api.codecov.io) + CODECOV_TOKEN.", scheme="Bearer"),
    dict(name="checkmarx", description="Checkmarx One AppSec REST API. ops get/post/put/delete (writes need confirm). e.g. /api/projects, /api/scans. Auth: CHECKMARX_BASE_URL (https://{tenant}.checkmarx.net) + CHECKMARX_TOKEN."),
    dict(name="veracode", description="Veracode REST API. ops get/post/put/delete (writes need confirm). e.g. /appsec/v1/applications, /appsec/v2/findings. Auth: VERACODE_BASE_URL (https://api.veracode.com) + VERACODE_TOKEN (HMAC key/secret).", scheme="VERACODE-HMAC-SHA-256"),
    dict(name="blackduck", description="Black Duck (Synopsys) SCA REST API. ops get/post/put/delete (writes need confirm). e.g. /api/projects, /api/vulnerabilities. Auth: BLACKDUCK_BASE_URL (https://{server}) + BLACKDUCK_TOKEN.", scheme="Bearer"),
    dict(name="fossa", description="FOSSA license/SCA REST API. ops get/post/put/delete (writes need confirm). e.g. /api/projects, /api/revisions/{locator}/issues. Auth: FOSSA_BASE_URL (https://app.fossa.com/api) + FOSSA_TOKEN."),
    dict(name="mend", description="Mend.io (WhiteSource) SCA REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2.0/projects, /api/v2.0/vulnerabilities. Auth: MEND_BASE_URL (https://api-saas.mend.io) + MEND_TOKEN."),
    dict(name="renovate", description="Mend Renovate Bot dashboard REST API. ops get/post/put/delete (writes need confirm). e.g. /api/repos, /api/dependencies. Auth: RENOVATE_BASE_URL (https://app.renovatebot.com/api) + RENOVATE_TOKEN."),
    dict(name="woocommerce", basic=True, description="WooCommerce REST API for WordPress stores. ops get/post/put/delete (writes need confirm). e.g. /wp-json/wc/v3/products, /wp-json/wc/v3/orders. Auth: WOOCOMMERCE_BASE_URL (https://{store}.com) + WOOCOMMERCE_TOKEN (consumer key as basic-auth user, consumer secret as pass)."),
    dict(name="prestashop", basic=True, description="PrestaShop Webservice REST. ops get/post/put/delete (writes need confirm). e.g. /api/products, /api/orders. Auth: PRESTASHOP_BASE_URL (https://{shop}.com) + PRESTASHOP_TOKEN (webservice key as basic-auth user)."),
    dict(name="opencart", description="OpenCart REST API (extension-based). ops get/post/put/delete (writes need confirm). e.g. /index.php?route=api/product. Auth: OPENCART_BASE_URL (https://{store}.com) + OPENCART_TOKEN (API key)."),
    dict(name="volusion", description="Volusion REST API. ops get/post/put/delete (writes need confirm). e.g. /2.0/api.asmx/DoSelect. Auth: VOLUSION_BASE_URL (https://{store}.com) + VOLUSION_TOKEN."),
    dict(name="ecwid", description="Ecwid REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/{storeId}/products, /api/v3/{storeId}/orders. Auth: ECWID_BASE_URL (https://app.ecwid.com) + ECWID_TOKEN (API token)."),
    dict(name="shopware", description="Shopware Admin/Store REST API. ops get/post/put/delete (writes need confirm). e.g. /api/product, /store-api/product. Auth: SHOPWARE_BASE_URL (https://{shop}.example.com) + SHOPWARE_TOKEN (OAuth2 client-credentials bearer)."),
    dict(name="commercetools", description="commercetools headless commerce REST/GraphQL. ops get/post/put/delete (writes need confirm). e.g. /{project}/products, /{project}/orders. Auth: COMMERCETOOLS_BASE_URL (https://api.{region}.commercetools.com) + COMMERCETOOLS_TOKEN (OAuth2 client-credentials bearer)."),
    dict(name="elastic_path", description="Elastic Path (Moltin) commerce REST. ops get/post/put/delete (writes need confirm). e.g. /v2/products, /v2/orders. Auth: ELASTIC_PATH_BASE_URL (https://useast.api.elasticpath.com) + ELASTIC_PATH_TOKEN (OAuth2 bearer)."),
    dict(name="vtex", description="VTEX commerce REST API. ops get/post/put/delete (writes need confirm). e.g. /api/catalog/pvt/product, /api/oms/pvt/orders. Auth: VTEX_BASE_URL (https://{account}.{env}.vtex.com) + VTEX_TOKEN (App Key/App Token bearer)."),
    dict(name="linnworks", description="Linnworks inventory/order-management REST. ops get/post/put/delete (writes need confirm). e.g. /api/Inventory/GetStockItems, /api/Orders/GetOrder. Auth: LINNWORKS_BASE_URL (https://eu-ext.linnworks.net) + LINNWORKS_TOKEN (application token)."),
    dict(name="channeladvisor", description="ChannelAdvisor marketplace-management REST. ops get/post/put/delete (writes need confirm). e.g. /v1/Products, /v1/Orders. Auth: CHANNELADVISOR_BASE_URL (https://api.channeladvisor.com) + CHANNELADVISOR_TOKEN (OAuth2 bearer)."),
    dict(name="cin7", basic=True, description="Cin7 (Core/DEAR) inventory REST. ops get/post/put/delete (writes need confirm). e.g. /ExternalApi/v2/product, /ExternalApi/v2/saleorder. Auth: CIN7_BASE_URL (https://inventory.dearsystems.com) + CIN7_TOKEN (account ID as basic-auth user, API key as pass)."),
    dict(name="brightpearl", description="Brightpearl retail-ops REST API. ops get/post/put/delete (writes need confirm). e.g. /public-api/{account}/product-service/product, /public-api/{account}/order-service/order. Auth: BRIGHTPEARL_BASE_URL (https://use1.brightpearlconnect.com) + BRIGHTPEARL_TOKEN (app ref + account token bearer)."),
    dict(name="veeqo", description="Veeqo inventory/order REST API. ops get/post/put/delete (writes need confirm). e.g. /products, /orders. Auth: VEEQO_BASE_URL (https://api.veeqo.com) + VEEQO_TOKEN (API key)."),
    dict(name="shipbob", description="ShipBob fulfillment REST API. ops get/post/put/delete (writes need confirm). e.g. /1.0/order, /1.0/product. Auth: SHIPBOB_BASE_URL (https://api.shipbob.com) + SHIPBOB_TOKEN (OAuth bearer)."),
    dict(name="shipstation", basic=True, description="ShipStation shipping/fulfillment REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /shipments. Auth: SHIPSTATION_BASE_URL (https://ssapi.shipstation.com) + SHIPSTATION_TOKEN (API key as basic-auth user, API secret as pass)."),
    dict(name="deliverr", description="Deliverr fulfillment REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /inventory. Auth: DELIVERR_BASE_URL (https://api.deliverr.com) + DELIVERR_TOKEN (bearer)."),
    dict(name="ordoro", description="Ordoro shipping/inventory REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /products. Auth: ORDORO_BASE_URL (https://app.ordoro.com) + ORDORO_TOKEN (API key)."),
    dict(name="skubana", description="Skubana (Extensiv) multichannel order-management REST. ops get/post/put/delete (writes need confirm). e.g. /orders, /products. Auth: SKUBANA_BASE_URL (https://api.skubana.com) + SKUBANA_TOKEN (OAuth bearer)."),
    dict(name="extensiv", description="Extensiv (3PL Warehouse Manager) REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /items. Auth: EXTENSIV_BASE_URL (https://secure-wms.com) + EXTENSIV_TOKEN (OAuth bearer)."),
    dict(name="ebay", description="eBay REST API (Sell/Buy platform). ops get/post/put/delete (writes need confirm). e.g. /sell/inventory/v1/inventory_item, /buy/browse/v1/item. Auth: EBAY_BASE_URL (https://api.ebay.com) + EBAY_TOKEN (OAuth bearer)."),
    dict(name="etsy", description="Etsy Open API v3. ops get/post/put/delete (writes need confirm). e.g. /v3/application/shops/{id}/listings, /v3/application/shops/{id}/receipts. Auth: ETSY_BASE_URL (https://openapi.etsy.com) + ETSY_TOKEN (OAuth bearer)."),
    dict(name="walmart_marketplace", description="Walmart Marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/items, /v3/orders. Auth: WALMART_MARKETPLACE_BASE_URL (https://marketplace.walmartapis.com) + WALMART_MARKETPLACE_TOKEN (OAuth bearer)."),
    dict(name="amazon_sp_api", description="Amazon Selling Partner API. ops get/post/put/delete (writes need confirm). e.g. /orders/v0/orders, /listings/2021-08-01/items. Auth: AMAZON_SP_API_BASE_URL (https://sellingpartnerapi-na.amazon.com) + AMAZON_SP_API_TOKEN (LWA OAuth bearer)."),
    dict(name="rakuten", description="Rakuten marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /order/items, /item/get. Auth: RAKUTEN_BASE_URL (https://api.rakuten.co.jp) + RAKUTEN_TOKEN (API key)."),
    dict(name="mercadolibre", description="Mercado Libre REST API. ops get/post/put/delete (writes need confirm). e.g. /items, /orders. Auth: MERCADOLIBRE_BASE_URL (https://api.mercadolibre.com) + MERCADOLIBRE_TOKEN (OAuth bearer)."),
    dict(name="wayfair", description="Wayfair Supplier REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/pos, /v1/orders. Auth: WAYFAIR_BASE_URL (https://api.wayfair.com) + WAYFAIR_TOKEN (OAuth bearer)."),
    dict(name="newegg", description="Newegg Marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /marketplace/order, /marketplace/item. Auth: NEWEGG_BASE_URL (https://api.newegg.com) + NEWEGG_TOKEN (API key)."),
    dict(name="back_market", description="Back Market marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /ws/listings, /ws/orders. Auth: BACK_MARKET_BASE_URL (https://www.backmarket.com) + BACK_MARKET_TOKEN (API key)."),
    dict(name="faire", description="Faire wholesale marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /external-api/v2/products, /external-api/v2/orders. Auth: FAIRE_BASE_URL (https://www.faire.com) + FAIRE_TOKEN (OAuth bearer)."),
    dict(name="wish", description="Wish Merchant REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/products, /api/v3/orders. Auth: WISH_BASE_URL (https://merchant.wish.com) + WISH_TOKEN (API key)."),
    dict(name="overstock", description="Overstock (Bed Bath & Beyond) marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /suppliers/items, /suppliers/orders. Auth: OVERSTOCK_BASE_URL (https://api.overstock.com) + OVERSTOCK_TOKEN (API key)."),
    dict(name="zalando", description="Zalando Partner Program REST API. ops get/post/put/delete (writes need confirm). e.g. /articles, /orders. Auth: ZALANDO_BASE_URL (https://api.zalando.com) + ZALANDO_TOKEN (OAuth bearer)."),
    dict(name="allegro", description="Allegro marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /sale/offers, /order/checkout-forms. Auth: ALLEGRO_BASE_URL (https://api.allegro.pl) + ALLEGRO_TOKEN (OAuth bearer)."),
    dict(name="flipkart", description="Flipkart Seller/Marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /sm/listings, /sm/orders. Auth: FLIPKART_BASE_URL (https://api.flipkart.net) + FLIPKART_TOKEN (OAuth bearer)."),
    dict(name="jd_com", description="JD.com Open Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /routerjson?method=jingdong.product.query. Auth: JD_COM_BASE_URL (https://api.jd.com) + JD_COM_TOKEN (app token)."),
    dict(name="alibaba", description="Alibaba.com Open Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /param2/1/system.oauth2, /product/list. Auth: ALIBABA_BASE_URL (https://gw.open.1688.com) + ALIBABA_TOKEN (OAuth bearer)."),
    dict(name="lazada", description="Lazada Open Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /products/get, /orders/get. Auth: LAZADA_BASE_URL (https://api.lazada.com) + LAZADA_TOKEN (OAuth bearer)."),
    dict(name="shopee", description="Shopee Open Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/product/get_item_list, /api/v2/order/get_order_list. Auth: SHOPEE_BASE_URL (https://partner.shopeemobile.com) + SHOPEE_TOKEN (OAuth bearer)."),
    dict(name="tokopedia", description="Tokopedia (GoTo) Seller REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/product/info, /v1/order/list. Auth: TOKOPEDIA_BASE_URL (https://fs.tokopedia.net) + TOKOPEDIA_TOKEN (OAuth bearer)."),
    dict(name="coupang", description="Coupang WING marketplace REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/providers/seller_api/apis/api/v1/marketplace/products, /v2/providers/seller_api/apis/api/v4/vendors/{id}/orders. Auth: COUPANG_BASE_URL (https://api-gateway.coupang.com) + COUPANG_TOKEN (HMAC-signed key)."),
    dict(name="mercado_pago", description="Mercado Pago payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/payments, /v1/orders. Auth: MERCADO_PAGO_BASE_URL (https://api.mercadopago.com) + MERCADO_PAGO_TOKEN (bearer access token)."),
    dict(name="klarna", basic=True, description="Klarna Payments/Checkout REST API. ops get/post/put/delete (writes need confirm). e.g. /payments/v1/authorizations, /ordermanagement/v1/orders. Auth: KLARNA_BASE_URL (https://api.klarna.com) + KLARNA_TOKEN (username as basic-auth user, password as pass)."),
    dict(name="afterpay", description="Afterpay/Clearpay Merchant REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/payments, /v2/orders. Auth: AFTERPAY_BASE_URL (https://api.afterpay.com) + AFTERPAY_TOKEN (OAuth bearer)."),
    dict(name="affirm", description="Affirm payments REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/charges, /api/v2/checkout. Auth: AFFIRM_BASE_URL (https://api.affirm.com) + AFFIRM_TOKEN (bearer, public/private key pair)."),
    dict(name="sezzle", description="Sezzle payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/order, /v2/payment. Auth: SEZZLE_BASE_URL (https://gateway.sezzle.com) + SEZZLE_TOKEN (bearer)."),
    dict(name="zip_co", description="Zip (Quadpay) merchant REST API. ops get/post/put/delete (writes need confirm). e.g. /merchant/v1/checkouts, /merchant/v1/charges. Auth: ZIP_CO_BASE_URL (https://api.zip.co) + ZIP_CO_TOKEN (bearer)."),
    dict(name="braintree", basic=True, description="Braintree (PayPal) payments REST/GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /merchants/{id}/transactions, /merchants/{id}/customers. Auth: BRAINTREE_BASE_URL (https://api.braintreegateway.com) + BRAINTREE_TOKEN (public key as basic-auth user, private key as pass)."),
    dict(name="worldpay", basic=True, description="Worldpay payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payments, /refunds. Auth: WORLDPAY_BASE_URL (https://try.access.worldpay.com) + WORLDPAY_TOKEN (username as basic-auth user, password as pass)."),
    dict(name="checkout_com", description="Checkout.com Payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payments, /payments/{id}/refunds. Auth: CHECKOUT_COM_BASE_URL (https://api.checkout.com) + CHECKOUT_COM_TOKEN (secret-key bearer)."),
    dict(name="razorpay", basic=True, description="Razorpay payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/payments, /v1/orders. Auth: RAZORPAY_BASE_URL (https://api.razorpay.com) + RAZORPAY_TOKEN (key ID as basic-auth user, key secret as pass)."),
    dict(name="paystack", description="Paystack payments REST API. ops get/post/put/delete (writes need confirm). e.g. /transaction, /customer. Auth: PAYSTACK_BASE_URL (https://api.paystack.co) + PAYSTACK_TOKEN (secret-key bearer)."),
    dict(name="flutterwave", description="Flutterwave payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/charges, /v3/transactions. Auth: FLUTTERWAVE_BASE_URL (https://api.flutterwave.com) + FLUTTERWAVE_TOKEN (secret-key bearer)."),
    dict(name="mollie", description="Mollie payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/payments, /v2/orders. Auth: MOLLIE_BASE_URL (https://api.mollie.com) + MOLLIE_TOKEN (API-key bearer)."),
    dict(name="payoneer", description="Payoneer payouts REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/programs/{id}/payouts, /v4/payees. Auth: PAYONEER_BASE_URL (https://api.payoneer.com) + PAYONEER_TOKEN (OAuth bearer)."),
    dict(name="revolut_business", description="Revolut Business (Merchant) REST API. ops get/post/put/delete (writes need confirm). e.g. /1.0/orders, /1.0/payments. Auth: REVOLUT_BUSINESS_BASE_URL (https://merchant.revolut.com) + REVOLUT_BUSINESS_TOKEN (secret-key bearer)."),
    dict(name="authorize_net", description="Authorize.Net Payments REST/XML API. ops get/post/put/delete (writes need confirm). e.g. /xml/v1/request.api (createTransactionRequest). Auth: AUTHORIZE_NET_BASE_URL (https://api.authorize.net) + AUTHORIZE_NET_TOKEN (API login ID + transaction key)."),
    dict(name="twocheckout", basic=True, description="2Checkout (Verifone) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /subscriptions. Auth: TWOCHECKOUT_BASE_URL (https://api.2checkout.com) + TWOCHECKOUT_TOKEN (merchant code as basic-auth user, secret key as pass)."),
    dict(name="bluesnap", basic=True, description="BlueSnap payments REST API. ops get/post/put/delete (writes need confirm). e.g. /services/2/transactions, /services/2/subscriptions. Auth: BLUESNAP_BASE_URL (https://ws.bluesnap.com) + BLUESNAP_TOKEN (username as basic-auth user, password as pass)."),
    dict(name="cybersource", description="CyberSource (Visa) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /pts/v2/payments, /pts/v2/refunds. Auth: CYBERSOURCE_BASE_URL (https://api.cybersource.com) + CYBERSOURCE_TOKEN (HTTP signature key/secret)."),
    dict(name="payu", description="PayU payments REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2_1/orders, /api/v2_1/payment-methods. Auth: PAYU_BASE_URL (https://secure.payu.com) + PAYU_TOKEN (OAuth bearer)."),
    dict(name="ingenico", description="Ingenico (Worldline Direct) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/{merchantId}/payments, /v1/{merchantId}/refunds. Auth: INGENICO_BASE_URL (https://api.direct.worldline-solutions.com) + INGENICO_TOKEN (API key/secret HMAC signature)."),
    dict(name="global_payments", description="Global Payments (GP-API) REST API. ops get/post/put/delete (writes need confirm). e.g. /transactions, /payment-methods. Auth: GLOBAL_PAYMENTS_BASE_URL (https://apis.globalpay.com) + GLOBAL_PAYMENTS_TOKEN (app ID/app key OAuth bearer)."),
    dict(name="fiserv", description="Fiserv (Commerce Hub) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payments, /refunds. Auth: FISERV_BASE_URL (https://cert.api.fiservapps.com) + FISERV_TOKEN (API key/secret HMAC signature)."),
    dict(name="clover", description="Clover (Fiserv) POS REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/merchants/{id}/orders, /v3/merchants/{id}/items. Auth: CLOVER_BASE_URL (https://api.clover.com) + CLOVER_TOKEN (OAuth bearer)."),
    dict(name="toast", description="Toast POS REST API. ops get/post/put/delete (writes need confirm). e.g. /orders/v2/orders, /config/v2/menus. Auth: TOAST_BASE_URL (https://ws-api.toasttab.com) + TOAST_TOKEN (OAuth bearer)."),
    dict(name="lightspeed", description="Lightspeed Retail/Restaurant POS REST API. ops get/post/put/delete (writes need confirm). e.g. /API/Account/{id}/Item.json, /API/Account/{id}/Sale.json. Auth: LIGHTSPEED_BASE_URL (https://api.lightspeedapp.com) + LIGHTSPEED_TOKEN (OAuth bearer)."),
    dict(name="vend", description="Vend (Lightspeed X-Series) POS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/2.0/products, /api/2.0/sales. Auth: VEND_BASE_URL (https://{domain}.vendhq.com) + VEND_TOKEN (OAuth bearer)."),
    dict(name="revel_systems", description="Revel Systems POS REST API. ops get/post/put/delete (writes need confirm). e.g. /resources/Product, /resources/Order. Auth: REVEL_SYSTEMS_BASE_URL (https://{establishment}.revelup.com) + REVEL_SYSTEMS_TOKEN (API key)."),
    dict(name="touchbistro", description="TouchBistro POS REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/orders, /v1/menu-items. Auth: TOUCHBISTRO_BASE_URL (https://api.touchbistro.com) + TOUCHBISTRO_TOKEN (OAuth bearer)."),
    dict(name="shopkeep", description="ShopKeep (Lightspeed) POS REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/products, /v1/sales. Auth: SHOPKEEP_BASE_URL (https://api.shopkeep.com) + SHOPKEEP_TOKEN (OAuth bearer)."),
    dict(name="loyverse", description="Loyverse POS REST API. ops get/post/put/delete (writes need confirm). e.g. /v1.0/items, /v1.0/receipts. Auth: LOYVERSE_BASE_URL (https://api.loyverse.com) + LOYVERSE_TOKEN (OAuth bearer)."),
    dict(name="ncr_voyix", description="NCR Voyix retail/POS REST API. ops get/post/put/delete (writes need confirm). e.g. /platform/orders, /platform/items. Auth: NCR_VOYIX_BASE_URL (https://gateway.ncr.com) + NCR_VOYIX_TOKEN (OAuth bearer)."),
    dict(name="heartland_payment", description="Heartland (Global Payments) POS/payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/transactions, /v2/items. Auth: HEARTLAND_PAYMENT_BASE_URL (https://api2.heartlandportico.com) + HEARTLAND_PAYMENT_TOKEN (secret-key bearer)."),
    dict(name="moneris", description="Moneris payments REST API. ops get/post/put/delete (writes need confirm). e.g. /gateway2/servlet/MpgRequest. Auth: MONERIS_BASE_URL (https://api.moneris.com) + MONERIS_TOKEN (store ID + API token)."),
    dict(name="verifone", description="Verifone payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payments, /refunds. Auth: VERIFONE_BASE_URL (https://api.verifone.cloud) + VERIFONE_TOKEN (OAuth bearer)."),
    dict(name="ebanx", description="EBANX payments REST API. ops get/post/put/delete (writes need confirm). e.g. /ws/direct, /ws/query. Auth: EBANX_BASE_URL (https://api.ebanx.com) + EBANX_TOKEN (integration key)."),
    dict(name="dlocal", description="dLocal payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payments, /payouts. Auth: DLOCAL_BASE_URL (https://api.dlocal.com) + DLOCAL_TOKEN (login/secret HMAC signature)."),
    dict(name="paddle", description="Paddle billing/payments REST API. ops get/post/put/delete (writes need confirm). e.g. /transactions, /subscriptions. Auth: PADDLE_BASE_URL (https://api.paddle.com) + PADDLE_TOKEN (API-key bearer)."),
    dict(name="fastspring", basic=True, description="FastSpring commerce/billing REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /subscriptions. Auth: FASTSPRING_BASE_URL (https://api.fastspring.com) + FASTSPRING_TOKEN (username as basic-auth user, password as pass)."),
    dict(name="gumroad", description="Gumroad REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/products, /v2/sales. Auth: GUMROAD_BASE_URL (https://api.gumroad.com) + GUMROAD_TOKEN (OAuth bearer)."),
    dict(name="sellfy", description="Sellfy REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/products, /v2/orders. Auth: SELLFY_BASE_URL (https://api.sellfy.com) + SELLFY_TOKEN (API key)."),
    dict(name="lemonsqueezy", description="Lemon Squeezy REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/products, /v1/orders. Auth: LEMONSQUEEZY_BASE_URL (https://api.lemonsqueezy.com) + LEMONSQUEEZY_TOKEN (API-key bearer)."),
    dict(name="payhip", description="Payhip REST API. ops get/post/put/delete (writes need confirm). e.g. /products, /sales. Auth: PAYHIP_BASE_URL (https://payhip.com/api/v1) + PAYHIP_TOKEN (API key)."),
    dict(name="thrivecart", description="ThriveCart REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/orders, /v1/products. Auth: THRIVECART_BASE_URL (https://thrivecart.com/api/v1) + THRIVECART_TOKEN (API key)."),
    dict(name="samcart", description="SamCart REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/orders, /v1/products. Auth: SAMCART_BASE_URL (https://api.samcart.com) + SAMCART_TOKEN (API key)."),
    dict(name="bold_commerce", description="Bold Commerce REST API. ops get/post/put/delete (writes need confirm). e.g. /api/orders, /api/subscriptions. Auth: BOLD_COMMERCE_BASE_URL (https://api.boldcommerce.com) + BOLD_COMMERCE_TOKEN (API key bearer)."),
    dict(name="swell", description="Swell headless commerce REST/GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /products, /orders. Auth: SWELL_BASE_URL (https://{store}.swell.store) + SWELL_TOKEN (API key)."),
    dict(name="saleor", description="Saleor headless commerce GraphQL API. ops get/post/put/delete (writes need confirm). e.g. POST /graphql/ for products/orders queries and mutations. Auth: SALEOR_BASE_URL (https://{instance}.saleor.cloud) + SALEOR_TOKEN (OAuth bearer)."),
    dict(name="medusajs", description="Medusa (MedusaJS) headless commerce REST API. ops get/post/put/delete (writes need confirm). e.g. /admin/products, /admin/orders. Auth: MEDUSAJS_BASE_URL (https://{store}.medusajs.com) + MEDUSAJS_TOKEN (API-key bearer)."),
    dict(name="spree_commerce", description="Spree Commerce REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/platform/products, /api/v2/platform/orders. Auth: SPREE_COMMERCE_BASE_URL (https://{store}.example.com) + SPREE_COMMERCE_TOKEN (bearer)."),
    dict(name="oro_commerce", description="OroCommerce REST API. ops get/post/put/delete (writes need confirm). e.g. /api/products, /api/orders. Auth: ORO_COMMERCE_BASE_URL (https://{store}.example.com) + ORO_COMMERCE_TOKEN (OAuth bearer)."),
    dict(name="nopcommerce", description="nopCommerce REST API (plugin-based). ops get/post/put/delete (writes need confirm). e.g. /odata/Products, /odata/Orders. Auth: NOPCOMMERCE_BASE_URL (https://{store}.example.com) + NOPCOMMERCE_TOKEN (OAuth bearer)."),
    dict(name="zen_cart", description="Zen Cart REST API (plugin-based). ops get/post/put/delete (writes need confirm). e.g. /index.php?main_page=api&action=products. Auth: ZEN_CART_BASE_URL (https://{store}.example.com) + ZEN_CART_TOKEN (API key)."),
    dict(name="x_cart", description="X-Cart REST API. ops get/post/put/delete (writes need confirm). e.g. /api/2.0/products, /api/2.0/orders. Auth: X_CART_BASE_URL (https://{store}.example.com) + X_CART_TOKEN (API key)."),
    dict(name="cs_cart", basic=True, description="CS-Cart REST API. ops get/post/put/delete (writes need confirm). e.g. /api/products, /api/orders. Auth: CS_CART_BASE_URL (https://{store}.example.com) + CS_CART_TOKEN (email as basic-auth user, API key as pass)."),
    dict(name="shift4shop", description="Shift4Shop (3dcart) REST API. ops get/post/put/delete (writes need confirm). e.g. /3dCartWebAPI/v2/Products, /3dCartWebAPI/v2/Orders. Auth: SHIFT4SHOP_BASE_URL (https://apirest.3dcart.com) + SHIFT4SHOP_TOKEN (OAuth bearer)."),
    dict(name="storenvy", description="Storenvy REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/products, /v1/orders. Auth: STORENVY_BASE_URL (https://api.storenvy.com) + STORENVY_TOKEN (OAuth bearer)."),
    dict(name="bigcartel", description="Big Cartel REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts/{id}/products, /v1/accounts/{id}/orders. Auth: BIGCARTEL_BASE_URL (https://api.bigcartel.com) + BIGCARTEL_TOKEN (OAuth bearer)."),
    dict(name="loop_returns", description="Loop Returns REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/returns, /api/v1/orders. Auth: LOOP_RETURNS_BASE_URL (https://api.loopreturns.com) + LOOP_RETURNS_TOKEN (API-key bearer)."),
    dict(name="narvar", description="Narvar post-purchase/returns REST API. ops get/post/put/delete (writes need confirm). e.g. /returns, /tracking. Auth: NARVAR_BASE_URL (https://api.narvar.com) + NARVAR_TOKEN (bearer)."),
    dict(name="happy_returns", description="Happy Returns (PayPal) REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/returns, /v1/rmas. Auth: HAPPY_RETURNS_BASE_URL (https://api.happyreturns.com) + HAPPY_RETURNS_TOKEN (bearer)."),
    dict(name="aftership", description="AfterShip tracking/returns REST API. ops get/post/put/delete (writes need confirm). e.g. /v4/trackings, /v4/returns. Auth: AFTERSHIP_BASE_URL (https://api.aftership.com) + AFTERSHIP_TOKEN (API key)."),
    dict(name="shipengine", description="ShipEngine shipping REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/labels, /v1/shipments. Auth: SHIPENGINE_BASE_URL (https://api.shipengine.com) + SHIPENGINE_TOKEN (API key)."),
    dict(name="unleashed_software", description="Unleashed Software inventory REST API. ops get/post/put/delete (writes need confirm). e.g. /Products, /SalesOrders. Auth: UNLEASHED_SOFTWARE_BASE_URL (https://api.unleashedsoftware.com) + UNLEASHED_SOFTWARE_TOKEN (API ID + signed HMAC key)."),
    dict(name="fishbowl_inventory", description="Fishbowl Inventory REST API. ops get/post/put/delete (writes need confirm). e.g. /api/products, /api/sales-orders. Auth: FISHBOWL_INVENTORY_BASE_URL (https://{host}:{port}) + FISHBOWL_INVENTORY_TOKEN (bearer)."),
    dict(name="katana_mrp", description="Katana Manufacturing ERP REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/products, /v1/sales_orders. Auth: KATANA_MRP_BASE_URL (https://api.katanamrp.com) + KATANA_MRP_TOKEN (API-key bearer)."),
    dict(name="ordway", description="Ordway subscription billing REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions, /invoices. Auth: ORDWAY_BASE_URL (https://api.ordwaylabs.com) + ORDWAY_TOKEN (API-key bearer)."),
    dict(name="billwerk", description="Billwerk+ subscription billing REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions, /invoices. Auth: BILLWERK_BASE_URL (https://api.billwerk.plus) + BILLWERK_TOKEN (OAuth bearer)."),
    dict(name="maxio", description="Maxio (SaaSOptics/Chargify) billing REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions, /invoices. Auth: MAXIO_BASE_URL (https://{site}.chargify.com) + MAXIO_TOKEN (API-key bearer)."),
    dict(name="chargify", basic=True, description="Chargify (Maxio) subscription billing REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions.json, /invoices.json. Auth: CHARGIFY_BASE_URL (https://{site}.chargify.com) + CHARGIFY_TOKEN (API key as basic-auth user, 'x' as pass)."),
    dict(name="quaderno", description="Quaderno tax/invoicing REST API. ops get/post/put/delete (writes need confirm). e.g. /invoices, /taxes/rates. Auth: QUADERNO_BASE_URL (https://{account}.quadernoapp.com) + QUADERNO_TOKEN (API-key bearer)."),
    dict(name="sovos", description="Sovos tax-compliance REST API. ops get/post/put/delete (writes need confirm). e.g. /tax-calc, /filings. Auth: SOVOS_BASE_URL (https://api.sovos.com) + SOVOS_TOKEN (OAuth bearer)."),
    dict(name="taxjar", description="TaxJar (Stripe) sales-tax REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/taxes, /v2/transactions/orders. Auth: TAXJAR_BASE_URL (https://api.taxjar.com) + TAXJAR_TOKEN (API-key bearer)."),
    dict(name="fonoa", description="Fonoa tax-automation REST API. ops get/post/put/delete (writes need confirm). e.g. /tax/calculate, /invoices. Auth: FONOA_BASE_URL (https://api.fonoa.com) + FONOA_TOKEN (API-key bearer)."),
    dict(name="poynt", description="Poynt (GoDaddy) POS/payments REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /transactions. Auth: POYNT_BASE_URL (https://services.poynt.net) + POYNT_TOKEN (OAuth bearer)."),
    dict(name="paytm", description="Paytm payments REST API. ops get/post/put/delete (writes need confirm). e.g. /order/create, /order/status. Auth: PAYTM_BASE_URL (https://securegw.paytm.in) + PAYTM_TOKEN (merchant key HMAC/JWT signature)."),
    dict(name="alipay", description="Alipay Open Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /gateway.do?method=alipay.trade.pay, ?method=alipay.trade.query. Auth: ALIPAY_BASE_URL (https://openapi.alipay.com) + ALIPAY_TOKEN (app private-key signature)."),
    dict(name="wechat_pay", description="WeChat Pay REST API. ops get/post/put/delete (writes need confirm). e.g. /v3/pay/transactions/native, /v3/refund/domestic/refunds. Auth: WECHAT_PAY_BASE_URL (https://api.mch.weixin.qq.com) + WECHAT_PAY_TOKEN (merchant certificate signature)."),
    dict(name="splitit", description="Splitit installment-payments REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/plans, /api/v1/installments. Auth: SPLITIT_BASE_URL (https://web-api.production.splitit.com) + SPLITIT_TOKEN (API-key bearer)."),
    dict(name="laybuy", description="Laybuy installment-payments REST API. ops get/post/put/delete (writes need confirm). e.g. /order, /order/{id}/refund. Auth: LAYBUY_BASE_URL (https://api.laybuy.com) + LAYBUY_TOKEN (API-key bearer)."),
    dict(name="cashfree", description="Cashfree Payments REST API. ops get/post/put/delete (writes need confirm). e.g. /pg/orders, /pg/refunds. Auth: CASHFREE_BASE_URL (https://api.cashfree.com) + CASHFREE_TOKEN (client ID/secret bearer)."),
    dict(name="instamojo", description="Instamojo payments REST API. ops get/post/put/delete (writes need confirm). e.g. /api/1.1/payment-requests, /api/1.1/refunds. Auth: INSTAMOJO_BASE_URL (https://api.instamojo.com) + INSTAMOJO_TOKEN (OAuth bearer)."),
    dict(name="juspay", description="Juspay payments REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /txns. Auth: JUSPAY_BASE_URL (https://api.juspay.in) + JUSPAY_TOKEN (API-key bearer)."),
    dict(name="paytabs", description="PayTabs payments REST API. ops get/post/put/delete (writes need confirm). e.g. /payment/request, /payment/query. Auth: PAYTABS_BASE_URL (https://secure.paytabs.com) + PAYTABS_TOKEN (server key)."),
    dict(name="hyperpay", description="HyperPay payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/checkouts, /v1/payments. Auth: HYPERPAY_BASE_URL (https://eu-prod.oppwa.com) + HYPERPAY_TOKEN (access-token bearer)."),
    dict(name="tap_payments", description="Tap Payments (MENA) REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/charges, /v2/refunds. Auth: TAP_PAYMENTS_BASE_URL (https://api.tap.company) + TAP_PAYMENTS_TOKEN (secret-key bearer)."),
    dict(name="worldline", description="Worldline payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/{merchantId}/payments, /v1/{merchantId}/refunds. Auth: WORLDLINE_BASE_URL (https://api.worldline-solutions.com) + WORLDLINE_TOKEN (API key/secret HMAC signature)."),
    dict(name="nexi", description="Nexi (XPay) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /orders, /payments. Auth: NEXI_BASE_URL (https://xpay.nexigroup.com) + NEXI_TOKEN (API-key bearer)."),
    dict(name="sumup", description="SumUp payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v0.1/checkouts, /v0.1/transactions. Auth: SUMUP_BASE_URL (https://api.sumup.com) + SUMUP_TOKEN (OAuth bearer)."),
    dict(name="izettle", description="iZettle (PayPal Zettle) REST API. ops get/post/put/delete (writes need confirm). e.g. /organizations/self/purchases, /products/v2. Auth: IZETTLE_BASE_URL (https://oauth.zettle.com) + IZETTLE_TOKEN (OAuth bearer)."),
    dict(name="mypos", description="myPOS payments REST API. ops get/post/put/delete (writes need confirm). e.g. /IPCPurchase, /IPCRefund. Auth: MYPOS_BASE_URL (https://mypos.eu/vmp/checkout-api) + MYPOS_TOKEN (store key/secret signature)."),
    dict(name="paysafe", description="Paysafe payments REST API. ops get/post/put/delete (writes need confirm). e.g. /paymenthub/v1/payments, /paymenthub/v1/refunds. Auth: PAYSAFE_BASE_URL (https://api.paysafe.com) + PAYSAFE_TOKEN (API-key bearer)."),
    dict(name="rapyd", description="Rapyd payments/fintech REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/payments, /v1/payouts. Auth: RAPYD_BASE_URL (https://api.rapyd.net) + RAPYD_TOKEN (access-key/secret HMAC signature)."),
    dict(name="airwallex", description="Airwallex payments REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/pa/payment_intents, /api/v1/pa/refunds. Auth: AIRWALLEX_BASE_URL (https://api.airwallex.com) + AIRWALLEX_TOKEN (OAuth bearer)."),
    dict(name="wepay", description="WePay (Chase) payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/checkout/create, /v2/withdrawal/create. Auth: WEPAY_BASE_URL (https://wepayapi.com/v2) + WEPAY_TOKEN (OAuth bearer)."),
    dict(name="venmo", description="Venmo (PayPal) business REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/payments, /v1/refunds. Auth: VENMO_BASE_URL (https://api.venmo.com) + VENMO_TOKEN (OAuth bearer)."),
    dict(name="bolt", description="Bolt checkout/payments REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/orders, /v1/transactions. Auth: BOLT_BASE_URL (https://api.bolt.com) + BOLT_TOKEN (API-key bearer)."),
    dict(name="recharge", description="Recharge Payments subscription REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions, /charges. Auth: RECHARGE_BASE_URL (https://api.rechargeapps.com) + RECHARGE_TOKEN (API-key bearer)."),
    dict(name="ordergroove", description="Ordergroove subscription/retention REST API. ops get/post/put/delete (writes need confirm). e.g. /subscriptions, /orders. Auth: ORDERGROOVE_BASE_URL (https://api.ordergroove.com) + ORDERGROOVE_TOKEN (OAuth bearer)."),
    dict(name="blue_yonder", description="Blue Yonder (JDA) supply-chain REST API. ops get/post/put/delete (writes need confirm). e.g. /forecasting/v1, /inventory/v1. Auth: BLUE_YONDER_BASE_URL (https://api.blueyonder.com) + BLUE_YONDER_TOKEN (OAuth bearer)."),
    dict(name="manhattan_associates", description="Manhattan Associates (Active Omni) REST API. ops get/post/put/delete (writes need confirm). e.g. /order, /inventory. Auth: MANHATTAN_ASSOCIATES_BASE_URL (https://{tenant}.manh.com) + MANHATTAN_ASSOCIATES_TOKEN (OAuth bearer)."),
    dict(name="korona_pos", description="KORONA POS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/products, /api/v3/sales. Auth: KORONA_POS_BASE_URL (https://api.koronacloud.com) + KORONA_POS_TOKEN (OAuth bearer)."),
    dict(name="talech", description="talech (U.S. Bank) POS REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/items, /v1/orders. Auth: TALECH_BASE_URL (https://api.talech.com) + TALECH_TOKEN (OAuth bearer)."),
    dict(name="erply", description="Erply POS/retail-management REST API. ops get/post/put/delete (writes need confirm). e.g. /api/?clientCode={c}&request=getProducts, request=getSalesDocuments. Auth: ERPLY_BASE_URL (https://{clientCode}.erply.com) + ERPLY_TOKEN (session key)."),
    dict(name="celerant", description="Celerant Technology retail POS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/products, /api/orders. Auth: CELERANT_BASE_URL (https://api.celerant.com) + CELERANT_TOKEN (API key)."),
    dict(name="springboard_retail", description="Springboard Retail POS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v3/products, /api/v3/sales. Auth: SPRINGBOARD_RETAIL_BASE_URL (https://api.springboardretail.com) + SPRINGBOARD_RETAIL_TOKEN (OAuth bearer)."),
    dict(name="epos_now", description="Epos Now POS REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v4/Product, /api/v4/SalesV2. Auth: EPOS_NOW_BASE_URL (https://api.eposnowhq.com) + EPOS_NOW_TOKEN (OAuth bearer)."),
    dict(name="commerce_layer", description="Commerce Layer headless commerce REST API. ops get/post/put/delete (writes need confirm). e.g. /api/skus, /api/orders. Auth: COMMERCE_LAYER_BASE_URL (https://{org}.commercelayer.io) + COMMERCE_LAYER_TOKEN (OAuth2 client-credentials bearer)."),
    dict(name="crystallize", description="Crystallize headless commerce GraphQL API. ops get/post/put/delete (writes need confirm). e.g. POST /graphql for product/order queries and mutations. Auth: CRYSTALLIZE_BASE_URL (https://api.crystallize.com/{tenant}) + CRYSTALLIZE_TOKEN (access-token bearer)."),
    dict(name="expensify", description="Expensify expense management REST. ops get/post/put/delete (writes need confirm). e.g. /api/policy, /api/expense. Auth: EXPENSIFY_BASE_URL (https://integrations.expensify.com) + EXPENSIFY_TOKEN (partner userID/secret)."),
    dict(name="certify", description="Certify (Emburse) expense/travel REST. ops get/post/put/delete (writes need confirm). Auth: CERTIFY_BASE_URL (https://api.certify.com) + CERTIFY_TOKEN (API key)."),
    dict(name="emburse", description="Emburse Spend/Expense REST. ops get/post/put/delete (writes need confirm). Auth: EMBURSE_BASE_URL (https://api.emburse.com) + EMBURSE_TOKEN (API key)."),
    dict(name="divvy", description="Divvy (BILL Spend & Expense) REST. ops get/post/put/delete (writes need confirm). Auth: DIVVY_BASE_URL (https://api.getdivvy.com) + DIVVY_TOKEN (API key)."),
    dict(name="tipalti", description="Tipalti payables automation REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/payees, /api/v1/payments. Auth: TIPALTI_BASE_URL (https://api.tipalti.com) + TIPALTI_TOKEN (API key)."),
    dict(name="melio", description="Melio B2B payments REST. ops get/post/put/delete (writes need confirm). Auth: MELIO_BASE_URL (https://api.melio.com) + MELIO_TOKEN (API key)."),
    dict(name="fundbox", description="Fundbox business financing REST. ops get/post/put/delete (writes need confirm). Auth: FUNDBOX_BASE_URL (https://api.fundbox.com) + FUNDBOX_TOKEN (API key)."),
    dict(name="bill_com_read", description="BILL.com read-only reporting REST (companion to billdotcom). ops get only. Auth: BILL_COM_READ_BASE_URL (https://api.bill.com) + BILL_COM_READ_TOKEN (API key, read scope)."),
    dict(name="plooto", description="Plooto payments automation REST. ops get/post/put/delete (writes need confirm). Auth: PLOOTO_BASE_URL (https://api.plooto.com) + PLOOTO_TOKEN (API key)."),
    dict(name="routable", description="Routable AP/AR automation REST. ops get/post/put/delete (writes need confirm). Auth: ROUTABLE_BASE_URL (https://api.routable.com) + ROUTABLE_TOKEN (API key)."),
    dict(name="stampli", description="Stampli AP automation REST. ops get/post/put/delete (writes need confirm). Auth: STAMPLI_BASE_URL (https://api.stampli.com) + STAMPLI_TOKEN (API key)."),
    dict(name="airbase_read", description="Airbase read-only spend management reporting REST (companion to airbase). ops get only. Auth: AIRBASE_READ_BASE_URL (https://api.airbase.com) + AIRBASE_READ_TOKEN (API key, read scope)."),
    dict(name="sap_ariba_network", description="SAP Ariba Network procurement REST (distinct from Ariba buyer API). ops get/post/put/delete (writes need confirm). Auth: SAP_ARIBA_NETWORK_BASE_URL (https://api.ariba.com) + SAP_ARIBA_NETWORK_TOKEN (OAuth bearer)."),
    dict(name="expensewatch", description="ExpenseWatch expense management REST. ops get/post/put/delete (writes need confirm). Auth: EXPENSEWATCH_BASE_URL (https://api.expensewatch.com) + EXPENSEWATCH_TOKEN (API key)."),
    dict(name="fyle", description="Fyle expense management REST. ops get/post/put/delete (writes need confirm). e.g. /platform/v1/expenses. Auth: FYLE_BASE_URL (https://{org}.fylehq.com) + FYLE_TOKEN (OAuth bearer)."),
    dict(name="rho", description="Rho business banking/spend REST. ops get/post/put/delete (writes need confirm). Auth: RHO_BASE_URL (https://api.rho.co) + RHO_TOKEN (API key)."),
    dict(name="highradius", description="HighRadius order-to-cash/treasury REST. ops get/post/put/delete (writes need confirm). Auth: HIGHRADIUS_BASE_URL (https://api.highradius.com) + HIGHRADIUS_TOKEN (OAuth bearer)."),
    dict(name="tabapay", description="TabaPay payment disbursement REST. ops get/post/put/delete (writes need confirm). Auth: TABAPAY_BASE_URL (https://api.tabapay.com) + TABAPAY_TOKEN (API key)."),
    dict(name="dwolla", description="Dwolla ACH payments REST. ops get/post/put/delete (writes need confirm). e.g. /customers, /transfers. Auth: DWOLLA_BASE_URL (https://api.dwolla.com) + DWOLLA_TOKEN (OAuth bearer)."),
    dict(name="marqeta", description="Marqeta card issuing REST. ops get/post/put/delete (writes need confirm). e.g. /cards, /users. Auth: MARQETA_BASE_URL (https://{env}.marqeta.com) + MARQETA_TOKEN.", basic=True),
    dict(name="galileo_fs", description="Galileo Financial Solutions card processing REST. ops get/post/put/delete (writes need confirm). Auth: GALILEO_FS_BASE_URL (https://api.gpsrt.galileo-ft.com) + GALILEO_FS_TOKEN (API key)."),
    dict(name="synctera", description="Synctera banking-as-a-service REST. ops get/post/put/delete (writes need confirm). Auth: SYNCTERA_BASE_URL (https://api.synctera.com) + SYNCTERA_TOKEN (API key)."),
    dict(name="unit_finance", description="Unit banking-as-a-service REST. ops get/post/put/delete (writes need confirm). Auth: UNIT_FINANCE_BASE_URL (https://api.s.unit.sh) + UNIT_FINANCE_TOKEN (API key)."),
    dict(name="increase", description="Increase banking infrastructure REST. ops get/post/put/delete (writes need confirm). Auth: INCREASE_BASE_URL (https://api.increase.com) + INCREASE_TOKEN (API key)."),
    dict(name="column_bank", description="Column banking API REST. ops get/post/put/delete (writes need confirm). Auth: COLUMN_BANK_BASE_URL (https://api.column.com) + COLUMN_BANK_TOKEN (API key).", basic=True),
    dict(name="treasury_prime", description="Treasury Prime banking-as-a-service REST. ops get/post/put/delete (writes need confirm). Auth: TREASURY_PRIME_BASE_URL (https://api.treasuryprime.com) + TREASURY_PRIME_TOKEN (API key)."),
    dict(name="finicity", description="Finicity (Mastercard) open banking REST. ops get/post/put/delete (writes need confirm). Auth: FINICITY_BASE_URL (https://api.finicity.com) + FINICITY_TOKEN (App key)."),
    dict(name="yodlee", description="Envestnet Yodlee account aggregation REST. ops get/post/put/delete (writes need confirm). Auth: YODLEE_BASE_URL (https://api.yodlee.com) + YODLEE_TOKEN (OAuth bearer)."),
    dict(name="mx_platform", description="MX financial data platform REST. ops get/post/put/delete (writes need confirm). Auth: MX_PLATFORM_BASE_URL (https://api.mx.com) + MX_PLATFORM_TOKEN (API key)."),
    dict(name="codat", description="Codat accounting/commerce data aggregation REST. ops get/post/put/delete (writes need confirm). Auth: CODAT_BASE_URL (https://api.codat.io) + CODAT_TOKEN (API key)."),
    dict(name="rutter", description="Rutter commerce/accounting unified REST. ops get/post/put/delete (writes need confirm). Auth: RUTTER_BASE_URL (https://production.rutterapi.com) + RUTTER_TOKEN (API key)."),
    dict(name="merge_dev", description="Merge.dev unified HR/accounting API REST. ops get/post/put/delete (writes need confirm). Auth: MERGE_DEV_BASE_URL (https://api.merge.dev) + MERGE_DEV_TOKEN (API key)."),
    dict(name="finch_hr", description="Finch unified HR/payroll API REST. ops get/post/put/delete (writes need confirm). Auth: FINCH_HR_BASE_URL (https://api.tryfinch.com) + FINCH_HR_TOKEN (OAuth bearer)."),
    dict(name="trinet", description="TriNet HR/PEO REST. ops get/post/put/delete (writes need confirm). Auth: TRINET_BASE_URL (https://api.trinet.com) + TRINET_TOKEN (OAuth bearer)."),
    dict(name="justworks", description="Justworks PEO/HR REST. ops get/post/put/delete (writes need confirm). Auth: JUSTWORKS_BASE_URL (https://api.justworks.com) + JUSTWORKS_TOKEN (API key)."),
    dict(name="insperity", description="Insperity HR/PEO REST. ops get/post/put/delete (writes need confirm). Auth: INSPERITY_BASE_URL (https://api.insperity.com) + INSPERITY_TOKEN (OAuth bearer)."),
    dict(name="oyster_hr", description="Oyster HR global employment REST. ops get/post/put/delete (writes need confirm). Auth: OYSTER_HR_BASE_URL (https://api.oysterhr.com) + OYSTER_HR_TOKEN (API key)."),
    dict(name="remote_com", description="Remote.com global employment/payroll REST. ops get/post/put/delete (writes need confirm). e.g. /v1/employees. Auth: REMOTE_COM_BASE_URL (https://gateway.remote.com) + REMOTE_COM_TOKEN (API key)."),
    dict(name="papaya_global", description="Papaya Global payroll/EOR REST. ops get/post/put/delete (writes need confirm). Auth: PAPAYA_GLOBAL_BASE_URL (https://api.papayaglobal.com) + PAPAYA_GLOBAL_TOKEN (API key)."),
    dict(name="multiplier_hr", description="Multiplier global payroll/EOR REST. ops get/post/put/delete (writes need confirm). Auth: MULTIPLIER_HR_BASE_URL (https://api.usemultiplier.com) + MULTIPLIER_HR_TOKEN (API key)."),
    dict(name="omnipresent", description="Omnipresent global employment REST. ops get/post/put/delete (writes need confirm). Auth: OMNIPRESENT_BASE_URL (https://api.omnipresent.com) + OMNIPRESENT_TOKEN (API key)."),
    dict(name="sequoia_hr", description="Sequoia One HR/benefits REST. ops get/post/put/delete (writes need confirm). Auth: SEQUOIA_HR_BASE_URL (https://api.sequoia.com) + SEQUOIA_HR_TOKEN (API key)."),
    dict(name="namely", description="Namely HR platform REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/profiles. Auth: NAMELY_BASE_URL (https://{company}.namely.com) + NAMELY_TOKEN (OAuth bearer)."),
    dict(name="zenefits", description="TriNet Zenefits HR REST. ops get/post/put/delete (writes need confirm). Auth: ZENEFITS_BASE_URL (https://api.zenefits.com) + ZENEFITS_TOKEN (OAuth bearer)."),
    dict(name="isolved", description="isolved HCM/payroll REST. ops get/post/put/delete (writes need confirm). Auth: ISOLVED_BASE_URL (https://api.isolvedhcm.com) + ISOLVED_TOKEN (OAuth bearer)."),
    dict(name="ceridian_dayforce", description="Ceridian Dayforce HCM REST. ops get/post/put/delete (writes need confirm). Auth: CERIDIAN_DAYFORCE_BASE_URL (https://{env}.dayforcehcm.com) + CERIDIAN_DAYFORCE_TOKEN (OAuth bearer)."),
    dict(name="oracle_hcm_cloud", description="Oracle HCM Cloud REST. ops get/post/put/delete (writes need confirm). e.g. /hcmRestApi/resources. Auth: ORACLE_HCM_CLOUD_BASE_URL (https://{pod}.oraclecloud.com) + ORACLE_HCM_CLOUD_TOKEN (OAuth bearer)."),
    dict(name="sap_successfactors_ec", description="SAP SuccessFactors Employee Central OData REST (distinct from generic successfactors entry). ops get/post/put/delete (writes need confirm). Auth: SAP_SUCCESSFACTORS_EC_BASE_URL (https://api{n}.successfactors.com) + SAP_SUCCESSFACTORS_EC_TOKEN (OAuth bearer)."),
    dict(name="workday_hcm", description="Workday HCM/Financials REST (distinct from generic workday entry). ops get/post/put/delete (writes need confirm). Auth: WORKDAY_HCM_BASE_URL (https://{tenant}.workday.com) + WORKDAY_HCM_TOKEN (OAuth bearer)."),
    dict(name="jobvite", description="Jobvite recruiting REST. ops get/post/put/delete (writes need confirm). Auth: JOBVITE_BASE_URL (https://api.jobvite.com) + JOBVITE_TOKEN (API key)."),
    dict(name="jazzhr", description="JazzHR recruiting REST. ops get/post/put/delete (writes need confirm). e.g. /applicants, /jobs. Auth: JAZZHR_BASE_URL (https://api.resumatorapi.com) + JAZZHR_TOKEN (API key)."),
    dict(name="breezyhr", description="Breezy HR recruiting REST. ops get/post/put/delete (writes need confirm). Auth: BREEZYHR_BASE_URL (https://api.breezy.hr) + BREEZYHR_TOKEN (API key)."),
    dict(name="recruitee", description="Recruitee recruiting REST. ops get/post/put/delete (writes need confirm). e.g. /c/{id}/candidates. Auth: RECRUITEE_BASE_URL (https://api.recruitee.com) + RECRUITEE_TOKEN (API key)."),
    dict(name="teamtailor", description="Teamtailor recruiting REST. ops get/post/put/delete (writes need confirm). Auth: TEAMTAILOR_BASE_URL (https://api.teamtailor.com) + TEAMTAILOR_TOKEN (API key)."),
    dict(name="ashby", description="Ashby recruiting/ATS REST. ops get/post/put/delete (writes need confirm). e.g. /candidate.list, /application.create. Auth: ASHBY_BASE_URL (https://api.ashbyhq.com) + ASHBY_TOKEN (API key).", basic=True),
    dict(name="fountain_ats", description="Fountain high-volume hiring REST. ops get/post/put/delete (writes need confirm). Auth: FOUNTAIN_ATS_BASE_URL (https://api.fountain.com) + FOUNTAIN_ATS_TOKEN (API key)."),
    dict(name="jobdiva", description="JobDiva ATS/staffing REST. ops get/post/put/delete (writes need confirm). Auth: JOBDIVA_BASE_URL (https://api.jobdiva.com) + JOBDIVA_TOKEN (OAuth bearer)."),
    dict(name="bullhorn", description="Bullhorn ATS/staffing REST. ops get/post/put/delete (writes need confirm). e.g. /entity/JobOrder, /entity/Candidate. Auth: BULLHORN_BASE_URL (https://rest.bullhornstaffing.com) + BULLHORN_TOKEN (OAuth bearer)."),
    dict(name="crelate", description="Crelate ATS/staffing REST. ops get/post/put/delete (writes need confirm). Auth: CRELATE_BASE_URL (https://app.crelate.com) + CRELATE_TOKEN (API key)."),
    dict(name="ceipal", description="Ceipal ATS/staffing REST. ops get/post/put/delete (writes need confirm). Auth: CEIPAL_BASE_URL (https://api.ceipal.com) + CEIPAL_TOKEN (OAuth bearer)."),
    dict(name="loxo", description="Loxo recruiting CRM/ATS REST. ops get/post/put/delete (writes need confirm). Auth: LOXO_BASE_URL (https://api.loxo.co) + LOXO_TOKEN (API key)."),
    dict(name="recruiterflow", description="Recruiterflow ATS/CRM REST. ops get/post/put/delete (writes need confirm). Auth: RECRUITERFLOW_BASE_URL (https://api.recruiterflow.com) + RECRUITERFLOW_TOKEN (API key)."),
    dict(name="manatal", description="Manatal recruiting/ATS REST. ops get/post/put/delete (writes need confirm). Auth: MANATAL_BASE_URL (https://api.manatal.com) + MANATAL_TOKEN (API key)."),
    dict(name="hireology", description="Hireology hiring platform REST. ops get/post/put/delete (writes need confirm). Auth: HIREOLOGY_BASE_URL (https://api.hireology.com) + HIREOLOGY_TOKEN (API key)."),
    dict(name="paradox_olivia", description="Paradox (Olivia) conversational recruiting REST. ops get/post/put/delete (writes need confirm). Auth: PARADOX_OLIVIA_BASE_URL (https://api.paradox.ai) + PARADOX_OLIVIA_TOKEN (API key)."),
    dict(name="hirevue", description="HireVue video interviewing REST. ops get/post/put/delete (writes need confirm). Auth: HIREVUE_BASE_URL (https://api.hirevue.com) + HIREVUE_TOKEN (OAuth bearer)."),
    dict(name="modern_hire", description="Modern Hire (HireVue) assessment REST. ops get/post/put/delete (writes need confirm). Auth: MODERN_HIRE_BASE_URL (https://api.modernhire.com) + MODERN_HIRE_TOKEN (API key)."),
    dict(name="criteria_corp", description="Criteria Corp pre-employment testing REST. ops get/post/put/delete (writes need confirm). Auth: CRITERIA_CORP_BASE_URL (https://api.criteriacorp.com) + CRITERIA_CORP_TOKEN (API key)."),
    dict(name="checkr", description="Checkr background check REST. ops get/post/put/delete (writes need confirm). e.g. /v1/candidates, /v1/reports. Auth: CHECKR_BASE_URL (https://api.checkr.com) + CHECKR_TOKEN (API key).", basic=True),
    dict(name="sterling_check", description="Sterling background check REST. ops get/post/put/delete (writes need confirm). Auth: STERLING_CHECK_BASE_URL (https://api.sterlingcheck.com) + STERLING_CHECK_TOKEN (OAuth bearer)."),
    dict(name="hireright", description="HireRight background screening REST. ops get/post/put/delete (writes need confirm). Auth: HIRERIGHT_BASE_URL (https://api.hireright.com) + HIRERIGHT_TOKEN (OAuth bearer)."),
    dict(name="goodhire", description="GoodHire background check REST. ops get/post/put/delete (writes need confirm). Auth: GOODHIRE_BASE_URL (https://api.goodhire.com) + GOODHIRE_TOKEN (API key)."),
    dict(name="culture_amp", description="Culture Amp employee engagement REST. ops get/post/put/delete (writes need confirm). Auth: CULTURE_AMP_BASE_URL (https://api.cultureamp.com) + CULTURE_AMP_TOKEN (OAuth bearer)."),
    dict(name="leapsome", description="Leapsome performance/engagement REST. ops get/post/put/delete (writes need confirm). Auth: LEAPSOME_BASE_URL (https://api.leapsome.com) + LEAPSOME_TOKEN (API key)."),
    dict(name="betterworks", description="Betterworks OKR/performance REST. ops get/post/put/delete (writes need confirm). Auth: BETTERWORKS_BASE_URL (https://api.betterworks.com) + BETTERWORKS_TOKEN (OAuth bearer)."),
    dict(name="peoplebox", description="Peoplebox OKR/performance REST. ops get/post/put/delete (writes need confirm). Auth: PEOPLEBOX_BASE_URL (https://api.peoplebox.ai) + PEOPLEBOX_TOKEN (API key)."),
    dict(name="lessonly", description="Lessonly (Seismic) training REST. ops get/post/put/delete (writes need confirm). Auth: LESSONLY_BASE_URL (https://api.lessonly.com) + LESSONLY_TOKEN (API key)."),
    dict(name="docebo", description="Docebo learning management REST. ops get/post/put/delete (writes need confirm). e.g. /learn/v1/courses. Auth: DOCEBO_BASE_URL (https://{subdomain}.docebosaas.com) + DOCEBO_TOKEN (OAuth bearer)."),
    dict(name="litmos", description="Litmos learning management REST. ops get/post/put/delete (writes need confirm). Auth: LITMOS_BASE_URL (https://api.litmos.com) + LITMOS_TOKEN (API key)."),
    dict(name="talentlms", description="TalentLMS learning management REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/users, /api/v1/courses. Auth: TALENTLMS_BASE_URL (https://{domain}.talentlms.com) + TALENTLMS_TOKEN (API key).", basic=True),
    dict(name="degreed", description="Degreed learning experience platform REST. ops get/post/put/delete (writes need confirm). Auth: DEGREED_BASE_URL (https://api.degreed.com) + DEGREED_TOKEN (OAuth bearer)."),
    dict(name="cornerstone_ondemand_lms", description="Cornerstone OnDemand learning module REST (distinct from generic cornerstone entry). ops get/post/put/delete (writes need confirm). Auth: CORNERSTONE_ONDEMAND_LMS_BASE_URL (https://{portal}.csod.com) + CORNERSTONE_ONDEMAND_LMS_TOKEN (OAuth bearer)."),
    dict(name="visier", description="Visier people analytics REST. ops get/post/put/delete (writes need confirm). Auth: VISIER_BASE_URL (https://{env}.visier.io) + VISIER_TOKEN (API key).", basic=True),
    dict(name="peakon", description="Workday Peakon employee listening REST. ops get/post/put/delete (writes need confirm). Auth: PEAKON_BASE_URL (https://api.peakon.com) + PEAKON_TOKEN (API key)."),
    dict(name="officevibe", description="Officevibe (Workleap) engagement REST. ops get/post/put/delete (writes need confirm). Auth: OFFICEVIBE_BASE_URL (https://api.officevibe.com) + OFFICEVIBE_TOKEN (API key)."),
    dict(name="deputy", description="Deputy workforce scheduling REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/resource/Employee. Auth: DEPUTY_BASE_URL (https://{subdomain}.deputy.com) + DEPUTY_TOKEN (OAuth bearer)."),
    dict(name="when_i_work", description="When I Work scheduling REST. ops get/post/put/delete (writes need confirm). Auth: WHEN_I_WORK_BASE_URL (https://api.wheniwork.com) + WHEN_I_WORK_TOKEN (API key)."),
    dict(name="humanity_app", description="Humanity (TCP Software) workforce scheduling REST. ops get/post/put/delete (writes need confirm). Auth: HUMANITY_APP_BASE_URL (https://www.humanity.com) + HUMANITY_APP_TOKEN (OAuth bearer)."),
    dict(name="tsheets", description="QuickBooks Time (TSheets) time tracking REST. ops get/post/put/delete (writes need confirm). e.g. /v1/timesheets. Auth: TSHEETS_BASE_URL (https://rest.tsheets.com) + TSHEETS_TOKEN (OAuth bearer)."),
    dict(name="clockify", description="Clockify time tracking REST. ops get/post/put/delete (writes need confirm). e.g. /v1/workspaces/{id}/time-entries. Auth: CLOCKIFY_BASE_URL (https://api.clockify.me) + CLOCKIFY_TOKEN (API key)."),
    dict(name="harvest_time", description="Harvest time tracking/invoicing REST. ops get/post/put/delete (writes need confirm). e.g. /v2/time_entries. Auth: HARVEST_TIME_BASE_URL (https://api.harvestapp.com) + HARVEST_TIME_TOKEN (OAuth bearer)."),
    dict(name="toggl", description="Toggl Track time tracking REST. ops get/post/put/delete (writes need confirm). e.g. /api/v9/time_entries. Auth: TOGGL_BASE_URL (https://api.track.toggl.com) + TOGGL_TOKEN (API token).", basic=True),
    dict(name="replicon", description="Replicon time/expense tracking REST. ops get/post/put/delete (writes need confirm). Auth: REPLICON_BASE_URL (https://{pod}.replicon.com) + REPLICON_TOKEN (OAuth bearer)."),
    dict(name="kronos_ukg_wfc", description="UKG Workforce Central (Kronos) REST (distinct from generic ukg entry). ops get/post/put/delete (writes need confirm). Auth: KRONOS_UKG_WFC_BASE_URL (https://{env}.workforceready.com) + KRONOS_UKG_WFC_TOKEN (OAuth bearer)."),
    dict(name="wagepoint", description="Wagepoint payroll REST. ops get/post/put/delete (writes need confirm). Auth: WAGEPOINT_BASE_URL (https://api.wagepoint.com) + WAGEPOINT_TOKEN (API key)."),
    dict(name="onpay", description="OnPay payroll REST. ops get/post/put/delete (writes need confirm). Auth: ONPAY_BASE_URL (https://app.onpay.com) + ONPAY_TOKEN (API key)."),
    dict(name="patriot_payroll", description="Patriot Software payroll REST. ops get/post/put/delete (writes need confirm). Auth: PATRIOT_PAYROLL_BASE_URL (https://api.patriotsoftware.com) + PATRIOT_PAYROLL_TOKEN (API key)."),
    dict(name="sage_payroll", description="Sage Payroll REST. ops get/post/put/delete (writes need confirm). Auth: SAGE_PAYROLL_BASE_URL (https://api.sage.com) + SAGE_PAYROLL_TOKEN (OAuth bearer)."),
    dict(name="payfit", description="PayFit European payroll REST. ops get/post/put/delete (writes need confirm). Auth: PAYFIT_BASE_URL (https://partner-api.payfit.com) + PAYFIT_TOKEN (OAuth bearer)."),
    dict(name="employment_hero", description="Employment Hero HR/payroll REST. ops get/post/put/delete (writes need confirm). Auth: EMPLOYMENT_HERO_BASE_URL (https://api.employmenthero.com) + EMPLOYMENT_HERO_TOKEN (OAuth bearer)."),
    dict(name="humaans", description="Humaans HR platform REST. ops get/post/put/delete (writes need confirm). e.g. /people, /employments. Auth: HUMAANS_BASE_URL (https://app.humaans.io) + HUMAANS_TOKEN (API key)."),
    dict(name="personio", description="Personio HR management REST. ops get/post/put/delete (writes need confirm). e.g. /v1/company/employees. Auth: PERSONIO_BASE_URL (https://api.personio.de) + PERSONIO_TOKEN (OAuth bearer)."),
    dict(name="factorial_hr", description="Factorial HR REST. ops get/post/put/delete (writes need confirm). Auth: FACTORIAL_HR_BASE_URL (https://api.factorialhr.com) + FACTORIAL_HR_TOKEN (API key)."),
    dict(name="sesame_hr", description="Sesame HR REST. ops get/post/put/delete (writes need confirm). Auth: SESAME_HR_BASE_URL (https://api.sesamehr.com) + SESAME_HR_TOKEN (API key)."),
    dict(name="charliehr", description="CharlieHR REST. ops get/post/put/delete (writes need confirm). Auth: CHARLIEHR_BASE_URL (https://api.charliehr.com) + CHARLIEHR_TOKEN (API key)."),
    dict(name="freshteam", description="Freshteam (Freshworks) HR/ATS REST. ops get/post/put/delete (writes need confirm). Auth: FRESHTEAM_BASE_URL (https://{domain}.freshteam.com) + FRESHTEAM_TOKEN (API key)."),
    dict(name="zoho_people", description="Zoho People HR REST. ops get/post/put/delete (writes need confirm). e.g. /people/api/forms/employee/records. Auth: ZOHO_PEOPLE_BASE_URL (https://people.zoho.com) + ZOHO_PEOPLE_TOKEN (OAuth bearer)."),
    dict(name="zoho_recruit", description="Zoho Recruit ATS REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_RECRUIT_BASE_URL (https://recruit.zoho.com) + ZOHO_RECRUIT_TOKEN (OAuth bearer)."),
    dict(name="zoho_books", description="Zoho Books accounting REST. ops get/post/put/delete (writes need confirm). e.g. /books/v3/invoices. Auth: ZOHO_BOOKS_BASE_URL (https://books.zoho.com) + ZOHO_BOOKS_TOKEN (OAuth bearer)."),
    dict(name="zoho_expense", description="Zoho Expense REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_EXPENSE_BASE_URL (https://expense.zoho.com) + ZOHO_EXPENSE_TOKEN (OAuth bearer)."),
    dict(name="myob", description="MYOB accounting REST. ops get/post/put/delete (writes need confirm). e.g. /accountright/{id}/Sale/Invoice. Auth: MYOB_BASE_URL (https://api.myob.com) + MYOB_TOKEN (OAuth bearer)."),
    dict(name="waveapps", description="Wave accounting REST/GraphQL. ops get/post/put/delete (writes need confirm). Auth: WAVEAPPS_BASE_URL (https://gql.waveapps.com) + WAVEAPPS_TOKEN (OAuth bearer)."),
    dict(name="kashoo", description="Kashoo accounting REST. ops get/post/put/delete (writes need confirm). Auth: KASHOO_BASE_URL (https://api.kashoo.com) + KASHOO_TOKEN (OAuth bearer)."),
    dict(name="freeagent", description="FreeAgent accounting REST. ops get/post/put/delete (writes need confirm). e.g. /v2/invoices. Auth: FREEAGENT_BASE_URL (https://api.freeagent.com) + FREEAGENT_TOKEN (OAuth bearer)."),
    dict(name="clearbooks", description="Clear Books accounting REST. ops get/post/put/delete (writes need confirm). Auth: CLEARBOOKS_BASE_URL (https://my.clearbooks.co.uk) + CLEARBOOKS_TOKEN (API key)."),
    dict(name="pandle", description="Pandle accounting REST. ops get/post/put/delete (writes need confirm). Auth: PANDLE_BASE_URL (https://pandle.com) + PANDLE_TOKEN (API key)."),
    dict(name="fresha_pay", description="Fresha payments/invoicing REST. ops get/post/put/delete (writes need confirm). Auth: FRESHA_PAY_BASE_URL (https://api.fresha.com) + FRESHA_PAY_TOKEN (OAuth bearer)."),
    dict(name="lightyear_ap", description="Lightyear AP automation REST. ops get/post/put/delete (writes need confirm). Auth: LIGHTYEAR_AP_BASE_URL (https://api.lightyear.cloud) + LIGHTYEAR_AP_TOKEN (API key)."),
    dict(name="dext_prepare", description="Dext Prepare bookkeeping automation REST. ops get/post/put/delete (writes need confirm). Auth: DEXT_PREPARE_BASE_URL (https://api.dext.com) + DEXT_PREPARE_TOKEN (API key)."),
    dict(name="receipt_bank", description="Receipt Bank (Dext) legacy receipt capture REST. ops get/post/put/delete (writes need confirm). Auth: RECEIPT_BANK_BASE_URL (https://api.receipt-bank.com) + RECEIPT_BANK_TOKEN (API key)."),
    dict(name="hubdoc", description="Hubdoc (Xero) document capture REST. ops get/post/put/delete (writes need confirm). Auth: HUBDOC_BASE_URL (https://app.hubdoc.com) + HUBDOC_TOKEN (OAuth bearer)."),
    dict(name="fathom_analytics_fin", description="Fathom financial reporting/analysis REST. ops get/post/put/delete (writes need confirm). Auth: FATHOM_ANALYTICS_FIN_BASE_URL (https://api.fathomhq.com) + FATHOM_ANALYTICS_FIN_TOKEN (API key)."),
    dict(name="spotlight_reporting", description="Spotlight Reporting financial reporting REST. ops get/post/put/delete (writes need confirm). Auth: SPOTLIGHT_REPORTING_BASE_URL (https://api.spotlightreporting.com) + SPOTLIGHT_REPORTING_TOKEN (API key)."),
    dict(name="syft_analytics", description="Syft Analytics financial reporting REST. ops get/post/put/delete (writes need confirm). Auth: SYFT_ANALYTICS_BASE_URL (https://api.syftanalytics.com) + SYFT_ANALYTICS_TOKEN (API key)."),
    dict(name="datarails", description="Datarails FP&A REST. ops get/post/put/delete (writes need confirm). Auth: DATARAILS_BASE_URL (https://api.datarails.com) + DATARAILS_TOKEN (API key)."),
    dict(name="mosaic_tech", description="Mosaic FP&A/strategic finance REST. ops get/post/put/delete (writes need confirm). Auth: MOSAIC_TECH_BASE_URL (https://api.mosaic.tech) + MOSAIC_TECH_TOKEN (API key)."),
    dict(name="cube_software", description="Cube FP&A REST. ops get/post/put/delete (writes need confirm). Auth: CUBE_SOFTWARE_BASE_URL (https://api.cubesoftware.com) + CUBE_SOFTWARE_TOKEN (API key)."),
    dict(name="jirav", description="Jirav FP&A/budgeting REST. ops get/post/put/delete (writes need confirm). Auth: JIRAV_BASE_URL (https://api.jirav.com) + JIRAV_TOKEN (API key)."),
    dict(name="vena_solutions", description="Vena Solutions FP&A REST. ops get/post/put/delete (writes need confirm). Auth: VENA_SOLUTIONS_BASE_URL (https://api.vena.io) + VENA_SOLUTIONS_TOKEN (OAuth bearer)."),
    dict(name="prophix", description="Prophix FP&A/CPM REST. ops get/post/put/delete (writes need confirm). Auth: PROPHIX_BASE_URL (https://api.prophix.com) + PROPHIX_TOKEN (API key)."),
    dict(name="board_cpm", description="Board CPM/BI platform REST. ops get/post/put/delete (writes need confirm). Auth: BOARD_CPM_BASE_URL (https://api.board.com) + BOARD_CPM_TOKEN (API key)."),
    dict(name="onestream", description="OneStream CPM/consolidation REST. ops get/post/put/delete (writes need confirm). Auth: ONESTREAM_BASE_URL (https://api.onestreamsoftware.com) + ONESTREAM_TOKEN (OAuth bearer)."),
    dict(name="host_analytics", description="Host Analytics (Planful legacy) CPM REST. ops get/post/put/delete (writes need confirm). Auth: HOST_ANALYTICS_BASE_URL (https://api.hostanalytics.com) + HOST_ANALYTICS_TOKEN (API key)."),
    dict(name="adaptive_insights", description="Workday Adaptive Planning (Adaptive Insights) REST. ops get/post/put/delete (writes need confirm). Auth: ADAPTIVE_INSIGHTS_BASE_URL (https://api.adaptiveinsights.com) + ADAPTIVE_INSIGHTS_TOKEN (API key)."),
    dict(name="centage", description="Centage budgeting/planning REST. ops get/post/put/delete (writes need confirm). Auth: CENTAGE_BASE_URL (https://api.centage.com) + CENTAGE_TOKEN (API key)."),
    dict(name="limelight_cpm", description="Limelight (Prophix) CPM REST. ops get/post/put/delete (writes need confirm). Auth: LIMELIGHT_CPM_BASE_URL (https://api.limelightsoftware.com) + LIMELIGHT_CPM_TOKEN (API key)."),
    dict(name="trintech", description="Trintech financial close/reconciliation REST. ops get/post/put/delete (writes need confirm). Auth: TRINTECH_BASE_URL (https://api.trintech.com) + TRINTECH_TOKEN (OAuth bearer)."),
    dict(name="fis_global", description="FIS Global banking/payments REST. ops get/post/put/delete (writes need confirm). Auth: FIS_GLOBAL_BASE_URL (https://api.fisglobal.com) + FIS_GLOBAL_TOKEN (OAuth bearer)."),
    dict(name="jack_henry", description="Jack Henry banking/core processing REST. ops get/post/put/delete (writes need confirm). Auth: JACK_HENRY_BASE_URL (https://api.jackhenry.com) + JACK_HENRY_TOKEN (OAuth bearer)."),
    dict(name="q2_banking", description="Q2 digital banking REST. ops get/post/put/delete (writes need confirm). Auth: Q2_BANKING_BASE_URL (https://api.q2.com) + Q2_BANKING_TOKEN (OAuth bearer)."),
    dict(name="plaid_transfer", description="Plaid Transfer (ACH) REST (distinct from generic plaid entry). ops get/post/put/delete (writes need confirm). Auth: PLAID_TRANSFER_BASE_URL (https://production.plaid.com) + PLAID_TRANSFER_TOKEN (client secret)."),
    dict(name="synapsefi", description="Synapse banking-as-a-service REST. ops get/post/put/delete (writes need confirm). Auth: SYNAPSEFI_BASE_URL (https://api.synapsefi.com) + SYNAPSEFI_TOKEN (API key)."),
    dict(name="currencycloud", description="Currencycloud (Visa) cross-border payments REST. ops get/post/put/delete (writes need confirm). Auth: CURRENCYCLOUD_BASE_URL (https://api.currencycloud.com) + CURRENCYCLOUD_TOKEN (OAuth bearer)."),
    dict(name="braintree_pay", description="Braintree (PayPal) payments REST/GraphQL. ops get/post/put/delete (writes need confirm). Auth: BRAINTREE_PAY_BASE_URL (https://payments.braintree-api.com) + BRAINTREE_PAY_TOKEN (OAuth bearer)."),
    dict(name="skrill", description="Skrill digital wallet/payments REST. ops get/post/put/delete (writes need confirm). Auth: SKRILL_BASE_URL (https://api.skrill.com) + SKRILL_TOKEN (API key)."),
    dict(name="starling_bank", description="Starling Bank business banking REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/accounts. Auth: STARLING_BANK_BASE_URL (https://api.starlingbank.com) + STARLING_BANK_TOKEN (OAuth bearer)."),
    dict(name="silicon_valley_bridge", description="Silicon Valley Bank (SVB) business banking REST. ops get/post/put/delete (writes need confirm). Auth: SILICON_VALLEY_BRIDGE_BASE_URL (https://api.svb.com) + SILICON_VALLEY_BRIDGE_TOKEN (OAuth bearer)."),
    dict(name="relay_financial", description="Relay small-business banking REST. ops get/post/put/delete (writes need confirm). Auth: RELAY_FINANCIAL_BASE_URL (https://api.relayfi.com) + RELAY_FINANCIAL_TOKEN (API key)."),
    dict(name="novo_bank", description="Novo small-business banking REST. ops get/post/put/delete (writes need confirm). Auth: NOVO_BANK_BASE_URL (https://api.novo.co) + NOVO_BANK_TOKEN (API key)."),
    dict(name="lili_bank", description="Lili small-business banking REST. ops get/post/put/delete (writes need confirm). Auth: LILI_BANK_BASE_URL (https://api.lili.co) + LILI_BANK_TOKEN (API key)."),
    dict(name="grasshopper_bank", description="Grasshopper Bank business banking REST. ops get/post/put/delete (writes need confirm). Auth: GRASSHOPPER_BANK_BASE_URL (https://api.grasshopperbank.com) + GRASSHOPPER_BANK_TOKEN (OAuth bearer)."),
    dict(name="lawpay", description="LawPay legal payment processing REST. ops get/post/put/delete (writes need confirm). Auth: LAWPAY_BASE_URL (https://api.lawpay.com) + LAWPAY_TOKEN (API key)."),
    dict(name="clio_payments", description="Clio Payments legal billing REST (distinct from generic clio entry). ops get/post/put/delete (writes need confirm). Auth: CLIO_PAYMENTS_BASE_URL (https://app.clio.com) + CLIO_PAYMENTS_TOKEN (OAuth bearer)."),
    dict(name="mycase", description="MyCase legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: MYCASE_BASE_URL (https://api.mycase.com) + MYCASE_TOKEN (OAuth bearer)."),
    dict(name="practicepanther", description="PracticePanther legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: PRACTICEPANTHER_BASE_URL (https://app.practicepanther.com) + PRACTICEPANTHER_TOKEN (OAuth bearer)."),
    dict(name="smokeball", description="Smokeball legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: SMOKEBALL_BASE_URL (https://api.smokeball.com) + SMOKEBALL_TOKEN (API key)."),
    dict(name="filevine", description="Filevine legal case management REST. ops get/post/put/delete (writes need confirm). Auth: FILEVINE_BASE_URL (https://api.filevineapp.com) + FILEVINE_TOKEN (OAuth bearer)."),
    dict(name="lawtoolbox", description="LawToolBox legal deadline/calendar REST. ops get/post/put/delete (writes need confirm). Auth: LAWTOOLBOX_BASE_URL (https://api.lawtoolbox.com) + LAWTOOLBOX_TOKEN (API key)."),
    dict(name="leanlaw", description="LeanLaw legal billing REST. ops get/post/put/delete (writes need confirm). Auth: LEANLAW_BASE_URL (https://api.leanlaw.co) + LEANLAW_TOKEN (API key)."),
    dict(name="lexisnexis", description="LexisNexis legal research REST. ops get/post/put/delete (writes need confirm). Auth: LEXISNEXIS_BASE_URL (https://api.lexisnexis.com) + LEXISNEXIS_TOKEN (OAuth bearer)."),
    dict(name="westlaw", description="Westlaw (Thomson Reuters) legal research REST. ops get/post/put/delete (writes need confirm). Auth: WESTLAW_BASE_URL (https://api.thomsonreuters.com) + WESTLAW_TOKEN (OAuth bearer)."),
    dict(name="fastcase", description="Fastcase legal research REST. ops get/post/put/delete (writes need confirm). Auth: FASTCASE_BASE_URL (https://api.fastcase.com) + FASTCASE_TOKEN (API key)."),
    dict(name="casetext", description="Casetext legal research REST. ops get/post/put/delete (writes need confirm). Auth: CASETEXT_BASE_URL (https://api.casetext.com) + CASETEXT_TOKEN (API key)."),
    dict(name="pacer", description="PACER federal court records REST. ops get only (federal records; writes need confirm). Auth: PACER_BASE_URL (https://pcl.uscourts.gov) + PACER_TOKEN (login token)."),
    dict(name="hellosign", description="HelloSign (Dropbox Sign) e-signature REST (distinct from docusign entry). ops get/post/put/delete (writes need confirm). e.g. /v3/signature_request/send. Auth: HELLOSIGN_BASE_URL (https://api.hellosign.com) + HELLOSIGN_TOKEN (API key).", basic=True),
    dict(name="dropbox_sign", description="Dropbox Sign e-signature REST (rebrand of HelloSign; distinct name entry). ops get/post/put/delete (writes need confirm). Auth: DROPBOX_SIGN_BASE_URL (https://api.hellosign.com) + DROPBOX_SIGN_TOKEN (API key).", basic=True),
    dict(name="pandadoc", description="PandaDoc document/e-signature REST. ops get/post/put/delete (writes need confirm). e.g. /public/v1/documents. Auth: PANDADOC_BASE_URL (https://api.pandadoc.com) + PANDADOC_TOKEN (API key).", token_header="Authorization", scheme="API-Key"),
    dict(name="signnow", description="SignNow e-signature REST. ops get/post/put/delete (writes need confirm). Auth: SIGNNOW_BASE_URL (https://api.signnow.com) + SIGNNOW_TOKEN (OAuth bearer)."),
    dict(name="adobe_sign", description="Adobe Acrobat Sign e-signature REST. ops get/post/put/delete (writes need confirm). e.g. /api/rest/v6/agreements. Auth: ADOBE_SIGN_BASE_URL (https://api.na1.adobesign.com) + ADOBE_SIGN_TOKEN (OAuth bearer)."),
    dict(name="oneflow", description="Oneflow contract/e-signature REST. ops get/post/put/delete (writes need confirm). Auth: ONEFLOW_BASE_URL (https://api.oneflow.com) + ONEFLOW_TOKEN (API key)."),
    dict(name="pandasign_esign", description="eversign e-signature REST. ops get/post/put/delete (writes need confirm). Auth: PANDASIGN_ESIGN_BASE_URL (https://api.eversign.com) + PANDASIGN_ESIGN_TOKEN (API key)."),
    dict(name="documill", description="Documill contract automation REST. ops get/post/put/delete (writes need confirm). Auth: DOCUMILL_BASE_URL (https://api.documill.com) + DOCUMILL_TOKEN (API key)."),
    dict(name="conga_contracts", description="Conga Contracts (CLM) REST. ops get/post/put/delete (writes need confirm). Auth: CONGA_CONTRACTS_BASE_URL (https://api.congamerge.com) + CONGA_CONTRACTS_TOKEN (OAuth bearer)."),
    dict(name="agiloft", description="Agiloft contract lifecycle management REST. ops get/post/put/delete (writes need confirm). Auth: AGILOFT_BASE_URL (https://api.agiloft.com) + AGILOFT_TOKEN (OAuth bearer)."),
    dict(name="determine_clm", description="Determine (Corcentric) CLM REST. ops get/post/put/delete (writes need confirm). Auth: DETERMINE_CLM_BASE_URL (https://api.determine.com) + DETERMINE_CLM_TOKEN (OAuth bearer)."),
    dict(name="icertis", description="Icertis contract intelligence REST. ops get/post/put/delete (writes need confirm). Auth: ICERTIS_BASE_URL (https://api.icertis.com) + ICERTIS_TOKEN (OAuth bearer)."),
    dict(name="onit_clm", description="Onit legal operations/CLM REST. ops get/post/put/delete (writes need confirm). Auth: ONIT_CLM_BASE_URL (https://api.onit.com) + ONIT_CLM_TOKEN (API key)."),
    dict(name="brightflag", description="Brightflag legal spend management REST. ops get/post/put/delete (writes need confirm). Auth: BRIGHTFLAG_BASE_URL (https://api.brightflag.com) + BRIGHTFLAG_TOKEN (API key)."),
    dict(name="legaltrek", description="LegalTrek legal spend/matter management REST. ops get/post/put/delete (writes need confirm). Auth: LEGALTREK_BASE_URL (https://api.legaltrek.com) + LEGALTREK_TOKEN (API key)."),
    dict(name="thomson_reuters_elite", description="Thomson Reuters Elite legal billing REST. ops get/post/put/delete (writes need confirm). Auth: THOMSON_REUTERS_ELITE_BASE_URL (https://api.thomsonreuters.com) + THOMSON_REUTERS_ELITE_TOKEN (OAuth bearer)."),
    dict(name="aderant", description="Aderant legal practice/billing management REST. ops get/post/put/delete (writes need confirm). Auth: ADERANT_BASE_URL (https://api.aderant.com) + ADERANT_TOKEN (OAuth bearer)."),
    dict(name="intapp", description="Intapp legal/professional services REST. ops get/post/put/delete (writes need confirm). Auth: INTAPP_BASE_URL (https://api.intapp.com) + INTAPP_TOKEN (OAuth bearer)."),
    dict(name="litera", description="Litera legal document workflow REST. ops get/post/put/delete (writes need confirm). Auth: LITERA_BASE_URL (https://api.litera.com) + LITERA_TOKEN (API key)."),
    dict(name="everlaw", description="Everlaw e-discovery/litigation REST. ops get/post/put/delete (writes need confirm). Auth: EVERLAW_BASE_URL (https://api.everlaw.com) + EVERLAW_TOKEN (API key)."),
    dict(name="relativity_ediscovery", description="Relativity e-discovery platform REST. ops get/post/put/delete (writes need confirm). e.g. /Relativity.REST/api. Auth: RELATIVITY_EDISCOVERY_BASE_URL (https://{instance}.relativity.com) + RELATIVITY_EDISCOVERY_TOKEN (OAuth bearer)."),
    dict(name="logikcull", description="Logikcull e-discovery REST. ops get/post/put/delete (writes need confirm). Auth: LOGIKCULL_BASE_URL (https://api.logikcull.com) + LOGIKCULL_TOKEN (API key)."),
    dict(name="disco_ediscovery", description="DISCO e-discovery/litigation REST. ops get/post/put/delete (writes need confirm). Auth: DISCO_EDISCOVERY_BASE_URL (https://api.csdisco.com) + DISCO_EDISCOVERY_TOKEN (OAuth bearer)."),
    dict(name="netdocuments", description="NetDocuments legal document management REST. ops get/post/put/delete (writes need confirm). Auth: NETDOCUMENTS_BASE_URL (https://api.netdocuments.com) + NETDOCUMENTS_TOKEN (OAuth bearer)."),
    dict(name="imanage", description="iManage document/matter management REST. ops get/post/put/delete (writes need confirm). e.g. /work/api/v2/customers. Auth: IMANAGE_BASE_URL (https://{instance}.imanage.work) + IMANAGE_TOKEN (OAuth bearer)."),
    dict(name="worldox", description="Worldox document management REST. ops get/post/put/delete (writes need confirm). Auth: WORLDOX_BASE_URL (https://api.worldox.com) + WORLDOX_TOKEN (API key)."),
    dict(name="corridor_gtc", description="Corridor legal spend/e-billing REST. ops get/post/put/delete (writes need confirm). Auth: CORRIDOR_GTC_BASE_URL (https://api.corridorcompany.com) + CORRIDOR_GTC_TOKEN (API key)."),
    dict(name="sirion_clm", description="SirionLabs contract lifecycle management REST. ops get/post/put/delete (writes need confirm). Auth: SIRION_CLM_BASE_URL (https://api.sirionlabs.com) + SIRION_CLM_TOKEN (OAuth bearer)."),
    dict(name="lawgeex", description="LawGeex contract review automation REST. ops get/post/put/delete (writes need confirm). Auth: LAWGEEX_BASE_URL (https://api.lawgeex.com) + LAWGEEX_TOKEN (API key)."),
    dict(name="lexcheck", description="LexCheck contract review AI REST. ops get/post/put/delete (writes need confirm). Auth: LEXCHECK_BASE_URL (https://api.lexcheck.com) + LEXCHECK_TOKEN (API key)."),
    dict(name="donotpay", description="DoNotPay consumer legal automation REST. ops get/post/put/delete (writes need confirm). Auth: DONOTPAY_BASE_URL (https://api.donotpay.com) + DONOTPAY_TOKEN (API key)."),
    dict(name="lawdepot", description="LawDepot legal document generation REST. ops get/post/put/delete (writes need confirm). Auth: LAWDEPOT_BASE_URL (https://api.lawdepot.com) + LAWDEPOT_TOKEN (API key)."),
    dict(name="rocket_lawyer", description="Rocket Lawyer legal document/services REST. ops get/post/put/delete (writes need confirm). Auth: ROCKET_LAWYER_BASE_URL (https://api.rocketlawyer.com) + ROCKET_LAWYER_TOKEN (API key)."),
    dict(name="legalzoom", description="LegalZoom legal services REST. ops get/post/put/delete (writes need confirm). Auth: LEGALZOOM_BASE_URL (https://api.legalzoom.com) + LEGALZOOM_TOKEN (API key)."),
    dict(name="trustarc", description="TrustArc privacy compliance management REST. ops get/post/put/delete (writes need confirm). Auth: TRUSTARC_BASE_URL (https://api.trustarc.com) + TRUSTARC_TOKEN (API key)."),
    dict(name="onetrust_privacy", description="OneTrust GRC/privacy platform REST (distinct from generic onetrust entry). ops get/post/put/delete (writes need confirm). Auth: ONETRUST_PRIVACY_BASE_URL (https://{tenant}.onetrust.com) + ONETRUST_PRIVACY_TOKEN (OAuth bearer)."),
    dict(name="metricstream", description="MetricStream GRC platform REST. ops get/post/put/delete (writes need confirm). Auth: METRICSTREAM_BASE_URL (https://api.metricstream.com) + METRICSTREAM_TOKEN (OAuth bearer)."),
    dict(name="logicmanager", description="LogicManager GRC/risk management REST. ops get/post/put/delete (writes need confirm). Auth: LOGICMANAGER_BASE_URL (https://api.logicmanager.com) + LOGICMANAGER_TOKEN (API key)."),
    dict(name="navex_global", description="NAVEX (ethics/compliance) REST. ops get/post/put/delete (writes need confirm). Auth: NAVEX_GLOBAL_BASE_URL (https://api.navexglobal.com) + NAVEX_GLOBAL_TOKEN (OAuth bearer)."),
    dict(name="convercent", description="Convercent (OneTrust) ethics/compliance REST. ops get/post/put/delete (writes need confirm). Auth: CONVERCENT_BASE_URL (https://api.convercent.com) + CONVERCENT_TOKEN (API key)."),
    dict(name="resolver_grc", description="Resolver GRC/risk management REST. ops get/post/put/delete (writes need confirm). Auth: RESOLVER_GRC_BASE_URL (https://api.resolver.com) + RESOLVER_GRC_TOKEN (API key)."),
    dict(name="galvanize_grc", description="Diligent (Galvanize/HighBond) GRC REST. ops get/post/put/delete (writes need confirm). Auth: GALVANIZE_GRC_BASE_URL (https://api.highbond.com) + GALVANIZE_GRC_TOKEN (OAuth bearer)."),
    dict(name="diligent_boards", description="Diligent Boards governance/board management REST. ops get/post/put/delete (writes need confirm). Auth: DILIGENT_BOARDS_BASE_URL (https://api.diligent.com) + DILIGENT_BOARDS_TOKEN (OAuth bearer)."),
    dict(name="alyne_grc", description="Alyne (Diligent) risk/GRC REST. ops get/post/put/delete (writes need confirm). Auth: ALYNE_GRC_BASE_URL (https://api.alyne.com) + ALYNE_GRC_TOKEN (API key)."),
    dict(name="riskonnect", description="Riskonnect integrated risk management REST. ops get/post/put/delete (writes need confirm). Auth: RISKONNECT_BASE_URL (https://api.riskonnect.com) + RISKONNECT_TOKEN (OAuth bearer)."),
    dict(name="archer_grc", description="RSA Archer GRC platform REST. ops get/post/put/delete (writes need confirm). e.g. /rsaarcher/api/core/system. Auth: ARCHER_GRC_BASE_URL (https://{instance}.archer.rsa.com) + ARCHER_GRC_TOKEN (session token)."),
    dict(name="workiva_grc", description="Workiva Wdesk connected risk/compliance REST (distinct from generic workiva entry). ops get/post/put/delete (writes need confirm). Auth: WORKIVA_GRC_BASE_URL (https://api.workiva.com) + WORKIVA_GRC_TOKEN (OAuth bearer)."),
    dict(name="complyadvantage", description="ComplyAdvantage AML/sanctions screening REST. ops get/post/put/delete (writes need confirm). e.g. /searches. Auth: COMPLYADVANTAGE_BASE_URL (https://api.complyadvantage.com) + COMPLYADVANTAGE_TOKEN (API key)."),
    dict(name="chainalysis", description="Chainalysis crypto compliance/AML REST. ops get/post/put/delete (writes need confirm). Auth: CHAINALYSIS_BASE_URL (https://api.chainalysis.com) + CHAINALYSIS_TOKEN (API key)."),
    dict(name="jumio", description="Jumio identity verification/KYC REST. ops get/post/put/delete (writes need confirm). Auth: JUMIO_BASE_URL (https://api.jumio.com) + JUMIO_TOKEN (API key).", basic=True),
    dict(name="onfido", description="Onfido identity verification/KYC REST. ops get/post/put/delete (writes need confirm). e.g. /v3.6/checks. Auth: ONFIDO_BASE_URL (https://api.onfido.com) + ONFIDO_TOKEN (API key).", token_header="Authorization", scheme="Token token="),
    dict(name="persona_kyc", description="Persona identity verification/KYC REST. ops get/post/put/delete (writes need confirm). Auth: PERSONA_KYC_BASE_URL (https://withpersona.com) + PERSONA_KYC_TOKEN (API key)."),
    dict(name="trulioo", description="Trulioo global identity verification REST. ops get/post/put/delete (writes need confirm). Auth: TRULIOO_BASE_URL (https://api.trulioo.com) + TRULIOO_TOKEN (API key)."),
    dict(name="alloy_kyc", description="Alloy identity/KYC decisioning REST. ops get/post/put/delete (writes need confirm). Auth: ALLOY_KYC_BASE_URL (https://api.alloy.co) + ALLOY_KYC_TOKEN (API key).", basic=True),
    dict(name="socure", description="Socure identity verification REST. ops get/post/put/delete (writes need confirm). Auth: SOCURE_BASE_URL (https://api.socure.com) + SOCURE_TOKEN (API key)."),
    dict(name="middesk", description="Middesk business identity verification REST. ops get/post/put/delete (writes need confirm). Auth: MIDDESK_BASE_URL (https://api.middesk.com) + MIDDESK_TOKEN (API key)."),
    dict(name="lexisnexis_risk", description="LexisNexis Risk Solutions REST. ops get/post/put/delete (writes need confirm). Auth: LEXISNEXIS_RISK_BASE_URL (https://api.risk.lexisnexis.com) + LEXISNEXIS_RISK_TOKEN (OAuth bearer)."),
    dict(name="refinitiv_worldcheck", description="Refinitiv World-Check sanctions screening REST. ops get/post/put/delete (writes need confirm). Auth: REFINITIV_WORLDCHECK_BASE_URL (https://api-worldcheck.refinitiv.com) + REFINITIV_WORLDCHECK_TOKEN (OAuth bearer)."),
    dict(name="dow_jones_riskcenter", description="Dow Jones Risk & Compliance REST. ops get/post/put/delete (writes need confirm). Auth: DOW_JONES_RISKCENTER_BASE_URL (https://api.dowjones.com) + DOW_JONES_RISKCENTER_TOKEN (OAuth bearer)."),
    dict(name="ncontracts", description="Ncontracts bank compliance/risk management REST. ops get/post/put/delete (writes need confirm). Auth: NCONTRACTS_BASE_URL (https://api.ncontracts.com) + NCONTRACTS_TOKEN (API key)."),
    dict(name="ascent_regtech", description="Ascent RegTech regulatory compliance REST. ops get/post/put/delete (writes need confirm). Auth: ASCENT_REGTECH_BASE_URL (https://api.ascentregtech.com) + ASCENT_REGTECH_TOKEN (API key)."),
    dict(name="quantexa", description="Quantexa financial crime/risk analytics REST. ops get/post/put/delete (writes need confirm). Auth: QUANTEXA_BASE_URL (https://api.quantexa.com) + QUANTEXA_TOKEN (OAuth bearer)."),
    dict(name="nice_actimize", description="NICE Actimize financial crime compliance REST. ops get/post/put/delete (writes need confirm). Auth: NICE_ACTIMIZE_BASE_URL (https://api.niceactimize.com) + NICE_ACTIMIZE_TOKEN (OAuth bearer)."),
    dict(name="featurespace", description="Featurespace fraud/AML detection REST. ops get/post/put/delete (writes need confirm). Auth: FEATURESPACE_BASE_URL (https://api.featurespace.co.uk) + FEATURESPACE_TOKEN (API key)."),
    dict(name="sift_fraud", description="Sift fraud detection REST. ops get/post/put/delete (writes need confirm). e.g. /v205/events. Auth: SIFT_FRAUD_BASE_URL (https://api.sift.com) + SIFT_FRAUD_TOKEN (API key)."),
    dict(name="kount", description="Kount (Equifax) fraud prevention REST. ops get/post/put/delete (writes need confirm). Auth: KOUNT_BASE_URL (https://api.kount.com) + KOUNT_TOKEN (OAuth bearer)."),
    dict(name="forter", description="Forter fraud prevention REST. ops get/post/put/delete (writes need confirm). Auth: FORTER_BASE_URL (https://api.forter.com) + FORTER_TOKEN (API key)."),
    dict(name="riskified", description="Riskified e-commerce fraud prevention REST. ops get/post/put/delete (writes need confirm). Auth: RISKIFIED_BASE_URL (https://api.riskified.com) + RISKIFIED_TOKEN (API key)."),
    dict(name="constantcontact", description="Constant Contact REST v3. ops get/post/put/delete (writes need confirm). Auth: CONSTANTCONTACT_BASE_URL (https://api.cc.email) + CONSTANTCONTACT_TOKEN (OAuth2 bearer)."),
    dict(name="campaignmonitor", basic=True, description="Campaign Monitor REST API. ops get/post/put/delete (writes need confirm). Auth: CAMPAIGNMONITOR_BASE_URL (https://api.createsend.com) + CAMPAIGNMONITOR_TOKEN (API key, basic auth)."),
    dict(name="convertkit", description="ConvertKit (Kit) REST API. ops get/post/put/delete (writes need confirm). Auth: CONVERTKIT_BASE_URL (https://api.convertkit.com) + CONVERTKIT_TOKEN (API secret/OAuth2 bearer)."),
    dict(name="mailerlite", description="MailerLite REST API. ops get/post/put/delete (writes need confirm). Auth: MAILERLITE_BASE_URL (https://connect.mailerlite.com) + MAILERLITE_TOKEN (API key)."),
    dict(name="getresponse", token_header="X-Auth-Token", scheme="api-key", description="GetResponse REST API. ops get/post/put/delete (writes need confirm). Auth: GETRESPONSE_BASE_URL (https://api.getresponse.com) + GETRESPONSE_TOKEN (API key)."),
    dict(name="aweber", description="AWeber REST API. ops get/post/put/delete (writes need confirm). Auth: AWEBER_BASE_URL (https://api.aweber.com) + AWEBER_TOKEN (OAuth2 bearer)."),
    dict(name="drip", basic=True, description="Drip REST API. ops get/post/put/delete (writes need confirm). Auth: DRIP_BASE_URL (https://api.getdrip.com) + DRIP_TOKEN (API key, basic auth)."),
    dict(name="omnisend", token_header="X-API-KEY", scheme="", description="Omnisend REST API. ops get/post/put/delete (writes need confirm). Auth: OMNISEND_BASE_URL (https://api.omnisend.com) + OMNISEND_TOKEN (API key)."),
    dict(name="benchmarkemail", description="Benchmark Email REST API. ops get/post/put/delete (writes need confirm). Auth: BENCHMARKEMAIL_BASE_URL + BENCHMARKEMAIL_TOKEN (OAuth2 bearer)."),
    dict(name="mailjet", basic=True, description="Mailjet REST API. ops get/post/put/delete (writes need confirm). Auth: MAILJET_BASE_URL (https://api.mailjet.com) + MAILJET_TOKEN (API key + secret, basic auth)."),
    dict(name="brevo", token_header="api-key", scheme="", description="Brevo (formerly Sendinblue) REST API. ops get/post/put/delete (writes need confirm). Auth: BREVO_BASE_URL (https://api.brevo.com) + BREVO_TOKEN (API key)."),
    dict(name="moosend", description="Moosend REST API. ops get/post/put/delete (writes need confirm). Auth: MOOSEND_BASE_URL + MOOSEND_TOKEN (API key)."),
    dict(name="emailoctopus", description="EmailOctopus REST API. ops get/post/put/delete (writes need confirm). Auth: EMAILOCTOPUS_BASE_URL (https://emailoctopus.com/api) + EMAILOCTOPUS_TOKEN (API key)."),
    dict(name="mailgun", basic=True, description="Mailgun REST API. ops get/post/put/delete (writes need confirm). Auth: MAILGUN_BASE_URL (https://api.mailgun.net) + MAILGUN_TOKEN (API key, basic auth, user 'api')."),
    dict(name="postmark", token_header="X-Postmark-Server-Token", scheme="", description="Postmark REST API. ops get/post/put/delete (writes need confirm). Auth: POSTMARK_BASE_URL (https://api.postmarkapp.com) + POSTMARK_TOKEN (server token)."),
    dict(name="sparkpost", token_header="Authorization", scheme="", description="SparkPost REST API. ops get/post/put/delete (writes need confirm). Auth: SPARKPOST_BASE_URL (https://api.sparkpost.com) + SPARKPOST_TOKEN (API key, raw header)."),
    dict(name="customerio", description="Customer.io REST API (Track + App API). ops get/post/put/delete (writes need confirm). Auth: CUSTOMERIO_BASE_URL (https://api.customer.io) + CUSTOMERIO_TOKEN (API key/bearer)."),
    dict(name="attentive", description="Attentive REST API (SMS/email marketing). ops get/post/put/delete (writes need confirm). Auth: ATTENTIVE_BASE_URL (https://api.attentivemobile.com) + ATTENTIVE_TOKEN (API key)."),
    dict(name="postscript", description="Postscript REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: POSTSCRIPT_BASE_URL (https://api.postscript.io) + POSTSCRIPT_TOKEN (API key)."),
    dict(name="simpletexting", description="SimpleTexting REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: SIMPLETEXTING_BASE_URL + SIMPLETEXTING_TOKEN (API key)."),
    dict(name="eztexting", description="EZ Texting REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: EZTEXTING_BASE_URL + EZTEXTING_TOKEN (API key/bearer)."),
    dict(name="textmagic", basic=True, description="TextMagic REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: TEXTMAGIC_BASE_URL (https://rest.textmagic.com) + TEXTMAGIC_TOKEN (API key, basic auth)."),
    dict(name="textedly", description="Textedly REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: TEXTEDLY_BASE_URL + TEXTEDLY_TOKEN (API key)."),
    dict(name="slicktext", description="SlickText REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: SLICKTEXT_BASE_URL + SLICKTEXT_TOKEN (API key)."),
    dict(name="podium", description="Podium REST API (customer messaging/engagement). ops get/post/put/delete (writes need confirm). Auth: PODIUM_BASE_URL (https://api.podium.com) + PODIUM_TOKEN (OAuth2 bearer)."),
    dict(name="tatango", description="Tatango REST API (SMS marketing). ops get/post/put/delete (writes need confirm). Auth: TATANGO_BASE_URL + TATANGO_TOKEN (API key)."),
    dict(name="googleads", description="Google Ads API. ops get/post/put/delete (writes need confirm). Auth: GOOGLEADS_BASE_URL (https://googleads.googleapis.com) + GOOGLEADS_TOKEN (OAuth2 bearer)."),
    dict(name="metaads", description="Meta (Facebook/Instagram) Marketing API. ops get/post/put/delete (writes need confirm). Auth: METAADS_BASE_URL (https://graph.facebook.com) + METAADS_TOKEN (OAuth2 bearer)."),
    dict(name="linkedinads", description="LinkedIn Marketing (Ads) API. ops get/post/put/delete (writes need confirm). Auth: LINKEDINADS_BASE_URL (https://api.linkedin.com) + LINKEDINADS_TOKEN (OAuth2 bearer)."),
    dict(name="tiktokads", description="TikTok Marketing API (Ads). ops get/post/put/delete (writes need confirm). Auth: TIKTOKADS_BASE_URL (https://business-api.tiktok.com) + TIKTOKADS_TOKEN (access token)."),
    dict(name="twitterads", description="X (Twitter) Ads API. ops get/post/put/delete (writes need confirm). Auth: TWITTERADS_BASE_URL (https://ads-api.x.com) + TWITTERADS_TOKEN (OAuth2 bearer)."),
    dict(name="snapchatads", description="Snapchat Marketing API. ops get/post/put/delete (writes need confirm). Auth: SNAPCHATADS_BASE_URL (https://adsapi.snapchat.com) + SNAPCHATADS_TOKEN (OAuth2 bearer)."),
    dict(name="pinterestads", description="Pinterest Ads API (Pinterest API v5). ops get/post/put/delete (writes need confirm). Auth: PINTERESTADS_BASE_URL (https://api.pinterest.com) + PINTERESTADS_TOKEN (OAuth2 bearer)."),
    dict(name="redditads", description="Reddit Ads API. ops get/post/put/delete (writes need confirm). Auth: REDDITADS_BASE_URL (https://ads-api.reddit.com) + REDDITADS_TOKEN (OAuth2 bearer)."),
    dict(name="bingads", description="Microsoft Advertising (Bing Ads) API. ops get/post/put/delete (writes need confirm). Auth: BINGADS_BASE_URL (https://campaign.api.bingads.microsoft.com) + BINGADS_TOKEN (OAuth2 bearer)."),
    dict(name="amazonads", description="Amazon Advertising API. ops get/post/put/delete (writes need confirm). Auth: AMAZONADS_BASE_URL (https://advertising-api.amazon.com) + AMAZONADS_TOKEN (OAuth2 bearer)."),
    dict(name="criteo", description="Criteo Marketing API. ops get/post/put/delete (writes need confirm). Auth: CRITEO_BASE_URL (https://api.criteo.com) + CRITEO_TOKEN (OAuth2 bearer)."),
    dict(name="taboola", description="Taboola Backstage REST API. ops get/post/put/delete (writes need confirm). Auth: TABOOLA_BASE_URL (https://backstage.taboola.com) + TABOOLA_TOKEN (OAuth2 bearer)."),
    dict(name="outbrain", description="Outbrain Amplify REST API. ops get/post/put/delete (writes need confirm). Auth: OUTBRAIN_BASE_URL (https://api.outbrain.com) + OUTBRAIN_TOKEN (OAuth2 bearer)."),
    dict(name="tradedesk", description="The Trade Desk REST API. ops get/post/put/delete (writes need confirm). Auth: TRADEDESK_BASE_URL (https://api.thetradedesk.com) + TRADEDESK_TOKEN (API token)."),
    dict(name="adroll", description="AdRoll REST API. ops get/post/put/delete (writes need confirm). Auth: ADROLL_BASE_URL (https://services.adroll.com) + ADROLL_TOKEN (OAuth2 bearer)."),
    dict(name="quantcast", description="Quantcast Platform REST API. ops get/post/put/delete (writes need confirm). Auth: QUANTCAST_BASE_URL + QUANTCAST_TOKEN (OAuth2 bearer)."),
    dict(name="clevertap", description="CleverTap REST API. ops get/post/put/delete (writes need confirm). Auth: CLEVERTAP_BASE_URL (https://api.clevertap.com) + CLEVERTAP_TOKEN (API key/passcode)."),
    dict(name="moengage", description="MoEngage REST API. ops get/post/put/delete (writes need confirm). Auth: MOENGAGE_BASE_URL + MOENGAGE_TOKEN (API key, basic auth style)."),
    dict(name="onesignal", description="OneSignal REST API (push notifications). ops get/post/put/delete (writes need confirm). Auth: ONESIGNAL_BASE_URL (https://api.onesignal.com) + ONESIGNAL_TOKEN (REST API key)."),
    dict(name="airship", basic=True, description="Airship (Urban Airship) REST API (push/engagement). ops get/post/put/delete (writes need confirm). Auth: AIRSHIP_BASE_URL (https://go.urbanairship.com) + AIRSHIP_TOKEN (app key + secret, basic auth)."),
    dict(name="pushengage", description="PushEngage REST API. ops get/post/put/delete (writes need confirm). Auth: PUSHENGAGE_BASE_URL + PUSHENGAGE_TOKEN (API key)."),
    dict(name="pushwoosh", description="Pushwoosh REST API. ops get/post/put/delete (writes need confirm). Auth: PUSHWOOSH_BASE_URL (https://api.pushwoosh.com) + PUSHWOOSH_TOKEN (API access token)."),
    dict(name="leanplum", description="Leanplum REST API. ops get/post/put/delete (writes need confirm). Auth: LEANPLUM_BASE_URL (https://api.leanplum.com) + LEANPLUM_TOKEN (client key)."),
    dict(name="appsflyer", description="AppsFlyer REST API. ops get/post/put/delete (writes need confirm). Auth: APPSFLYER_BASE_URL (https://hq1.appsflyer.com) + APPSFLYER_TOKEN (API v2 token)."),
    dict(name="adjust", description="Adjust REST API. ops get/post/put/delete (writes need confirm). Auth: ADJUST_BASE_URL (https://api.adjust.com) + ADJUST_TOKEN (API token)."),
    dict(name="branchio", description="Branch.io REST API (mobile attribution/deep linking). ops get/post/put/delete (writes need confirm). Auth: BRANCHIO_BASE_URL (https://api2.branch.io) + BRANCHIO_TOKEN (branch key/secret)."),
    dict(name="singular", description="Singular REST API. ops get/post/put/delete (writes need confirm). Auth: SINGULAR_BASE_URL (https://api.singular.net) + SINGULAR_TOKEN (API key)."),
    dict(name="kochava", description="Kochava REST API. ops get/post/put/delete (writes need confirm). Auth: KOCHAVA_BASE_URL + KOCHAVA_TOKEN (API key)."),
    dict(name="hootsuite", description="Hootsuite REST API. ops get/post/put/delete (writes need confirm). Auth: HOOTSUITE_BASE_URL (https://platform.hootsuite.com) + HOOTSUITE_TOKEN (OAuth2 bearer)."),
    dict(name="buffer", description="Buffer REST API (social media scheduling). ops get/post/put/delete (writes need confirm). Auth: BUFFER_BASE_URL (https://api.bufferapp.com) + BUFFER_TOKEN (OAuth2 bearer)."),
    dict(name="sproutsocial", description="Sprout Social REST API. ops get/post/put/delete (writes need confirm). Auth: SPROUTSOCIAL_BASE_URL (https://api.sproutsocial.com) + SPROUTSOCIAL_TOKEN (OAuth2 bearer)."),
    dict(name="agorapulse", description="Agorapulse REST API. ops get/post/put/delete (writes need confirm). Auth: AGORAPULSE_BASE_URL + AGORAPULSE_TOKEN (OAuth2 bearer)."),
    dict(name="socialpilot", description="SocialPilot REST API. ops get/post/put/delete (writes need confirm). Auth: SOCIALPILOT_BASE_URL + SOCIALPILOT_TOKEN (API key)."),
    dict(name="coschedule", description="CoSchedule REST API. ops get/post/put/delete (writes need confirm). Auth: COSCHEDULE_BASE_URL + COSCHEDULE_TOKEN (API key)."),
    dict(name="loomly", description="Loomly REST API. ops get/post/put/delete (writes need confirm). Auth: LOOMLY_BASE_URL (https://api.loomly.com) + LOOMLY_TOKEN (API key)."),
    dict(name="tidio", description="Tidio REST API. ops get/post/put/delete (writes need confirm). Auth: TIDIO_BASE_URL (https://api.tidio.com) + TIDIO_TOKEN (API key)."),
    dict(name="unbounce", description="Unbounce REST API (landing pages). ops get/post/put/delete (writes need confirm). Auth: UNBOUNCE_BASE_URL (https://api.unbounce.com) + UNBOUNCE_TOKEN (OAuth2 bearer)."),
    dict(name="instapage", description="Instapage REST API (landing pages). ops get/post/put/delete (writes need confirm). Auth: INSTAPAGE_BASE_URL + INSTAPAGE_TOKEN (API key)."),
    dict(name="leadpages", description="Leadpages REST API. ops get/post/put/delete (writes need confirm). Auth: LEADPAGES_BASE_URL + LEADPAGES_TOKEN (API key)."),
    dict(name="typeform", description="Typeform REST API. ops get/post/put/delete (writes need confirm). Auth: TYPEFORM_BASE_URL (https://api.typeform.com) + TYPEFORM_TOKEN (personal access token)."),
    dict(name="wufoo", basic=True, description="Wufoo REST API (forms). ops get/post/put/delete (writes need confirm). Auth: WUFOO_BASE_URL (https://{sub}.wufoo.com/api) + WUFOO_TOKEN (API key, basic auth)."),
    dict(name="jotform", description="JotForm REST API. ops get/post/put/delete (writes need confirm). Auth: JOTFORM_BASE_URL (https://api.jotform.com) + JOTFORM_TOKEN (API key)."),
    dict(name="formstack", description="Formstack REST API. ops get/post/put/delete (writes need confirm). Auth: FORMSTACK_BASE_URL (https://www.formstack.com/api) + FORMSTACK_TOKEN (OAuth2 bearer)."),
    dict(name="vwo", description="VWO REST API (A/B testing). ops get/post/put/delete (writes need confirm). Auth: VWO_BASE_URL (https://app.vwo.com) + VWO_TOKEN (API token)."),
    dict(name="abtasty", description="AB Tasty REST API. ops get/post/put/delete (writes need confirm). Auth: ABTASTY_BASE_URL + ABTASTY_TOKEN (OAuth2 bearer)."),
    dict(name="convertcom", description="Convert.com REST API (A/B testing). ops get/post/put/delete (writes need confirm). Auth: CONVERTCOM_BASE_URL + CONVERTCOM_TOKEN (API key)."),
    dict(name="semrush", description="Semrush REST API. ops get/post/put/delete (writes need confirm). Auth: SEMRUSH_BASE_URL (https://api.semrush.com) + SEMRUSH_TOKEN (API key)."),
    dict(name="ahrefs", description="Ahrefs REST API. ops get/post/put/delete (writes need confirm). Auth: AHREFS_BASE_URL (https://api.ahrefs.com) + AHREFS_TOKEN (API token)."),
    dict(name="buzzsumo", description="BuzzSumo REST API. ops get/post/put/delete (writes need confirm). Auth: BUZZSUMO_BASE_URL (https://api.buzzsumo.com) + BUZZSUMO_TOKEN (API key)."),
    dict(name="referralcandy", description="ReferralCandy REST API. ops get/post/put/delete (writes need confirm). Auth: REFERRALCANDY_BASE_URL + REFERRALCANDY_TOKEN (API secret)."),
    dict(name="impactcom", basic=True, description="Impact (impact.com) partnership REST API. ops get/post/put/delete (writes need confirm). Auth: IMPACTCOM_BASE_URL (https://api.impact.com) + IMPACTCOM_TOKEN (account SID + auth token, basic auth)."),
    dict(name="partnerstack", basic=True, description="PartnerStack REST API. ops get/post/put/delete (writes need confirm). Auth: PARTNERSTACK_BASE_URL (https://api.partnerstack.com) + PARTNERSTACK_TOKEN (public + secret key, basic auth)."),
    dict(name="tapfiliate", description="Tapfiliate REST API. ops get/post/put/delete (writes need confirm). Auth: TAPFILIATE_BASE_URL (https://api.tapfiliate.com) + TAPFILIATE_TOKEN (API key)."),
    dict(name="shareasale", description="ShareASale REST API (affiliate marketing). ops get/post/put/delete (writes need confirm). Auth: SHAREASALE_BASE_URL + SHAREASALE_TOKEN (API token + secret)."),
    dict(name="awin", description="Awin REST API (affiliate marketing). ops get/post/put/delete (writes need confirm). Auth: AWIN_BASE_URL (https://api.awin.com) + AWIN_TOKEN (OAuth2 bearer)."),
    dict(name="cjaffiliate", description="CJ Affiliate (Commission Junction) REST API. ops get/post/put/delete (writes need confirm). Auth: CJAFFILIATE_BASE_URL + CJAFFILIATE_TOKEN (personal access token)."),
    dict(name="rakutenadvertising", description="Rakuten Advertising REST API (affiliate marketing). ops get/post/put/delete (writes need confirm). Auth: RAKUTENADVERTISING_BASE_URL + RAKUTENADVERTISING_TOKEN (OAuth2 bearer)."),
    dict(name="yotpo", description="Yotpo REST API (reviews & loyalty). ops get/post/put/delete (writes need confirm). Auth: YOTPO_BASE_URL (https://api.yotpo.com) + YOTPO_TOKEN (API key/OAuth token)."),
    dict(name="trustpilot", description="Trustpilot REST API. ops get/post/put/delete (writes need confirm). Auth: TRUSTPILOT_BASE_URL (https://api.trustpilot.com) + TRUSTPILOT_TOKEN (OAuth2 bearer)."),
    dict(name="bazaarvoice", description="Bazaarvoice REST API. ops get/post/put/delete (writes need confirm). Auth: BAZAARVOICE_BASE_URL + BAZAARVOICE_TOKEN (API passkey)."),
    dict(name="powerreviews", description="PowerReviews REST API. ops get/post/put/delete (writes need confirm). Auth: POWERREVIEWS_BASE_URL + POWERREVIEWS_TOKEN (API key)."),
    dict(name="okendo", description="Okendo REST API (reviews & loyalty). ops get/post/put/delete (writes need confirm). Auth: OKENDO_BASE_URL + OKENDO_TOKEN (API key)."),
    dict(name="judgeme", description="Judge.me REST API (reviews). ops get/post/put/delete (writes need confirm). Auth: JUDGEME_BASE_URL (https://judge.me/api) + JUDGEME_TOKEN (API token)."),
    dict(name="dynamicyield", description="Dynamic Yield REST API (personalization). ops get/post/put/delete (writes need confirm). Auth: DYNAMICYIELD_BASE_URL (https://dy-api.com) + DYNAMICYIELD_TOKEN (section access token)."),
    dict(name="monetate", description="Monetate REST API (personalization). ops get/post/put/delete (writes need confirm). Auth: MONETATE_BASE_URL + MONETATE_TOKEN (OAuth2 bearer)."),
    dict(name="nosto", description="Nosto REST API (personalization). ops get/post/put/delete (writes need confirm). Auth: NOSTO_BASE_URL (https://api.nosto.com) + NOSTO_TOKEN (OAuth2 bearer)."),
    dict(name="tealium", description="Tealium REST API (CDP/tag management). ops get/post/put/delete (writes need confirm). Auth: TEALIUM_BASE_URL + TEALIUM_TOKEN (API key)."),
    dict(name="lytics", description="Lytics REST API (CDP). ops get/post/put/delete (writes need confirm). Auth: LYTICS_BASE_URL (https://api.lytics.io) + LYTICS_TOKEN (API token)."),
    dict(name="blueconic", description="BlueConic REST API (CDP). ops get/post/put/delete (writes need confirm). Auth: BLUECONIC_BASE_URL + BLUECONIC_TOKEN (OAuth2 bearer)."),
    dict(name="treasuredata", description="Treasure Data REST API (CDP). ops get/post/put/delete (writes need confirm). Auth: TREASUREDATA_BASE_URL (https://api.treasuredata.com) + TREASUREDATA_TOKEN (API key)."),
    dict(name="wistia", description="Wistia REST API (video marketing). ops get/post/put/delete (writes need confirm). Auth: WISTIA_BASE_URL (https://api.wistia.com) + WISTIA_TOKEN (API access token)."),
    dict(name="clickfunnels", description="ClickFunnels REST API. ops get/post/put/delete (writes need confirm). Auth: CLICKFUNNELS_BASE_URL (https://{sub}.myclickfunnels.com/api) + CLICKFUNNELS_TOKEN (OAuth2 bearer)."),
    dict(name="kartra", description="Kartra REST API. ops get/post/put/delete (writes need confirm). Auth: KARTRA_BASE_URL (https://app.kartra.com/api) + KARTRA_TOKEN (API key + secret)."),
    dict(name="responsys", description="Oracle Responsys REST API. ops get/post/put/delete (writes need confirm). Auth: RESPONSYS_BASE_URL + RESPONSYS_TOKEN (OAuth2 bearer)."),
    dict(name="acoustic", description="Acoustic Campaign REST API. ops get/post/put/delete (writes need confirm). Auth: ACOUSTIC_BASE_URL + ACOUSTIC_TOKEN (OAuth2 bearer)."),
    dict(name="eloqua", description="Oracle Eloqua REST API. ops get/post/put/delete (writes need confirm). Auth: ELOQUA_BASE_URL (https://{sub}.eloqua.com/api) + ELOQUA_TOKEN (OAuth2 bearer)."),
    dict(name="listrak", description="Listrak REST API. ops get/post/put/delete (writes need confirm). Auth: LISTRAK_BASE_URL (https://api.listrak.com) + LISTRAK_TOKEN (OAuth2 bearer)."),
    dict(name="cordial", description="Cordial REST API. ops get/post/put/delete (writes need confirm). Auth: CORDIAL_BASE_URL + CORDIAL_TOKEN (API key/secret)."),
    dict(name="bluecore", description="Bluecore REST API. ops get/post/put/delete (writes need confirm). Auth: BLUECORE_BASE_URL + BLUECORE_TOKEN (API key)."),
    dict(name="movableink", description="Movable Ink REST API. ops get/post/put/delete (writes need confirm). Auth: MOVABLEINK_BASE_URL + MOVABLEINK_TOKEN (API key)."),
    dict(name="liveintent", description="LiveIntent REST API. ops get/post/put/delete (writes need confirm). Auth: LIVEINTENT_BASE_URL + LIVEINTENT_TOKEN (OAuth2 bearer)."),
    dict(name="replyio", description="Reply.io REST API (sales/marketing engagement). ops get/post/put/delete (writes need confirm). Auth: REPLYIO_BASE_URL (https://api.reply.io) + REPLYIO_TOKEN (API key)."),
    dict(name="wordstream", description="WordStream REST API (ad management). ops get/post/put/delete (writes need confirm). Auth: WORDSTREAM_BASE_URL + WORDSTREAM_TOKEN (API key)."),
    dict(name="skai", description="Skai (formerly Kenshoo) REST API (ad management). ops get/post/put/delete (writes need confirm). Auth: SKAI_BASE_URL + SKAI_TOKEN (OAuth2 bearer)."),
    dict(name="smartlyio", description="Smartly.io REST API (social ad management). ops get/post/put/delete (writes need confirm). Auth: SMARTLYIO_BASE_URL (https://api.smartly.io) + SMARTLYIO_TOKEN (API token)."),
    dict(name="rockerbox", description="Rockerbox REST API (marketing attribution). ops get/post/put/delete (writes need confirm). Auth: ROCKERBOX_BASE_URL + ROCKERBOX_TOKEN (API key)."),
    dict(name="northbeam", description="Northbeam REST API (marketing attribution). ops get/post/put/delete (writes need confirm). Auth: NORTHBEAM_BASE_URL + NORTHBEAM_TOKEN (API key)."),
    dict(name="triplewhale", description="Triple Whale REST API (marketing analytics). ops get/post/put/delete (writes need confirm). Auth: TRIPLEWHALE_BASE_URL (https://api.triplewhale.com) + TRIPLEWHALE_TOKEN (API key)."),
    dict(name="hyros", description="Hyros REST API (ad tracking/attribution). ops get/post/put/delete (writes need confirm). Auth: HYROS_BASE_URL (https://api.hyros.com) + HYROS_TOKEN (API key)."),
    dict(name="sharethis", description="ShareThis REST API (social sharing analytics). ops get/post/put/delete (writes need confirm). Auth: SHARETHIS_BASE_URL + SHARETHIS_TOKEN (API key)."),
    dict(name="twitter", description="Twitter/X API v2 REST. ops get/post/put/delete (writes need confirm). Auth: TWITTER_BASE_URL (https://api.twitter.com) + TWITTER_TOKEN (OAuth2 bearer)."),
    dict(name="facebook", description="Facebook Graph API REST. ops get/post/put/delete (writes need confirm). Auth: FACEBOOK_BASE_URL (https://graph.facebook.com) + FACEBOOK_TOKEN (page/user access token)."),
    dict(name="instagram", description="Instagram Graph API REST. ops get/post/put/delete (writes need confirm). Auth: INSTAGRAM_BASE_URL (https://graph.instagram.com) + INSTAGRAM_TOKEN (access token)."),
    dict(name="linkedin", description="LinkedIn REST API. ops get/post/put/delete (writes need confirm). Auth: LINKEDIN_BASE_URL (https://api.linkedin.com) + LINKEDIN_TOKEN (OAuth2 bearer)."),
    dict(name="pinterest", description="Pinterest REST API. ops get/post/put/delete (writes need confirm). Auth: PINTEREST_BASE_URL (https://api.pinterest.com) + PINTEREST_TOKEN (OAuth2 bearer)."),
    dict(name="tiktok", description="TikTok for Developers REST API. ops get/post/put/delete (writes need confirm). Auth: TIKTOK_BASE_URL (https://open.tiktokapis.com) + TIKTOK_TOKEN (OAuth2 bearer)."),
    dict(name="snapchat", description="Snapchat Marketing/Ads REST API. ops get/post/put/delete (writes need confirm). Auth: SNAPCHAT_BASE_URL (https://adsapi.snapchat.com) + SNAPCHAT_TOKEN (OAuth2 bearer)."),
    dict(name="tumblr", description="Tumblr REST API. ops get/post/put/delete (writes need confirm). Auth: TUMBLR_BASE_URL (https://api.tumblr.com) + TUMBLR_TOKEN (OAuth2 bearer)."),
    dict(name="mastodon", description="Mastodon REST API (instance-hosted microblogging). ops get/post/put/delete (writes need confirm). Auth: MASTODON_BASE_URL (https://{instance}) + MASTODON_TOKEN (app access token)."),
    dict(name="bluesky", description="Bluesky AT Protocol XRPC REST API. ops get/post/put/delete (writes need confirm). Auth: BLUESKY_BASE_URL (https://bsky.social) + BLUESKY_TOKEN (app password / session token)."),
    dict(name="threads", description="Meta Threads API REST. ops get/post/put/delete (writes need confirm). Auth: THREADS_BASE_URL (https://graph.threads.net) + THREADS_TOKEN (OAuth2 bearer)."),
    dict(name="nextdoor", description="Nextdoor Business Partner REST API. ops get/post/put/delete (writes need confirm). Auth: NEXTDOOR_BASE_URL (https://api.nextdoor.com) + NEXTDOOR_TOKEN (OAuth2 bearer)."),
    dict(name="discourse", description="Discourse forum REST API (self-hosted community platform). ops get/post/put/delete (writes need confirm). Auth: DISCOURSE_BASE_URL (https://{community}.example.com) + DISCOURSE_TOKEN (Api-Key)."),
    dict(name="medium", description="Medium REST API for publishing. ops get/post/put/delete (writes need confirm). Auth: MEDIUM_BASE_URL (https://api.medium.com) + MEDIUM_TOKEN (integration token)."),
    dict(name="wechat", description="WeChat Official Account REST API. ops get/post/put/delete (writes need confirm). Auth: WECHAT_BASE_URL (https://api.weixin.qq.com) + WECHAT_TOKEN (access token)."),
    dict(name="line_messaging", description="LINE Messaging API REST. ops get/post/put/delete (writes need confirm). Auth: LINE_MESSAGING_BASE_URL (https://api.line.me) + LINE_MESSAGING_TOKEN (channel access token)."),
    dict(name="kakao", description="Kakao (KakaoTalk) REST API. ops get/post/put/delete (writes need confirm). Auth: KAKAO_BASE_URL (https://kapi.kakao.com) + KAKAO_TOKEN (OAuth2 bearer)."),
    dict(name="vk", description="VKontakte (VK) REST API. ops get/post/put/delete (writes need confirm). Auth: VK_BASE_URL (https://api.vk.com) + VK_TOKEN (access token)."),
    dict(name="weibo", description="Sina Weibo REST API. ops get/post/put/delete (writes need confirm). Auth: WEIBO_BASE_URL (https://api.weibo.com) + WEIBO_TOKEN (OAuth2 access token)."),
    dict(name="whatsapp_business", description="WhatsApp Business Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: WHATSAPP_BUSINESS_BASE_URL (https://graph.facebook.com) + WHATSAPP_BUSINESS_TOKEN (access token)."),
    dict(name="messenger", description="Facebook Messenger Platform REST API. ops get/post/put/delete (writes need confirm). Auth: MESSENGER_BASE_URL (https://graph.facebook.com) + MESSENGER_TOKEN (page access token)."),
    dict(name="vimeo", description="Vimeo REST API for video hosting. ops get/post/put/delete (writes need confirm). Auth: VIMEO_BASE_URL (https://api.vimeo.com) + VIMEO_TOKEN (OAuth2 bearer)."),
    dict(name="soundcloud", description="SoundCloud REST API. ops get/post/put/delete (writes need confirm). Auth: SOUNDCLOUD_BASE_URL (https://api.soundcloud.com) + SOUNDCLOUD_TOKEN (OAuth2 bearer)."),
    dict(name="brightcove", description="Brightcove video cloud REST API. ops get/post/put/delete (writes need confirm). Auth: BRIGHTCOVE_BASE_URL (https://cms.api.brightcove.com) + BRIGHTCOVE_TOKEN (OAuth2 bearer)."),
    dict(name="mux", description="Mux video streaming REST API. ops get/post/put/delete (writes need confirm). Auth: MUX_BASE_URL (https://api.mux.com) + MUX_TOKEN (access token).", basic=True),
    dict(name="deezer", description="Deezer REST API. ops get/post/put/delete (writes need confirm). Auth: DEEZER_BASE_URL (https://api.deezer.com) + DEEZER_TOKEN (OAuth2 access token)."),
    dict(name="dailymotion", description="Dailymotion REST API. ops get/post/put/delete (writes need confirm). Auth: DAILYMOTION_BASE_URL (https://api.dailymotion.com) + DAILYMOTION_TOKEN (OAuth2 bearer)."),
    dict(name="rumble", description="Rumble creator REST API. ops get/post/put/delete (writes need confirm). Auth: RUMBLE_BASE_URL (https://api.rumble.com) + RUMBLE_TOKEN (API key)."),
    dict(name="kaltura", description="Kaltura video platform REST API. ops get/post/put/delete (writes need confirm). Auth: KALTURA_BASE_URL (https://cdnapi-ev.kaltura.com) + KALTURA_TOKEN (session/API key)."),
    dict(name="jwplayer", description="JW Player video REST API. ops get/post/put/delete (writes need confirm). Auth: JWPLAYER_BASE_URL (https://api.jwplayer.com) + JWPLAYER_TOKEN (API secret/bearer)."),
    dict(name="audiomack", description="Audiomack REST API. ops get/post/put/delete (writes need confirm). Auth: AUDIOMACK_BASE_URL (https://api.audiomack.com) + AUDIOMACK_TOKEN (OAuth1/2 token)."),
    dict(name="imgur", description="Imgur REST API. ops get/post/put/delete (writes need confirm). Auth: IMGUR_BASE_URL (https://api.imgur.com) + IMGUR_TOKEN (OAuth2 bearer or Client-ID)."),
    dict(name="giphy", description="GIPHY REST API. ops get/post/put/delete (writes need confirm). Auth: GIPHY_BASE_URL (https://api.giphy.com) + GIPHY_TOKEN (API key)."),
    dict(name="tenor", description="Tenor GIF REST API. ops get/post/put/delete (writes need confirm). Auth: TENOR_BASE_URL (https://tenor.googleapis.com) + TENOR_TOKEN (API key)."),
    dict(name="flickr", description="Flickr REST API. ops get/post/put/delete (writes need confirm). Auth: FLICKR_BASE_URL (https://api.flickr.com) + FLICKR_TOKEN (OAuth1 access token)."),
    dict(name="shutterstock", description="Shutterstock REST API. ops get/post/put/delete (writes need confirm). Auth: SHUTTERSTOCK_BASE_URL (https://api.shutterstock.com) + SHUTTERSTOCK_TOKEN (OAuth2 bearer)."),
    dict(name="getty_images", description="Getty Images REST API. ops get/post/put/delete (writes need confirm). Auth: GETTY_IMAGES_BASE_URL (https://api.gettyimages.com) + GETTY_IMAGES_TOKEN (OAuth2 bearer)."),
    dict(name="unsplash", description="Unsplash REST API. ops get/post/put/delete (writes need confirm). Auth: UNSPLASH_BASE_URL (https://api.unsplash.com) + UNSPLASH_TOKEN (Client-ID / access token)."),
    dict(name="pexels", description="Pexels REST API. ops get/post/put/delete (writes need confirm). Auth: PEXELS_BASE_URL (https://api.pexels.com) + PEXELS_TOKEN (API key)."),
    dict(name="pixabay", description="Pixabay REST API. ops get/post/put/delete (writes need confirm). Auth: PIXABAY_BASE_URL (https://pixabay.com/api) + PIXABAY_TOKEN (API key)."),
    dict(name="bunnycdn", description="bunny.net (BunnyCDN/Bunny Stream) REST API. ops get/post/put/delete (writes need confirm). Auth: BUNNYCDN_BASE_URL (https://api.bunny.net) + BUNNYCDN_TOKEN (AccessKey)."),
    dict(name="tidal", description="Tidal music REST API. ops get/post/put/delete (writes need confirm). Auth: TIDAL_BASE_URL (https://openapi.tidal.com) + TIDAL_TOKEN (OAuth2 bearer)."),
    dict(name="panopto", description="Panopto video platform REST API. ops get/post/put/delete (writes need confirm). Auth: PANOPTO_BASE_URL (https://{tenant}.panopto.com) + PANOPTO_TOKEN (OAuth2 bearer)."),
    dict(name="frameio", description="Frame.io (Adobe) REST API. ops get/post/put/delete (writes need confirm). Auth: FRAMEIO_BASE_URL (https://api.frame.io) + FRAMEIO_TOKEN (OAuth2 bearer)."),
    dict(name="wowza", description="Wowza Streaming Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: WOWZA_BASE_URL (https://api.cloud.wowza.com) + WOWZA_TOKEN (API access key)."),
    dict(name="apivideo", description="api.video REST API for video hosting. ops get/post/put/delete (writes need confirm). Auth: APIVIDEO_BASE_URL (https://ws.api.video) + APIVIDEO_TOKEN (API key/bearer)."),
    dict(name="canva", description="Canva Connect REST API. ops get/post/put/delete (writes need confirm). Auth: CANVA_BASE_URL (https://api.canva.com) + CANVA_TOKEN (OAuth2 bearer)."),
    dict(name="sketch", description="Sketch Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: SKETCH_BASE_URL (https://api.sketch.com) + SKETCH_TOKEN (access token)."),
    dict(name="framer", description="Framer REST API. ops get/post/put/delete (writes need confirm). Auth: FRAMER_BASE_URL (https://api.framer.com) + FRAMER_TOKEN (API token)."),
    dict(name="invision", description="InVision REST API. ops get/post/put/delete (writes need confirm). Auth: INVISION_BASE_URL (https://api.invisionapp.com) + INVISION_TOKEN (API key)."),
    dict(name="zeplin", description="Zeplin REST API. ops get/post/put/delete (writes need confirm). Auth: ZEPLIN_BASE_URL (https://api.zeplin.dev) + ZEPLIN_TOKEN (personal access token)."),
    dict(name="dribbble", description="Dribbble REST API. ops get/post/put/delete (writes need confirm). Auth: DRIBBBLE_BASE_URL (https://api.dribbble.com) + DRIBBBLE_TOKEN (OAuth2 bearer)."),
    dict(name="behance", description="Behance REST API. ops get/post/put/delete (writes need confirm). Auth: BEHANCE_BASE_URL (https://api.behance.net) + BEHANCE_TOKEN (API key)."),
    dict(name="lottiefiles", description="LottieFiles REST API. ops get/post/put/delete (writes need confirm). Auth: LOTTIEFILES_BASE_URL (https://api.lottiefiles.com) + LOTTIEFILES_TOKEN (API key)."),
    dict(name="noun_project", description="The Noun Project REST API. ops get/post/put/delete (writes need confirm). Auth: NOUN_PROJECT_BASE_URL (https://api.thenounproject.com) + NOUN_PROJECT_TOKEN (OAuth1 key/secret)."),
    dict(name="picsart", description="Picsart REST API. ops get/post/put/delete (writes need confirm). Auth: PICSART_BASE_URL (https://api.picsart.io) + PICSART_TOKEN (API key)."),
    dict(name="removebg", description="remove.bg REST API. ops get/post/put/delete (writes need confirm). Auth: REMOVEBG_BASE_URL (https://api.remove.bg) + REMOVEBG_TOKEN (API key)."),
    dict(name="marvelapp", description="Marvel App prototyping REST API. ops get/post/put/delete (writes need confirm). Auth: MARVELAPP_BASE_URL (https://api.marvelapp.com) + MARVELAPP_TOKEN (access token)."),
    dict(name="steam", description="Steam Web REST API. ops get/post/put/delete (writes need confirm). Auth: STEAM_BASE_URL (https://api.steampowered.com) + STEAM_TOKEN (Web API key)."),
    dict(name="epicgames", description="Epic Games / Epic Online Services REST API. ops get/post/put/delete (writes need confirm). Auth: EPICGAMES_BASE_URL (https://api.epicgames.dev) + EPICGAMES_TOKEN (OAuth2 bearer)."),
    dict(name="xbox", description="Xbox Live REST API. ops get/post/put/delete (writes need confirm). Auth: XBOX_BASE_URL (https://xbl.io) + XBOX_TOKEN (OAuth2/XSTS token)."),
    dict(name="playstation", description="PlayStation Network REST API. ops get/post/put/delete (writes need confirm). Auth: PLAYSTATION_BASE_URL (https://m.np.playstation.com) + PLAYSTATION_TOKEN (OAuth2 bearer)."),
    dict(name="riotgames", description="Riot Games REST API (League of Legends/Valorant). ops get/post/put/delete (writes need confirm). Auth: RIOTGAMES_BASE_URL (https://{region}.api.riotgames.com) + RIOTGAMES_TOKEN (API key)."),
    dict(name="battlenet", description="Battle.net (Blizzard) REST API. ops get/post/put/delete (writes need confirm). Auth: BATTLENET_BASE_URL (https://{region}.api.blizzard.com) + BATTLENET_TOKEN (OAuth2 bearer)."),
    dict(name="roblox", description="Roblox Open Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: ROBLOX_BASE_URL (https://apis.roblox.com) + ROBLOX_TOKEN (API key)."),
    dict(name="unity", description="Unity Cloud Services REST API. ops get/post/put/delete (writes need confirm). Auth: UNITY_BASE_URL (https://services.api.unity.com) + UNITY_TOKEN (OAuth2 bearer/key)."),
    dict(name="gamejolt", description="Game Jolt REST API. ops get/post/put/delete (writes need confirm). Auth: GAMEJOLT_BASE_URL (https://api.gamejolt.com) + GAMEJOLT_TOKEN (private key)."),
    dict(name="itchio", description="itch.io REST API. ops get/post/put/delete (writes need confirm). Auth: ITCHIO_BASE_URL (https://itch.io/api/1) + ITCHIO_TOKEN (API key)."),
    dict(name="curseforge", description="CurseForge REST API. ops get/post/put/delete (writes need confirm). Auth: CURSEFORGE_BASE_URL (https://api.curseforge.com) + CURSEFORGE_TOKEN (API key)."),
    dict(name="opendota", description="OpenDota REST API (Dota 2 stats). ops get/post/put/delete (writes need confirm). Auth: OPENDOTA_BASE_URL (https://api.opendota.com) + OPENDOTA_TOKEN (API key)."),
    dict(name="faceit", description="FACEIT REST API (esports platform). ops get/post/put/delete (writes need confirm). Auth: FACEIT_BASE_URL (https://open.faceit.com) + FACEIT_TOKEN (API key)."),
    dict(name="toornament", description="Toornament REST API (esports tournaments). ops get/post/put/delete (writes need confirm). Auth: TOORNAMENT_BASE_URL (https://api.toornament.com) + TOORNAMENT_TOKEN (OAuth2 bearer)."),
    dict(name="battlefy", description="Battlefy REST API (esports tournaments). ops get/post/put/delete (writes need confirm). Auth: BATTLEFY_BASE_URL (https://api.battlefy.com) + BATTLEFY_TOKEN (API key)."),
    dict(name="mojang", description="Mojang/Minecraft Services REST API. ops get/post/put/delete (writes need confirm). Auth: MOJANG_BASE_URL (https://api.minecraftservices.com) + MOJANG_TOKEN (OAuth2 bearer)."),
    dict(name="igdb", description="IGDB (Internet Game Database, Twitch-owned) REST API. ops get/post/put/delete (writes need confirm). Auth: IGDB_BASE_URL (https://api.igdb.com) + IGDB_TOKEN (Twitch OAuth2 bearer)."),
    dict(name="speedrun", description="speedrun.com REST API. ops get/post/put/delete (writes need confirm). Auth: SPEEDRUN_BASE_URL (https://www.speedrun.com/api) + SPEEDRUN_TOKEN (API key)."),
    dict(name="playfab", description="Microsoft Azure PlayFab REST API (game backend). ops get/post/put/delete (writes need confirm). Auth: PLAYFAB_BASE_URL (https://{title}.playfabapi.com) + PLAYFAB_TOKEN (secret key)."),
    dict(name="beamable", description="Beamable game backend REST API. ops get/post/put/delete (writes need confirm). Auth: BEAMABLE_BASE_URL (https://api.beamable.com) + BEAMABLE_TOKEN (OAuth2 bearer)."),
    dict(name="twitch", description="Twitch Helix REST API. ops get/post/put/delete (writes need confirm). Auth: TWITCH_BASE_URL (https://api.twitch.tv) + TWITCH_TOKEN (OAuth2 bearer)."),
    dict(name="modrinth", description="Modrinth REST API (game mod hosting). ops get/post/put/delete (writes need confirm). Auth: MODRINTH_BASE_URL (https://api.modrinth.com) + MODRINTH_TOKEN (personal access token)."),
    dict(name="rawg", description="RAWG Video Games Database REST API. ops get/post/put/delete (writes need confirm). Auth: RAWG_BASE_URL (https://api.rawg.io) + RAWG_TOKEN (API key)."),
    dict(name="giantbomb", description="Giant Bomb REST API (video game wiki/database). ops get/post/put/delete (writes need confirm). Auth: GIANTBOMB_BASE_URL (https://www.giantbomb.com/api) + GIANTBOMB_TOKEN (API key)."),
    dict(name="photonengine", description="Photon Engine REST/Webhook API (realtime game multiplayer). ops get/post/put/delete (writes need confirm). Auth: PHOTONENGINE_BASE_URL (https://api.photonengine.com) + PHOTONENGINE_TOKEN (secret/app key)."),
    dict(name="linode", description="Linode (Akamai) Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: LINODE_BASE_URL (https://api.linode.com) + LINODE_TOKEN (personal access token)."),
    dict(name="vultr", description="Vultr Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: VULTR_BASE_URL (https://api.vultr.com) + VULTR_TOKEN (API key)."),
    dict(name="ovhcloud", description="OVHcloud REST API. ops get/post/put/delete (writes need confirm). Auth: OVHCLOUD_BASE_URL (https://api.ovh.com) + OVHCLOUD_TOKEN (application key/secret + consumer key)."),
    dict(name="render", description="Render.com Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: RENDER_BASE_URL (https://api.render.com) + RENDER_TOKEN (API key)."),
    dict(name="railway", description="Railway Cloud GraphQL API. ops get/post/put/delete (writes need confirm). Auth: RAILWAY_BASE_URL (https://backboard.railway.app/graphql/v2) + RAILWAY_TOKEN (API token)."),
    dict(name="netlify", description="Netlify REST API. ops get/post/put/delete (writes need confirm). Auth: NETLIFY_BASE_URL (https://api.netlify.com) + NETLIFY_TOKEN (personal access token)."),
    dict(name="heroku", description="Heroku Platform REST API. ops get/post/put/delete (writes need confirm). Auth: HEROKU_BASE_URL (https://api.heroku.com) + HEROKU_TOKEN (API key)."),
    dict(name="fly_io", description="Fly.io Machines REST API. ops get/post/put/delete (writes need confirm). Auth: FLY_IO_BASE_URL (https://api.machines.dev) + FLY_IO_TOKEN (access token)."),
    dict(name="rackspace", description="Rackspace Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: RACKSPACE_BASE_URL (https://identity.api.rackspacecloud.com) + RACKSPACE_TOKEN (API key)."),
    dict(name="upcloud", description="UpCloud REST API. ops get/post/put/delete (writes need confirm). Auth: UPCLOUD_BASE_URL (https://api.upcloud.com) + UPCLOUD_TOKEN (API username/password).", basic=True),
    dict(name="backblaze", description="Backblaze B2 Cloud Storage REST API. ops get/post/put/delete (writes need confirm). Auth: BACKBLAZE_BASE_URL (https://api.backblazeb2.com) + BACKBLAZE_TOKEN (application key)."),
    dict(name="wasabi", description="Wasabi Cloud Storage REST API (S3-compatible). ops get/post/put/delete (writes need confirm). Auth: WASABI_BASE_URL (https://s3.wasabisys.com) + WASABI_TOKEN (access key/secret)."),
    dict(name="ionos", description="IONOS Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: IONOS_BASE_URL (https://api.ionos.com) + IONOS_TOKEN (API token)."),
    dict(name="civo", description="Civo Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: CIVO_BASE_URL (https://api.civo.com) + CIVO_TOKEN (API key)."),
    dict(name="exoscale", description="Exoscale Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: EXOSCALE_BASE_URL (https://api.exoscale.com) + EXOSCALE_TOKEN (API key/secret)."),
    dict(name="contabo", description="Contabo Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: CONTABO_BASE_URL (https://api.contabo.com) + CONTABO_TOKEN (OAuth2 bearer)."),
    dict(name="kamatera", description="Kamatera Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: KAMATERA_BASE_URL (https://cloudcli.kamatera.com) + KAMATERA_TOKEN (API client ID/secret)."),
    dict(name="stackpath", description="StackPath Edge/CDN REST API. ops get/post/put/delete (writes need confirm). Auth: STACKPATH_BASE_URL (https://gateway.stackpath.com) + STACKPATH_TOKEN (OAuth2 bearer)."),
    dict(name="cachefly", description="CacheFly CDN REST API. ops get/post/put/delete (writes need confirm). Auth: CACHEFLY_BASE_URL (https://api.cachefly.com) + CACHEFLY_TOKEN (API token)."),
    dict(name="cockroachcloud", description="CockroachDB Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: COCKROACHCLOUD_BASE_URL (https://cockroachlabs.cloud/api/v1) + COCKROACHCLOUD_TOKEN (service account token)."),
    dict(name="timescale", description="Timescale Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: TIMESCALE_BASE_URL (https://console.cloud.timescale.com/api) + TIMESCALE_TOKEN (API access key/secret)."),
    dict(name="faunadb", description="Fauna REST/GraphQL API. ops get/post/put/delete (writes need confirm). Auth: FAUNADB_BASE_URL (https://db.fauna.com) + FAUNADB_TOKEN (secret key)."),
    dict(name="koyeb", description="Koyeb Serverless Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: KOYEB_BASE_URL (https://app.koyeb.com/v1) + KOYEB_TOKEN (API token)."),
    dict(name="northflank", description="Northflank REST API (cloud PaaS). ops get/post/put/delete (writes need confirm). Auth: NORTHFLANK_BASE_URL (https://api.northflank.com) + NORTHFLANK_TOKEN (API token)."),
    dict(name="qovery", description="Qovery REST API (cloud deployment platform). ops get/post/put/delete (writes need confirm). Auth: QOVERY_BASE_URL (https://api.qovery.com) + QOVERY_TOKEN (API token)."),
    dict(name="aiven", description="Aiven Cloud Data Platform REST API. ops get/post/put/delete (writes need confirm). Auth: AIVEN_BASE_URL (https://api.aiven.io) + AIVEN_TOKEN (API token)."),
    dict(name="back4app", description="Back4App (Parse Platform hosting) REST API. ops get/post/put/delete (writes need confirm). Auth: BACK4APP_BASE_URL (https://parseapi.back4app.com) + BACK4APP_TOKEN (REST API key)."),
    dict(name="firebase", description="Firebase (Google) REST API. ops get/post/put/delete (writes need confirm). Auth: FIREBASE_BASE_URL (https://{project}.firebaseio.com) + FIREBASE_TOKEN (OAuth2 bearer / database secret)."),
    dict(name="beyondtrust", description="BeyondTrust Password Safe/PRA REST. ops get/post/put/delete (writes need confirm). Auth: BEYONDTRUST_BASE_URL (https://{host}/BeyondTrust/api/public/v3) + BEYONDTRUST_TOKEN (API key)."),
    dict(name="lastpass", description="LastPass Enterprise Admin REST. ops get/post/put/delete (writes need confirm). Auth: LASTPASS_BASE_URL (https://lastpass.com/enterpriseapi.php) + LASTPASS_TOKEN (provisioning hash)."),
    dict(name="bitwarden", description="Bitwarden Organization/Public API REST. ops get/post/put/delete (writes need confirm). Auth: BITWARDEN_BASE_URL (https://api.bitwarden.com) + BITWARDEN_TOKEN (OAuth client credentials bearer)."),
    dict(name="onepassword", description="1Password Connect/Business REST. ops get/post/put/delete (writes need confirm). Auth: ONEPASSWORD_BASE_URL (https://{connect-host}) + ONEPASSWORD_TOKEN (Connect API token)."),
    dict(name="yubico", description="Yubico YubiEnterprise/KeyManager REST. ops get/post/put/delete (writes need confirm). Auth: YUBICO_BASE_URL (https://api.yubico.com) + YUBICO_TOKEN (API key)."),
    dict(name="forgerock", description="ForgeRock Identity Platform REST. ops get/post/put/delete (writes need confirm). Auth: FORGEROCK_BASE_URL (https://{tenant}.forgeblocks.com) + FORGEROCK_TOKEN (OAuth bearer token)."),
    dict(name="saviynt", description="Saviynt Identity Cloud REST. ops get/post/put/delete (writes need confirm). Auth: SAVIYNT_BASE_URL (https://{tenant}.saviyntcloud.com) + SAVIYNT_TOKEN (OAuth bearer token)."),
    dict(name="transmit_security", description="Transmit Security (Identity Orchestration) REST. ops get/post/put/delete (writes need confirm). Auth: TRANSMIT_SECURITY_BASE_URL (https://{env}.identity.security) + TRANSMIT_SECURITY_TOKEN (OAuth bearer)."),
    dict(name="hypr", description="HYPR Control Center REST. ops get/post/put/delete (writes need confirm). Auth: HYPR_BASE_URL (https://{tenant}.hypr.com) + HYPR_TOKEN (API access token)."),
    dict(name="imprivata", description="Imprivata OneSign/Enterprise REST. ops get/post/put/delete (writes need confirm). Auth: IMPRIVATA_BASE_URL (https://{host}) + IMPRIVATA_TOKEN (API key)."),
    dict(name="entra_id", description="Microsoft Entra ID (Azure AD) Graph REST. ops get/post/put/delete (writes need confirm). e.g. /v1.0/users, /v1.0/groups. Auth: ENTRA_ID_BASE_URL (https://graph.microsoft.com) + ENTRA_ID_TOKEN (OAuth bearer token)."),
    dict(name="carbon_black", token_header="X-Auth-Token", scheme="", description="VMware Carbon Black Cloud REST. ops get/post/put/delete (writes need confirm). e.g. /appservices/v6/orgs/{org}/alerts. Auth: CARBON_BLACK_BASE_URL (https://defense.conferdeploy.net) + CARBON_BLACK_TOKEN (API key)."),
    dict(name="cybereason", description="Cybereason Defense Platform REST. ops get/post/put/delete (writes need confirm). Auth: CYBEREASON_BASE_URL (https://{tenant}.cybereason.net) + CYBEREASON_TOKEN (session/API token)."),
    dict(name="sophos", description="Sophos Central REST. ops get/post/put/delete (writes need confirm). e.g. /endpoint/v1/endpoints. Auth: SOPHOS_BASE_URL (https://api-{region}.central.sophos.com) + SOPHOS_TOKEN (OAuth bearer token)."),
    dict(name="trend_micro", token_header="Authorization", scheme="Bearer", description="Trend Micro Vision One REST. ops get/post/put/delete (writes need confirm). e.g. /v3.0/workbench/alerts. Auth: TREND_MICRO_BASE_URL (https://api.xdr.trendmicro.com) + TREND_MICRO_TOKEN (API token)."),
    dict(name="mcafee", description="McAfee (Trellix) ePO/MVISION REST. ops get/post/put/delete (writes need confirm). Auth: MCAFEE_BASE_URL (https://api.manage.trellix.com) + MCAFEE_TOKEN (OAuth bearer token)."),
    dict(name="trellix", description="Trellix (Helix/EDR) REST. ops get/post/put/delete (writes need confirm). Auth: TRELLIX_BASE_URL (https://api.manage.trellix.com) + TRELLIX_TOKEN (OAuth bearer token)."),
    dict(name="eset", description="ESET PROTECT REST. ops get/post/put/delete (writes need confirm). Auth: ESET_BASE_URL (https://{region}.eset.com/automation/v1) + ESET_TOKEN (OAuth bearer token)."),
    dict(name="bitdefender", description="Bitdefender GravityZone REST/JSON-RPC. ops get/post/put/delete (writes need confirm). Auth: BITDEFENDER_BASE_URL (https://cloud.gravityzone.bitdefender.com/api) + BITDEFENDER_TOKEN (API key)."),
    dict(name="malwarebytes", description="Malwarebytes Nebula/OneView REST. ops get/post/put/delete (writes need confirm). Auth: MALWAREBYTES_BASE_URL (https://api.nebula.malwarebytes.com) + MALWAREBYTES_TOKEN (OAuth bearer token)."),
    dict(name="checkpoint", description="Check Point Infinity/Smart-1 Cloud REST. ops get/post/put/delete (writes need confirm). Auth: CHECKPOINT_BASE_URL (https://cloudinfra-gw.portal.checkpoint.com) + CHECKPOINT_TOKEN (API key)."),
    dict(name="f5", description="F5 BIG-IP/Distributed Cloud REST. ops get/post/put/delete (writes need confirm). e.g. /mgmt/tm/ltm/pool. Auth: F5_BASE_URL (https://{host}/mgmt) + F5_TOKEN (auth token)."),
    dict(name="imperva", description="Imperva Cloud WAF/API Security REST. ops get/post/put/delete (writes need confirm). Auth: IMPERVA_BASE_URL (https://api.imperva.com) + IMPERVA_TOKEN (API key + API ID)."),
    dict(name="barracuda", description="Barracuda Cloud Control REST. ops get/post/put/delete (writes need confirm). Auth: BARRACUDA_BASE_URL (https://api.barracudanetworks.com) + BARRACUDA_TOKEN (OAuth bearer token)."),
    dict(name="extrahop", description="ExtraHop Reveal(x) REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/devices. Auth: EXTRAHOP_BASE_URL (https://{host}/api/v1) + EXTRAHOP_TOKEN (API key)."),
    dict(name="vectra", description="Vectra AI Platform REST. ops get/post/put/delete (writes need confirm). Auth: VECTRA_BASE_URL (https://{portal}.vectra.ai) + VECTRA_TOKEN (OAuth bearer token)."),
    dict(name="darktrace", description="Darktrace Threat Visualizer REST. ops get/post/put/delete (writes need confirm). Auth: DARKTRACE_BASE_URL (https://{host}) + DARKTRACE_TOKEN (public/private key pair token)."),
    dict(name="anchore", description="Anchore Enterprise REST. ops get/post/put/delete (writes need confirm). Auth: ANCHORE_BASE_URL (https://{host}/v2) + ANCHORE_TOKEN."),
    dict(name="coverity", description="Synopsys Coverity Connect REST. ops get/post/put/delete (writes need confirm). Auth: COVERITY_BASE_URL (https://{host}/api/v2) + COVERITY_TOKEN (auth key)."),
    dict(name="fortify", description="OpenText Fortify on Demand REST. ops get/post/put/delete (writes need confirm). Auth: FORTIFY_BASE_URL (https://api.ams.fortify.com) + FORTIFY_TOKEN (OAuth bearer token)."),
    dict(name="contrast_security", token_header="Authorization", scheme="", description="Contrast Security REST. ops get/post/put/delete (writes need confirm). e.g. /Contrast/api/ng/{org}/applications. Auth: CONTRAST_SECURITY_BASE_URL (https://app.contrastsecurity.com) + CONTRAST_SECURITY_TOKEN (API key)."),
    dict(name="invicti", description="Invicti (Netsparker) Enterprise REST. ops get/post/put/delete (writes need confirm). Auth: INVICTI_BASE_URL (https://{tenant}.invicti.com/api/1.0) + INVICTI_TOKEN (API ID/key)."),
    dict(name="acunetix", description="Acunetix Premium REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/targets. Auth: ACUNETIX_BASE_URL (https://{host}:3443/api/v1) + ACUNETIX_TOKEN (API key)."),
    dict(name="burp_suite", description="PortSwigger Burp Suite Enterprise REST. ops get/post/put/delete (writes need confirm). Auth: BURP_SUITE_BASE_URL (https://{host}/graphql) + BURP_SUITE_TOKEN (API key)."),
    dict(name="greenbone", description="Greenbone Enterprise (OpenVAS) GMP/REST. ops get/post/put/delete (writes need confirm). Auth: GREENBONE_BASE_URL (https://{host}/gmp) + GREENBONE_TOKEN (API key)."),
    dict(name="kenna_security", description="Cisco Kenna Security (Vulnerability Response) REST. ops get/post/put/delete (writes need confirm). Auth: KENNA_SECURITY_BASE_URL (https://api.kennasecurity.com) + KENNA_SECURITY_TOKEN (API key)."),
    dict(name="vulcan_cyber", description="Vulcan Cyber (Voyager18) REST. ops get/post/put/delete (writes need confirm). Auth: VULCAN_CYBER_BASE_URL (https://free.vulcancyber.com/api) + VULCAN_CYBER_TOKEN (API token)."),
    dict(name="brinqa", description="Brinqa Cyber Risk Platform REST. ops get/post/put/delete (writes need confirm). Auth: BRINQA_BASE_URL (https://{tenant}.brinqa.net) + BRINQA_TOKEN (OAuth bearer token)."),
    dict(name="nucleus_security", description="Nucleus Security REST. ops get/post/put/delete (writes need confirm). Auth: NUCLEUS_SECURITY_BASE_URL (https://nucleussec.com/nucleus/api) + NUCLEUS_SECURITY_TOKEN (API key)."),
    dict(name="edgescan", description="Edgescan Fullstack Vulnerability Management REST. ops get/post/put/delete (writes need confirm). Auth: EDGESCAN_BASE_URL (https://api.edgescan.com) + EDGESCAN_TOKEN (API key)."),
    dict(name="intruder", description="Intruder.io Vulnerability Scanning REST. ops get/post/put/delete (writes need confirm). Auth: INTRUDER_BASE_URL (https://api.intruder.io) + INTRUDER_TOKEN (API key)."),
    dict(name="detectify", description="Detectify Surface Monitoring REST. ops get/post/put/delete (writes need confirm). Auth: DETECTIFY_BASE_URL (https://api.detectify.com) + DETECTIFY_TOKEN (API key)."),
    dict(name="probely", description="Probely Web/API Vulnerability Scanning REST. ops get/post/put/delete (writes need confirm). Auth: PROBELY_BASE_URL (https://api.probely.com) + PROBELY_TOKEN (API key)."),
    dict(name="archer", description="RSA Archer GRC REST. ops get/post/put/delete (writes need confirm). Auth: ARCHER_BASE_URL (https://{host}/rsaarcher/api) + ARCHER_TOKEN (session token)."),
    dict(name="resolver", description="Resolver GRC/Risk Management REST. ops get/post/put/delete (writes need confirm). Auth: RESOLVER_BASE_URL (https://{org}.resolver.com/rest/v1) + RESOLVER_TOKEN (API key)."),
    dict(name="navex", description="NAVEX Global (IntegrityPoint/RiskRate) REST. ops get/post/put/delete (writes need confirm). Auth: NAVEX_BASE_URL (https://{tenant}.navexone.com) + NAVEX_TOKEN (OAuth bearer token)."),
    dict(name="diligent", description="Diligent One (HighBond) GRC REST. ops get/post/put/delete (writes need confirm). Auth: DILIGENT_BASE_URL (https://{org}.highbond.com/api/v1) + DILIGENT_TOKEN (API key)."),
    dict(name="auditboard", description="AuditBoard GRC REST. ops get/post/put/delete (writes need confirm). Auth: AUDITBOARD_BASE_URL (https://{tenant}.auditboard.com/api/v1) + AUDITBOARD_TOKEN (API key)."),
    dict(name="hyperproof", description="Hyperproof Compliance Operations REST. ops get/post/put/delete (writes need confirm). Auth: HYPERPROOF_BASE_URL (https://api.hyperproof.app) + HYPERPROOF_TOKEN (OAuth bearer token)."),
    dict(name="scrut", description="Scrut Automation GRC REST. ops get/post/put/delete (writes need confirm). Auth: SCRUT_BASE_URL (https://app.scrut.io/api) + SCRUT_TOKEN (API key)."),
    dict(name="sprinto", description="Sprinto Compliance Automation REST. ops get/post/put/delete (writes need confirm). Auth: SPRINTO_BASE_URL (https://api.sprinto.com) + SPRINTO_TOKEN (API key)."),
    dict(name="thoropass", description="Thoropass Compliance Automation REST. ops get/post/put/delete (writes need confirm). Auth: THOROPASS_BASE_URL (https://app.thoropass.com/api) + THOROPASS_TOKEN (API key)."),
    dict(name="onspring", description="Onspring GRC Platform REST. ops get/post/put/delete (writes need confirm). Auth: ONSPRING_BASE_URL (https://api.onspring.com) + ONSPRING_TOKEN (API key)."),
    dict(name="bitsight", description="BitSight Security Ratings REST. ops get/post/put/delete (writes need confirm). Auth: BITSIGHT_BASE_URL (https://api.bitsighttech.com) + BITSIGHT_TOKEN (API key)."),
    dict(name="securityscorecard", description="SecurityScorecard Ratings REST. ops get/post/put/delete (writes need confirm). Auth: SECURITYSCORECARD_BASE_URL (https://api.securityscorecard.io) + SECURITYSCORECARD_TOKEN (API key)."),
    dict(name="upguard", description="UpGuard CyberRisk REST. ops get/post/put/delete (writes need confirm). Auth: UPGUARD_BASE_URL (https://cyber-risk.upguard.com/api/public) + UPGUARD_TOKEN (API key)."),
    dict(name="panorays", description="Panorays Third-Party Risk REST. ops get/post/put/delete (writes need confirm). Auth: PANORAYS_BASE_URL (https://api.panorays.com) + PANORAYS_TOKEN (API key)."),
    dict(name="prevalent", description="Prevalent Third-Party Risk Management REST. ops get/post/put/delete (writes need confirm). Auth: PREVALENT_BASE_URL (https://{tenant}.prevalent.net) + PREVALENT_TOKEN (API key)."),
    dict(name="shodan", description="Shodan Internet Intelligence REST. ops get/post/put/delete (writes need confirm). e.g. /shodan/host/{ip}. Auth: SHODAN_BASE_URL (https://api.shodan.io) + SHODAN_TOKEN (API key)."),
    dict(name="censys", description="Censys Search/Platform REST. ops get/post/put/delete (writes need confirm). e.g. /v2/hosts/{ip}. Auth: CENSYS_BASE_URL (https://search.censys.io/api) + CENSYS_TOKEN (API ID/secret bearer)."),
    dict(name="recordedfuture", description="Recorded Future Threat Intelligence REST. ops get/post/put/delete (writes need confirm). Auth: RECORDEDFUTURE_BASE_URL (https://api.recordedfuture.com) + RECORDEDFUTURE_TOKEN (API key)."),
    dict(name="mandiant", description="Mandiant Advantage Threat Intelligence REST. ops get/post/put/delete (writes need confirm). Auth: MANDIANT_BASE_URL (https://api.intelligence.mandiant.com) + MANDIANT_TOKEN (OAuth bearer token)."),
    dict(name="misp", description="MISP Threat Sharing Platform REST. ops get/post/put/delete (writes need confirm). e.g. /events/restSearch. Auth: MISP_BASE_URL (https://{host}) + MISP_TOKEN (Automation API key)."),
    dict(name="anomali", description="Anomali ThreatStream REST. ops get/post/put/delete (writes need confirm). Auth: ANOMALI_BASE_URL (https://api.threatstream.com) + ANOMALI_TOKEN (username:apikey token)."),
    dict(name="threatconnect", description="ThreatConnect Threat Intelligence Platform REST. ops get/post/put/delete (writes need confirm). Auth: THREATCONNECT_BASE_URL (https://api.threatconnect.com) + THREATCONNECT_TOKEN (HMAC access id/secret key)."),
    dict(name="virustotal", token_header="x-apikey", scheme="", description="VirusTotal REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/files/{id}. Auth: VIRUSTOTAL_BASE_URL (https://www.virustotal.com/api/v3) + VIRUSTOTAL_TOKEN (API key)."),
    dict(name="abuseipdb", token_header="Key", scheme="", description="AbuseIPDB REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/check. Auth: ABUSEIPDB_BASE_URL (https://api.abuseipdb.com/api/v2) + ABUSEIPDB_TOKEN (API key)."),
    dict(name="crowdsec", description="CrowdSec Console/CTI REST. ops get/post/put/delete (writes need confirm). Auth: CROWDSEC_BASE_URL (https://api.crowdsec.net) + CROWDSEC_TOKEN (API key)."),
    dict(name="mimecast", description="Mimecast Email Security REST. ops get/post/put/delete (writes need confirm). Auth: MIMECAST_BASE_URL (https://{region}-api.mimecast.com) + MIMECAST_TOKEN (OAuth bearer token)."),
    dict(name="knowbe4", description="KnowBe4 Security Awareness Training REST. ops get/post/put/delete (writes need confirm). e.g. /v1/users, /v1/phishing/campaigns. Auth: KNOWBE4_BASE_URL (https://us.api.knowbe4.com) + KNOWBE4_TOKEN (API key)."),
    dict(name="abnormal_security", description="Abnormal Security REST. ops get/post/put/delete (writes need confirm). Auth: ABNORMAL_SECURITY_BASE_URL (https://api.abnormalplatform.com) + ABNORMAL_SECURITY_TOKEN (API token)."),
    dict(name="ironscales", description="IRONSCALES Email Security REST. ops get/post/put/delete (writes need confirm). Auth: IRONSCALES_BASE_URL (https://appapi.ironscales.com) + IRONSCALES_TOKEN (API key)."),
    dict(name="cofense", description="Cofense Triage/Intelligence REST. ops get/post/put/delete (writes need confirm). Auth: COFENSE_BASE_URL (https://{tenant}.cofense.com/api) + COFENSE_TOKEN (API token)."),
    dict(name="cherwell", description="Cherwell Service Management REST. ops get/post/put/delete (writes need confirm). Auth: CHERWELL_BASE_URL (https://{host}/CherwellAPI) + CHERWELL_TOKEN (OAuth bearer token)."),
    dict(name="topdesk", basic=True, description="TOPdesk Service Management REST. ops get/post/put/delete (writes need confirm). e.g. /tas/api/incidents. Auth: TOPDESK_BASE_URL (https://{tenant}.topdesk.net) + TOPDESK_TOKEN (application password)."),
    dict(name="haloitsm", description="Halo ITSM REST. ops get/post/put/delete (writes need confirm). e.g. /api/Tickets. Auth: HALOITSM_BASE_URL (https://{tenant}.haloitsm.com/api) + HALOITSM_TOKEN (OAuth bearer token)."),
    dict(name="sysaid", description="SysAid ITSM REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/incidents. Auth: SYSAID_BASE_URL (https://{tenant}.sysaidit.com/api/v1) + SYSAID_TOKEN (session/API token)."),
    dict(name="easyvista", description="EasyVista Service Manager REST. ops get/post/put/delete (writes need confirm). Auth: EASYVISTA_BASE_URL (https://{tenant}.easyvista.com/api/v1) + EASYVISTA_TOKEN (API key)."),
    dict(name="matrix42", description="Matrix42 Service Management REST. ops get/post/put/delete (writes need confirm). Auth: MATRIX42_BASE_URL (https://{host}/M42Services/api) + MATRIX42_TOKEN (OAuth bearer token)."),
    dict(name="symantec_endpoint", description="Broadcom Symantec Endpoint Security REST. ops get/post/put/delete (writes need confirm). Auth: SYMANTEC_ENDPOINT_BASE_URL (https://{tenant}.sep.securitycloud.symantec.com) + SYMANTEC_ENDPOINT_TOKEN (OAuth bearer token)."),
    dict(name="cortex_xdr", description="Palo Alto Networks Cortex XDR REST. ops get/post/put/delete (writes need confirm). Auth: CORTEX_XDR_BASE_URL (https://api-{tenant}.xdr.us.paloaltonetworks.com) + CORTEX_XDR_TOKEN (API key ID + key)."),
    dict(name="trufflesecurity", description="Truffle Security (TruffleHog Enterprise) REST. ops get/post/put/delete (writes need confirm). Auth: TRUFFLESECURITY_BASE_URL (https://{tenant}.trufflesecurity.com/api) + TRUFFLESECURITY_TOKEN (API key)."),
    dict(name="gitguardian", description="GitGuardian Secrets Detection REST. ops get/post/put/delete (writes need confirm). e.g. /v1/incidents. Auth: GITGUARDIAN_BASE_URL (https://api.gitguardian.com) + GITGUARDIAN_TOKEN (API key)."),
    dict(name="orca_iam", description="AppOmni SaaS Security Posture Management REST. ops get/post/put/delete (writes need confirm). Auth: ORCA_IAM_BASE_URL (https://{tenant}.appomni.com/api) + ORCA_IAM_TOKEN (API key)."),
    dict(name="axonius", description="Axonius Cyber Asset Management REST. ops get/post/put/delete (writes need confirm). e.g. /api/devices. Auth: AXONIUS_BASE_URL (https://{host}/api) + AXONIUS_TOKEN (API key + API secret)."),
    dict(name="expel", description="Expel MDR Workbench REST. ops get/post/put/delete (writes need confirm). Auth: EXPEL_BASE_URL (https://workbench.expel.io/api/v2) + EXPEL_TOKEN (OAuth bearer token)."),
    dict(name="arctic_wolf", description="Arctic Wolf Platform REST. ops get/post/put/delete (writes need confirm). Auth: ARCTIC_WOLF_BASE_URL (https://api.arcticwolf.com) + ARCTIC_WOLF_TOKEN (OAuth bearer token)."),
    dict(name="huntress", description="Huntress Managed EDR REST. ops get/post/put/delete (writes need confirm). e.g. /v1/incident_reports. Auth: HUNTRESS_BASE_URL (https://api.huntress.io) + HUNTRESS_TOKEN (API key + API secret)."),
    dict(name="deepwatch", description="Deepwatch MDR Platform REST. ops get/post/put/delete (writes need confirm). Auth: DEEPWATCH_BASE_URL (https://{tenant}.deepwatch.com/api) + DEEPWATCH_TOKEN (API token)."),
    dict(name="obsidian_security", description="Obsidian Security SaaS Security Posture REST. ops get/post/put/delete (writes need confirm). Auth: OBSIDIAN_SECURITY_BASE_URL (https://{tenant}.obsidiansecurity.com/api) + OBSIDIAN_SECURITY_TOKEN (API key)."),
    dict(name="wallarm", description="Wallarm API Security REST. ops get/post/put/delete (writes need confirm). e.g. /v1/objects/scope. Auth: WALLARM_BASE_URL (https://api.wallarm.com) + WALLARM_TOKEN (API token)."),
    dict(name="salt_security", description="Salt Security API Protection REST. ops get/post/put/delete (writes need confirm). Auth: SALT_SECURITY_BASE_URL (https://{tenant}.saltsecurity.io/api) + SALT_SECURITY_TOKEN (OAuth bearer token)."),
    dict(name="noname_security", description="Noname Security API Security Platform REST. ops get/post/put/delete (writes need confirm). Auth: NONAME_SECURITY_BASE_URL (https://{tenant}.nonamesecurity.com/api) + NONAME_SECURITY_TOKEN (API key)."),
    dict(name="cequence", description="Cequence Security (Unified API Protection) REST. ops get/post/put/delete (writes need confirm). Auth: CEQUENCE_BASE_URL (https://{tenant}.cequence.ai/api) + CEQUENCE_TOKEN (API key)."),
    dict(name="lookout", description="Lookout Mobile Endpoint Security REST. ops get/post/put/delete (writes need confirm). Auth: LOOKOUT_BASE_URL (https://api.lookout.com) + LOOKOUT_TOKEN (OAuth bearer token)."),
    dict(name="zimperium", description="Zimperium Mobile Threat Defense REST. ops get/post/put/delete (writes need confirm). Auth: ZIMPERIUM_BASE_URL (https://{tenant}.zimperium.com/api) + ZIMPERIUM_TOKEN (API key)."),
    dict(name="wandera", description="Wandera (Jamf Threat Defense) REST. ops get/post/put/delete (writes need confirm). Auth: WANDERA_BASE_URL (https://api.wandera.com) + WANDERA_TOKEN (API key)."),
    dict(name="corelight", description="Corelight Network Detection REST. ops get/post/put/delete (writes need confirm). Auth: CORELIGHT_BASE_URL (https://{host}/api) + CORELIGHT_TOKEN (API key)."),
    dict(name="stellar_cyber", description="Stellar Cyber Open XDR REST. ops get/post/put/delete (writes need confirm). Auth: STELLAR_CYBER_BASE_URL (https://{tenant}.stellarcyber.ai) + STELLAR_CYBER_TOKEN (OAuth bearer token)."),
    dict(name="swimlane", description="Swimlane SOAR REST. ops get/post/put/delete (writes need confirm). e.g. /api/app. Auth: SWIMLANE_BASE_URL (https://{host}/api) + SWIMLANE_TOKEN (API token)."),
    dict(name="tines", description="Tines Security Automation REST. ops get/post/put/delete (writes need confirm). Auth: TINES_BASE_URL (https://{tenant}.tines.com) + TINES_TOKEN (API key)."),
    dict(name="torq", description="Torq Security Automation REST. ops get/post/put/delete (writes need confirm). Auth: TORQ_BASE_URL (https://api.torq.io) + TORQ_TOKEN (API token)."),
    dict(name="cortex_xsoar", description="Palo Alto Networks Cortex XSOAR REST. ops get/post/put/delete (writes need confirm). e.g. /incident/search. Auth: CORTEX_XSOAR_BASE_URL (https://{host}/xsoar) + CORTEX_XSOAR_TOKEN (API key)."),
    dict(name="devo", description="Devo Security Data Platform REST. ops get/post/put/delete (writes need confirm). Auth: DEVO_BASE_URL (https://api.{region}.devo.com) + DEVO_TOKEN (OAuth bearer token)."),
    dict(name="exabeam", description="Exabeam Security Operations Platform REST. ops get/post/put/delete (writes need confirm). Auth: EXABEAM_BASE_URL (https://{tenant}.exabeam.com/api) + EXABEAM_TOKEN (API key)."),
    dict(name="securonix", description="Securonix Unified Defense SIEM REST. ops get/post/put/delete (writes need confirm). Auth: SECURONIX_BASE_URL (https://{tenant}.securonix.net) + SECURONIX_TOKEN (OAuth bearer token)."),
    dict(name="cyware", description="Cyware Threat Intelligence/SOAR REST. ops get/post/put/delete (writes need confirm). Auth: CYWARE_BASE_URL (https://{tenant}.cyware.com/api) + CYWARE_TOKEN (API key)."),
    dict(name="pentera", description="Pentera Automated Security Validation REST. ops get/post/put/delete (writes need confirm). Auth: PENTERA_BASE_URL (https://{host}/api) + PENTERA_TOKEN (API key)."),
    dict(name="cymulate", description="Cymulate Exposure Management/BAS REST. ops get/post/put/delete (writes need confirm). e.g. /api/app/v1/agent. Auth: CYMULATE_BASE_URL (https://app.cymulate.com/api) + CYMULATE_TOKEN (API key)."),
    dict(name="attackiq", description="AttackIQ Security Optimization Platform REST. ops get/post/put/delete (writes need confirm). Auth: ATTACKIQ_BASE_URL (https://{tenant}.attackiq.com/api) + ATTACKIQ_TOKEN (API token)."),
    dict(name="safebreach", description="SafeBreach Breach and Attack Simulation REST. ops get/post/put/delete (writes need confirm). Auth: SAFEBREACH_BASE_URL (https://{host}/api) + SAFEBREACH_TOKEN (API key)."),
    dict(name="cyera", description="Cyera Data Security Posture Management REST. ops get/post/put/delete (writes need confirm). Auth: CYERA_BASE_URL (https://api.cyera.io) + CYERA_TOKEN (OAuth bearer token)."),
    dict(name="varonis", description="Varonis Data Security Platform REST. ops get/post/put/delete (writes need confirm). Auth: VARONIS_BASE_URL (https://{tenant}.varonis.com/api) + VARONIS_TOKEN (OAuth bearer token)."),
    dict(name="bigid", description="BigID Data Intelligence Platform REST. ops get/post/put/delete (writes need confirm). Auth: BIGID_BASE_URL (https://{host}/api/v1) + BIGID_TOKEN (session/API token)."),
    dict(name="immuta", description="Immuta Data Security Platform REST. ops get/post/put/delete (writes need confirm). Auth: IMMUTA_BASE_URL (https://{tenant}.immuta.com/api) + IMMUTA_TOKEN (API key)."),
    dict(name="satori_cyber", description="Satori Data Security Platform REST. ops get/post/put/delete (writes need confirm). Auth: SATORI_CYBER_BASE_URL (https://app.satoricyber.com/api) + SATORI_CYBER_TOKEN (API token)."),
    dict(name="normalyze", description="Normalyze Data Security Posture Management REST. ops get/post/put/delete (writes need confirm). Auth: NORMALYZE_BASE_URL (https://{tenant}.normalyze.app/api) + NORMALYZE_TOKEN (API key)."),
    dict(name="veza", description="Veza Access Security Platform REST. ops get/post/put/delete (writes need confirm). Auth: VEZA_BASE_URL (https://{tenant}.vezacloud.com/api) + VEZA_TOKEN (OAuth bearer token)."),
    dict(name="britive", description="Britive Cloud Privileged Access Management REST. ops get/post/put/delete (writes need confirm). Auth: BRITIVE_BASE_URL (https://{tenant}.britive-app.com/api) + BRITIVE_TOKEN (OAuth bearer token)."),
    dict(name="p0_security", description="P0 Security Just-in-Time Access REST. ops get/post/put/delete (writes need confirm). Auth: P0_SECURITY_BASE_URL (https://api.p0.dev) + P0_SECURITY_TOKEN (API token)."),
    dict(name="opal_security", description="Opal Security Access Management REST. ops get/post/put/delete (writes need confirm). Auth: OPAL_SECURITY_BASE_URL (https://api.opal.dev) + OPAL_SECURITY_TOKEN (API token)."),
    dict(name="conductorone", description="ConductorOne Access Governance REST. ops get/post/put/delete (writes need confirm). Auth: CONDUCTORONE_BASE_URL (https://{tenant}.conductor.one/api) + CONDUCTORONE_TOKEN (API token)."),
    dict(name="yardi", description="Yardi Voyager property management REST/SOAP gateway. ops get/post/put/delete (writes need confirm). Auth: YARDI_BASE_URL (https://{client}.yardi.com) + YARDI_TOKEN (API key)."),
    dict(name="appfolio", basic=True, description="AppFolio Property Manager Report/Data REST API. ops get/post/put/delete (writes need confirm). Auth: APPFOLIO_BASE_URL (https://{db}.appfolio.com/api) + APPFOLIO_TOKEN (client id:secret)."),
    dict(name="buildium", description="Buildium property management REST. ops get/post/put/delete (writes need confirm). e.g. /v1/rentals, /v1/leases. Auth: BUILDIUM_BASE_URL (https://api.buildium.com) + BUILDIUM_TOKEN (client id/secret pair)."),
    dict(name="rentmanager", description="Rent Manager property management REST. ops get/post/put/delete (writes need confirm). Auth: RENTMANAGER_BASE_URL (https://{db}.api.rentmanager.com) + RENTMANAGER_TOKEN (API key)."),
    dict(name="entrata", description="Entrata property management REST/JSON-RPC API. ops get/post/put/delete (writes need confirm). Auth: ENTRATA_BASE_URL (https://{sub}.entrata.com/api) + ENTRATA_TOKEN."),
    dict(name="realpage", description="RealPage OneSite property management REST. ops get/post/put/delete (writes need confirm). Auth: REALPAGE_BASE_URL + REALPAGE_TOKEN (OAuth2 bearer)."),
    dict(name="costar", description="CoStar commercial real estate data REST API. ops get/post (writes need confirm). Auth: COSTAR_BASE_URL (https://api.costar.com) + COSTAR_TOKEN (OAuth2 bearer)."),
    dict(name="mrisoftware", description="MRI Software property management REST API. ops get/post/put/delete (writes need confirm). Auth: MRISOFTWARE_BASE_URL + MRISOFTWARE_TOKEN (OAuth2 bearer)."),
    dict(name="propertyware", description="Propertyware (RealPage) property management REST/XML API. ops get/post/put/delete (writes need confirm). Auth: PROPERTYWARE_BASE_URL + PROPERTYWARE_TOKEN."),
    dict(name="doorloop", description="DoorLoop property management REST. ops get/post/put/delete (writes need confirm). Auth: DOORLOOP_BASE_URL (https://app.doorloop.com/api) + DOORLOOP_TOKEN (API key)."),
    dict(name="dotloop", description="Dotloop real estate transaction management REST. ops get/post/put/delete (writes need confirm). e.g. /my/account, /loop. Auth: DOTLOOP_BASE_URL (https://api-gateway.dotloop.com) + DOTLOOP_TOKEN (OAuth2 bearer)."),
    dict(name="showingtime", description="ShowingTime real estate showing-scheduling REST partner API. ops get/post/put (writes need confirm). Auth: SHOWINGTIME_BASE_URL + SHOWINGTIME_TOKEN."),
    dict(name="loopnet", description="LoopNet commercial real estate listings partner REST API. ops get/post (writes need confirm). Auth: LOOPNET_BASE_URL + LOOPNET_TOKEN."),
    dict(name="matterport", description="Matterport Cloud REST/GraphQL API for 3D property tours. ops get/post/put/delete (writes need confirm). Auth: MATTERPORT_BASE_URL (https://api.matterport.com) + MATTERPORT_TOKEN."),
    dict(name="rentredi", description="RentRedi property management REST API. ops get/post/put/delete (writes need confirm). Auth: RENTREDI_BASE_URL + RENTREDI_TOKEN (API key)."),
    dict(name="epic_fhir", description="Epic on FHIR REST API for EHR data. ops get/post (writes need confirm). e.g. /api/FHIR/R4/Patient. Auth: EPIC_FHIR_BASE_URL (https://fhir.epic.com) + EPIC_FHIR_TOKEN (OAuth2 bearer)."),
    dict(name="cerner", description="Oracle Cerner (Millennium) FHIR REST API for EHR data. ops get/post (writes need confirm). e.g. /r4/Patient. Auth: CERNER_BASE_URL (https://fhir-open.cerner.com/r4) + CERNER_TOKEN (OAuth2 bearer)."),
    dict(name="athenahealth", description="athenahealth athenaOne REST API for practice/EHR data. ops get/post/put (writes need confirm). e.g. /v1/{practiceid}/patients. Auth: ATHENAHEALTH_BASE_URL (https://api.athenahealth.com) + ATHENAHEALTH_TOKEN (OAuth2 bearer)."),
    dict(name="veradigm", description="Veradigm (Allscripts) EHR REST/FHIR API. ops get/post (writes need confirm). Auth: VERADIGM_BASE_URL + VERADIGM_TOKEN (OAuth2 bearer)."),
    dict(name="nextgen_healthcare", description="NextGen Healthcare EHR REST API. ops get/post/put (writes need confirm). Auth: NEXTGEN_HEALTHCARE_BASE_URL + NEXTGEN_HEALTHCARE_TOKEN (OAuth2 bearer)."),
    dict(name="practice_fusion", description="Practice Fusion EHR REST API. ops get/post/put (writes need confirm). Auth: PRACTICE_FUSION_BASE_URL + PRACTICE_FUSION_TOKEN (OAuth2 bearer)."),
    dict(name="kareo", description="Kareo (Tebra) medical practice management REST API. ops get/post/put (writes need confirm). Auth: KAREO_BASE_URL + KAREO_TOKEN (API key)."),
    dict(name="drchrono", description="DrChrono EHR REST API. ops get/post/put/delete (writes need confirm). e.g. /api/patients, /api/appointments. Auth: DRCHRONO_BASE_URL (https://app.drchrono.com/api) + DRCHRONO_TOKEN (OAuth2 bearer)."),
    dict(name="eclinicalworks", description="eClinicalWorks EHR REST/FHIR API. ops get/post (writes need confirm). Auth: ECLINICALWORKS_BASE_URL + ECLINICALWORKS_TOKEN (OAuth2 bearer)."),
    dict(name="meditech", description="MEDITECH Expanse EHR REST/FHIR API. ops get/post (writes need confirm). Auth: MEDITECH_BASE_URL + MEDITECH_TOKEN (OAuth2 bearer)."),
    dict(name="greenway_health", description="Greenway Health (Intergy) EHR REST API. ops get/post/put (writes need confirm). Auth: GREENWAY_HEALTH_BASE_URL + GREENWAY_HEALTH_TOKEN (OAuth2 bearer)."),
    dict(name="carecloud", description="CareCloud practice management/EHR REST API. ops get/post/put (writes need confirm). Auth: CARECLOUD_BASE_URL + CARECLOUD_TOKEN (OAuth2 bearer)."),
    dict(name="advancedmd", description="AdvancedMD practice management/EHR REST API. ops get/post/put (writes need confirm). Auth: ADVANCEDMD_BASE_URL + ADVANCEDMD_TOKEN (API key)."),
    dict(name="healthgorilla", description="Health Gorilla clinical data network REST/FHIR API. ops get/post (writes need confirm). Auth: HEALTHGORILLA_BASE_URL (https://api.healthgorilla.com) + HEALTHGORILLA_TOKEN (OAuth2 bearer)."),
    dict(name="redoxengine", description="Redox healthcare data integration REST API. ops get/post (writes need confirm). Auth: REDOXENGINE_BASE_URL (https://api.redoxengine.com) + REDOXENGINE_TOKEN (OAuth2 bearer)."),
    dict(name="particle_health", description="Particle Health clinical data REST/FHIR API. ops get/post (writes need confirm). Auth: PARTICLE_HEALTH_BASE_URL (https://api.particlehealth.com) + PARTICLE_HEALTH_TOKEN."),
    dict(name="candidhealth", description="Candid Health medical billing REST API. ops get/post/put (writes need confirm). Auth: CANDIDHEALTH_BASE_URL (https://api.joincandidhealth.com) + CANDIDHEALTH_TOKEN (bearer)."),
    dict(name="zocdoc", description="Zocdoc partner REST API for appointment booking. ops get/post (writes need confirm). Auth: ZOCDOC_BASE_URL + ZOCDOC_TOKEN (OAuth2 bearer)."),
    dict(name="simplepractice", description="SimplePractice practice management REST API. ops get/post/put (writes need confirm). Auth: SIMPLEPRACTICE_BASE_URL + SIMPLEPRACTICE_TOKEN."),
    dict(name="therapynotes", description="TherapyNotes EHR REST API. ops get/post/put (writes need confirm). Auth: THERAPYNOTES_BASE_URL + THERAPYNOTES_TOKEN."),
    dict(name="webpt", description="WebPT physical therapy EHR REST API. ops get/post/put (writes need confirm). Auth: WEBPT_BASE_URL + WEBPT_TOKEN (OAuth2 bearer)."),
    dict(name="tebra", description="Tebra (Kareo) medical practice management REST API. ops get/post/put (writes need confirm). Auth: TEBRA_BASE_URL + TEBRA_TOKEN."),
    dict(name="modmed", description="ModMed (Modernizing Medicine) EHR REST/FHIR API. ops get/post (writes need confirm). Auth: MODMED_BASE_URL + MODMED_TOKEN (OAuth2 bearer)."),
    dict(name="healthie", description="Healthie EHR/practice management GraphQL API. ops get/post (writes need confirm). Auth: HEALTHIE_BASE_URL (https://api.gethealthie.com/graphql) + HEALTHIE_TOKEN (API key)."),
    dict(name="elation_health", description="Elation Health EHR REST API. ops get/post/put (writes need confirm). Auth: ELATION_HEALTH_BASE_URL (https://sandbox.elationemr.com/api/2.0) + ELATION_HEALTH_TOKEN (OAuth2 bearer)."),
    dict(name="canvas_medical", description="Canvas Medical EHR REST/FHIR API. ops get/post/put (writes need confirm). Auth: CANVAS_MEDICAL_BASE_URL + CANVAS_MEDICAL_TOKEN (OAuth2 bearer)."),
    dict(name="canvaslms", description="Canvas LMS REST API for courses/grades. ops get/post/put/delete (writes need confirm). e.g. /api/v1/courses. Auth: CANVASLMS_BASE_URL (https://{school}.instructure.com) + CANVASLMS_TOKEN (API token)."),
    dict(name="blackboard", description="Blackboard Learn REST API. ops get/post/put/delete (writes need confirm). e.g. /learn/api/public/v1/courses. Auth: BLACKBOARD_BASE_URL + BLACKBOARD_TOKEN (OAuth2 bearer)."),
    dict(name="moodle", description="Moodle LMS REST (webservice) API. ops get/post (writes need confirm). e.g. /webservice/rest/server.php. Auth: MOODLE_BASE_URL (https://{site}/webservice) + MOODLE_TOKEN."),
    dict(name="schoology", description="Schoology LMS REST API. ops get/post/put/delete (writes need confirm). Auth: SCHOOLOGY_BASE_URL (https://api.schoology.com/v1) + SCHOOLOGY_TOKEN (OAuth1/API key)."),
    dict(name="brightspace", description="D2L Brightspace LMS REST API. ops get/post/put/delete (writes need confirm). Auth: BRIGHTSPACE_BASE_URL (https://{org}.brightspace.com) + BRIGHTSPACE_TOKEN (OAuth2 bearer)."),
    dict(name="powerschool", description="PowerSchool SIS REST API. ops get/post/put/delete (writes need confirm). Auth: POWERSCHOOL_BASE_URL (https://{district}.powerschool.com) + POWERSCHOOL_TOKEN (OAuth2 bearer)."),
    dict(name="infinite_campus", description="Infinite Campus SIS REST API. ops get/post/put (writes need confirm). Auth: INFINITE_CAMPUS_BASE_URL + INFINITE_CAMPUS_TOKEN."),
    dict(name="clever", description="Clever education data-sync REST API. ops get/post (writes need confirm). e.g. /v3.0/students. Auth: CLEVER_BASE_URL (https://api.clever.com) + CLEVER_TOKEN (OAuth2 bearer)."),
    dict(name="classlink", description="ClassLink OneRoster REST API. ops get/post (writes need confirm). Auth: CLASSLINK_BASE_URL + CLASSLINK_TOKEN (OAuth2 bearer)."),
    dict(name="google_classroom", description="Google Classroom REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/courses. Auth: GOOGLE_CLASSROOM_BASE_URL (https://classroom.googleapis.com) + GOOGLE_CLASSROOM_TOKEN (OAuth2 bearer)."),
    dict(name="coursera", description="Coursera Business/Partner REST API. ops get/post (writes need confirm). Auth: COURSERA_BASE_URL (https://api.coursera.org) + COURSERA_TOKEN (OAuth2 bearer)."),
    dict(name="udemy", description="Udemy Business REST API. ops get/post (writes need confirm). Auth: UDEMY_BASE_URL (https://{account}.udemy.com/api-2.0) + UDEMY_TOKEN (client id/secret)."),
    dict(name="edx", description="Open edX REST API. ops get/post/put (writes need confirm). Auth: EDX_BASE_URL + EDX_TOKEN (OAuth2 bearer)."),
    dict(name="illuminate_education", description="Illuminate Education (DnA) REST API for assessment data. ops get/post (writes need confirm). Auth: ILLUMINATE_EDUCATION_BASE_URL + ILLUMINATE_EDUCATION_TOKEN."),
    dict(name="parchment", description="Parchment transcript-exchange REST API. ops get/post (writes need confirm). Auth: PARCHMENT_BASE_URL + PARCHMENT_TOKEN (OAuth2 bearer)."),
    dict(name="turnitin", description="Turnitin similarity/originality REST API. ops get/post (writes need confirm). Auth: TURNITIN_BASE_URL (https://api.turnitin.com) + TURNITIN_TOKEN (API key)."),
    dict(name="kahoot", description="Kahoot! REST API for games/reports. ops get/post (writes need confirm). Auth: KAHOOT_BASE_URL + KAHOOT_TOKEN (OAuth2 bearer)."),
    dict(name="remind", description="Remind school messaging REST API. ops get/post (writes need confirm). Auth: REMIND_BASE_URL + REMIND_TOKEN (OAuth2 bearer)."),
    dict(name="seesaw", description="Seesaw learning journal REST API. ops get/post (writes need confirm). Auth: SEESAW_BASE_URL + SEESAW_TOKEN (OAuth2 bearer)."),
    dict(name="classdojo", description="ClassDojo REST API for classroom communication. ops get/post (writes need confirm). Auth: CLASSDOJO_BASE_URL + CLASSDOJO_TOKEN (OAuth2 bearer)."),
    dict(name="edmentum", description="Edmentum online curriculum REST API. ops get/post (writes need confirm). Auth: EDMENTUM_BASE_URL + EDMENTUM_TOKEN."),
    dict(name="expedia", description="Expedia Partner Solutions (Rapid) REST API for bookings. ops get/post (writes need confirm). Auth: EXPEDIA_BASE_URL (https://api.ean.com) + EXPEDIA_TOKEN (API key)."),
    dict(name="bookingcom", description="Booking.com Connectivity/XML-REST API for property inventory. ops get/post/put (writes need confirm). Auth: BOOKINGCOM_BASE_URL (https://distribution-xml.booking.com) + BOOKINGCOM_TOKEN."),
    dict(name="tripadvisor", description="Tripadvisor Content REST API. ops get (writes need confirm). e.g. /api/v1/location/search. Auth: TRIPADVISOR_BASE_URL (https://api.content.tripadvisor.com) + TRIPADVISOR_TOKEN (API key)."),
    dict(name="sabre", description="Sabre travel/airline REST API. ops get/post (writes need confirm). Auth: SABRE_BASE_URL (https://api.sabre.com) + SABRE_TOKEN (OAuth2 bearer)."),
    dict(name="amadeus", description="Amadeus for Developers travel REST API. ops get/post (writes need confirm). e.g. /v2/shopping/flight-offers. Auth: AMADEUS_BASE_URL (https://api.amadeus.com) + AMADEUS_TOKEN (OAuth2 bearer)."),
    dict(name="travelport", description="Travelport Universal API for travel distribution. ops get/post (writes need confirm). Auth: TRAVELPORT_BASE_URL + TRAVELPORT_TOKEN (OAuth2 bearer)."),
    dict(name="opentable", description="OpenTable restaurant reservations REST API. ops get/post (writes need confirm). Auth: OPENTABLE_BASE_URL + OPENTABLE_TOKEN (OAuth2 bearer)."),
    dict(name="resy", description="Resy restaurant reservations REST API. ops get/post (writes need confirm). Auth: RESY_BASE_URL (https://api.resy.com) + RESY_TOKEN (API key)."),
    dict(name="toast_pos", description="Toast restaurant POS REST API. ops get/post/put (writes need confirm). Auth: TOAST_POS_BASE_URL (https://ws-api.toasttab.com) + TOAST_POS_TOKEN (OAuth2 bearer)."),
    dict(name="mews", description="Mews hotel PMS REST API. ops get/post/put (writes need confirm). Auth: MEWS_BASE_URL (https://api.mews.com) + MEWS_TOKEN (API key)."),
    dict(name="cloudbeds", description="Cloudbeds hotel PMS REST API. ops get/post/put (writes need confirm). Auth: CLOUDBEDS_BASE_URL (https://api.cloudbeds.com) + CLOUDBEDS_TOKEN (OAuth2 bearer)."),
    dict(name="opera_pms", description="Oracle Hospitality OPERA Cloud PMS REST API. ops get/post/put (writes need confirm). Auth: OPERA_PMS_BASE_URL + OPERA_PMS_TOKEN (OAuth2 bearer)."),
    dict(name="shiji", description="Shiji hospitality platform REST API. ops get/post/put (writes need confirm). Auth: SHIJI_BASE_URL + SHIJI_TOKEN (OAuth2 bearer)."),
    dict(name="siteminder", description="SiteMinder hotel channel-manager REST API. ops get/post/put (writes need confirm). Auth: SITEMINDER_BASE_URL + SITEMINDER_TOKEN (API key)."),
    dict(name="hotelogix", description="Hotelogix hotel PMS REST API. ops get/post/put (writes need confirm). Auth: HOTELOGIX_BASE_URL + HOTELOGIX_TOKEN (API key)."),
    dict(name="skyscanner", description="Skyscanner travel search REST API. ops get (writes need confirm). Auth: SKYSCANNER_BASE_URL (https://partners.api.skyscanner.net) + SKYSCANNER_TOKEN (API key)."),
    dict(name="kayak", description="KAYAK travel search partner REST API. ops get (writes need confirm). Auth: KAYAK_BASE_URL + KAYAK_TOKEN (API key)."),
    dict(name="hopper", description="Hopper travel booking partner REST API. ops get/post (writes need confirm). Auth: HOPPER_BASE_URL + HOPPER_TOKEN (OAuth2 bearer)."),
    dict(name="duetto", description="Duetto hotel revenue-management REST API. ops get/post (writes need confirm). Auth: DUETTO_BASE_URL (https://api.duettocloud.com) + DUETTO_TOKEN (OAuth2 bearer)."),
    dict(name="guestline", description="Guestline hotel PMS REST API. ops get/post/put (writes need confirm). Auth: GUESTLINE_BASE_URL + GUESTLINE_TOKEN (API key)."),
    dict(name="project44", description="project44 supply-chain visibility REST API. ops get/post (writes need confirm). Auth: PROJECT44_BASE_URL (https://api.project44.com) + PROJECT44_TOKEN (OAuth2 bearer)."),
    dict(name="fourkites", description="FourKites supply-chain visibility REST API. ops get/post (writes need confirm). Auth: FOURKITES_BASE_URL (https://api.fourkites.com) + FOURKITES_TOKEN (API key)."),
    dict(name="convoy", description="Convoy digital freight REST API. ops get/post (writes need confirm). Auth: CONVOY_BASE_URL + CONVOY_TOKEN (OAuth2 bearer)."),
    dict(name="transfix", description="Transfix freight brokerage REST API. ops get/post (writes need confirm). Auth: TRANSFIX_BASE_URL + TRANSFIX_TOKEN (API key)."),
    dict(name="loadsmart", description="Loadsmart freight/logistics REST API. ops get/post (writes need confirm). Auth: LOADSMART_BASE_URL (https://api.loadsmart.com) + LOADSMART_TOKEN (API key)."),
    dict(name="kuebix", description="Kuebix (Trimble) transportation management REST API. ops get/post (writes need confirm). Auth: KUEBIX_BASE_URL + KUEBIX_TOKEN (API key)."),
    dict(name="descartes", description="Descartes Systems logistics/customs REST API. ops get/post (writes need confirm). Auth: DESCARTES_BASE_URL + DESCARTES_TOKEN (OAuth2 bearer)."),
    dict(name="flexe", description="Flexe warehousing/fulfillment REST API. ops get/post (writes need confirm). Auth: FLEXE_BASE_URL (https://api.flexe.com) + FLEXE_TOKEN (OAuth2 bearer)."),
    dict(name="stord", description="Stord supply-chain/warehousing REST API. ops get/post (writes need confirm). Auth: STORD_BASE_URL + STORD_TOKEN (OAuth2 bearer)."),
    dict(name="freightos", description="Freightos freight-rate REST API. ops get/post (writes need confirm). Auth: FREIGHTOS_BASE_URL (https://ratesapi.freightos.com) + FREIGHTOS_TOKEN (API key)."),
    dict(name="cargowise", description="WiseTech CargoWise logistics REST API. ops get/post/put (writes need confirm). Auth: CARGOWISE_BASE_URL + CARGOWISE_TOKEN (OAuth2 bearer)."),
    dict(name="turvo", description="Turvo transportation-management REST API. ops get/post/put (writes need confirm). Auth: TURVO_BASE_URL (https://app.turvo.com/api) + TURVO_TOKEN (OAuth2 bearer)."),
    dict(name="motive_fleet", description="Motive (KeepTruckin) fleet-management REST API. ops get/post/put (writes need confirm). Auth: MOTIVE_FLEET_BASE_URL (https://api.gomotive.com) + MOTIVE_FLEET_TOKEN (API key)."),
    dict(name="omnitracs", description="Omnitracs fleet-management REST API. ops get/post (writes need confirm). Auth: OMNITRACS_BASE_URL + OMNITRACS_TOKEN (OAuth2 bearer)."),
    dict(name="geotab", description="Geotab fleet-telematics REST API (MyGeotab). ops get/post (writes need confirm). Auth: GEOTAB_BASE_URL (https://my.geotab.com/apiv1) + GEOTAB_TOKEN (session token)."),
    dict(name="verizon_connect", description="Verizon Connect fleet-management REST API. ops get/post (writes need confirm). Auth: VERIZON_CONNECT_BASE_URL + VERIZON_CONNECT_TOKEN (OAuth2 bearer)."),
    dict(name="guidewire", description="Guidewire InsuranceSuite REST API. ops get/post/put (writes need confirm). Auth: GUIDEWIRE_BASE_URL + GUIDEWIRE_TOKEN (OAuth2 bearer)."),
    dict(name="duck_creek", description="Duck Creek Technologies insurance-platform REST API. ops get/post/put (writes need confirm). Auth: DUCK_CREEK_BASE_URL + DUCK_CREEK_TOKEN (OAuth2 bearer)."),
    dict(name="applied_epic", description="Applied Epic insurance-agency-management REST API. ops get/post/put (writes need confirm). Auth: APPLIED_EPIC_BASE_URL + APPLIED_EPIC_TOKEN (OAuth2 bearer)."),
    dict(name="ezlynx", description="EZLynx insurance-agency-management REST API. ops get/post/put (writes need confirm). Auth: EZLYNX_BASE_URL + EZLYNX_TOKEN (API key)."),
    dict(name="vertafore", description="Vertafore (AMS360) insurance REST API. ops get/post/put (writes need confirm). Auth: VERTAFORE_BASE_URL + VERTAFORE_TOKEN (OAuth2 bearer)."),
    dict(name="majesco", description="Majesco insurance core-platform REST API. ops get/post/put (writes need confirm). Auth: MAJESCO_BASE_URL + MAJESCO_TOKEN (OAuth2 bearer)."),
    dict(name="insurity", description="Insurity insurance core-platform REST API. ops get/post/put (writes need confirm). Auth: INSURITY_BASE_URL + INSURITY_TOKEN (OAuth2 bearer)."),
    dict(name="socotra", description="Socotra cloud insurance-core-platform REST API. ops get/post/put (writes need confirm). Auth: SOCOTRA_BASE_URL (https://api.socotra.com) + SOCOTRA_TOKEN (OAuth2 bearer)."),
    dict(name="zywave", description="Zywave insurance agency-management REST API. ops get/post (writes need confirm). Auth: ZYWAVE_BASE_URL + ZYWAVE_TOKEN (OAuth2 bearer)."),
    dict(name="origami_risk", description="Origami Risk insurance/risk-management REST API. ops get/post/put (writes need confirm). Auth: ORIGAMI_RISK_BASE_URL + ORIGAMI_RISK_TOKEN (OAuth2 bearer)."),
    dict(name="sapiens", description="Sapiens insurance core-platform REST API. ops get/post/put (writes need confirm). Auth: SAPIENS_BASE_URL + SAPIENS_TOKEN (OAuth2 bearer)."),
    dict(name="cdk_global", description="CDK Global automotive dealer-management REST API. ops get/post/put (writes need confirm). Auth: CDK_GLOBAL_BASE_URL + CDK_GLOBAL_TOKEN (OAuth2 bearer)."),
    dict(name="reynolds_reynolds", description="Reynolds and Reynolds dealer-management REST API. ops get/post/put (writes need confirm). Auth: REYNOLDS_REYNOLDS_BASE_URL + REYNOLDS_REYNOLDS_TOKEN (OAuth2 bearer)."),
    dict(name="dealersocket", description="DealerSocket automotive CRM/DMS REST API. ops get/post/put (writes need confirm). Auth: DEALERSOCKET_BASE_URL + DEALERSOCKET_TOKEN (OAuth2 bearer)."),
    dict(name="vinsolutions", description="VinSolutions automotive CRM REST API. ops get/post/put (writes need confirm). Auth: VINSOLUTIONS_BASE_URL + VINSOLUTIONS_TOKEN (OAuth2 bearer)."),
    dict(name="carfax", description="Carfax vehicle-history REST API. ops get (writes need confirm). Auth: CARFAX_BASE_URL (https://api.carfax.com) + CARFAX_TOKEN (API key)."),
    dict(name="dealertrack", description="Dealertrack (Cox Automotive) F&I REST API. ops get/post (writes need confirm). Auth: DEALERTRACK_BASE_URL + DEALERTRACK_TOKEN (OAuth2 bearer)."),
    dict(name="fleetio", description="Fleetio fleet-maintenance REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/vehicles. Auth: FLEETIO_BASE_URL (https://secure.fleetio.com/api) + FLEETIO_TOKEN (API key)."),
    dict(name="johndeere_ops", description="John Deere Operations Center REST API for farm equipment. ops get/post (writes need confirm). Auth: JOHNDEERE_OPS_BASE_URL (https://sandboxapi.deere.com) + JOHNDEERE_OPS_TOKEN (OAuth2 bearer)."),
    dict(name="climate_fieldview", description="Climate FieldView agriculture REST API. ops get/post (writes need confirm). Auth: CLIMATE_FIELDVIEW_BASE_URL (https://platform.climate.com) + CLIMATE_FIELDVIEW_TOKEN (OAuth2 bearer)."),
    dict(name="granular", description="Granular (Corteva) farm-management REST API. ops get/post (writes need confirm). Auth: GRANULAR_BASE_URL + GRANULAR_TOKEN (OAuth2 bearer)."),
    dict(name="agworld", description="Agworld farm-management REST API. ops get/post/put (writes need confirm). Auth: AGWORLD_BASE_URL (https://api.agworld.com) + AGWORLD_TOKEN (OAuth2 bearer)."),
    dict(name="cropin", description="Cropin agriculture-intelligence REST API. ops get/post (writes need confirm). Auth: CROPIN_BASE_URL + CROPIN_TOKEN (OAuth2 bearer)."),
    dict(name="opower", description="Oracle Opower utility customer-engagement REST API. ops get/post (writes need confirm). Auth: OPOWER_BASE_URL + OPOWER_TOKEN (OAuth2 bearer)."),
    dict(name="itron", description="Itron utility metering/analytics REST API. ops get/post (writes need confirm). Auth: ITRON_BASE_URL + ITRON_TOKEN (OAuth2 bearer)."),
    dict(name="aveva", description="AVEVA (Wonderware) industrial/energy REST API. ops get/post (writes need confirm). Auth: AVEVA_BASE_URL + AVEVA_TOKEN (OAuth2 bearer)."),
    dict(name="osisoft_pi", description="OSIsoft PI System (AVEVA) REST API for industrial time-series data. ops get/post (writes need confirm). e.g. /piwebapi/streams. Auth: OSISOFT_PI_BASE_URL (https://{server}/piwebapi) + OSISOFT_PI_TOKEN."),
    dict(name="landis_gyr", description="Landis+Gyr utility smart-metering REST API. ops get/post (writes need confirm). Auth: LANDIS_GYR_BASE_URL + LANDIS_GYR_TOKEN (OAuth2 bearer)."),
    dict(name="enphase", description="Enphase Energy solar-monitoring REST API. ops get (writes need confirm). e.g. /api/v4/systems. Auth: ENPHASE_BASE_URL (https://api.enphaseenergy.com) + ENPHASE_TOKEN (OAuth2 bearer)."),
    dict(name="solaredge", description="SolarEdge solar-monitoring REST API. ops get (writes need confirm). e.g. /site/{id}/overview. Auth: SOLAREDGE_BASE_URL (https://monitoringapi.solaredge.com) + SOLAREDGE_TOKEN (API key)."),
    dict(name="n26", description="N26 neobank REST. ops get/post/put/delete (writes need confirm). e.g. /api/accounts, /api/transactions. Auth: N26_BASE_URL (https://api.tech26.de) + N26_TOKEN."),
    dict(name="monzo", description="Monzo open banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: MONZO_BASE_URL (https://api.monzo.com) + MONZO_TOKEN."),
    dict(name="varo", description="Varo Bank REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: VARO_BASE_URL + VARO_TOKEN."),
    dict(name="bunq", description="bunq banking REST. ops get/post/put/delete (writes need confirm). e.g. /v1/user, /v1/monetary-account. Auth: BUNQ_BASE_URL (https://api.bunq.com) + BUNQ_TOKEN."),
    dict(name="tide_bank", description="Tide business banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /payments. Auth: TIDE_BANK_BASE_URL + TIDE_BANK_TOKEN."),
    dict(name="monese", description="Monese banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /payments. Auth: MONESE_BASE_URL + MONESE_TOKEN."),
    dict(name="aspiration", description="Aspiration banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: ASPIRATION_BASE_URL + ASPIRATION_TOKEN."),
    dict(name="greenlight_card", description="Greenlight family debit card REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /cards. Auth: GREENLIGHT_CARD_BASE_URL + GREENLIGHT_CARD_TOKEN."),
    dict(name="step_banking", description="Step teen banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /cards. Auth: STEP_BANKING_BASE_URL + STEP_BANKING_TOKEN."),
    dict(name="one_finance", description="One (Walmart) fintech app REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: ONE_FINANCE_BASE_URL + ONE_FINANCE_TOKEN."),
    dict(name="marcus_gs", description="Marcus by Goldman Sachs banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: MARCUS_GS_BASE_URL + MARCUS_GS_TOKEN."),
    dict(name="sofi", description="SoFi money/invest REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: SOFI_BASE_URL + SOFI_TOKEN."),
    dict(name="ally_bank", description="Ally Bank REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: ALLY_BANK_BASE_URL + ALLY_BANK_TOKEN."),
    dict(name="discover_bank", description="Discover Bank REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: DISCOVER_BANK_BASE_URL + DISCOVER_BANK_TOKEN."),
    dict(name="current_banking", description="Current neobank REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: CURRENT_BANKING_BASE_URL + CURRENT_BANKING_TOKEN."),
    dict(name="dave_banking", description="Dave neobank REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /advances. Auth: DAVE_BANKING_BASE_URL + DAVE_BANKING_TOKEN."),
    dict(name="coinbase", description="Coinbase Advanced Trade REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/brokerage/accounts, /orders. Auth: COINBASE_BASE_URL (https://api.coinbase.com) + COINBASE_TOKEN (API key + secret)."),
    dict(name="kraken", description="Kraken exchange REST. ops get/post/put/delete (writes need confirm). e.g. /0/private/Balance, /0/private/AddOrder. Auth: KRAKEN_BASE_URL (https://api.kraken.com) + KRAKEN_TOKEN (API key + private key)."),
    dict(name="binance", description="Binance exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/account, /api/v3/order. Auth: BINANCE_BASE_URL (https://api.binance.com) + BINANCE_TOKEN (API key)."),
    dict(name="binanceus", description="Binance.US exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/account, /api/v3/order. Auth: BINANCEUS_BASE_URL (https://api.binance.us) + BINANCEUS_TOKEN."),
    dict(name="bitstamp", description="Bitstamp exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/balance/, /api/v2/order/. Auth: BITSTAMP_BASE_URL (https://www.bitstamp.net) + BITSTAMP_TOKEN."),
    dict(name="gemini_exchange", description="Gemini crypto exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v1/balances, /v1/order/new. Auth: GEMINI_EXCHANGE_BASE_URL (https://api.gemini.com) + GEMINI_EXCHANGE_TOKEN (API key + secret)."),
    dict(name="kucoin", description="KuCoin exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/accounts, /api/v1/orders. Auth: KUCOIN_BASE_URL (https://api.kucoin.com) + KUCOIN_TOKEN."),
    dict(name="okx", description="OKX exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v5/account/balance, /api/v5/trade/order. Auth: OKX_BASE_URL (https://www.okx.com) + OKX_TOKEN."),
    dict(name="bybit", description="Bybit exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v5/account/wallet-balance, /v5/order/create. Auth: BYBIT_BASE_URL (https://api.bybit.com) + BYBIT_TOKEN."),
    dict(name="crypto_com", description="Crypto.com Exchange REST. ops get/post/put/delete (writes need confirm). e.g. /private/get-account-summary, /private/create-order. Auth: CRYPTO_COM_BASE_URL (https://api.crypto.com) + CRYPTO_COM_TOKEN."),
    dict(name="gate_io", description="Gate.io exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v4/spot/accounts, /api/v4/spot/orders. Auth: GATE_IO_BASE_URL (https://api.gateio.ws) + GATE_IO_TOKEN."),
    dict(name="huobi", description="Huobi (HTX) exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v1/account/accounts, /v1/order/orders. Auth: HUOBI_BASE_URL (https://api.huobi.pro) + HUOBI_TOKEN."),
    dict(name="bitfinex", description="Bitfinex exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v2/auth/r/wallets, /v2/auth/w/order/submit. Auth: BITFINEX_BASE_URL (https://api.bitfinex.com) + BITFINEX_TOKEN."),
    dict(name="bitflyer", description="bitFlyer exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v1/me/getbalance, /v1/me/sendchildorder. Auth: BITFLYER_BASE_URL (https://api.bitflyer.com) + BITFLYER_TOKEN."),
    dict(name="luno", description="Luno exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/1/balance, /api/1/postorder. Auth: LUNO_BASE_URL (https://api.luno.com) + LUNO_TOKEN."),
    dict(name="bitpanda", description="Bitpanda exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v1/wallets, /v1/trades. Auth: BITPANDA_BASE_URL (https://api.bitpanda.com) + BITPANDA_TOKEN."),
    dict(name="robinhood", description="Robinhood trading REST. ops get/post/put/delete (writes need confirm). e.g. /accounts/, /orders/. Auth: ROBINHOOD_BASE_URL (https://api.robinhood.com) + ROBINHOOD_TOKEN."),
    dict(name="metamask", description="MetaMask/Infura wallet RPC REST bridge. ops get/post/put/delete (writes need confirm). e.g. /v3/{project_id}, eth_sendTransaction. Auth: METAMASK_BASE_URL + METAMASK_TOKEN."),
    dict(name="trust_wallet", description="Trust Wallet API. ops get/post/put/delete (writes need confirm). e.g. /v1/wallets, /v1/transactions. Auth: TRUST_WALLET_BASE_URL + TRUST_WALLET_TOKEN."),
    dict(name="ledger_live", description="Ledger Live wallet REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /operations. Auth: LEDGER_LIVE_BASE_URL + LEDGER_LIVE_TOKEN."),
    dict(name="blockchain_com", description="Blockchain.com wallet/exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v3/exchange/accounts, /v3/exchange/orders. Auth: BLOCKCHAIN_COM_BASE_URL (https://api.blockchain.com) + BLOCKCHAIN_COM_TOKEN."),
    dict(name="bitso", description="Bitso exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v3/balance/, /v3/orders/. Auth: BITSO_BASE_URL (https://api.bitso.com) + BITSO_TOKEN."),
    dict(name="wazirx", description="WazirX exchange REST. ops get/post/put/delete (writes need confirm). e.g. /sapi/v1/account, /sapi/v1/order. Auth: WAZIRX_BASE_URL (https://api.wazirx.com) + WAZIRX_TOKEN."),
    dict(name="coindcx", description="CoinDCX exchange REST. ops get/post/put/delete (writes need confirm). e.g. /exchange/v1/users/balances, /exchange/v1/orders/create. Auth: COINDCX_BASE_URL (https://api.coindcx.com) + COINDCX_TOKEN."),
    dict(name="upbit", description="Upbit exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts, /v1/orders. Auth: UPBIT_BASE_URL (https://api.upbit.com) + UPBIT_TOKEN."),
    dict(name="bithumb", description="Bithumb exchange REST. ops get/post/put/delete (writes need confirm). e.g. /info/balance, /trade/place. Auth: BITHUMB_BASE_URL (https://api.bithumb.com) + BITHUMB_TOKEN."),
    dict(name="poloniex", description="Poloniex exchange REST. ops get/post/put/delete (writes need confirm). e.g. /accounts/balances, /orders. Auth: POLONIEX_BASE_URL (https://api.poloniex.com) + POLONIEX_TOKEN."),
    dict(name="deribit", description="Deribit derivatives exchange REST. ops get/post/put/delete (writes need confirm). e.g. /private/get_account_summary, /private/buy. Auth: DERIBIT_BASE_URL (https://www.deribit.com) + DERIBIT_TOKEN."),
    dict(name="phemex", description="Phemex exchange REST. ops get/post/put/delete (writes need confirm). e.g. /accounts/accountPositions, /orders. Auth: PHEMEX_BASE_URL (https://api.phemex.com) + PHEMEX_TOKEN."),
    dict(name="mexc", description="MEXC exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v3/account, /api/v3/order. Auth: MEXC_BASE_URL (https://api.mexc.com) + MEXC_TOKEN."),
    dict(name="coinone", description="Coinone exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v2.1/account/balance, /v2.1/order. Auth: COINONE_BASE_URL (https://api.coinone.co.kr) + COINONE_TOKEN."),
    dict(name="independent_reserve", description="Independent Reserve exchange REST. ops get/post/put/delete (writes need confirm). e.g. /Private/GetAccounts, /Private/PlaceLimitOrder. Auth: INDEPENDENT_RESERVE_BASE_URL (https://api.independentreserve.com) + INDEPENDENT_RESERVE_TOKEN."),
    dict(name="coinspot", description="CoinSpot exchange REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/ro/my/balances, /api/v2/my/buy. Auth: COINSPOT_BASE_URL (https://www.coinspot.com.au) + COINSPOT_TOKEN."),
    dict(name="swyftx", description="Swyftx exchange REST. ops get/post/put/delete (writes need confirm). e.g. /user/balance/, /orders/. Auth: SWYFTX_BASE_URL (https://api.swyftx.com.au) + SWYFTX_TOKEN."),
    dict(name="bitvavo", description="Bitvavo exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v2/balance, /v2/order. Auth: BITVAVO_BASE_URL (https://api.bitvavo.com) + BITVAVO_TOKEN."),
    dict(name="coinjar", description="CoinJar exchange REST. ops get/post/put/delete (writes need confirm). e.g. /v2/accounts, /v2/orders. Auth: COINJAR_BASE_URL (https://api.coinjar.com) + COINJAR_TOKEN."),
    dict(name="circle_usdc", description="Circle (USDC) REST. ops get/post/put/delete (writes need confirm). e.g. /v1/balances, /v1/payments. Auth: CIRCLE_USDC_BASE_URL (https://api.circle.com) + CIRCLE_USDC_TOKEN."),
    dict(name="paxos", description="Paxos REST. ops get/post/put/delete (writes need confirm). e.g. /v2/accounts, /v2/trades. Auth: PAXOS_BASE_URL (https://api.paxos.com) + PAXOS_TOKEN."),
    dict(name="moonpay", description="MoonPay on/off-ramp REST. ops get/post/put/delete (writes need confirm). e.g. /v3/transactions, /v3/currencies. Auth: MOONPAY_BASE_URL (https://api.moonpay.com) + MOONPAY_TOKEN."),
    dict(name="transak", description="Transak on/off-ramp REST. ops get/post/put/delete (writes need confirm). e.g. /partners/orders, /partners/webhook. Auth: TRANSAK_BASE_URL (https://api.transak.com) + TRANSAK_TOKEN."),
    dict(name="banxa", description="Banxa on/off-ramp REST. ops get/post/put/delete (writes need confirm). e.g. /api/orders, /api/prices. Auth: BANXA_BASE_URL + BANXA_TOKEN."),
    dict(name="simplex_crypto", description="Simplex crypto payments REST. ops get/post/put/delete (writes need confirm). e.g. /wallet/merchant/v2/quote, /wallet/merchant/v2/payments/partner/data. Auth: SIMPLEX_CRYPTO_BASE_URL + SIMPLEX_CRYPTO_TOKEN."),
    dict(name="fireblocks", description="Fireblocks custody REST. ops get/post/put/delete (writes need confirm). e.g. /v1/vault/accounts, /v1/transactions. Auth: FIREBLOCKS_BASE_URL (https://api.fireblocks.io) + FIREBLOCKS_TOKEN (API key + private key signed JWT)."),
    dict(name="bitgo", description="BitGo custody REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/{coin}/wallet, /api/v2/{coin}/wallet/{id}/tx. Auth: BITGO_BASE_URL (https://app.bitgo.com) + BITGO_TOKEN."),
    dict(name="anchorage_digital", description="Anchorage Digital custody REST. ops get/post/put/delete (writes need confirm). e.g. /v2/vaults, /v2/transfers. Auth: ANCHORAGE_DIGITAL_BASE_URL + ANCHORAGE_DIGITAL_TOKEN."),
    dict(name="coinmarketcap", description="CoinMarketCap market-data REST. ops get (read-only). e.g. /v1/cryptocurrency/quotes/latest, /v1/cryptocurrency/listings/latest. Auth: COINMARKETCAP_BASE_URL (https://pro-api.coinmarketcap.com) + COINMARKETCAP_TOKEN.", token_header="X-CMC_PRO_API_KEY", scheme=""),
    dict(name="coingecko", description="CoinGecko market-data REST. ops get (read-only). e.g. /api/v3/coins/markets, /api/v3/simple/price. Auth: COINGECKO_BASE_URL (https://api.coingecko.com) + COINGECKO_TOKEN.", token_header="x-cg-pro-api-key", scheme=""),
    dict(name="messari", description="Messari crypto research REST. ops get (read-only). e.g. /api/v1/assets/{id}/metrics, /api/v2/assets. Auth: MESSARI_BASE_URL (https://data.messari.io) + MESSARI_TOKEN.", token_header="x-messari-api-key", scheme=""),
    dict(name="etherscan", description="Etherscan blockchain explorer REST. ops get (read-only). e.g. /api?module=account&action=balance, /api?module=account&action=txlist. Auth: ETHERSCAN_BASE_URL (https://api.etherscan.io) + ETHERSCAN_TOKEN (apikey query param)."),
    dict(name="blockchair", description="Blockchair blockchain explorer REST. ops get (read-only). e.g. /bitcoin/dashboards/address/, /ethereum/raw/transaction/. Auth: BLOCKCHAIR_BASE_URL (https://api.blockchair.com) + BLOCKCHAIR_TOKEN (key query param)."),
    dict(name="bitpay", description="BitPay crypto payments REST. ops get/post/put/delete (writes need confirm). e.g. /invoices, /bills. Auth: BITPAY_BASE_URL (https://bitpay.com/api) + BITPAY_TOKEN."),
    dict(name="btcpay_server", description="BTCPay Server self-hosted crypto payments REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/stores/{id}/invoices, /api/v1/stores. Auth: BTCPAY_SERVER_BASE_URL + BTCPAY_SERVER_TOKEN."),
    dict(name="coinpayments", description="CoinPayments REST. ops get/post/put/delete (writes need confirm). e.g. /api.php?cmd=get_basic_info, /api.php?cmd=create_transaction. Auth: COINPAYMENTS_BASE_URL (https://www.coinpayments.net) + COINPAYMENTS_TOKEN."),
    dict(name="nowpayments", description="NOWPayments crypto payments REST. ops get/post/put/delete (writes need confirm). e.g. /v1/payment, /v1/status. Auth: NOWPAYMENTS_BASE_URL (https://api.nowpayments.io) + NOWPAYMENTS_TOKEN.", token_header="x-api-key", scheme=""),
    dict(name="opennode", description="OpenNode Bitcoin/Lightning payments REST. ops get/post/put/delete (writes need confirm). e.g. /v1/charges, /v2/withdrawals. Auth: OPENNODE_BASE_URL (https://api.opennode.com) + OPENNODE_TOKEN.", token_header="Authorization", scheme=""),
    dict(name="etrade", description="E*TRADE brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts/list, /v1/accounts/{id}/orders. Auth: ETRADE_BASE_URL (https://api.etrade.com) + ETRADE_TOKEN (OAuth1 access token)."),
    dict(name="td_ameritrade", description="TD Ameritrade brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts, /v1/accounts/{id}/orders. Auth: TD_AMERITRADE_BASE_URL (https://api.tdameritrade.com) + TD_AMERITRADE_TOKEN."),
    dict(name="schwab", description="Charles Schwab trading REST. ops get/post/put/delete (writes need confirm). e.g. /trader/v1/accounts, /trader/v1/accounts/{id}/orders. Auth: SCHWAB_BASE_URL (https://api.schwabapi.com) + SCHWAB_TOKEN."),
    dict(name="interactive_brokers", description="Interactive Brokers Web API. ops get/post/put/delete (writes need confirm). e.g. /v1/api/portfolio/accounts, /v1/api/iserver/account/orders. Auth: INTERACTIVE_BROKERS_BASE_URL (https://api.ibkr.com) + INTERACTIVE_BROKERS_TOKEN."),
    dict(name="fidelity", description="Fidelity investments REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: FIDELITY_BASE_URL + FIDELITY_TOKEN."),
    dict(name="tradestation", description="TradeStation brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /v3/brokerage/accounts, /v3/orderexecution/orders. Auth: TRADESTATION_BASE_URL (https://api.tradestation.com) + TRADESTATION_TOKEN."),
    dict(name="tastytrade", description="Tastytrade brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /accounts/{id}/orders. Auth: TASTYTRADE_BASE_URL (https://api.tastyworks.com) + TASTYTRADE_TOKEN."),
    dict(name="webull", description="Webull brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /account/list, /trade/order/place. Auth: WEBULL_BASE_URL + WEBULL_TOKEN."),
    dict(name="m1_finance", description="M1 Finance brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: M1_FINANCE_BASE_URL + M1_FINANCE_TOKEN."),
    dict(name="betterment", description="Betterment robo-advisor REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: BETTERMENT_BASE_URL + BETTERMENT_TOKEN."),
    dict(name="wealthfront", description="Wealthfront robo-advisor REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transfers. Auth: WEALTHFRONT_BASE_URL + WEALTHFRONT_TOKEN."),
    dict(name="acorns", description="Acorns micro-investing REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: ACORNS_BASE_URL + ACORNS_TOKEN."),
    dict(name="stash_invest", description="Stash investing REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: STASH_INVEST_BASE_URL + STASH_INVEST_TOKEN."),
    dict(name="public_com", description="Public.com brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: PUBLIC_COM_BASE_URL + PUBLIC_COM_TOKEN."),
    dict(name="alpaca_markets", description="Alpaca brokerage/trading REST. ops get/post/put/delete (writes need confirm). e.g. /v2/account, /v2/orders. Auth: ALPACA_MARKETS_BASE_URL (https://api.alpaca.markets) + ALPACA_MARKETS_TOKEN (APCA-API-KEY-ID + secret).", token_header="APCA-API-KEY-ID", scheme=""),
    dict(name="tradier", description="Tradier brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts/{id}/balances, /v1/accounts/{id}/orders. Auth: TRADIER_BASE_URL (https://api.tradier.com) + TRADIER_TOKEN."),
    dict(name="questrade", description="Questrade brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /v1/accounts, /v1/accounts/{id}/orders. Auth: QUESTRADE_BASE_URL + QUESTRADE_TOKEN."),
    dict(name="wealthsimple", description="Wealthsimple Trade REST. ops get/post/put/delete (writes need confirm). e.g. /account, /orders. Auth: WEALTHSIMPLE_BASE_URL + WEALTHSIMPLE_TOKEN."),
    dict(name="degiro", description="DEGIRO brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /trading/secure/v5/accountoverview, /trading/secure/v5/checkOrder. Auth: DEGIRO_BASE_URL + DEGIRO_TOKEN."),
    dict(name="trading212", description="Trading 212 REST. ops get/post/put/delete (writes need confirm). e.g. /api/v0/equity/account/cash, /api/v0/equity/orders. Auth: TRADING212_BASE_URL (https://live.trading212.com) + TRADING212_TOKEN."),
    dict(name="ig_group", description="IG trading platform REST. ops get/post/put/delete (writes need confirm). e.g. /gateway/deal/accounts, /gateway/deal/positions. Auth: IG_GROUP_BASE_URL (https://api.ig.com) + IG_GROUP_TOKEN."),
    dict(name="oanda", description="OANDA forex trading REST. ops get/post/put/delete (writes need confirm). e.g. /v3/accounts/{id}/summary, /v3/accounts/{id}/orders. Auth: OANDA_BASE_URL (https://api-fxtrade.oanda.com) + OANDA_TOKEN."),
    dict(name="plus500", description="Plus500 trading REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /positions. Auth: PLUS500_BASE_URL + PLUS500_TOKEN."),
    dict(name="saxo_bank", description="Saxo Bank OpenAPI. ops get/post/put/delete (writes need confirm). e.g. /port/v1/accounts/me, /trade/v2/orders. Auth: SAXO_BANK_BASE_URL (https://gateway.saxobank.com) + SAXO_BANK_TOKEN."),
    dict(name="ninjatrader", description="NinjaTrader brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: NINJATRADER_BASE_URL + NINJATRADER_TOKEN."),
    dict(name="drivewealth", description="DriveWealth brokerage-as-a-service REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: DRIVEWEALTH_BASE_URL (https://api.drivewealth.com) + DRIVEWEALTH_TOKEN."),
    dict(name="apex_clearing", description="Apex Clearing brokerage-as-a-service REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /orders. Auth: APEX_CLEARING_BASE_URL + APEX_CLEARING_TOKEN."),
    dict(name="ally_invest", description="Ally Invest brokerage REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /accounts/{id}/orders. Auth: ALLY_INVEST_BASE_URL (https://api.tradeking.com) + ALLY_INVEST_TOKEN."),
    dict(name="morningstar", description="Morningstar investment data REST. ops get (read-only). e.g. /v1/funds, /v1/securities. Auth: MORNINGSTAR_BASE_URL (https://api.morningstar.com) + MORNINGSTAR_TOKEN."),
    dict(name="tradingview", description="TradingView REST/broker-integration API. ops get/post/put/delete (writes need confirm). e.g. /orders, /positions. Auth: TRADINGVIEW_BASE_URL + TRADINGVIEW_TOKEN."),
    dict(name="iex_cloud", description="IEX Cloud market-data REST. ops get (read-only). e.g. /stable/stock/{symbol}/quote, /stable/stock/market/batch. Auth: IEX_CLOUD_BASE_URL (https://cloud.iexapis.com) + IEX_CLOUD_TOKEN (token query param)."),
    dict(name="tiingo", description="Tiingo market-data REST. ops get (read-only). e.g. /tiingo/daily/{ticker}/prices, /tiingo/crypto/prices. Auth: TIINGO_BASE_URL (https://api.tiingo.com) + TIINGO_TOKEN."),
    dict(name="marketstack", description="Marketstack market-data REST. ops get (read-only). e.g. /v1/eod, /v1/intraday. Auth: MARKETSTACK_BASE_URL (https://api.marketstack.com) + MARKETSTACK_TOKEN (access_key query param)."),
    dict(name="twelvedata", description="Twelve Data market-data REST. ops get (read-only). e.g. /time_series, /quote. Auth: TWELVEDATA_BASE_URL (https://api.twelvedata.com) + TWELVEDATA_TOKEN (apikey query param)."),
    dict(name="quandl", description="Nasdaq Data Link (Quandl) REST. ops get (read-only). e.g. /api/v3/datasets/{db}/{code}/data.json. Auth: QUANDL_BASE_URL (https://data.nasdaq.com) + QUANDL_TOKEN (api_key query param)."),
    dict(name="ynab", description="You Need A Budget (YNAB) REST. ops get/post/put/delete (writes need confirm). e.g. /v1/budgets, /v1/budgets/{id}/transactions. Auth: YNAB_BASE_URL (https://api.ynab.com) + YNAB_TOKEN."),
    dict(name="copilot_money", description="Copilot Money personal finance REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: COPILOT_MONEY_BASE_URL + COPILOT_MONEY_TOKEN."),
    dict(name="monarch_money", description="Monarch Money personal finance REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: MONARCH_MONEY_BASE_URL + MONARCH_MONEY_TOKEN."),
    dict(name="tiller_money", description="Tiller Money budgeting REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: TILLER_MONEY_BASE_URL + TILLER_MONEY_TOKEN."),
    dict(name="pocketguard", description="PocketGuard budgeting REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: POCKETGUARD_BASE_URL + POCKETGUARD_TOKEN."),
    dict(name="quicken", description="Quicken personal finance REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: QUICKEN_BASE_URL + QUICKEN_TOKEN."),
    dict(name="empower_personal", description="Empower (Personal Capital) wealth REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /holdings. Auth: EMPOWER_PERSONAL_BASE_URL + EMPOWER_PERSONAL_TOKEN."),
    dict(name="simplifi", description="Simplifi by Quicken budgeting REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: SIMPLIFI_BASE_URL + SIMPLIFI_TOKEN."),
    dict(name="tink", description="Tink open-banking REST. ops get/post/put/delete (writes need confirm). e.g. /data/v2/accounts, /data/v2/transactions. Auth: TINK_BASE_URL (https://api.tink.com) + TINK_TOKEN."),
    dict(name="yapily", description="Yapily open-banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /payments. Auth: YAPILY_BASE_URL (https://api.yapily.com) + YAPILY_TOKEN."),
    dict(name="salt_edge", description="Salt Edge open-banking REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: SALT_EDGE_BASE_URL (https://www.saltedge.com/api) + SALT_EDGE_TOKEN."),
    dict(name="akoya", description="Akoya open-banking data REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: AKOYA_BASE_URL (https://api.akoya.com) + AKOYA_TOKEN."),
    dict(name="flinks", description="Flinks open-banking REST. ops get/post/put/delete (writes need confirm). e.g. /v3/{customerId}/BankingServices/GetAccountsDetail, /v3/{customerId}/BankingServices/GetAccounts Summary. Auth: FLINKS_BASE_URL + FLINKS_TOKEN."),
    dict(name="basiq", description="Basiq open-banking REST. ops get/post/put/delete (writes need confirm). e.g. /users/{id}/accounts, /users/{id}/transactions. Auth: BASIQ_BASE_URL (https://au-api.basiq.io) + BASIQ_TOKEN."),
    dict(name="remitly", description="Remitly remittance REST. ops get/post/put/delete (writes need confirm). e.g. /v2/quotes, /v2/transfers. Auth: REMITLY_BASE_URL + REMITLY_TOKEN."),
    dict(name="worldremit", description="WorldRemit remittance REST. ops get/post/put/delete (writes need confirm). e.g. /quotes, /transactions. Auth: WORLDREMIT_BASE_URL + WORLDREMIT_TOKEN."),
    dict(name="azimo", description="Azimo remittance REST. ops get/post/put/delete (writes need confirm). e.g. /quotes, /transfers. Auth: AZIMO_BASE_URL + AZIMO_TOKEN."),
    dict(name="currencyfair", description="CurrencyFair remittance REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /trades. Auth: CURRENCYFAIR_BASE_URL + CURRENCYFAIR_TOKEN."),
    dict(name="moneygram", description="MoneyGram remittance REST. ops get/post/put/delete (writes need confirm). e.g. /transactions, /quotes. Auth: MONEYGRAM_BASE_URL + MONEYGRAM_TOKEN."),
    dict(name="wise_platform", description="Wise (TransferWise) Platform REST. ops get/post/put/delete (writes need confirm). e.g. /v1/profiles, /v3/profiles/{id}/transfers. Auth: WISE_PLATFORM_BASE_URL (https://api.wise.com) + WISE_PLATFORM_TOKEN."),
    dict(name="upgrade_inc", description="Upgrade Inc. personal loans REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /loans. Auth: UPGRADE_INC_BASE_URL + UPGRADE_INC_TOKEN."),
    dict(name="avant_credit", description="Avant consumer credit REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /loans. Auth: AVANT_CREDIT_BASE_URL + AVANT_CREDIT_TOKEN."),
    dict(name="prosper", description="Prosper peer-to-peer lending REST. ops get/post/put/delete (writes need confirm). e.g. /listings, /notes. Auth: PROSPER_BASE_URL (https://api.prosper.com) + PROSPER_TOKEN."),
    dict(name="lendingclub", description="LendingClub REST. ops get/post/put/delete (writes need confirm). e.g. /api/investor/v1/accounts, /api/investor/v1/loans/listing. Auth: LENDINGCLUB_BASE_URL (https://api.lendingclub.com) + LENDINGCLUB_TOKEN."),
    dict(name="upstart", description="Upstart lending REST. ops get/post/put/delete (writes need confirm). e.g. /loans, /applications. Auth: UPSTART_BASE_URL + UPSTART_TOKEN."),
    dict(name="kabbage", description="Kabbage (Amex) business lending REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /loans. Auth: KABBAGE_BASE_URL + KABBAGE_TOKEN."),
    dict(name="bluevine", description="BlueVine business banking/lending REST. ops get/post/put/delete (writes need confirm). e.g. /accounts, /transactions. Auth: BLUEVINE_BASE_URL + BLUEVINE_TOKEN."),
    dict(name="ondeck", description="OnDeck small-business lending REST. ops get/post/put/delete (writes need confirm). e.g. /loans, /applications. Auth: ONDECK_BASE_URL + ONDECK_TOKEN."),
    dict(name="funding_circle", description="Funding Circle lending REST. ops get/post/put/delete (writes need confirm). e.g. /loans, /investments. Auth: FUNDING_CIRCLE_BASE_URL + FUNDING_CIRCLE_TOKEN."),
    dict(name="kiva", description="Kiva microloans REST. ops get/post/put/delete (writes need confirm). e.g. /v3/loans, /v3/lenders. Auth: KIVA_BASE_URL (https://api.kivaws.org) + KIVA_TOKEN."),
    dict(name="surveymonkey", description="SurveyMonkey REST. ops get/post/put/delete (writes need confirm). e.g. /v3/surveys, /v3/surveys/{id}/responses. Auth: SURVEYMONKEY_BASE_URL (https://api.surveymonkey.com) + SURVEYMONKEY_TOKEN."),
    dict(name="qualtrics", token_header="X-API-TOKEN", scheme="", description="Qualtrics XM Platform REST. ops get/post/put/delete (writes need confirm). e.g. /API/v3/surveys, /API/v3/surveys/{id}/responses. Auth: QUALTRICS_BASE_URL (https://{dc}.qualtrics.com) + QUALTRICS_TOKEN."),
    dict(name="google_forms", description="Google Forms REST. ops get/post/put/delete (writes need confirm). e.g. /v1/forms/{formId}, /v1/forms/{formId}/responses. Auth: GOOGLE_FORMS_BASE_URL (https://forms.googleapis.com) + GOOGLE_FORMS_TOKEN."),
    dict(name="microsoft_forms", description="Microsoft Forms REST (Graph). ops get/post/put/delete (writes need confirm). Auth: MICROSOFT_FORMS_BASE_URL (https://graph.microsoft.com) + MICROSOFT_FORMS_TOKEN."),
    dict(name="formsite", description="Formsite REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/{server}/forms, /api/v2/{server}/forms/{id}/results. Auth: FORMSITE_BASE_URL (https://fs{id}.formsite.com) + FORMSITE_TOKEN."),
    dict(name="cognito_forms", description="Cognito Forms REST. ops get/post/put/delete (writes need confirm). Auth: COGNITO_FORMS_BASE_URL (https://www.cognitoforms.com/api) + COGNITO_FORMS_TOKEN."),
    dict(name="formassembly", description="FormAssembly REST. ops get/post/put/delete (writes need confirm). Auth: FORMASSEMBLY_BASE_URL (https://www.tfaforms.com) + FORMASSEMBLY_TOKEN."),
    dict(name="paperform", description="Paperform REST. ops get/post/put/delete (writes need confirm). Auth: PAPERFORM_BASE_URL (https://api.paperform.co) + PAPERFORM_TOKEN."),
    dict(name="gravity_forms", basic=True, description="Gravity Forms (WordPress) REST. ops get/post/put/delete (writes need confirm). e.g. /wp-json/gf/v2/forms. Auth: GRAVITY_FORMS_BASE_URL (site + /wp-json/gf/v2) + GRAVITY_FORMS_TOKEN (consumer key/secret)."),
    dict(name="ninja_forms", basic=True, description="Ninja Forms (WordPress) REST. ops get/post/put/delete (writes need confirm). Auth: NINJA_FORMS_BASE_URL (site + /wp-json) + NINJA_FORMS_TOKEN."),
    dict(name="surveysparrow", description="SurveySparrow REST. ops get/post/put/delete (writes need confirm). Auth: SURVEYSPARROW_BASE_URL (https://api.surveysparrow.com) + SURVEYSPARROW_TOKEN."),
    dict(name="alchemer", description="Alchemer (formerly SurveyGizmo) REST. ops get/post/put/delete (writes need confirm). e.g. /v5/survey. Auth: ALCHEMER_BASE_URL (https://api.alchemer.com) + ALCHEMER_TOKEN."),
    dict(name="zoho_survey", scheme="Zoho-oauthtoken", description="Zoho Survey REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_SURVEY_BASE_URL (https://survey.zoho.com/api) + ZOHO_SURVEY_TOKEN."),
    dict(name="delighted", basic=True, description="Delighted (NPS/CX) REST. ops get/post/put/delete (writes need confirm). e.g. /v1/people.json, /v1/survey_responses.json. Auth: DELIGHTED_BASE_URL (https://api.delighted.com) + DELIGHTED_TOKEN (API key)."),
    dict(name="pollfish", description="Pollfish REST. ops get/post/put/delete (writes need confirm). Auth: POLLFISH_BASE_URL (https://api.pollfish.com) + POLLFISH_TOKEN."),
    dict(name="surveylegend", description="SurveyLegend REST. ops get/post/put/delete (writes need confirm). Auth: SURVEYLEGEND_BASE_URL + SURVEYLEGEND_TOKEN."),
    dict(name="crowdsignal", description="Crowdsignal (formerly Polldaddy) REST. ops get/post/put/delete (writes need confirm). Auth: CROWDSIGNAL_BASE_URL (https://api.crowdsignal.com) + CROWDSIGNAL_TOKEN."),
    dict(name="tallyforms", description="Tally forms REST. ops get/post/put/delete (writes need confirm). Auth: TALLYFORMS_BASE_URL (https://api.tally.so) + TALLYFORMS_TOKEN."),
    dict(name="qualaroo", basic=True, description="Qualaroo REST. ops get/post/put/delete (writes need confirm). Auth: QUALAROO_BASE_URL (https://api.qualaroo.com) + QUALAROO_TOKEN (API key/secret)."),
    dict(name="wootric", description="Wootric (InMoment CX) REST. ops get/post/put/delete (writes need confirm). Auth: WOOTRIC_BASE_URL (https://production.wootric.com) + WOOTRIC_TOKEN."),
    dict(name="getfeedback", description="GetFeedback REST. ops get/post/put/delete (writes need confirm). Auth: GETFEEDBACK_BASE_URL (https://api.getfeedback.com) + GETFEEDBACK_TOKEN."),
    dict(name="medallia", description="Medallia Experience Cloud REST. ops get/post/put/delete (writes need confirm). Auth: MEDALLIA_BASE_URL (https://{instance}.medallia.com) + MEDALLIA_TOKEN."),
    dict(name="forsta", description="Forsta (Confirmit/FocusVision) survey REST. ops get/post/put/delete (writes need confirm). Auth: FORSTA_BASE_URL + FORSTA_TOKEN."),
    dict(name="snap_surveys", description="Snap Surveys REST. ops get/post/put/delete (writes need confirm). Auth: SNAP_SURVEYS_BASE_URL + SNAP_SURVEYS_TOKEN."),
    dict(name="zonka", description="Zonka Feedback REST. ops get/post/put/delete (writes need confirm). Auth: ZONKA_BASE_URL (https://api.zonkafeedback.com) + ZONKA_TOKEN."),
    dict(name="usersnap", description="Usersnap REST. ops get/post/put/delete (writes need confirm). Auth: USERSNAP_BASE_URL (https://api.usersnap.com) + USERSNAP_TOKEN."),
    dict(name="nicereply", basic=True, description="Nicereply REST. ops get/post/put/delete (writes need confirm). Auth: NICEREPLY_BASE_URL (https://{company}.nicereply.com) + NICEREPLY_TOKEN."),
    dict(name="feedier", description="Feedier REST. ops get/post/put/delete (writes need confirm). Auth: FEEDIER_BASE_URL + FEEDIER_TOKEN."),
    dict(name="contractsafe", description="ContractSafe REST. ops get/post/put/delete (writes need confirm). Auth: CONTRACTSAFE_BASE_URL (https://api.contractsafe.com) + CONTRACTSAFE_TOKEN."),
    dict(name="linksquares", description="LinkSquares CLM REST. ops get/post/put/delete (writes need confirm). Auth: LINKSQUARES_BASE_URL (https://api.linksquares.com) + LINKSQUARES_TOKEN."),
    dict(name="docjuris", description="DocJuris CLM REST. ops get/post/put/delete (writes need confirm). Auth: DOCJURIS_BASE_URL + DOCJURIS_TOKEN."),
    dict(name="summize", description="Summize CLM REST. ops get/post/put/delete (writes need confirm). Auth: SUMMIZE_BASE_URL + SUMMIZE_TOKEN."),
    dict(name="evisort", description="Evisort CLM REST. ops get/post/put/delete (writes need confirm). Auth: EVISORT_BASE_URL (https://api.evisort.com) + EVISORT_TOKEN."),
    dict(name="lexion", description="Lexion CLM REST. ops get/post/put/delete (writes need confirm). Auth: LEXION_BASE_URL + LEXION_TOKEN."),
    dict(name="contractworks", description="ContractWorks REST. ops get/post/put/delete (writes need confirm). Auth: CONTRACTWORKS_BASE_URL + CONTRACTWORKS_TOKEN."),
    dict(name="contractpodai", description="ContractPodAi CLM REST. ops get/post/put/delete (writes need confirm). Auth: CONTRACTPODAI_BASE_URL + CONTRACTPODAI_TOKEN."),
    dict(name="spotdraft", description="SpotDraft CLM REST. ops get/post/put/delete (writes need confirm). Auth: SPOTDRAFT_BASE_URL (https://api.spotdraft.com) + SPOTDRAFT_TOKEN."),
    dict(name="boldsign", token_header="X-API-KEY", scheme="", description="BoldSign e-signature REST. ops get/post/put/delete (writes need confirm). Auth: BOLDSIGN_BASE_URL (https://api.boldsign.com) + BOLDSIGN_TOKEN."),
    dict(name="rightsignature", description="RightSignature REST. ops get/post/put/delete (writes need confirm). Auth: RIGHTSIGNATURE_BASE_URL + RIGHTSIGNATURE_TOKEN."),
    dict(name="yousign", description="Yousign REST. ops get/post/put/delete (writes need confirm). Auth: YOUSIGN_BASE_URL (https://api.yousign.com) + YOUSIGN_TOKEN."),
    dict(name="skribble", description="Skribble e-signature REST. ops get/post/put/delete (writes need confirm). Auth: SKRIBBLE_BASE_URL (https://api.skribble.com) + SKRIBBLE_TOKEN."),
    dict(name="securedsigning", description="Secured Signing REST. ops get/post/put/delete (writes need confirm). Auth: SECUREDSIGNING_BASE_URL + SECUREDSIGNING_TOKEN."),
    dict(name="signaturely", description="Signaturely e-signature REST. ops get/post/put/delete (writes need confirm). Auth: SIGNATURELY_BASE_URL + SIGNATURELY_TOKEN."),
    dict(name="esigngenie", description="eSign Genie REST. ops get/post/put/delete (writes need confirm). Auth: ESIGNGENIE_BASE_URL + ESIGNGENIE_TOKEN."),
    dict(name="gatekeeper_clm", description="GateKeeper contract management REST. ops get/post/put/delete (writes need confirm). Auth: GATEKEEPER_CLM_BASE_URL (https://api.gatekeeperhq.com) + GATEKEEPER_CLM_TOKEN."),
    dict(name="malbek", description="Malbek CLM REST. ops get/post/put/delete (writes need confirm). Auth: MALBEK_BASE_URL + MALBEK_TOKEN."),
    dict(name="concord", description="Concord CLM REST. ops get/post/put/delete (writes need confirm). Auth: CONCORD_BASE_URL (https://api.concordnow.com) + CONCORD_TOKEN."),
    dict(name="signeasy", description="SignEasy REST. ops get/post/put/delete (writes need confirm). Auth: SIGNEASY_BASE_URL (https://api.signeasy.com) + SIGNEASY_TOKEN."),
    dict(name="signrequest", scheme="Token", description="SignRequest REST. ops get/post/put/delete (writes need confirm). e.g. /api/v1/documents/. Auth: SIGNREQUEST_BASE_URL (https://signrequest.com/api/v1) + SIGNREQUEST_TOKEN."),
    dict(name="eversign", description="eversign REST. ops get/post/put/delete (writes need confirm). Auth: EVERSIGN_BASE_URL (https://api.eversign.com) + EVERSIGN_TOKEN (access key)."),
    dict(name="signable", basic=True, description="Signable e-signature REST. ops get/post/put/delete (writes need confirm). Auth: SIGNABLE_BASE_URL (https://api.signable.co.uk) + SIGNABLE_TOKEN."),
    dict(name="zoho_sign", scheme="Zoho-oauthtoken", description="Zoho Sign REST. ops get/post/put/delete (writes need confirm). Auth: ZOHO_SIGN_BASE_URL (https://sign.zoho.com/api) + ZOHO_SIGN_TOKEN."),
    dict(name="nitro_sign", description="Nitro Sign REST. ops get/post/put/delete (writes need confirm). Auth: NITRO_SIGN_BASE_URL + NITRO_SIGN_TOKEN."),
    dict(name="getaccept", description="GetAccept REST. ops get/post/put/delete (writes need confirm). Auth: GETACCEPT_BASE_URL (https://api.getaccept.com) + GETACCEPT_TOKEN."),
    dict(name="proposify", description="Proposify REST. ops get/post/put/delete (writes need confirm). Auth: PROPOSIFY_BASE_URL (https://api.proposify.com) + PROPOSIFY_TOKEN."),
    dict(name="qwilr", description="Qwilr REST. ops get/post/put/delete (writes need confirm). Auth: QWILR_BASE_URL (https://api.qwilr.com) + QWILR_TOKEN."),
    dict(name="betterproposals", description="Better Proposals REST. ops get/post/put/delete (writes need confirm). Auth: BETTERPROPOSALS_BASE_URL + BETTERPROPOSALS_TOKEN."),
    dict(name="bidsketch", description="Bidsketch proposal REST. ops get/post/put/delete (writes need confirm). Auth: BIDSKETCH_BASE_URL + BIDSKETCH_TOKEN."),
    dict(name="signaturit", description="Signaturit e-signature REST. ops get/post/put/delete (writes need confirm). Auth: SIGNATURIT_BASE_URL (https://api.signaturit.com) + SIGNATURIT_TOKEN."),
    dict(name="approveme", basic=True, description="ApproveMe (WordPress e-signature) REST. ops get/post/put/delete (writes need confirm). Auth: APPROVEME_BASE_URL (site + /wp-json) + APPROVEME_TOKEN."),
    dict(name="egnyte", description="Egnyte content/document REST. ops get/post/put/delete (writes need confirm). Auth: EGNYTE_BASE_URL (https://{domain}.egnyte.com) + EGNYTE_TOKEN."),
    dict(name="docuware", description="DocuWare document management REST. ops get/post/put/delete (writes need confirm). Auth: DOCUWARE_BASE_URL (https://{org}.docuware.cloud) + DOCUWARE_TOKEN."),
    dict(name="laserfiche", description="Laserfiche document management REST. ops get/post/put/delete (writes need confirm). Auth: LASERFICHE_BASE_URL (https://api.laserfiche.com) + LASERFICHE_TOKEN."),
    dict(name="alfresco", basic=True, description="Alfresco content services REST. ops get/post/put/delete (writes need confirm). e.g. /alfresco/api/-default-/public/alfresco/versions/1/nodes. Auth: ALFRESCO_BASE_URL + ALFRESCO_TOKEN."),
    dict(name="opentext", description="OpenText content management REST. ops get/post/put/delete (writes need confirm). Auth: OPENTEXT_BASE_URL + OPENTEXT_TOKEN."),
    dict(name="sharefile", description="Citrix ShareFile REST. ops get/post/put/delete (writes need confirm). Auth: SHAREFILE_BASE_URL (https://{subdomain}.sf-api.com) + SHAREFILE_TOKEN."),
    dict(name="sharepoint", description="Microsoft SharePoint REST. ops get/post/put/delete (writes need confirm). Auth: SHAREPOINT_BASE_URL (https://{tenant}.sharepoint.com) + SHAREPOINT_TOKEN."),
    dict(name="docsend", description="DocSend (Dropbox) REST. ops get/post/put/delete (writes need confirm). Auth: DOCSEND_BASE_URL (https://api.docsend.com) + DOCSEND_TOKEN."),
    dict(name="pdffiller", description="pdfFiller REST. ops get/post/put/delete (writes need confirm). Auth: PDFFILLER_BASE_URL (https://api.pdffiller.com) + PDFFILLER_TOKEN."),
    dict(name="smallpdf", description="Smallpdf REST. ops get/post/put/delete (writes need confirm). Auth: SMALLPDF_BASE_URL (https://api.smallpdf.com) + SMALLPDF_TOKEN."),
    dict(name="ilovepdf", description="iLovePDF REST. ops get/post/put/delete (writes need confirm). Auth: ILOVEPDF_BASE_URL (https://api.ilovepdf.com) + ILOVEPDF_TOKEN (public/secret key JWT)."),
    dict(name="nitro_pdf", description="Nitro PDF REST. ops get/post/put/delete (writes need confirm). Auth: NITRO_PDF_BASE_URL + NITRO_PDF_TOKEN."),
    dict(name="pdfco", token_header="x-api-key", scheme="", description="PDF.co REST. ops get/post/put/delete (writes need confirm). Auth: PDFCO_BASE_URL (https://api.pdf.co) + PDFCO_TOKEN."),
    dict(name="soda_pdf", description="Soda PDF REST. ops get/post/put/delete (writes need confirm). Auth: SODA_PDF_BASE_URL + SODA_PDF_TOKEN."),
    dict(name="efax", description="eFax REST. ops get/post/put/delete (writes need confirm). Auth: EFAX_BASE_URL (https://secure.efaxdeveloper.com) + EFAX_TOKEN."),
    dict(name="sfax", basic=True, description="sFax REST. ops get/post/put/delete (writes need confirm). Auth: SFAX_BASE_URL (https://www.sfax.com/api) + SFAX_TOKEN."),
    dict(name="srfax", description="SRFax REST. ops get/post/put/delete (writes need confirm). Auth: SRFAX_BASE_URL (https://www.srfax.com/SRF_SecWebSvc.php) + SRFAX_TOKEN (access ID/password)."),
    dict(name="metrofax", description="MetroFax REST. ops get/post/put/delete (writes need confirm). Auth: METROFAX_BASE_URL + METROFAX_TOKEN."),
    dict(name="updox", description="Updox (fax/document) REST. ops get/post/put/delete (writes need confirm). Auth: UPDOX_BASE_URL (https://api.updox.com) + UPDOX_TOKEN."),
    dict(name="mfiles", description="M-Files REST. ops get/post/put/delete (writes need confirm). Auth: MFILES_BASE_URL (https://{vault}.m-files.com) + MFILES_TOKEN."),
    dict(name="filehold", basic=True, description="FileHold document management REST. ops get/post/put/delete (writes need confirm). Auth: FILEHOLD_BASE_URL + FILEHOLD_TOKEN."),
    dict(name="filecloud", description="FileCloud REST. ops get/post/put/delete (writes need confirm). Auth: FILECLOUD_BASE_URL (https://{domain}/core) + FILECLOUD_TOKEN."),
    dict(name="onehub", description="Onehub REST. ops get/post/put/delete (writes need confirm). Auth: ONEHUB_BASE_URL (https://api.onehub.com) + ONEHUB_TOKEN."),
    dict(name="panzura", description="Panzura data management REST. ops get/post/put/delete (writes need confirm). Auth: PANZURA_BASE_URL + PANZURA_TOKEN."),
    dict(name="hightail", description="Hightail (formerly YouSendIt) REST. ops get/post/put/delete (writes need confirm). Auth: HIGHTAIL_BASE_URL (https://api.hightail.com) + HIGHTAIL_TOKEN."),
    dict(name="setmore", description="Setmore scheduling REST. ops get/post/put/delete (writes need confirm). Auth: SETMORE_BASE_URL (https://developer.setmore.com/api) + SETMORE_TOKEN."),
    dict(name="appointlet", description="Appointlet scheduling REST. ops get/post/put/delete (writes need confirm). Auth: APPOINTLET_BASE_URL (https://api.appointlet.com) + APPOINTLET_TOKEN."),
    dict(name="simplybookme", description="SimplyBook.me scheduling REST. ops get/post/put/delete (writes need confirm). Auth: SIMPLYBOOKME_BASE_URL (https://user-api.simplybook.me) + SIMPLYBOOKME_TOKEN."),
    dict(name="vagaro", description="Vagaro scheduling REST. ops get/post/put/delete (writes need confirm). Auth: VAGARO_BASE_URL + VAGARO_TOKEN."),
    dict(name="mindbody", description="Mindbody scheduling REST. ops get/post/put/delete (writes need confirm). Auth: MINDBODY_BASE_URL (https://api.mindbodyonline.com) + MINDBODY_TOKEN."),
    dict(name="schedulicity", description="Schedulicity REST. ops get/post/put/delete (writes need confirm). Auth: SCHEDULICITY_BASE_URL + SCHEDULICITY_TOKEN."),
    dict(name="bookly", basic=True, description="Bookly (WordPress) scheduling REST. ops get/post/put/delete (writes need confirm). Auth: BOOKLY_BASE_URL (site + /wp-json) + BOOKLY_TOKEN."),
    dict(name="picktime", description="Picktime scheduling REST. ops get/post/put/delete (writes need confirm). Auth: PICKTIME_BASE_URL + PICKTIME_TOKEN."),
    dict(name="appointy", description="Appointy scheduling REST. ops get/post/put/delete (writes need confirm). Auth: APPOINTY_BASE_URL + APPOINTY_TOKEN."),
    dict(name="ovatu", description="Ovatu scheduling REST. ops get/post/put/delete (writes need confirm). Auth: OVATU_BASE_URL + OVATU_TOKEN."),
    dict(name="booksy", description="Booksy scheduling REST. ops get/post/put/delete (writes need confirm). Auth: BOOKSY_BASE_URL + BOOKSY_TOKEN."),
    dict(name="timetap", basic=True, description="TimeTap scheduling REST. ops get/post/put/delete (writes need confirm). Auth: TIMETAP_BASE_URL (https://api.timetap.com) + TIMETAP_TOKEN (key/secret)."),
    dict(name="genbook", description="Genbook scheduling REST. ops get/post/put/delete (writes need confirm). Auth: GENBOOK_BASE_URL + GENBOOK_TOKEN."),
    dict(name="schedulista", basic=True, description="Schedulista REST. ops get/post/put/delete (writes need confirm). Auth: SCHEDULISTA_BASE_URL (https://api.schedulista.com) + SCHEDULISTA_TOKEN."),
    dict(name="youcanbookme", description="YouCanBook.me REST. ops get/post/put/delete (writes need confirm). Auth: YOUCANBOOKME_BASE_URL (https://api.youcanbook.me) + YOUCANBOOKME_TOKEN."),
    dict(name="calendarhero", description="CalendarHero scheduling REST. ops get/post/put/delete (writes need confirm). Auth: CALENDARHERO_BASE_URL (https://api.calendarhero.com) + CALENDARHERO_TOKEN."),
    dict(name="housecall_pro", description="Housecall Pro REST. ops get/post/put/delete (writes need confirm). Auth: HOUSECALL_PRO_BASE_URL (https://api.housecallpro.com) + HOUSECALL_PRO_TOKEN."),
    dict(name="jobber", description="Jobber GraphQL API. ops get/post/put/delete (writes need confirm). e.g. /api/graphql. Auth: JOBBER_BASE_URL (https://api.getjobber.com) + JOBBER_TOKEN."),
    dict(name="servicetitan", description="ServiceTitan REST. ops get/post/put/delete (writes need confirm). Auth: SERVICETITAN_BASE_URL (https://api.servicetitan.io) + SERVICETITAN_TOKEN."),
    dict(name="planday", description="Planday scheduling REST. ops get/post/put/delete (writes need confirm). Auth: PLANDAY_BASE_URL (https://openapi.planday.com) + PLANDAY_TOKEN."),
    dict(name="findmyshift", description="FindMyShift REST. ops get/post/put/delete (writes need confirm). Auth: FINDMYSHIFT_BASE_URL + FINDMYSHIFT_TOKEN."),
    dict(name="shiftboard", description="Shiftboard scheduling REST. ops get/post/put/delete (writes need confirm). Auth: SHIFTBOARD_BASE_URL (https://api.shiftboard.com) + SHIFTBOARD_TOKEN."),
    dict(name="sling", description="Sling (workforce scheduling) REST. ops get/post/put/delete (writes need confirm). Auth: SLING_BASE_URL (https://api.getsling.com) + SLING_TOKEN."),
    dict(name="quinyx", description="Quinyx workforce scheduling REST. ops get/post/put/delete (writes need confirm). Auth: QUINYX_BASE_URL (https://api.quinyx.com) + QUINYX_TOKEN."),
    dict(name="ubindi", description="Ubindi class scheduling REST. ops get/post/put/delete (writes need confirm). Auth: UBINDI_BASE_URL + UBINDI_TOKEN."),
    dict(name="eventzilla", description="Eventzilla REST. ops get/post/put/delete (writes need confirm). Auth: EVENTZILLA_BASE_URL (https://www.eventzilla.net) + EVENTZILLA_TOKEN."),
    dict(name="tickettailor", basic=True, description="Ticket Tailor REST. ops get/post/put/delete (writes need confirm). Auth: TICKETTAILOR_BASE_URL (https://api.tickettailor.com) + TICKETTAILOR_TOKEN."),
    dict(name="universe_events", description="Universe events/ticketing REST. ops get/post/put/delete (writes need confirm). Auth: UNIVERSE_EVENTS_BASE_URL (https://www.universe.com/api) + UNIVERSE_EVENTS_TOKEN."),
    dict(name="booker", description="Booker (Booker Software) appointment REST. ops get/post/put/delete (writes need confirm). Auth: BOOKER_BASE_URL + BOOKER_TOKEN."),
    dict(name="mytime", description="MyTime scheduling REST. ops get/post/put/delete (writes need confirm). Auth: MYTIME_BASE_URL + MYTIME_TOKEN."),
    dict(name="appointmentplus", description="AppointmentPlus REST. ops get/post/put/delete (writes need confirm). Auth: APPOINTMENTPLUS_BASE_URL + APPOINTMENTPLUS_TOKEN."),
    dict(name="timetrade", description="TimeTrade appointment scheduling REST. ops get/post/put/delete (writes need confirm). Auth: TIMETRADE_BASE_URL + TIMETRADE_TOKEN."),
    dict(name="oncehub", description="OnceHub (ScheduleOnce) REST. ops get/post/put/delete (writes need confirm). Auth: ONCEHUB_BASE_URL (https://api.oncehub.com) + ONCEHUB_TOKEN."),
    dict(name="bookeo", description="Bookeo REST. ops get/post/put/delete (writes need confirm). Auth: BOOKEO_BASE_URL (https://api.bookeo.com) + BOOKEO_TOKEN (API key + secret key)."),
    dict(name="tentoeight", description="10to8 scheduling REST. ops get/post/put/delete (writes need confirm). Auth: TENTOEIGHT_BASE_URL (https://api.10to8.com) + TENTOEIGHT_TOKEN."),
    dict(name="checkfront", basic=True, description="Checkfront booking REST. ops get/post/put/delete (writes need confirm). Auth: CHECKFRONT_BASE_URL (https://{company}.checkfront.com) + CHECKFRONT_TOKEN (API key/secret)."),
    dict(name="peekpro", description="Peek Pro booking REST. ops get/post/put/delete (writes need confirm). Auth: PEEKPRO_BASE_URL + PEEKPRO_TOKEN."),
    dict(name="resdiary", description="ResDiary restaurant booking REST. ops get/post/put/delete (writes need confirm). Auth: RESDIARY_BASE_URL (https://api.rdbranch.com) + RESDIARY_TOKEN."),
    dict(name="tablecheck", description="TableCheck reservation REST. ops get/post/put/delete (writes need confirm). Auth: TABLECHECK_BASE_URL (https://api.tablecheck.com) + TABLECHECK_TOKEN."),
    dict(name="ncr_aloha", description="NCR Aloha POS REST. ops get/post/put/delete (writes need confirm). Auth: NCR_ALOHA_BASE_URL + NCR_ALOHA_TOKEN."),
    dict(name="par_brink", token_header="AccessToken", scheme="", description="PAR Brink POS REST. ops get/post/put/delete (writes need confirm). Auth: PAR_BRINK_BASE_URL + PAR_BRINK_TOKEN (AccessToken header)."),
    dict(name="focus_pos", description="Focus POS REST. ops get/post/put/delete (writes need confirm). Auth: FOCUS_POS_BASE_URL + FOCUS_POS_TOKEN."),
    dict(name="squirrel_systems", description="Squirrel POS REST. ops get/post/put/delete (writes need confirm). Auth: SQUIRREL_SYSTEMS_BASE_URL + SQUIRREL_SYSTEMS_TOKEN."),
    dict(name="oracle_micros", description="Oracle MICROS Simphony REST. ops get/post/put/delete (writes need confirm). Auth: ORACLE_MICROS_BASE_URL + ORACLE_MICROS_TOKEN."),
    dict(name="lavu_pos", description="Poslavu (POS Lavu) REST. ops get/post/put/delete (writes need confirm). Auth: LAVU_POS_BASE_URL (https://{sub}.poslavu.com) + LAVU_POS_TOKEN."),
    dict(name="upserve", description="Upserve (Lightspeed Restaurant) REST. ops get/post/put/delete (writes need confirm). Auth: UPSERVE_BASE_URL (https://api.upserve.com) + UPSERVE_TOKEN."),
    dict(name="breadcrumb_pos", description="Breadcrumb POS REST. ops get/post/put/delete (writes need confirm). Auth: BREADCRUMB_POS_BASE_URL + BREADCRUMB_POS_TOKEN."),
    dict(name="spoton_pos", description="SpotOn Restaurant POS REST. ops get/post/put/delete (writes need confirm). Auth: SPOTON_POS_BASE_URL + SPOTON_POS_TOKEN."),
    dict(name="cake_pos", description="Sysco CAKE POS REST. ops get/post/put/delete (writes need confirm). Auth: CAKE_POS_BASE_URL + CAKE_POS_TOKEN."),
    dict(name="rpower_pos", description="RPOWER POS REST. ops get/post/put/delete (writes need confirm). Auth: RPOWER_POS_BASE_URL + RPOWER_POS_TOKEN."),
    dict(name="rezku", description="Rezku POS REST. ops get/post/put/delete (writes need confirm). Auth: REZKU_BASE_URL + REZKU_TOKEN."),
    dict(name="hungerrush", description="HungerRush POS REST. ops get/post/put/delete (writes need confirm). Auth: HUNGERRUSH_BASE_URL + HUNGERRUSH_TOKEN."),
    dict(name="punchh", description="Punchh loyalty REST. ops get/post/put/delete (writes need confirm). Auth: PUNCHH_BASE_URL + PUNCHH_TOKEN."),
    dict(name="paytronix", description="Paytronix loyalty/gift REST. ops get/post/put/delete (writes need confirm). Auth: PAYTRONIX_BASE_URL + PAYTRONIX_TOKEN."),
    dict(name="thanx", description="Thanx loyalty REST. ops get/post/put/delete (writes need confirm). Auth: THANX_BASE_URL (https://api.thanx.com) + THANX_TOKEN."),
    dict(name="fivestars", description="Fivestars loyalty REST. ops get/post/put/delete (writes need confirm). Auth: FIVESTARS_BASE_URL + FIVESTARS_TOKEN."),
    dict(name="olo", description="Olo ordering REST. ops get/post/put/delete (writes need confirm). Auth: OLO_BASE_URL (https://api.olo.com) + OLO_TOKEN."),
    dict(name="chownow", description="ChowNow ordering REST. ops get/post/put/delete (writes need confirm). Auth: CHOWNOW_BASE_URL + CHOWNOW_TOKEN."),
    dict(name="ritual", description="Ritual ordering REST. ops get/post/put/delete (writes need confirm). Auth: RITUAL_BASE_URL + RITUAL_TOKEN."),
    dict(name="slicelife", description="Slice (slicelife.com) pizzeria ordering REST. ops get/post/put/delete (writes need confirm). Auth: SLICELIFE_BASE_URL + SLICELIFE_TOKEN."),
    dict(name="bbot", description="Bbot in-venue ordering REST. ops get/post/put/delete (writes need confirm). Auth: BBOT_BASE_URL + BBOT_TOKEN."),
    dict(name="popmenu", description="Popmenu REST. ops get/post/put/delete (writes need confirm). Auth: POPMENU_BASE_URL + POPMENU_TOKEN."),
    dict(name="flipdish", description="Flipdish ordering REST. ops get/post/put/delete (writes need confirm). Auth: FLIPDISH_BASE_URL (https://api.flipdish.co) + FLIPDISH_TOKEN."),
    dict(name="gloriafood", description="GloriaFood ordering REST. ops get/post/put/delete (writes need confirm). Auth: GLORIAFOOD_BASE_URL + GLORIAFOOD_TOKEN (API key)."),
    dict(name="cuboh", description="Cuboh order aggregation REST. ops get/post/put/delete (writes need confirm). Auth: CUBOH_BASE_URL + CUBOH_TOKEN."),
    dict(name="getotter", description="Otter (getotter.com) order management REST. ops get/post/put/delete (writes need confirm). Auth: GETOTTER_BASE_URL + GETOTTER_TOKEN."),
    dict(name="deliverect", description="Deliverect delivery integration REST. ops get/post/put/delete (writes need confirm). Auth: DELIVERECT_BASE_URL (https://api.deliverect.com) + DELIVERECT_TOKEN."),
    dict(name="ordermark", description="Ordermark (Nextbite) order aggregation REST. ops get/post/put/delete (writes need confirm). Auth: ORDERMARK_BASE_URL + ORDERMARK_TOKEN."),
    dict(name="relay_delivery", description="Relay restaurant delivery REST. ops get/post/put/delete (writes need confirm). Auth: RELAY_DELIVERY_BASE_URL + RELAY_DELIVERY_TOKEN."),
    dict(name="shipday", description="Shipday delivery management REST. ops get/post/put/delete (writes need confirm). Auth: SHIPDAY_BASE_URL (https://api.shipday.com) + SHIPDAY_TOKEN."),
    dict(name="onfleet", basic=True, description="Onfleet last-mile delivery REST. ops get/post/put/delete (writes need confirm). Auth: ONFLEET_BASE_URL (https://onfleet.com/api/v2) + ONFLEET_TOKEN (API key, basic auth)."),
    dict(name="doordash", description="DoorDash Drive/Marketplace REST. ops get/post/put/delete (writes need confirm). Auth: DOORDASH_BASE_URL (https://openapi.doordash.com) + DOORDASH_TOKEN."),
    dict(name="ubereats", description="Uber Eats REST. ops get/post/put/delete (writes need confirm). Auth: UBEREATS_BASE_URL (https://api.uber.com) + UBEREATS_TOKEN."),
    dict(name="postmates", description="Postmates (Uber) delivery REST. ops get/post/put/delete (writes need confirm). Auth: POSTMATES_BASE_URL + POSTMATES_TOKEN."),
    dict(name="deliveroo", description="Deliveroo REST. ops get/post/put/delete (writes need confirm). Auth: DELIVEROO_BASE_URL (https://api.deliveroo.com) + DELIVEROO_TOKEN."),
    dict(name="justeat", description="Just Eat Takeaway REST. ops get/post/put/delete (writes need confirm). Auth: JUSTEAT_BASE_URL + JUSTEAT_TOKEN."),
    dict(name="deliveryhero", description="Delivery Hero REST. ops get/post/put/delete (writes need confirm). Auth: DELIVERYHERO_BASE_URL + DELIVERYHERO_TOKEN."),
    dict(name="wolt", description="Wolt delivery REST. ops get/post/put/delete (writes need confirm). Auth: WOLT_BASE_URL + WOLT_TOKEN."),
    dict(name="zomato", token_header="user-key", scheme="", description="Zomato REST. ops get/post/put/delete (writes need confirm). Auth: ZOMATO_BASE_URL (https://developers.zomato.com/api) + ZOMATO_TOKEN (user-key header)."),
    dict(name="swiggy", description="Swiggy partner REST. ops get/post/put/delete (writes need confirm). Auth: SWIGGY_BASE_URL + SWIGGY_TOKEN."),
    dict(name="foodpanda", description="foodpanda (Delivery Hero) REST. ops get/post/put/delete (writes need confirm). Auth: FOODPANDA_BASE_URL + FOODPANDA_TOKEN."),
    dict(name="glovo", description="Glovo REST. ops get/post/put/delete (writes need confirm). Auth: GLOVO_BASE_URL + GLOVO_TOKEN."),
    dict(name="rappi", description="Rappi REST. ops get/post/put/delete (writes need confirm). Auth: RAPPI_BASE_URL + RAPPI_TOKEN."),
    dict(name="instacart", description="Instacart Connect REST. ops get/post/put/delete (writes need confirm). Auth: INSTACART_BASE_URL (https://connect.instacart.com) + INSTACART_TOKEN."),
    dict(name="gopuff", description="Gopuff REST. ops get/post/put/delete (writes need confirm). Auth: GOPUFF_BASE_URL + GOPUFF_TOKEN."),
    dict(name="grubhub", description="Grubhub partner REST. ops get/post/put/delete (writes need confirm). Auth: GRUBHUB_BASE_URL + GRUBHUB_TOKEN."),
    dict(name="glofox", description="Glofox gym/studio management REST. ops get/post/put/delete (writes need confirm). Auth: GLOFOX_BASE_URL + GLOFOX_TOKEN."),
    dict(name="zenplanner", description="Zen Planner REST. ops get/post/put/delete (writes need confirm). Auth: ZENPLANNER_BASE_URL + ZENPLANNER_TOKEN."),
    dict(name="wellnessliving", description="WellnessLiving REST. ops get/post/put/delete (writes need confirm). Auth: WELLNESSLIVING_BASE_URL + WELLNESSLIVING_TOKEN."),
    dict(name="trainerize", description="Trainerize REST. ops get/post/put/delete (writes need confirm). Auth: TRAINERIZE_BASE_URL + TRAINERIZE_TOKEN."),
    dict(name="myzone", description="MyZone REST. ops get/post/put/delete (writes need confirm). Auth: MYZONE_BASE_URL + MYZONE_TOKEN."),
    dict(name="exercise_com", description="Exercise.com REST. ops get/post/put/delete (writes need confirm). Auth: EXERCISE_COM_BASE_URL + EXERCISE_COM_TOKEN."),
    dict(name="gymmaster", token_header="api-key", scheme="", description="GymMaster REST. ops get/post/put/delete (writes need confirm). Auth: GYMMASTER_BASE_URL + GYMMASTER_TOKEN (api-key header)."),
    dict(name="clubready", description="ClubReady REST. ops get/post/put/delete (writes need confirm). Auth: CLUBREADY_BASE_URL + CLUBREADY_TOKEN."),
    dict(name="abc_fitness", description="ABC Fitness (ABC Trainerize/Ignite) REST. ops get/post/put/delete (writes need confirm). Auth: ABC_FITNESS_BASE_URL + ABC_FITNESS_TOKEN."),
    dict(name="pike13", description="Pike13 REST. ops get/post/put/delete (writes need confirm). Auth: PIKE13_BASE_URL (https://{sub}.pike13.com) + PIKE13_TOKEN."),
    dict(name="perfectgym", description="PerfectGym REST. ops get/post/put/delete (writes need confirm). Auth: PERFECTGYM_BASE_URL + PERFECTGYM_TOKEN."),
    dict(name="virtuagym", description="Virtuagym REST. ops get/post/put/delete (writes need confirm). Auth: VIRTUAGYM_BASE_URL (https://api.virtuagym.com) + VIRTUAGYM_TOKEN."),
    dict(name="wodify", description="Wodify REST. ops get/post/put/delete (writes need confirm). Auth: WODIFY_BASE_URL + WODIFY_TOKEN."),
    dict(name="sugarwod", description="SugarWOD REST. ops get/post/put/delete (writes need confirm). Auth: SUGARWOD_BASE_URL + SUGARWOD_TOKEN."),
    dict(name="pushpress", description="PushPress REST. ops get/post/put/delete (writes need confirm). Auth: PUSHPRESS_BASE_URL + PUSHPRESS_TOKEN."),
    dict(name="zingfit", description="Zingfit REST. ops get/post/put/delete (writes need confirm). Auth: ZINGFIT_BASE_URL + ZINGFIT_TOKEN."),
    dict(name="mariana_tek", description="Mariana Tek REST. ops get/post/put/delete (writes need confirm). Auth: MARIANA_TEK_BASE_URL (https://{sub}.marianatek.com) + MARIANA_TEK_TOKEN."),
    dict(name="fresha", description="Fresha booking REST. ops get/post/put/delete (writes need confirm). Auth: FRESHA_BASE_URL + FRESHA_TOKEN."),
    dict(name="walla_fitness", description="Walla studio management REST. ops get/post/put/delete (writes need confirm). Auth: WALLA_FITNESS_BASE_URL + WALLA_FITNESS_TOKEN."),
    dict(name="punchpass", description="Punchpass REST. ops get/post/put/delete (writes need confirm). Auth: PUNCHPASS_BASE_URL + PUNCHPASS_TOKEN."),
    dict(name="gymcatch", description="GymCatch REST. ops get/post/put/delete (writes need confirm). Auth: GYMCATCH_BASE_URL + GYMCATCH_TOKEN."),
    dict(name="trainingpeaks", description="TrainingPeaks REST. ops get/post/put/delete (writes need confirm). Auth: TRAININGPEAKS_BASE_URL + TRAININGPEAKS_TOKEN."),
    dict(name="trainheroic", description="TrainHeroic REST. ops get/post/put/delete (writes need confirm). Auth: TRAINHEROIC_BASE_URL + TRAINHEROIC_TOKEN."),
    dict(name="teambuildr", description="TeamBuildr REST. ops get/post/put/delete (writes need confirm). Auth: TEAMBUILDR_BASE_URL + TEAMBUILDR_TOKEN."),
    dict(name="catapult_sports", description="Catapult Sports REST. ops get/post/put/delete (writes need confirm). Auth: CATAPULT_SPORTS_BASE_URL + CATAPULT_SPORTS_TOKEN."),
    dict(name="hudl", description="Hudl REST. ops get/post/put/delete (writes need confirm). Auth: HUDL_BASE_URL (https://api.hudl.com) + HUDL_TOKEN."),
    dict(name="teamworks", description="Teamworks REST. ops get/post/put/delete (writes need confirm). Auth: TEAMWORKS_BASE_URL + TEAMWORKS_TOKEN."),
    dict(name="sportsengine", description="SportsEngine REST. ops get/post/put/delete (writes need confirm). Auth: SPORTSENGINE_BASE_URL + SPORTSENGINE_TOKEN."),
    dict(name="teamsnap", description="TeamSnap REST. ops get/post/put/delete (writes need confirm). Auth: TEAMSNAP_BASE_URL (https://api.teamsnap.com) + TEAMSNAP_TOKEN."),
    dict(name="leagueapps", description="LeagueApps REST. ops get/post/put/delete (writes need confirm). Auth: LEAGUEAPPS_BASE_URL + LEAGUEAPPS_TOKEN."),
    dict(name="spond", description="Spond REST. ops get/post/put/delete (writes need confirm). Auth: SPOND_BASE_URL + SPOND_TOKEN."),
    dict(name="playmetrics", description="PlayMetrics REST. ops get/post/put/delete (writes need confirm). Auth: PLAYMETRICS_BASE_URL + PLAYMETRICS_TOKEN."),
    dict(name="stack_sports", description="Stack Sports REST. ops get/post/put/delete (writes need confirm). Auth: STACK_SPORTS_BASE_URL + STACK_SPORTS_TOKEN."),
    dict(name="arbiter_sports", description="ArbiterSports REST. ops get/post/put/delete (writes need confirm). Auth: ARBITER_SPORTS_BASE_URL + ARBITER_SPORTS_TOKEN."),
    dict(name="exposure_events", description="Exposure Events REST. ops get/post/put/delete (writes need confirm). Auth: EXPOSURE_EVENTS_BASE_URL + EXPOSURE_EVENTS_TOKEN."),
    dict(name="active_network", description="Active Network REST. ops get/post/put/delete (writes need confirm). Auth: ACTIVE_NETWORK_BASE_URL (https://api.amp.active.com) + ACTIVE_NETWORK_TOKEN."),
    dict(name="runsignup", description="RunSignup REST. ops get/post/put/delete (writes need confirm). Auth: RUNSIGNUP_BASE_URL (https://runsignup.com/Rest) + RUNSIGNUP_TOKEN (API key)."),
    dict(name="chronotrack", description="ChronoTrack REST. ops get/post/put/delete (writes need confirm). Auth: CHRONOTRACK_BASE_URL + CHRONOTRACK_TOKEN."),
    dict(name="race_roster", description="Race Roster REST. ops get/post/put/delete (writes need confirm). Auth: RACE_ROSTER_BASE_URL + RACE_ROSTER_TOKEN."),
    dict(name="strava", description="Strava REST. ops get/post/put/delete (writes need confirm). Auth: STRAVA_BASE_URL (https://www.strava.com/api/v3) + STRAVA_TOKEN."),
    dict(name="garmin_connect", description="Garmin Connect/Health REST. ops get/post/put/delete (writes need confirm). Auth: GARMIN_CONNECT_BASE_URL + GARMIN_CONNECT_TOKEN."),
    dict(name="fitbit", description="Fitbit Web API REST. ops get/post/put/delete (writes need confirm). Auth: FITBIT_BASE_URL (https://api.fitbit.com) + FITBIT_TOKEN."),
    dict(name="polar_flow", description="Polar Flow/AccessLink REST. ops get/post/put/delete (writes need confirm). Auth: POLAR_FLOW_BASE_URL (https://www.polaraccesslink.com) + POLAR_FLOW_TOKEN."),
    dict(name="wahoo_fitness", description="Wahoo Fitness REST. ops get/post/put/delete (writes need confirm). Auth: WAHOO_FITNESS_BASE_URL (https://api.wahooligan.com) + WAHOO_FITNESS_TOKEN."),
    dict(name="whoop", description="Whoop REST. ops get/post/put/delete (writes need confirm). Auth: WHOOP_BASE_URL (https://api.prod.whoop.com) + WHOOP_TOKEN."),
    dict(name="oura", description="Oura Ring REST. ops get/post/put/delete (writes need confirm). Auth: OURA_BASE_URL (https://api.ouraring.com) + OURA_TOKEN."),
    dict(name="myfitnesspal", description="MyFitnessPal partner REST. ops get/post/put/delete (writes need confirm). Auth: MYFITNESSPAL_BASE_URL + MYFITNESSPAL_TOKEN."),
    dict(name="final_surge", description="Final Surge REST. ops get/post/put/delete (writes need confirm). Auth: FINAL_SURGE_BASE_URL + FINAL_SURGE_TOKEN."),
    dict(name="teamup_fitness", description="TeamUp fitness scheduling REST. ops get/post/put/delete (writes need confirm). Auth: TEAMUP_FITNESS_BASE_URL (https://goteamup.com/api) + TEAMUP_FITNESS_TOKEN."),
    dict(name="gymdesk", description="Gymdesk REST. ops get/post/put/delete (writes need confirm). Auth: GYMDESK_BASE_URL + GYMDESK_TOKEN."),
    dict(name="omnify", description="Omnify booking REST. ops get/post/put/delete (writes need confirm). Auth: OMNIFY_BASE_URL + OMNIFY_TOKEN."),
    dict(name="mangomint", description="Mangomint REST. ops get/post/put/delete (writes need confirm). Auth: MANGOMINT_BASE_URL + MANGOMINT_TOKEN."),
    dict(name="zenoti", description="Zenoti spa/wellness REST. ops get/post/put/delete (writes need confirm). Auth: ZENOTI_BASE_URL (https://api.zenoti.com) + ZENOTI_TOKEN."),
    dict(name="tock", description="Tock reservations REST. ops get/post/put/delete (writes need confirm). Auth: TOCK_BASE_URL + TOCK_TOKEN."),
    dict(name="sevenrooms", description="SevenRooms REST. ops get/post/put/delete (writes need confirm). Auth: SEVENROOMS_BASE_URL (https://api.sevenrooms.com) + SEVENROOMS_TOKEN."),
    dict(name="yelp_reservations", description="Yelp Guest Manager (Reservations) REST. ops get/post/put/delete (writes need confirm). Auth: YELP_RESERVATIONS_BASE_URL + YELP_RESERVATIONS_TOKEN."),
    dict(name="bentobox", description="BentoBox restaurant site/ordering REST. ops get/post/put/delete (writes need confirm). Auth: BENTOBOX_BASE_URL + BENTOBOX_TOKEN."),
    dict(name="wisely", description="Wisely restaurant CRM REST. ops get/post/put/delete (writes need confirm). Auth: WISELY_BASE_URL + WISELY_TOKEN."),
    dict(name="eat_app", description="Eat App reservations REST. ops get/post/put/delete (writes need confirm). Auth: EAT_APP_BASE_URL + EAT_APP_TOKEN."),
    dict(name="plex_systems", description="Plex Manufacturing Cloud (Rockwell) REST. ops get/post/put/delete (writes need confirm). Auth: PLEX_SYSTEMS_BASE_URL + PLEX_SYSTEMS_TOKEN."),
    dict(name="factorytalk", description="Rockwell FactoryTalk Cloud REST. ops get/post/put/delete (writes need confirm). Auth: FACTORYTALK_BASE_URL + FACTORYTALK_TOKEN."),
    dict(name="opcenter", description="Siemens Opcenter MES REST. ops get/post/put/delete (writes need confirm). Auth: OPCENTER_BASE_URL + OPCENTER_TOKEN."),
    dict(name="mindsphere", description="Siemens MindSphere industrial IoT REST. ops get/post/put/delete (writes need confirm). Auth: MINDSPHERE_BASE_URL (https://gateway.{region}.mindsphere.io) + MINDSPHERE_TOKEN."),
    dict(name="proficy", description="GE Digital Proficy (historian/MES) REST. ops get/post/put/delete (writes need confirm). Auth: PROFICY_BASE_URL + PROFICY_TOKEN."),
    dict(name="honeywell_forge", description="Honeywell Forge industrial IoT REST. ops get/post/put/delete (writes need confirm). Auth: HONEYWELL_FORGE_BASE_URL + HONEYWELL_FORGE_TOKEN."),
    dict(name="tulip_mes", description="Tulip frontline operations platform REST. ops get/post/put/delete (writes need confirm). Auth: TULIP_MES_BASE_URL (https://{instance}.tulip.co) + TULIP_MES_TOKEN."),
    dict(name="machinemetrics", description="MachineMetrics manufacturing analytics REST. ops get/post/put/delete (writes need confirm). Auth: MACHINEMETRICS_BASE_URL + MACHINEMETRICS_TOKEN."),
    dict(name="plataine", description="Plataine industrial IoT optimization REST. ops get/post/put/delete (writes need confirm). Auth: PLATAINE_BASE_URL + PLATAINE_TOKEN."),
    dict(name="critical_manufacturing", description="Critical Manufacturing MES REST. ops get/post/put/delete (writes need confirm). Auth: CRITICAL_MANUFACTURING_BASE_URL + CRITICAL_MANUFACTURING_TOKEN."),
    dict(name="traksys", description="Parsec TrakSYS MES REST. ops get/post/put/delete (writes need confirm). Auth: TRAKSYS_BASE_URL + TRAKSYS_TOKEN."),
    dict(name="solumina", description="iBase-t Solumina MES REST. ops get/post/put/delete (writes need confirm). Auth: SOLUMINA_BASE_URL + SOLUMINA_TOKEN."),
    dict(name="factorylogix", description="Aegis FactoryLogix MES REST. ops get/post/put/delete (writes need confirm). Auth: FACTORYLOGIX_BASE_URL + FACTORYLOGIX_TOKEN."),
    dict(name="sap_dmc", description="SAP Digital Manufacturing Cloud REST. ops get/post/put/delete (writes need confirm). Auth: SAP_DMC_BASE_URL + SAP_DMC_TOKEN."),
    dict(name="oracle_mfg_cloud", description="Oracle Manufacturing Cloud REST. ops get/post/put/delete (writes need confirm). Auth: ORACLE_MFG_CLOUD_BASE_URL + ORACLE_MFG_CLOUD_TOKEN."),
    dict(name="cloudsuite_industrial", description="Infor CloudSuite Industrial (SyteLine) REST. ops get/post/put/delete (writes need confirm). Auth: CLOUDSUITE_INDUSTRIAL_BASE_URL + CLOUDSUITE_INDUSTRIAL_TOKEN."),
    dict(name="iqms", description="IQMS / DELMIAworks ERP-MES REST. ops get/post/put/delete (writes need confirm). Auth: IQMS_BASE_URL + IQMS_TOKEN."),
    dict(name="windchill", description="PTC Windchill PLM REST. ops get/post/put/delete (writes need confirm). Auth: WINDCHILL_BASE_URL + WINDCHILL_TOKEN."),
    dict(name="teamcenter", description="Siemens Teamcenter PLM REST. ops get/post/put/delete (writes need confirm). Auth: TEAMCENTER_BASE_URL + TEAMCENTER_TOKEN."),
    dict(name="enovia", description="Dassault Systemes ENOVIA PLM REST. ops get/post/put/delete (writes need confirm). Auth: ENOVIA_BASE_URL + ENOVIA_TOKEN."),
    dict(name="threedexperience", description="Dassault Systemes 3DEXPERIENCE platform REST. ops get/post/put/delete (writes need confirm). Auth: THREEDEXPERIENCE_BASE_URL + THREEDEXPERIENCE_TOKEN."),
    dict(name="fusion_lifecycle", description="Autodesk Fusion Manage (Fusion Lifecycle) PLM REST. ops get/post/put/delete (writes need confirm). Auth: FUSION_LIFECYCLE_BASE_URL + FUSION_LIFECYCLE_TOKEN."),
    dict(name="aras_innovator", description="Aras Innovator PLM REST. ops get/post/put/delete (writes need confirm). Auth: ARAS_INNOVATOR_BASE_URL + ARAS_INNOVATOR_TOKEN."),
    dict(name="propel_plm", description="Propel PLM (built on Salesforce) REST. ops get/post/put/delete (writes need confirm). Auth: PROPEL_PLM_BASE_URL + PROPEL_PLM_TOKEN."),
    dict(name="arena_plm", description="PTC Arena PLM REST. ops get/post/put/delete (writes need confirm). Auth: ARENA_PLM_BASE_URL + ARENA_PLM_TOKEN."),
    dict(name="openbom", description="OpenBOM bill-of-materials REST. ops get/post/put/delete (writes need confirm). Auth: OPENBOM_BASE_URL (https://developers.openbom.com) + OPENBOM_TOKEN."),
    dict(name="duro_plm", description="Duro PLM REST. ops get/post/put/delete (writes need confirm). Auth: DURO_PLM_BASE_URL + DURO_PLM_TOKEN."),
    dict(name="koerber_wms", description="Koerber Supply Chain (HighJump) WMS REST. ops get/post/put/delete (writes need confirm). Auth: KOERBER_WMS_BASE_URL + KOERBER_WMS_TOKEN."),
    dict(name="snapfulfil", description="SnapFulfil WMS REST. ops get/post/put/delete (writes need confirm). Auth: SNAPFULFIL_BASE_URL + SNAPFULFIL_TOKEN."),
    dict(name="made4net", description="Made4net WMS REST. ops get/post/put/delete (writes need confirm). Auth: MADE4NET_BASE_URL + MADE4NET_TOKEN."),
    dict(name="softeon", description="Softeon WMS REST. ops get/post/put/delete (writes need confirm). Auth: SOFTEON_BASE_URL + SOFTEON_TOKEN."),
    dict(name="synapse_wms", description="Synapse WMS REST. ops get/post/put/delete (writes need confirm). Auth: SYNAPSE_WMS_BASE_URL + SYNAPSE_WMS_TOKEN."),
    dict(name="deposco", description="Deposco Bright Suite WMS/OMS REST. ops get/post/put/delete (writes need confirm). Auth: DEPOSCO_BASE_URL + DEPOSCO_TOKEN."),
    dict(name="logiwa", description="Logiwa WMS REST. ops get/post/put/delete (writes need confirm). Auth: LOGIWA_BASE_URL + LOGIWA_TOKEN."),
    dict(name="shiphero", description="ShipHero WMS GraphQL. ops get/post/put/delete (writes need confirm). Auth: SHIPHERO_BASE_URL (https://public-api.shiphero.com/graphql) + SHIPHERO_TOKEN."),
    dict(name="oracle_wms_cloud", description="Oracle WMS Cloud REST. ops get/post/put/delete (writes need confirm). Auth: ORACLE_WMS_CLOUD_BASE_URL + ORACLE_WMS_CLOUD_TOKEN."),
    dict(name="sap_ewm", description="SAP Extended Warehouse Management REST. ops get/post/put/delete (writes need confirm). Auth: SAP_EWM_BASE_URL + SAP_EWM_TOKEN."),
    dict(name="zebra_reflexis", description="Zebra Reflexis workforce/task management REST. ops get/post/put/delete (writes need confirm). Auth: ZEBRA_REFLEXIS_BASE_URL + ZEBRA_REFLEXIS_TOKEN."),
    dict(name="kinaxis", description="Kinaxis RapidResponse supply-chain planning REST. ops get/post/put/delete (writes need confirm). Auth: KINAXIS_BASE_URL + KINAXIS_TOKEN."),
    dict(name="o9_solutions", description="o9 Solutions integrated planning REST. ops get/post/put/delete (writes need confirm). Auth: O9_SOLUTIONS_BASE_URL + O9_SOLUTIONS_TOKEN."),
    dict(name="omp_plus", description="OMP Plus supply-chain planning REST. ops get/post/put/delete (writes need confirm). Auth: OMP_PLUS_BASE_URL + OMP_PLUS_TOKEN."),
    dict(name="logility", description="Logility Digital Supply Chain REST. ops get/post/put/delete (writes need confirm). Auth: LOGILITY_BASE_URL + LOGILITY_TOKEN."),
    dict(name="toolsgroup", description="ToolsGroup demand/supply planning REST. ops get/post/put/delete (writes need confirm). Auth: TOOLSGROUP_BASE_URL + TOOLSGROUP_TOKEN."),
    dict(name="atlas_planning", description="John Galt Solutions Atlas Planning Platform REST. ops get/post/put/delete (writes need confirm). Auth: ATLAS_PLANNING_BASE_URL + ATLAS_PLANNING_TOKEN."),
    dict(name="sap_ibp", description="SAP Integrated Business Planning REST. ops get/post/put/delete (writes need confirm). Auth: SAP_IBP_BASE_URL + SAP_IBP_TOKEN."),
    dict(name="oracle_scm_cloud", description="Oracle SCM Cloud REST. ops get/post/put/delete (writes need confirm). Auth: ORACLE_SCM_CLOUD_BASE_URL + ORACLE_SCM_CLOUD_TOKEN."),
    dict(name="e2open", description="E2open supply-chain management REST. ops get/post/put/delete (writes need confirm). Auth: E2OPEN_BASE_URL + E2OPEN_TOKEN."),
    dict(name="arkieva", description="Arkieva supply-chain planning REST. ops get/post/put/delete (writes need confirm). Auth: ARKIEVA_BASE_URL + ARKIEVA_TOKEN."),
    dict(name="slimstock", description="Slimstock (Slim4) inventory optimization REST. ops get/post/put/delete (writes need confirm). Auth: SLIMSTOCK_BASE_URL + SLIMSTOCK_TOKEN."),
    dict(name="servicemax", description="ServiceMax field service management REST. ops get/post/put/delete (writes need confirm). Auth: SERVICEMAX_BASE_URL + SERVICEMAX_TOKEN."),
    dict(name="dynamics_fsm", description="Microsoft Dynamics 365 Field Service REST. ops get/post/put/delete (writes need confirm). Auth: DYNAMICS_FSM_BASE_URL + DYNAMICS_FSM_TOKEN."),
    dict(name="fieldedge", description="FieldEdge field service management REST. ops get/post/put/delete (writes need confirm). Auth: FIELDEDGE_BASE_URL + FIELDEDGE_TOKEN."),
    dict(name="fieldaware", description="FieldAware field service management REST. ops get/post/put/delete (writes need confirm). Auth: FIELDAWARE_BASE_URL + FIELDAWARE_TOKEN."),
    dict(name="astea_alliance", description="Astea Alliance field service management REST. ops get/post/put/delete (writes need confirm). Auth: ASTEA_ALLIANCE_BASE_URL + ASTEA_ALLIANCE_TOKEN."),
    dict(name="praxedo", description="Praxedo field service management REST. ops get/post/put/delete (writes need confirm). Auth: PRAXEDO_BASE_URL + PRAXEDO_TOKEN."),
    dict(name="oneserve", description="Oneserve field service management REST. ops get/post/put/delete (writes need confirm). Auth: ONESERVE_BASE_URL + ONESERVE_TOKEN."),
    dict(name="mhelpdesk", description="mHelpDesk field service management REST. ops get/post/put/delete (writes need confirm). Auth: MHELPDESK_BASE_URL + MHELPDESK_TOKEN."),
    dict(name="workwave", description="WorkWave field service/routing REST. ops get/post/put/delete (writes need confirm). Auth: WORKWAVE_BASE_URL + WORKWAVE_TOKEN."),
    dict(name="fieldwire", description="Fieldwire construction field management REST. ops get/post/put/delete (writes need confirm). Auth: FIELDWIRE_BASE_URL + FIELDWIRE_TOKEN."),
    dict(name="simpro", description="simPRO field service/job management REST. ops get/post/put/delete (writes need confirm). Auth: SIMPRO_BASE_URL + SIMPRO_TOKEN."),
    dict(name="fergus", description="Fergus job management for trades REST. ops get/post/put/delete (writes need confirm). Auth: FERGUS_BASE_URL + FERGUS_TOKEN."),
    dict(name="bigchange", description="BigChange (JobWatch) field service REST. ops get/post/put/delete (writes need confirm). Auth: BIGCHANGE_BASE_URL + BIGCHANGE_TOKEN."),
    dict(name="fieldpoint", description="Fieldpoint field service management REST. ops get/post/put/delete (writes need confirm). Auth: FIELDPOINT_BASE_URL + FIELDPOINT_TOKEN."),
    dict(name="vonigo", description="Vonigo field service scheduling REST. ops get/post/put/delete (writes need confirm). Auth: VONIGO_BASE_URL + VONIGO_TOKEN."),
    dict(name="thingworx", description="PTC ThingWorx industrial IoT REST. ops get/post/put/delete (writes need confirm). Auth: THINGWORX_BASE_URL + THINGWORX_TOKEN (appKey)."),
    dict(name="losant", description="Losant industrial IoT platform REST. ops get/post/put/delete (writes need confirm). Auth: LOSANT_BASE_URL (https://api.losant.com) + LOSANT_TOKEN."),
    dict(name="particle_iot", description="Particle IoT device cloud REST. ops get/post/put/delete (writes need confirm). Auth: PARTICLE_IOT_BASE_URL (https://api.particle.io) + PARTICLE_IOT_TOKEN."),
    dict(name="cumulocity", description="Software AG Cumulocity IoT REST. ops get/post/put/delete (writes need confirm). Auth: CUMULOCITY_BASE_URL + CUMULOCITY_TOKEN."),
    dict(name="ubidots", description="Ubidots IoT platform REST. ops get/post/put/delete (writes need confirm). Auth: UBIDOTS_BASE_URL (https://industrial.api.ubidots.com) + UBIDOTS_TOKEN.", token_header="X-Auth-Token", scheme=""),
    dict(name="clearblade", description="ClearBlade industrial IoT edge platform REST. ops get/post/put/delete (writes need confirm). Auth: CLEARBLADE_BASE_URL + CLEARBLADE_TOKEN."),
    dict(name="foghorn", description="FogHorn Systems edge intelligence REST. ops get/post/put/delete (writes need confirm). Auth: FOGHORN_BASE_URL + FOGHORN_TOKEN."),
    dict(name="c3ai", description="C3 AI enterprise AI platform REST. ops get/post/put/delete (writes need confirm). Auth: C3AI_BASE_URL + C3AI_TOKEN."),
    dict(name="uptake", description="Uptake industrial asset performance REST. ops get/post/put/delete (writes need confirm). Auth: UPTAKE_BASE_URL + UPTAKE_TOKEN."),
    dict(name="seeq", description="Seeq industrial process data analytics REST. ops get/post/put/delete (writes need confirm). Auth: SEEQ_BASE_URL + SEEQ_TOKEN."),
    dict(name="sight_machine", description="Sight Machine manufacturing analytics REST. ops get/post/put/delete (writes need confirm). Auth: SIGHT_MACHINE_BASE_URL + SIGHT_MACHINE_TOKEN."),
    dict(name="kepware", description="PTC Kepware KEPServerEX industrial connectivity REST. ops get/post/put/delete (writes need confirm). Auth: KEPWARE_BASE_URL + KEPWARE_TOKEN."),
    dict(name="ignition_scada", description="Inductive Automation Ignition SCADA REST. ops get/post/put/delete (writes need confirm). Auth: IGNITION_SCADA_BASE_URL + IGNITION_SCADA_TOKEN."),
    dict(name="canary_labs", description="Canary Labs industrial process historian REST. ops get/post/put/delete (writes need confirm). Auth: CANARY_LABS_BASE_URL + CANARY_LABS_TOKEN."),
    dict(name="maximo", description="IBM Maximo Application Suite (EAM) REST. ops get/post/put/delete (writes need confirm). Auth: MAXIMO_BASE_URL + MAXIMO_TOKEN (apikey)."),
    dict(name="infor_eam", description="Infor EAM asset management REST. ops get/post/put/delete (writes need confirm). Auth: INFOR_EAM_BASE_URL + INFOR_EAM_TOKEN."),
    dict(name="upkeep", description="UpKeep CMMS REST. ops get/post/put/delete (writes need confirm). Auth: UPKEEP_BASE_URL (https://api.onupkeep.com) + UPKEEP_TOKEN."),
    dict(name="fiix_cmms", description="Fiix CMMS (Rockwell) REST. ops get/post/put/delete (writes need confirm). Auth: FIIX_CMMS_BASE_URL + FIIX_CMMS_TOKEN."),
    dict(name="limble_cmms", description="Limble CMMS REST. ops get/post/put/delete (writes need confirm). Auth: LIMBLE_CMMS_BASE_URL + LIMBLE_CMMS_TOKEN.", basic=True),
    dict(name="emaint", description="eMaint (Fluke) CMMS REST. ops get/post/put/delete (writes need confirm). Auth: EMAINT_BASE_URL + EMAINT_TOKEN."),
    dict(name="hippo_cmms", description="Hippo CMMS REST. ops get/post/put/delete (writes need confirm). Auth: HIPPO_CMMS_BASE_URL + HIPPO_CMMS_TOKEN."),
    dict(name="micromain", description="MicroMain CMMS REST. ops get/post/put/delete (writes need confirm). Auth: MICROMAIN_BASE_URL + MICROMAIN_TOKEN."),
    dict(name="fracttal", description="Fracttal One CMMS REST. ops get/post/put/delete (writes need confirm). Auth: FRACTTAL_BASE_URL + FRACTTAL_TOKEN."),
    dict(name="loftware", description="Loftware enterprise labeling REST. ops get/post/put/delete (writes need confirm). Auth: LOFTWARE_BASE_URL + LOFTWARE_TOKEN."),
    dict(name="bartender_label", description="Seagull Scientific BarTender label management REST. ops get/post/put/delete (writes need confirm). Auth: BARTENDER_LABEL_BASE_URL + BARTENDER_LABEL_TOKEN."),
    dict(name="nicelabel", description="NiceLabel Cloud label management REST. ops get/post/put/delete (writes need confirm). Auth: NICELABEL_BASE_URL + NICELABEL_TOKEN."),
    dict(name="mastercontrol", description="MasterControl quality management REST. ops get/post/put/delete (writes need confirm). Auth: MASTERCONTROL_BASE_URL + MASTERCONTROL_TOKEN."),
    dict(name="trackwise", description="Sparta Systems TrackWise quality management REST. ops get/post/put/delete (writes need confirm). Auth: TRACKWISE_BASE_URL + TRACKWISE_TOKEN."),
    dict(name="veeva_vault_qms", description="Veeva Vault QMS REST. ops get/post/put/delete (writes need confirm). Auth: VEEVA_VAULT_QMS_BASE_URL + VEEVA_VAULT_QMS_TOKEN."),
    dict(name="intelex", description="Intelex EHS/quality management REST. ops get/post/put/delete (writes need confirm). Auth: INTELEX_BASE_URL + INTELEX_TOKEN."),
    dict(name="etq_reliance", description="ETQ Reliance quality management REST. ops get/post/put/delete (writes need confirm). Auth: ETQ_RELIANCE_BASE_URL + ETQ_RELIANCE_TOKEN."),
    dict(name="assurx", description="AssurX quality/compliance management REST. ops get/post/put/delete (writes need confirm). Auth: ASSURX_BASE_URL + ASSURX_TOKEN."),
    dict(name="qad", description="QAD Adaptive ERP REST. ops get/post/put/delete (writes need confirm). Auth: QAD_BASE_URL + QAD_TOKEN."),
    dict(name="syspro", description="SYSPRO ERP REST. ops get/post/put/delete (writes need confirm). Auth: SYSPRO_BASE_URL + SYSPRO_TOKEN."),
    dict(name="onshape", description="Onshape (PTC) cloud CAD/PLM REST. ops get/post/put/delete (writes need confirm). Auth: ONSHAPE_BASE_URL (https://cad.onshape.com) + ONSHAPE_TOKEN."),
    dict(name="cognite", description="Cognite Data Fusion industrial DataOps REST. ops get/post/put/delete (writes need confirm). Auth: COGNITE_BASE_URL + COGNITE_TOKEN."),
    dict(name="augury", description="Augury machine health monitoring REST. ops get/post/put/delete (writes need confirm). Auth: AUGURY_BASE_URL + AUGURY_TOKEN."),
    dict(name="falkonry", description="Falkonry predictive industrial analytics REST. ops get/post/put/delete (writes need confirm). Auth: FALKONRY_BASE_URL + FALKONRY_TOKEN."),
    dict(name="teletrac_navman", description="Teletrac Navman fleet/telematics REST. ops get/post/put/delete (writes need confirm). Auth: TELETRAC_NAVMAN_BASE_URL + TELETRAC_NAVMAN_TOKEN."),
    dict(name="route4me", description="Route4Me route planning REST. ops get/post/put/delete (writes need confirm). Auth: ROUTE4ME_BASE_URL (https://api.route4me.com) + ROUTE4ME_TOKEN."),
    dict(name="tookan", description="Tookan field service/delivery management REST. ops get/post/put/delete (writes need confirm). Auth: TOOKAN_BASE_URL + TOOKAN_TOKEN."),
    dict(name="zinier", description="Zinier field service automation REST. ops get/post/put/delete (writes need confirm). Auth: ZINIER_BASE_URL + ZINIER_TOKEN."),
    dict(name="joblogic", description="Joblogic field service management REST. ops get/post/put/delete (writes need confirm). Auth: JOBLOGIC_BASE_URL + JOBLOGIC_TOKEN."),
    dict(name="commusoft", description="Commusoft field service management REST. ops get/post/put/delete (writes need confirm). Auth: COMMUSOFT_BASE_URL + COMMUSOFT_TOKEN."),
    dict(name="service_fusion", description="Service Fusion field service management REST. ops get/post/put/delete (writes need confirm). Auth: SERVICE_FUSION_BASE_URL + SERVICE_FUSION_TOKEN."),
    dict(name="kickserv", description="Kickserv field service/CRM REST. ops get/post/put/delete (writes need confirm). Auth: KICKSERV_BASE_URL + KICKSERV_TOKEN."),
    dict(name="corrigo", description="Corrigo (JLL Technologies) facilities work-order REST. ops get/post/put/delete (writes need confirm). Auth: CORRIGO_BASE_URL + CORRIGO_TOKEN."),
    dict(name="syncron", description="Syncron aftermarket/service parts REST. ops get/post/put/delete (writes need confirm). Auth: SYNCRON_BASE_URL + SYNCRON_TOKEN."),
    dict(name="infor_nexus", description="Infor Nexus supply-chain network REST. ops get/post/put/delete (writes need confirm). Auth: INFOR_NEXUS_BASE_URL + INFOR_NEXUS_TOKEN."),
    dict(name="infor_visual", description="Infor VISUAL manufacturing ERP REST. ops get/post/put/delete (writes need confirm). Auth: INFOR_VISUAL_BASE_URL + INFOR_VISUAL_TOKEN."),
    dict(name="global_shop_solutions", description="Global Shop Solutions ERP/MES REST. ops get/post/put/delete (writes need confirm). Auth: GLOBAL_SHOP_SOLUTIONS_BASE_URL + GLOBAL_SHOP_SOLUTIONS_TOKEN."),
    dict(name="jobboss", description="JobBOSS2 shop management REST. ops get/post/put/delete (writes need confirm). Auth: JOBBOSS_BASE_URL + JOBBOSS_TOKEN."),
    dict(name="e2_shop_system", description="Shoptech E2 Shop System REST. ops get/post/put/delete (writes need confirm). Auth: E2_SHOP_SYSTEM_BASE_URL + E2_SHOP_SYSTEM_TOKEN."),
    dict(name="fulcrum_mfg", description="Fulcrum manufacturing operations platform REST. ops get/post/put/delete (writes need confirm). Auth: FULCRUM_MFG_BASE_URL + FULCRUM_MFG_TOKEN."),
    dict(name="mrpeasy", description="MRPeasy manufacturing/MRP REST. ops get/post/put/delete (writes need confirm). Auth: MRPEASY_BASE_URL + MRPEASY_TOKEN."),
    dict(name="prodsmart", description="Prodsmart (Autodesk) shop-floor MES REST. ops get/post/put/delete (writes need confirm). Auth: PRODSMART_BASE_URL + PRODSMART_TOKEN."),
    dict(name="worximity", description="Worximity manufacturing performance REST. ops get/post/put/delete (writes need confirm). Auth: WORXIMITY_BASE_URL + WORXIMITY_TOKEN."),
    dict(name="l2l", description="L2L connected worker/andon manufacturing platform REST. ops get/post/put/delete (writes need confirm). Auth: L2L_BASE_URL + L2L_TOKEN."),
    dict(name="bubble_io", description="Bubble.io Data API REST. ops get/post/put/delete (writes need confirm). e.g. /api/1.1/obj/{type}. Auth: BUBBLE_IO_BASE_URL (https://{app}.bubbleapps.io) + BUBBLE_IO_TOKEN."),
    dict(name="glide_apps", description="Glide Apps Tables REST. ops get/post/put/delete (writes need confirm). e.g. /api/function/mutateTables. Auth: GLIDE_APPS_BASE_URL (https://api.glideapps.com) + GLIDE_APPS_TOKEN."),
    dict(name="softr", description="Softr REST. ops get/post/put/delete (writes need confirm). Auth: SOFTR_BASE_URL (https://api.softr.io) + SOFTR_TOKEN (API key)."),
    dict(name="stacker", description="Stacker REST. ops get/post/put/delete (writes need confirm). Auth: STACKER_BASE_URL (https://{app}.stackerhq.com) + STACKER_TOKEN."),
    dict(name="adalo", description="Adalo Collections REST. ops get/post/put/delete (writes need confirm). e.g. /v0/apps/{appId}/collections/{collectionId}. Auth: ADALO_BASE_URL (https://api.adalo.com) + ADALO_TOKEN."),
    dict(name="knack", description="Knack REST. ops get/post/put/delete (writes need confirm). e.g. /v1/objects/{key}/records. Auth: KNACK_BASE_URL (https://api.knack.com) + KNACK_TOKEN (application/API key)."),
    dict(name="quickbase", token_header="QB-User-Token", scheme="", description="Quickbase REST. ops get/post/put/delete (writes need confirm). e.g. /v1/records/query, /v1/records. Auth: QUICKBASE_BASE_URL (https://api.quickbase.com) + QUICKBASE_TOKEN (user token)."),
    dict(name="caspio", description="Caspio Bridge REST. ops get/post/put/delete (writes need confirm). e.g. /rest/v2/tables/{table}/records. Auth: CASPIO_BASE_URL (https://{account}.caspio.com) + CASPIO_TOKEN (OAuth token)."),
    dict(name="betty_blocks", description="Betty Blocks REST. ops get/post/put/delete (writes need confirm). Auth: BETTY_BLOCKS_BASE_URL + BETTY_BLOCKS_TOKEN."),
    dict(name="outsystems", description="OutSystems REST (Integration Studio / Forge APIs). ops get/post/put/delete (writes need confirm). Auth: OUTSYSTEMS_BASE_URL (https://{env}.outsystemscloud.com) + OUTSYSTEMS_TOKEN."),
    dict(name="mendix", description="Mendix REST (Runtime / Platform API). ops get/post/put/delete (writes need confirm). Auth: MENDIX_BASE_URL (https://{app}.mendixcloud.com) + MENDIX_TOKEN."),
    dict(name="appian", description="Appian REST (Integration / Records API). ops get/post/put/delete (writes need confirm). Auth: APPIAN_BASE_URL (https://{site}.appiancloud.com) + APPIAN_TOKEN (API key)."),
    dict(name="powerapps", description="Microsoft Power Apps / Dataverse REST. ops get/post/put/delete (writes need confirm). e.g. /api/data/v9.2/. Auth: POWERAPPS_BASE_URL (https://{org}.crm.dynamics.com) + POWERAPPS_TOKEN (OAuth bearer)."),
    dict(name="retool", description="Retool REST (Workflows / Resources API). ops get/post/put/delete (writes need confirm). Auth: RETOOL_BASE_URL (https://api.retool.com) + RETOOL_TOKEN."),
    dict(name="appsheet", description="Google AppSheet REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/apps/{appId}/tables/{table}/Action. Auth: APPSHEET_BASE_URL (https://api.appsheet.com) + APPSHEET_TOKEN (application access key)."),
    dict(name="nocodb", token_header="xc-token", scheme="", description="NocoDB REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/tables/{tableId}/records. Auth: NOCODB_BASE_URL (self-hosted or https://app.nocodb.com) + NOCODB_TOKEN."),
    dict(name="baserow", scheme="Token", description="Baserow REST. ops get/post/put/delete (writes need confirm). e.g. /api/database/rows/table/{table_id}/. Auth: BASEROW_BASE_URL (https://api.baserow.io) + BASEROW_TOKEN."),
    dict(name="rows_com", description="Rows.com REST API. ops get/post/put/delete (writes need confirm). Auth: ROWS_COM_BASE_URL (https://api.rows.com) + ROWS_COM_TOKEN."),
    dict(name="seatable", scheme="Token", description="SeaTable REST API. ops get/post/put/delete (writes need confirm). e.g. /api-gateway/api/v2/dtables/{dtable_uuid}/rows/. Auth: SEATABLE_BASE_URL (https://cloud.seatable.io) + SEATABLE_TOKEN."),
    dict(name="tadabase", description="Tadabase REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v1/data-tables/{table_id}/records. Auth: TADABASE_BASE_URL (https://api.tadabase.io) + TADABASE_TOKEN (App/API key pair)."),
    dict(name="zoho_creator", description="Zoho Creator REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2.1/{account}/{app}/report/{report}. Auth: ZOHO_CREATOR_BASE_URL (https://creator.zoho.com) + ZOHO_CREATOR_TOKEN (OAuth bearer)."),
    dict(name="fillout", description="Fillout Forms REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/api/forms/{formId}/submissions. Auth: FILLOUT_BASE_URL (https://api.fillout.com) + FILLOUT_TOKEN."),
    dict(name="tally_forms", description="Tally Forms REST API. ops get/post/put/delete (writes need confirm). e.g. /api/forms/{formId}/submissions. Auth: TALLY_FORMS_BASE_URL (https://api.tally.so) + TALLY_FORMS_TOKEN."),
    dict(name="pory", description="Pory REST API (no-code website builder from data). ops get/post/put/delete (writes need confirm). Auth: PORY_BASE_URL + PORY_TOKEN."),
    dict(name="thunkable", description="Thunkable REST API (app builder resources). ops get/post/put/delete (writes need confirm). Auth: THUNKABLE_BASE_URL + THUNKABLE_TOKEN."),
    dict(name="draftbit", description="Draftbit REST API. ops get/post/put/delete (writes need confirm). Auth: DRAFTBIT_BASE_URL + DRAFTBIT_TOKEN."),
    dict(name="flutterflow", description="FlutterFlow REST API. ops get/post/put/delete (writes need confirm). Auth: FLUTTERFLOW_BASE_URL (https://api.flutterflow.io) + FLUTTERFLOW_TOKEN."),
    dict(name="anvil_works", description="Anvil (anvil.works) REST/App Server API. ops get/post/put/delete (writes need confirm). Auth: ANVIL_WORKS_BASE_URL (https://anvil.works) + ANVIL_WORKS_TOKEN."),
    dict(name="pipedream", description="Pipedream REST API (workflows/sources). ops get/post/put/delete (writes need confirm). e.g. /v1/workflows, /v1/sources. Auth: PIPEDREAM_BASE_URL (https://api.pipedream.com) + PIPEDREAM_TOKEN."),
    dict(name="mable_iot", description="Formstack Forms REST API. ops get/post/put/delete (writes need confirm). e.g. /api/v2/form/{id}/submission.json. Auth: MABLE_IOT_BASE_URL + MABLE_IOT_TOKEN."),
    dict(name="lightning_platform", description="Salesforce Lightning Platform (Force.com) REST API. ops get/post/put/delete (writes need confirm). e.g. /services/data/v60.0/sobjects/. Auth: LIGHTNING_PLATFORM_BASE_URL (https://{org}.my.salesforce.com) + LIGHTNING_PLATFORM_TOKEN (OAuth bearer)."),
    dict(name="huggingface_inference", description="Hugging Face Inference API / Inference Endpoints REST. ops get/post/put/delete (writes need confirm). e.g. /models/{model}, https://api-inference.huggingface.co/models/. Auth: HUGGINGFACE_INFERENCE_BASE_URL (https://api-inference.huggingface.co) + HUGGINGFACE_INFERENCE_TOKEN."),
    dict(name="replicate_ai", scheme="Token", description="Replicate REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/predictions, /v1/models. Auth: REPLICATE_AI_BASE_URL (https://api.replicate.com) + REPLICATE_AI_TOKEN."),
    dict(name="openai_platform", description="OpenAI Platform REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions, /v1/models. Auth: OPENAI_PLATFORM_BASE_URL (https://api.openai.com) + OPENAI_PLATFORM_TOKEN."),
    dict(name="anthropic_api", token_header="x-api-key", scheme="", description="Anthropic Messages REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/messages, /v1/models. Auth: ANTHROPIC_API_BASE_URL (https://api.anthropic.com) + ANTHROPIC_API_TOKEN."),
    dict(name="cohere_ai", description="Cohere REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/chat, /v1/embed. Auth: COHERE_AI_BASE_URL (https://api.cohere.com) + COHERE_AI_TOKEN."),
    dict(name="mistral_ai", description="Mistral AI REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions, /v1/embeddings. Auth: MISTRAL_AI_BASE_URL (https://api.mistral.ai) + MISTRAL_AI_TOKEN."),
    dict(name="stability_ai", description="Stability AI REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/generation/{engine_id}/text-to-image. Auth: STABILITY_AI_BASE_URL (https://api.stability.ai) + STABILITY_AI_TOKEN."),
    dict(name="elevenlabs", token_header="xi-api-key", scheme="", description="ElevenLabs REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/text-to-speech/{voice_id}, /v1/voices. Auth: ELEVENLABS_BASE_URL (https://api.elevenlabs.io) + ELEVENLABS_TOKEN."),
    dict(name="assemblyai", description="AssemblyAI REST API. ops get/post/put/delete (writes need confirm). e.g. /v2/transcript. Auth: ASSEMBLYAI_BASE_URL (https://api.assemblyai.com) + ASSEMBLYAI_TOKEN."),
    dict(name="deepgram", scheme="Token", description="Deepgram REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/listen, /v1/speak. Auth: DEEPGRAM_BASE_URL (https://api.deepgram.com) + DEEPGRAM_TOKEN."),
    dict(name="runwayml", description="Runway ML REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/image_to_video. Auth: RUNWAYML_BASE_URL (https://api.dev.runwayml.com) + RUNWAYML_TOKEN."),
    dict(name="fal_ai", scheme="Key", description="fal.ai REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/run/{model}. Auth: FAL_AI_BASE_URL (https://fal.run) + FAL_AI_TOKEN."),
    dict(name="together_ai", description="Together AI REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions, /v1/models. Auth: TOGETHER_AI_BASE_URL (https://api.together.xyz) + TOGETHER_AI_TOKEN."),
    dict(name="fireworks_ai", description="Fireworks AI REST API. ops get/post/put/delete (writes need confirm). e.g. /inference/v1/chat/completions. Auth: FIREWORKS_AI_BASE_URL (https://api.fireworks.ai) + FIREWORKS_AI_TOKEN."),
    dict(name="groq_cloud", description="Groq Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /openai/v1/chat/completions. Auth: GROQ_CLOUD_BASE_URL (https://api.groq.com) + GROQ_CLOUD_TOKEN."),
    dict(name="perplexity_ai", description="Perplexity AI REST API. ops get/post/put/delete (writes need confirm). e.g. /chat/completions. Auth: PERPLEXITY_AI_BASE_URL (https://api.perplexity.ai) + PERPLEXITY_AI_TOKEN."),
    dict(name="anyscale_endpoints", description="Anyscale Endpoints REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions. Auth: ANYSCALE_ENDPOINTS_BASE_URL (https://api.endpoints.anyscale.com) + ANYSCALE_ENDPOINTS_TOKEN."),
    dict(name="baseten", description="Baseten REST API (model deployment/inference). ops get/post/put/delete (writes need confirm). e.g. /v1/models/{id}/predict. Auth: BASETEN_BASE_URL (https://app.baseten.co) + BASETEN_TOKEN (Api-Key)."),
    dict(name="modal_labs", description="Modal REST API (serverless functions/model deployments). ops get/post/put/delete (writes need confirm). Auth: MODAL_LABS_BASE_URL (https://api.modal.com) + MODAL_LABS_TOKEN."),
    dict(name="banana_dev", description="Banana.dev REST API (model inference). ops get/post/put/delete (writes need confirm). e.g. /start/v4/. Auth: BANANA_DEV_BASE_URL (https://api.banana.dev) + BANANA_DEV_TOKEN."),
    dict(name="deepinfra", description="DeepInfra REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/openai/chat/completions. Auth: DEEPINFRA_BASE_URL (https://api.deepinfra.com) + DEEPINFRA_TOKEN."),
    dict(name="octoai", description="OctoAI REST API (model inference). ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions. Auth: OCTOAI_BASE_URL (https://text.octoai.run) + OCTOAI_TOKEN."),
    dict(name="clarifai", scheme="Key", description="Clarifai REST API (AI/ML model hosting). ops get/post/put/delete (writes need confirm). e.g. /v2/models/{model_id}/outputs. Auth: CLARIFAI_BASE_URL (https://api.clarifai.com) + CLARIFAI_TOKEN (PAT)."),
    dict(name="nlp_cloud", description="NLP Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/{model}/generation. Auth: NLP_CLOUD_BASE_URL (https://api.nlpcloud.io) + NLP_CLOUD_TOKEN."),
    dict(name="ai21_labs", description="AI21 Labs REST API. ops get/post/put/delete (writes need confirm). e.g. /studio/v1/{model}/complete. Auth: AI21_LABS_BASE_URL (https://api.ai21.com) + AI21_LABS_TOKEN."),
    dict(name="google_vertex_ai", description="Google Cloud Vertex AI REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/projects/{project}/locations/{loc}/publishers/google/models/{model}:predict. Auth: GOOGLE_VERTEX_AI_BASE_URL (https://{region}-aiplatform.googleapis.com) + GOOGLE_VERTEX_AI_TOKEN (OAuth bearer)."),
    dict(name="azure_openai", token_header="api-key", scheme="", description="Azure OpenAI Service REST API. ops get/post/put/delete (writes need confirm). e.g. /openai/deployments/{deployment}/chat/completions. Auth: AZURE_OPENAI_BASE_URL (https://{resource}.openai.azure.com) + AZURE_OPENAI_TOKEN."),
    dict(name="aws_sagemaker", description="AWS SageMaker Runtime REST API (model inference). ops get/post/put/delete (writes need confirm). e.g. /endpoints/{name}/invocations. Auth: AWS_SAGEMAKER_BASE_URL (https://runtime.sagemaker.{region}.amazonaws.com) + AWS_SAGEMAKER_TOKEN (SigV4 credentials)."),
    dict(name="ibm_watsonx", description="IBM watsonx.ai REST API. ops get/post/put/delete (writes need confirm). e.g. /ml/v1/text/generation. Auth: IBM_WATSONX_BASE_URL (https://{region}.ml.cloud.ibm.com) + IBM_WATSONX_TOKEN."),
    dict(name="nvidia_nim", description="NVIDIA NIM/NGC REST API (model inference). ops get/post/put/delete (writes need confirm). e.g. /v1/chat/completions. Auth: NVIDIA_NIM_BASE_URL (https://integrate.api.nvidia.com) + NVIDIA_NIM_TOKEN."),
    dict(name="scale_ai", description="Scale AI REST API (data/model platform). ops get/post/put/delete (writes need confirm). Auth: SCALE_AI_BASE_URL (https://api.scale.com) + SCALE_AI_TOKEN."),
    dict(name="runpod", description="RunPod REST/GraphQL API (GPU inference endpoints). ops get/post/put/delete (writes need confirm). e.g. /v2/{endpoint_id}/run. Auth: RUNPOD_BASE_URL (https://api.runpod.ai) + RUNPOD_TOKEN."),
    dict(name="lambda_labs_cloud", description="Lambda Labs Cloud REST API (GPU instances). ops get/post/put/delete (writes need confirm). e.g. /api/v1/instances. Auth: LAMBDA_LABS_CLOUD_BASE_URL (https://cloud.lambdalabs.com) + LAMBDA_LABS_CLOUD_TOKEN."),
    dict(name="vast_ai", description="Vast.ai REST API (GPU compute marketplace). ops get/post/put/delete (writes need confirm). e.g. /api/v0/instances/. Auth: VAST_AI_BASE_URL (https://console.vast.ai) + VAST_AI_TOKEN."),
    dict(name="coreweave", description="CoreWeave REST API (GPU cloud/model serving). ops get/post/put/delete (writes need confirm). Auth: COREWEAVE_BASE_URL + COREWEAVE_TOKEN."),
    dict(name="predibase", description="Predibase REST API (LLM fine-tuning/serving). ops get/post/put/delete (writes need confirm). Auth: PREDIBASE_BASE_URL (https://api.predibase.com) + PREDIBASE_TOKEN."),
    dict(name="mosaicml", description="MosaicML/Databricks Model Serving REST API. ops get/post/put/delete (writes need confirm). Auth: MOSAICML_BASE_URL + MOSAICML_TOKEN."),
    dict(name="wandb_weave", description="Weights & Biases REST API (experiment/model tracking). ops get/post/put/delete (writes need confirm). e.g. /graphql. Auth: WANDB_WEAVE_BASE_URL (https://api.wandb.ai) + WANDB_WEAVE_TOKEN."),
    dict(name="comet_ml", description="Comet ML REST API (experiment/model tracking). ops get/post/put/delete (writes need confirm). e.g. /api/rest/v2/. Auth: COMET_ML_BASE_URL (https://www.comet.com) + COMET_ML_TOKEN."),
    dict(name="neptune_ai", description="Neptune.ai REST API (ML experiment tracking). ops get/post/put/delete (writes need confirm). Auth: NEPTUNE_AI_BASE_URL (https://app.neptune.ai) + NEPTUNE_AI_TOKEN."),
    dict(name="clearml", description="ClearML REST API (MLOps platform). ops get/post/put/delete (writes need confirm). e.g. /api/v2.23/tasks.get_all. Auth: CLEARML_BASE_URL (https://api.clear.ml) + CLEARML_TOKEN."),
    dict(name="valohai", description="Valohai REST API (MLOps orchestration). ops get/post/put/delete (writes need confirm). e.g. /api/v0/executions/. Auth: VALOHAI_BASE_URL (https://app.valohai.com) + VALOHAI_TOKEN."),
    dict(name="pinecone", token_header="Api-Key", scheme="", description="Pinecone vector database REST API. ops get/post/put/delete (writes need confirm). e.g. /vectors/upsert, /query, /indexes. Auth: PINECONE_BASE_URL (https://api.pinecone.io) + PINECONE_TOKEN."),
    dict(name="weaviate", description="Weaviate REST API (vector database). ops get/post/put/delete (writes need confirm). e.g. /v1/objects, /v1/graphql. Auth: WEAVIATE_BASE_URL (https://{cluster}.weaviate.network) + WEAVIATE_TOKEN."),
    dict(name="qdrant", token_header="api-key", scheme="", description="Qdrant vector database REST API. ops get/post/put/delete (writes need confirm). e.g. /collections/{name}/points. Auth: QDRANT_BASE_URL (https://{cluster}.qdrant.io) + QDRANT_TOKEN."),
    dict(name="milvus_zilliz", description="Zilliz Cloud (Milvus) REST API. ops get/post/put/delete (writes need confirm). e.g. /v1/vector/insert, /v1/vector/search. Auth: MILVUS_ZILLIZ_BASE_URL (https://{cluster}.zillizcloud.com) + MILVUS_ZILLIZ_TOKEN."),
    dict(name="chroma_cloud", description="Chroma Cloud REST API (vector database). ops get/post/put/delete (writes need confirm). Auth: CHROMA_CLOUD_BASE_URL (https://api.trychroma.com) + CHROMA_CLOUD_TOKEN."),
    dict(name="pgvector_cloud", description="Vector-enabled Postgres (pgvector-as-a-service) REST gateway. ops get/post/put/delete (writes need confirm). Auth: PGVECTOR_CLOUD_BASE_URL + PGVECTOR_CLOUD_TOKEN."),
    dict(name="vespa_ai", description="Vespa.ai REST API (search/vector engine). ops get/post/put/delete (writes need confirm). e.g. /document/v1/, /search/. Auth: VESPA_AI_BASE_URL + VESPA_AI_TOKEN."),
    dict(name="marqo", description="Marqo REST API (vector search engine). ops get/post/put/delete (writes need confirm). e.g. /indexes/{index}/search. Auth: MARQO_BASE_URL (https://api.marqo.ai) + MARQO_TOKEN."),
    dict(name="vald_search", description="Vald REST/gRPC-gateway API (distributed vector search). ops get/post/put/delete (writes need confirm). Auth: VALD_SEARCH_BASE_URL + VALD_SEARCH_TOKEN."),
    dict(name="lancedb_cloud", description="LanceDB Cloud REST API (vector database). ops get/post/put/delete (writes need confirm). Auth: LANCEDB_CLOUD_BASE_URL (https://db.lancedb.com) + LANCEDB_CLOUD_TOKEN."),
    dict(name="vectara", scheme="Bearer", description="Vectara REST API (RAG-as-a-service search platform). ops get/post/put/delete (writes need confirm). e.g. /v2/corpora/{corpus}/query. Auth: VECTARA_BASE_URL (https://api.vectara.io) + VECTARA_TOKEN."),
    dict(name="pgvecto_serverless", description="Neon serverless Postgres vector REST gateway. ops get/post/put/delete (writes need confirm). Auth: PGVECTO_SERVERLESS_BASE_URL + PGVECTO_SERVERLESS_TOKEN."),
    dict(name="elasticsearch_vector", description="Elasticsearch/Elastic Cloud vector search REST API (dense_vector/kNN). ops get/post/put/delete (writes need confirm). e.g. /{index}/_search. Auth: ELASTICSEARCH_VECTOR_BASE_URL + ELASTICSEARCH_VECTOR_TOKEN."),
    dict(name="opensearch_vector", description="OpenSearch k-NN vector search REST API. ops get/post/put/delete (writes need confirm). e.g. /{index}/_search. Auth: OPENSEARCH_VECTOR_BASE_URL + OPENSEARCH_VECTOR_TOKEN."),
    dict(name="algolia_neuralsearch", description="Algolia NeuralSearch REST API (vector-enhanced search). ops get/post/put/delete (writes need confirm). e.g. /1/indexes/{index}/query. Auth: ALGOLIA_NEURALSEARCH_BASE_URL (https://{app_id}.algolia.net) + ALGOLIA_NEURALSEARCH_TOKEN (API key)."),
    dict(name="meilisearch_cloud", description="Meilisearch Cloud REST API. ops get/post/put/delete (writes need confirm). e.g. /indexes/{index}/search. Auth: MEILISEARCH_CLOUD_BASE_URL + MEILISEARCH_CLOUD_TOKEN."),
    dict(name="rockset_search", description="Rockset REST API (real-time search/analytics). ops get/post/put/delete (writes need confirm). e.g. /v1/orgs/self/ws/{workspace}/qlambdas. Auth: ROCKSET_SEARCH_BASE_URL (https://api.rs2.usw2.rockset.com) + ROCKSET_SEARCH_TOKEN."),
    dict(name="swiftype", description="Swiftype (Elastic App Search) REST API. ops get/post/put/delete (writes need confirm). e.g. /api/as/v1/engines/{engine}/search. Auth: SWIFTYPE_BASE_URL (https://api.swiftype.com) + SWIFTYPE_TOKEN."),
    dict(name="constructor_io", description="Constructor.io REST API (search/discovery). ops get/post/put/delete (writes need confirm). e.g. /search/{query}. Auth: CONSTRUCTOR_IO_BASE_URL (https://ac.cnstrc.com) + CONSTRUCTOR_IO_TOKEN."),
    dict(name="coveo", description="Coveo REST API (enterprise search/relevance). ops get/post/put/delete (writes need confirm). e.g. /rest/search/v2. Auth: COVEO_BASE_URL (https://platform.cloud.coveo.com) + COVEO_TOKEN."),
    dict(name="glean_search", description="Glean REST API (enterprise search/AI assistant). ops get/post/put/delete (writes need confirm). e.g. /rest/api/v1/search. Auth: GLEAN_SEARCH_BASE_URL (https://{instance}-be.glean.com) + GLEAN_SEARCH_TOKEN."),
    dict(name="deepset_cloud", description="deepset Cloud REST API (Haystack RAG pipelines). ops get/post/put/delete (writes need confirm). e.g. /api/v1/workspaces/{workspace}/pipelines. Auth: DEEPSET_CLOUD_BASE_URL (https://api.cloud.deepset.ai) + DEEPSET_CLOUD_TOKEN."),
    dict(name="langsmith", token_header="x-api-key", scheme="", description="LangSmith REST API (LLM tracing/eval platform). ops get/post/put/delete (writes need confirm). e.g. /runs, /datasets. Auth: LANGSMITH_BASE_URL (https://api.smith.langchain.com) + LANGSMITH_TOKEN."),
    dict(name="humanloop", description="Humanloop REST API (LLM eval/prompt management). ops get/post/put/delete (writes need confirm). e.g. /v5/prompts, /v5/logs. Auth: HUMANLOOP_BASE_URL (https://api.humanloop.com) + HUMANLOOP_TOKEN."),
    dict(name="braintrust_data", description="Braintrust REST API (LLM eval platform). ops get/post/put/delete (writes need confirm). e.g. /v1/project, /v1/experiment. Auth: BRAINTRUST_DATA_BASE_URL (https://api.braintrust.dev) + BRAINTRUST_DATA_TOKEN."),
    dict(name="galileo_ai", description="Galileo AI REST API (LLM observability/eval). ops get/post/put/delete (writes need confirm). Auth: GALILEO_AI_BASE_URL + GALILEO_AI_TOKEN."),
    dict(name="airbyte_cloud", description="Airbyte Cloud REST API (data integration/EL(T)). ops get/post/put/delete (writes need confirm). e.g. /v1/connections. Auth: AIRBYTE_CLOUD_BASE_URL (https://api.airbyte.com) + AIRBYTE_CLOUD_TOKEN."),
    dict(name="dify_ai", description="Dify REST API (LLM app builder). ops get/post/put/delete (writes need confirm). e.g. /v1/chat-messages, /v1/completion-messages. Auth: DIFY_AI_BASE_URL (https://api.dify.ai) + DIFY_AI_TOKEN."),
    dict(name="flowise_ai", description="Flowise REST API (LLM flow builder). ops get/post/put/delete (writes need confirm). e.g. /api/v1/prediction/{chatflow_id}. Auth: FLOWISE_AI_BASE_URL + FLOWISE_AI_TOKEN."),
    dict(name="langflow", description="Langflow REST API (visual LLM flow builder). ops get/post/put/delete (writes need confirm). e.g. /api/v1/run/{flow_id}. Auth: LANGFLOW_BASE_URL + LANGFLOW_TOKEN."),
    dict(name="voiceflow", description="Voiceflow REST API (conversational app builder). ops get/post/put/delete (writes need confirm). e.g. /state/user/{userID}/interact. Auth: VOICEFLOW_BASE_URL (https://general-runtime.voiceflow.com) + VOICEFLOW_TOKEN."),
    dict(name="landbot", description="Landbot REST API (no-code chatbot builder). ops get/post/put/delete (writes need confirm). e.g. /v1/customers, /v1/webhook. Auth: LANDBOT_BASE_URL (https://api.landbot.io) + LANDBOT_TOKEN."),
    dict(name="botpress", description="Botpress REST API (chatbot builder). ops get/post/put/delete (writes need confirm). e.g. /api/v1/bots/{botId}/converse. Auth: BOTPRESS_BASE_URL (https://api.botpress.cloud) + BOTPRESS_TOKEN."),
    dict(name="chatfuel", description="Chatfuel REST API (chatbot builder). ops get/post/put/delete (writes need confirm). e.g. /bots/{bot_id}/users. Auth: CHATFUEL_BASE_URL (https://api.chatfuel.com) + CHATFUEL_TOKEN."),
    dict(name="manychat", description="ManyChat REST API (chatbot builder). ops get/post/put/delete (writes need confirm). e.g. /fb/subscriber/getInfo. Auth: MANYCHAT_BASE_URL (https://api.manychat.com) + MANYCHAT_TOKEN."),
    dict(name="tars_chatbot", description="Tars REST API (no-code chatbot/landing page builder). ops get/post/put/delete (writes need confirm). Auth: TARS_CHATBOT_BASE_URL (https://api.hellotars.com) + TARS_CHATBOT_TOKEN."),
    dict(name="jotform_ai_agents", description="Jotform AI Agents REST API. ops get/post/put/delete (writes need confirm). e.g. /ai-agent-builder/agents. Auth: JOTFORM_AI_AGENTS_BASE_URL (https://api.jotform.com) + JOTFORM_AI_AGENTS_TOKEN."),
    dict(name="smartsuite", description="SmartSuite REST API (no-code work management/database). ops get/post/put/delete (writes need confirm). e.g. /api/v1/applications/{id}/records/list/. Auth: SMARTSUITE_BASE_URL (https://app.smartsuite.com) + SMARTSUITE_TOKEN."),
    dict(name="ninox", description="Ninox REST API (no-code database). ops get/post/put/delete (writes need confirm). e.g. /v1/teams/{teamId}/databases/{databaseId}/tables/{tableId}/records. Auth: NINOX_BASE_URL (https://api.ninox.com) + NINOX_TOKEN."),
    dict(name="memberstack", description="Memberstack REST API (no-code membership/auth). ops get/post/put/delete (writes need confirm). e.g. /v1/members. Auth: MEMBERSTACK_BASE_URL (https://admin.memberstack.com) + MEMBERSTACK_TOKEN."),
    dict(name="xano", description="Xano REST API (no-code backend/database). ops get/post/put/delete (writes need confirm). e.g. /api:{group}/{table}. Auth: XANO_BASE_URL (https://{instance}.xano.io) + XANO_TOKEN."),
    dict(name="backendless", description="Backendless REST API (no-code backend). ops get/post/put/delete (writes need confirm). e.g. /{appId}/{apiKey}/data/{table}. Auth: BACKENDLESS_BASE_URL (https://api.backendless.com) + BACKENDLESS_TOKEN."),
    dict(name="draftbase_no_code", description="Draftbase REST API (no-code web app builder). ops get/post/put/delete (writes need confirm). Auth: DRAFTBASE_NO_CODE_BASE_URL + DRAFTBASE_NO_CODE_TOKEN."),
    dict(name="tooljet", description="ToolJet REST API (low-code internal tool builder). ops get/post/put/delete (writes need confirm). e.g. /api/apps. Auth: TOOLJET_BASE_URL + TOOLJET_TOKEN."),
    dict(name="appgyver", description="SAP AppGyver REST API (no-code app builder). ops get/post/put/delete (writes need confirm). Auth: APPGYVER_BASE_URL + APPGYVER_TOKEN."),
    dict(name="internal_io", description="Internal.io REST API (low-code internal tools). ops get/post/put/delete (writes need confirm). Auth: INTERNAL_IO_BASE_URL (https://api.internal.io) + INTERNAL_IO_TOKEN."),
    dict(name="corteza", description="Corteza low-code REST API. ops get/post/put/delete (writes need confirm). e.g. /api/compose/namespace/{ns}/module/{mod}/record. Auth: CORTEZA_BASE_URL + CORTEZA_TOKEN."),
    dict(name="apitable", token_header="Authorization", scheme="Bearer", description="APITable REST API (Airtable-like spreadsheet database). ops get/post/put/delete (writes need confirm). e.g. /fusion/v1/datasheets/{datasheetId}/records. Auth: APITABLE_BASE_URL (https://api.apitable.com) + APITABLE_TOKEN."),
    dict(name="clay_run", description="Clay.run REST API (no-code data enrichment tables). ops get/post/put/delete (writes need confirm). Auth: CLAY_RUN_BASE_URL (https://api.clay.com) + CLAY_RUN_TOKEN."),
    dict(name="wonderchat", description="Wonderchat REST API (no-code AI chatbot builder). ops get/post/put/delete (writes need confirm). Auth: WONDERCHAT_BASE_URL + WONDERCHAT_TOKEN."),
    dict(name="chatbase", description="Chatbase REST API (no-code AI chatbot builder). ops get/post/put/delete (writes need confirm). e.g. /api/v1/chat. Auth: CHATBASE_BASE_URL (https://www.chatbase.co) + CHATBASE_TOKEN."),
    dict(name="crewai_studio", description="CrewAI Enterprise REST API (multi-agent orchestration platform). ops get/post/put/delete (writes need confirm). Auth: CREWAI_STUDIO_BASE_URL (https://app.crewai.com) + CREWAI_STUDIO_TOKEN."),
    dict(name="relevance_ai", description="Relevance AI REST API (no-code AI agent/workflow builder). ops get/post/put/delete (writes need confirm). e.g. /latest/agents/trigger. Auth: RELEVANCE_AI_BASE_URL (https://api-{region}.stack.tryrelevance.com) + RELEVANCE_AI_TOKEN."),
    dict(name="gumloop", description="Gumloop REST API (no-code AI automation builder). ops get/post/put/delete (writes need confirm). e.g. /api/v1/start_pipeline. Auth: GUMLOOP_BASE_URL (https://api.gumloop.com) + GUMLOOP_TOKEN."),
    dict(name="lindy_ai", description="Lindy AI REST API (no-code AI agent builder). ops get/post/put/delete (writes need confirm). Auth: LINDY_AI_BASE_URL (https://api.lindy.ai) + LINDY_AI_TOKEN."),
    dict(name="stack_ai", description="Stack AI REST API (no-code AI workflow builder). ops get/post/put/delete (writes need confirm). Auth: STACK_AI_BASE_URL (https://api.stack-ai.com) + STACK_AI_TOKEN."),
    dict(name="beam_cloud", description="Beam Cloud REST API (serverless GPU/model deployment). ops get/post/put/delete (writes need confirm). Auth: BEAM_CLOUD_BASE_URL (https://api.beam.cloud) + BEAM_CLOUD_TOKEN."),
    dict(name="cerebrium", description="Cerebrium REST API (serverless ML model deployment). ops get/post/put/delete (writes need confirm). Auth: CEREBRIUM_BASE_URL (https://rest.cerebrium.ai) + CEREBRIUM_TOKEN."),
    dict(name="ollama_cloud", description="Ollama Cloud REST API (self-hosted LLM inference). ops get/post/put/delete (writes need confirm). e.g. /api/generate, /api/chat. Auth: OLLAMA_CLOUD_BASE_URL + OLLAMA_CLOUD_TOKEN."),
    dict(name="voltage_park", description="Voltage Park REST API (GPU cloud compute). ops get/post/put/delete (writes need confirm). Auth: VOLTAGE_PARK_BASE_URL + VOLTAGE_PARK_TOKEN."),
    dict(name="paperspace_gradient", description="Paperspace Gradient REST API (ML model training/deployment). ops get/post/put/delete (writes need confirm). e.g. /deployments, /models. Auth: PAPERSPACE_GRADIENT_BASE_URL (https://api.paperspace.com) + PAPERSPACE_GRADIENT_TOKEN."),
    dict(name="floydhub", description="FloydHub REST API (ML model training platform). ops get/post/put/delete (writes need confirm). Auth: FLOYDHUB_BASE_URL (https://www.floydhub.com) + FLOYDHUB_TOKEN."),
    dict(name="determined_ai", description="Determined AI REST API (deep learning training platform). ops get/post/put/delete (writes need confirm). e.g. /api/v1/experiments. Auth: DETERMINED_AI_BASE_URL + DETERMINED_AI_TOKEN."),
    dict(name="raisers_edge", description="Blackbaud Raiser's Edge NXT REST (SKY API). ops get/post/put/delete (writes need confirm). Auth: RAISERS_EDGE_BASE_URL (https://api.sky.blackbaud.com) + RAISERS_EDGE_TOKEN."),
    dict(name="financial_edge", description="Blackbaud Financial Edge NXT REST (SKY API). ops get/post/put/delete (writes need confirm). Auth: FINANCIAL_EDGE_BASE_URL (https://api.sky.blackbaud.com) + FINANCIAL_EDGE_TOKEN."),
    dict(name="etapestry", description="Blackbaud eTapestry REST. ops get/post/put/delete (writes need confirm). Auth: ETAPESTRY_BASE_URL (https://www.etapestry.com) + ETAPESTRY_TOKEN."),
    dict(name="donorperfect", description="DonorPerfect REST. ops get/post/put/delete (writes need confirm). Auth: DONORPERFECT_BASE_URL (https://www.donorperfect.net) + DONORPERFECT_TOKEN."),
    dict(name="bloomerang", description="Bloomerang REST. ops get/post/put/delete (writes need confirm). Auth: BLOOMERANG_BASE_URL (https://api.bloomerang.co) + BLOOMERANG_TOKEN."),
    dict(name="neoncrm", description="Neon CRM (Neon One) REST. ops get/post/put/delete (writes need confirm). Auth: NEONCRM_BASE_URL (https://api.neoncrm.com) + NEONCRM_TOKEN."),
    dict(name="littlegreenlight", description="Little Green Light donor CRM REST. ops get/post/put/delete (writes need confirm). Auth: LITTLEGREENLIGHT_BASE_URL (https://api.littlegreenlight.com) + LITTLEGREENLIGHT_TOKEN."),
    dict(name="charityengine", description="CharityEngine REST. ops get/post/put/delete (writes need confirm). Auth: CHARITYENGINE_BASE_URL + CHARITYENGINE_TOKEN."),
    dict(name="salesforce_npsp", description="Salesforce Nonprofit Success Pack (NPSP) REST, on Salesforce REST API. ops get/post/put/delete (writes need confirm). Auth: SALESFORCE_NPSP_BASE_URL (https://{org}.my.salesforce.com) + SALESFORCE_NPSP_TOKEN."),
    dict(name="classy", description="Classy fundraising REST. ops get/post/put/delete (writes need confirm). Auth: CLASSY_BASE_URL (https://api.classy.org) + CLASSY_TOKEN."),
    dict(name="givesmart", description="GiveSmart REST. ops get/post/put/delete (writes need confirm). Auth: GIVESMART_BASE_URL + GIVESMART_TOKEN."),
    dict(name="onecause", description="OneCause fundraising/auctions REST. ops get/post/put/delete (writes need confirm). Auth: ONECAUSE_BASE_URL + ONECAUSE_TOKEN."),
    dict(name="networkforgood", description="Network for Good REST. ops get/post/put/delete (writes need confirm). Auth: NETWORKFORGOOD_BASE_URL + NETWORKFORGOOD_TOKEN."),
    dict(name="donorsnap", description="DonorSnap REST. ops get/post/put/delete (writes need confirm). Auth: DONORSNAP_BASE_URL + DONORSNAP_TOKEN."),
    dict(name="donorbox", description="Donorbox REST. ops get/post/put/delete (writes need confirm). Auth: DONORBOX_BASE_URL (https://donorbox.org) + DONORBOX_TOKEN."),
    dict(name="givebutter", description="Givebutter REST. ops get/post/put/delete (writes need confirm). Auth: GIVEBUTTER_BASE_URL (https://api.givebutter.com) + GIVEBUTTER_TOKEN."),
    dict(name="qgiv", description="Qgiv REST. ops get/post/put/delete (writes need confirm). Auth: QGIV_BASE_URL (https://api.qgiv.com) + QGIV_TOKEN."),
    dict(name="fundly", description="Fundly crowdfunding REST. ops get/post/put/delete (writes need confirm). Auth: FUNDLY_BASE_URL + FUNDLY_TOKEN."),
    dict(name="causevox", description="CauseVox fundraising REST. ops get/post/put/delete (writes need confirm). Auth: CAUSEVOX_BASE_URL + CAUSEVOX_TOKEN."),
    dict(name="fundraiseup", description="FundraiseUp REST. ops get/post/put/delete (writes need confirm). Auth: FUNDRAISEUP_BASE_URL + FUNDRAISEUP_TOKEN."),
    dict(name="virtuous", description="Virtuous CRM REST. ops get/post/put/delete (writes need confirm). Auth: VIRTUOUS_BASE_URL (https://api.virtuoussoftware.com) + VIRTUOUS_TOKEN."),
    dict(name="bonterra", description="Bonterra (Social Solutions/EveryAction case management) REST. ops get/post/put/delete (writes need confirm). Auth: BONTERRA_BASE_URL + BONTERRA_TOKEN."),
    dict(name="everyaction", basic=True, description="EveryAction / NGP VAN REST. ops get/post/put/delete (writes need confirm). Auth: EVERYACTION_BASE_URL (https://api.securevan.com) + EVERYACTION_TOKEN (app name + API key, HTTP basic)."),
    dict(name="nationbuilder", description="NationBuilder REST. ops get/post/put/delete (writes need confirm). Auth: NATIONBUILDER_BASE_URL (https://{slug}.nationbuilder.com) + NATIONBUILDER_TOKEN."),
    dict(name="keela", description="Keela nonprofit CRM REST. ops get/post/put/delete (writes need confirm). Auth: KEELA_BASE_URL + KEELA_TOKEN."),
    dict(name="funraise", description="Funraise REST. ops get/post/put/delete (writes need confirm). Auth: FUNRAISE_BASE_URL + FUNRAISE_TOKEN."),
    dict(name="salsalabs", description="Salsa Labs / SalsaEngage REST. ops get/post/put/delete (writes need confirm). Auth: SALSALABS_BASE_URL (https://api.salsalabs.org) + SALSALABS_TOKEN."),
    dict(name="iwave", description="iWave prospect research REST. ops get/post/put/delete (writes need confirm). Auth: IWAVE_BASE_URL + IWAVE_TOKEN."),
    dict(name="donorsearch", description="DonorSearch wealth screening REST. ops get/post/put/delete (writes need confirm). Auth: DONORSEARCH_BASE_URL + DONORSEARCH_TOKEN."),
    dict(name="wealthengine", description="WealthEngine REST. ops get/post/put/delete (writes need confirm). Auth: WEALTHENGINE_BASE_URL (https://api.wealthengine.com) + WEALTHENGINE_TOKEN."),
    dict(name="givegab", description="GiveGab (Bonterra Volunteer) REST. ops get/post/put/delete (writes need confirm). Auth: GIVEGAB_BASE_URL + GIVEGAB_TOKEN."),
    dict(name="aplos", description="Aplos nonprofit accounting REST. ops get/post/put/delete (writes need confirm). Auth: APLOS_BASE_URL + APLOS_TOKEN."),
    dict(name="mip_fund_accounting", description="Abila MIP Fund Accounting (Community Brands) REST. ops get/post/put/delete (writes need confirm). Auth: MIP_FUND_ACCOUNTING_BASE_URL + MIP_FUND_ACCOUNTING_TOKEN."),
    dict(name="sumac", description="Sumac nonprofit CRM REST. ops get/post/put/delete (writes need confirm). Auth: SUMAC_BASE_URL + SUMAC_TOKEN."),
    dict(name="givelively", description="GiveLively REST. ops get/post/put/delete (writes need confirm). Auth: GIVELIVELY_BASE_URL + GIVELIVELY_TOKEN."),
    dict(name="mightycause", description="Mightycause REST. ops get/post/put/delete (writes need confirm). Auth: MIGHTYCAUSE_BASE_URL + MIGHTYCAUSE_TOKEN."),
    dict(name="greatergiving", description="Greater Giving events/auction REST. ops get/post/put/delete (writes need confirm). Auth: GREATERGIVING_BASE_URL + GREATERGIVING_TOKEN."),
    dict(name="auctria", description="Auctria auction management REST. ops get/post/put/delete (writes need confirm). Auth: AUCTRIA_BASE_URL + AUCTRIA_TOKEN."),
    dict(name="biddingowl", description="BiddingOwl auction REST. ops get/post/put/delete (writes need confirm). Auth: BIDDINGOWL_BASE_URL + BIDDINGOWL_TOKEN."),
    dict(name="handbid", description="Handbid auction/fundraising REST. ops get/post/put/delete (writes need confirm). Auth: HANDBID_BASE_URL (https://api.handbid.com) + HANDBID_TOKEN."),
    dict(name="donordock", description="DonorDock nonprofit CRM REST. ops get/post/put/delete (writes need confirm). Auth: DONORDOCK_BASE_URL + DONORDOCK_TOKEN."),
    dict(name="almabase", description="Almabase alumni/fundraising engagement REST. ops get/post/put/delete (writes need confirm). Auth: ALMABASE_BASE_URL + ALMABASE_TOKEN."),
    dict(name="graduway", description="Graduway alumni engagement REST. ops get/post/put/delete (writes need confirm). Auth: GRADUWAY_BASE_URL + GRADUWAY_TOKEN."),
    dict(name="evertrue", description="EverTrue alumni/advancement CRM REST. ops get/post/put/delete (writes need confirm). Auth: EVERTRUE_BASE_URL + EVERTRUE_TOKEN."),
    dict(name="givewp", description="GiveWP (WordPress donation plugin) REST. ops get/post/put/delete (writes need confirm). Auth: GIVEWP_BASE_URL + GIVEWP_TOKEN."),
    dict(name="wildapricot", description="WildApricot membership/association management REST. ops get/post/put/delete (writes need confirm). Auth: WILDAPRICOT_BASE_URL (https://api.wildapricot.org) + WILDAPRICOT_TOKEN."),
    dict(name="memberclicks", description="MemberClicks association management REST. ops get/post/put/delete (writes need confirm). Auth: MEMBERCLICKS_BASE_URL + MEMBERCLICKS_TOKEN."),
    dict(name="charityproud", description="CharityProud donor management REST. ops get/post/put/delete (writes need confirm). Auth: CHARITYPROUD_BASE_URL + CHARITYPROUD_TOKEN."),
    dict(name="firespring", description="Firespring nonprofit website/donor tools REST. ops get/post/put/delete (writes need confirm). Auth: FIRESPRING_BASE_URL + FIRESPRING_TOKEN."),
    dict(name="doublethedonation", description="Double the Donation matching-gifts REST. ops get/post/put/delete (writes need confirm). Auth: DOUBLETHEDONATION_BASE_URL + DOUBLETHEDONATION_TOKEN."),
    dict(name="benevity", description="Benevity corporate giving/volunteering REST. ops get/post/put/delete (writes need confirm). Auth: BENEVITY_BASE_URL + BENEVITY_TOKEN."),
    dict(name="yourcause", description="YourCause (Blackbaud CSR) REST. ops get/post/put/delete (writes need confirm). Auth: YOURCAUSE_BASE_URL + YOURCAUSE_TOKEN."),
    dict(name="fluxx", description="Fluxx Grantmaker REST. ops get/post/put/delete (writes need confirm). Auth: FLUXX_BASE_URL (https://{org}.fluxx.io) + FLUXX_TOKEN."),
    dict(name="submittable", description="Submittable grants/applications REST. ops get/post/put/delete (writes need confirm). Auth: SUBMITTABLE_BASE_URL (https://api.submittable.com) + SUBMITTABLE_TOKEN."),
    dict(name="smartsimple", description="SmartSimple grants management REST. ops get/post/put/delete (writes need confirm). Auth: SMARTSIMPLE_BASE_URL + SMARTSIMPLE_TOKEN."),
    dict(name="ecivis", description="eCivis grants management REST. ops get/post/put/delete (writes need confirm). Auth: ECIVIS_BASE_URL + ECIVIS_TOKEN."),
    dict(name="amplifund", description="AmpliFund grants management REST. ops get/post/put/delete (writes need confirm). Auth: AMPLIFUND_BASE_URL + AMPLIFUND_TOKEN."),
    dict(name="granthub", description="GrantHub grant tracking REST. ops get/post/put/delete (writes need confirm). Auth: GRANTHUB_BASE_URL + GRANTHUB_TOKEN."),
    dict(name="grantwatch", description="GrantWatch grant search REST. ops get/post/put/delete (writes need confirm). Auth: GRANTWATCH_BASE_URL + GRANTWATCH_TOKEN."),
    dict(name="slideroom", description="SlideRoom applications/admissions REST. ops get/post/put/delete (writes need confirm). Auth: SLIDEROOM_BASE_URL + SLIDEROOM_TOKEN."),
    dict(name="zoomgrants", description="ZoomGrants grants management REST. ops get/post/put/delete (writes need confirm). Auth: ZOOMGRANTS_BASE_URL + ZOOMGRANTS_TOKEN."),
    dict(name="blackbaud_grantmaking", description="Blackbaud Grantmaking (formerly MicroEdge GIFTS) REST. ops get/post/put/delete (writes need confirm). Auth: BLACKBAUD_GRANTMAKING_BASE_URL + BLACKBAUD_GRANTMAKING_TOKEN."),
    dict(name="cybergrants", description="CyberGrants corporate/foundation grantmaking REST. ops get/post/put/delete (writes need confirm). Auth: CYBERGRANTS_BASE_URL + CYBERGRANTS_TOKEN."),
    dict(name="foundant", description="Foundant Technologies Grants Lifecycle Manager (GLM) REST. ops get/post/put/delete (writes need confirm). Auth: FOUNDANT_BASE_URL + FOUNDANT_TOKEN."),
    dict(name="opengov", description="OpenGov public-sector budgeting/permitting REST. ops get/post/put/delete (writes need confirm). Auth: OPENGOV_BASE_URL + OPENGOV_TOKEN."),
    dict(name="tylertechnologies", description="Tyler Technologies (Munis/EnerGov) REST. ops get/post/put/delete (writes need confirm). Auth: TYLERTECHNOLOGIES_BASE_URL + TYLERTECHNOLOGIES_TOKEN."),
    dict(name="accela", description="Accela Civic Platform REST. ops get/post/put/delete (writes need confirm). Auth: ACCELA_BASE_URL (https://apis.accela.com) + ACCELA_TOKEN."),
    dict(name="granicus", description="Granicus government engagement REST. ops get/post/put/delete (writes need confirm). Auth: GRANICUS_BASE_URL + GRANICUS_TOKEN."),
    dict(name="civicplus", description="CivicPlus government CMS/engagement REST. ops get/post/put/delete (writes need confirm). Auth: CIVICPLUS_BASE_URL + CIVICPLUS_TOKEN."),
    dict(name="bonfirehub", description="Bonfire public-sector procurement/sourcing REST. ops get/post/put/delete (writes need confirm). Auth: BONFIREHUB_BASE_URL + BONFIREHUB_TOKEN."),
    dict(name="govqa", description="GovQA public-records request management REST. ops get/post/put/delete (writes need confirm). Auth: GOVQA_BASE_URL + GOVQA_TOKEN."),
    dict(name="neogov", description="NEOGOV public-sector HR/talent REST. ops get/post/put/delete (writes need confirm). Auth: NEOGOV_BASE_URL + NEOGOV_TOKEN."),
    dict(name="centralsquare", description="CentralSquare public-safety/government REST. ops get/post/put/delete (writes need confirm). Auth: CENTRALSQUARE_BASE_URL + CENTRALSQUARE_TOKEN."),
    dict(name="onbase", description="Hyland OnBase content management REST. ops get/post/put/delete (writes need confirm). Auth: ONBASE_BASE_URL + ONBASE_TOKEN."),
    dict(name="seeclickfix", description="SeeClickFix (CivicPlus) 311/citizen engagement REST. ops get/post/put/delete (writes need confirm). Auth: SEECLICKFIX_BASE_URL (https://seeclickfix.com) + SEECLICKFIX_TOKEN."),
    dict(name="govdelivery", description="GovDelivery (Granicus) citizen communications REST. ops get/post/put/delete (writes need confirm). Auth: GOVDELIVERY_BASE_URL + GOVDELIVERY_TOKEN."),
    dict(name="citybase", description="CityBase government payments REST. ops get/post/put/delete (writes need confirm). Auth: CITYBASE_BASE_URL + CITYBASE_TOKEN."),
    dict(name="payit_gov", description="PayIt government digital services/payments REST. ops get/post/put/delete (writes need confirm). Auth: PAYIT_GOV_BASE_URL + PAYIT_GOV_TOKEN."),
    dict(name="socrata", token_header="X-App-Token", scheme="", description="Socrata (Tyler Data & Insights) open data REST. ops get/post/put/delete (writes need confirm). Auth: SOCRATA_BASE_URL (https://api.us.socrata.com) + SOCRATA_TOKEN (app token)."),
    dict(name="grants_gov", description="Grants.gov federal grants REST. ops get/post/put/delete (writes need confirm). Auth: GRANTS_GOV_BASE_URL (https://api.grants.gov) + GRANTS_GOV_TOKEN."),
    dict(name="skyward", description="Skyward SIS REST. ops get/post/put/delete (writes need confirm). Auth: SKYWARD_BASE_URL + SKYWARD_TOKEN."),
    dict(name="ellucian", description="Ellucian Ethos Integration REST (Banner/Colleague). ops get/post/put/delete (writes need confirm). Auth: ELLUCIAN_BASE_URL (https://integration.elluciancloud.com) + ELLUCIAN_TOKEN."),
    dict(name="jenzabar", description="Jenzabar One higher-ed SIS REST. ops get/post/put/delete (writes need confirm). Auth: JENZABAR_BASE_URL + JENZABAR_TOKEN."),
    dict(name="anthology", description="Anthology Student (formerly Campus Management) REST. ops get/post/put/delete (writes need confirm). Auth: ANTHOLOGY_BASE_URL + ANTHOLOGY_TOKEN."),
    dict(name="veracross", description="Veracross independent-school SIS REST. ops get/post/put/delete (writes need confirm). Auth: VERACROSS_BASE_URL + VERACROSS_TOKEN."),
    dict(name="blackbaud_sis", description="Blackbaud SIS (Education Management) REST. ops get/post/put/delete (writes need confirm). Auth: BLACKBAUD_SIS_BASE_URL + BLACKBAUD_SIS_TOKEN."),
    dict(name="facts_sis", description="FACTS SIS (RenWeb, Nelnet) REST. ops get/post/put/delete (writes need confirm). Auth: FACTS_SIS_BASE_URL + FACTS_SIS_TOKEN."),
    dict(name="aeries", description="Aeries SIS REST. ops get/post/put/delete (writes need confirm). Auth: AERIES_BASE_URL + AERIES_TOKEN."),
    dict(name="gradelink", description="Gradelink SIS REST. ops get/post/put/delete (writes need confirm). Auth: GRADELINK_BASE_URL + GRADELINK_TOKEN."),
    dict(name="titan_school", description="Titan School Solutions (meal payment/SIS) REST. ops get/post/put/delete (writes need confirm). Auth: TITAN_SCHOOL_BASE_URL + TITAN_SCHOOL_TOKEN."),
    dict(name="myschoolbucks", description="MySchoolBucks school payments REST. ops get/post/put/delete (writes need confirm). Auth: MYSCHOOLBUCKS_BASE_URL + MYSCHOOLBUCKS_TOKEN."),
    dict(name="schoolcafe", description="SchoolCafe (Heartland) school nutrition/payments REST. ops get/post/put/delete (writes need confirm). Auth: SCHOOLCAFE_BASE_URL + SCHOOLCAFE_TOKEN."),
    dict(name="frontline_education", description="Frontline Education (Central/Absence Management) REST. ops get/post/put/delete (writes need confirm). Auth: FRONTLINE_EDUCATION_BASE_URL + FRONTLINE_EDUCATION_TOKEN."),
    dict(name="bloomz", description="Bloomz school-family communication REST. ops get/post/put/delete (writes need confirm). Auth: BLOOMZ_BASE_URL + BLOOMZ_TOKEN."),
    dict(name="parentsquare", description="ParentSquare school-family communication REST. ops get/post/put/delete (writes need confirm). Auth: PARENTSQUARE_BASE_URL + PARENTSQUARE_TOKEN."),
    dict(name="schoolstatus", description="SchoolStatus family engagement/analytics REST. ops get/post/put/delete (writes need confirm). Auth: SCHOOLSTATUS_BASE_URL + SCHOOLSTATUS_TOKEN."),
    dict(name="otus", description="Otus K-12 assessment/gradebook REST. ops get/post/put/delete (writes need confirm). Auth: OTUS_BASE_URL + OTUS_TOKEN."),
    dict(name="panorama_education", description="Panorama Education survey/analytics REST. ops get/post/put/delete (writes need confirm). Auth: PANORAMA_EDUCATION_BASE_URL + PANORAMA_EDUCATION_TOKEN."),
    dict(name="dreambox", description="DreamBox Learning REST. ops get/post/put/delete (writes need confirm). Auth: DREAMBOX_BASE_URL + DREAMBOX_TOKEN."),
    dict(name="ixl", description="IXL Learning REST. ops get/post/put/delete (writes need confirm). Auth: IXL_BASE_URL + IXL_TOKEN."),
    dict(name="newsela", description="Newsela REST. ops get/post/put/delete (writes need confirm). Auth: NEWSELA_BASE_URL + NEWSELA_TOKEN."),
    dict(name="nearpod", description="Nearpod REST. ops get/post/put/delete (writes need confirm). Auth: NEARPOD_BASE_URL + NEARPOD_TOKEN."),
    dict(name="edulastic", description="Edulastic assessment REST. ops get/post/put/delete (writes need confirm). Auth: EDULASTIC_BASE_URL + EDULASTIC_TOKEN."),
    dict(name="masteryconnect", description="Mastery Connect assessment REST. ops get/post/put/delete (writes need confirm). Auth: MASTERYCONNECT_BASE_URL + MASTERYCONNECT_TOKEN."),
    dict(name="renaissance_learning", description="Renaissance Learning (Accelerated Reader/STAR) REST. ops get/post/put/delete (writes need confirm). Auth: RENAISSANCE_LEARNING_BASE_URL + RENAISSANCE_LEARNING_TOKEN."),
    dict(name="myon", description="myON reading platform REST. ops get/post/put/delete (writes need confirm). Auth: MYON_BASE_URL + MYON_TOKEN."),
    dict(name="lexia", description="Lexia Learning REST. ops get/post/put/delete (writes need confirm). Auth: LEXIA_BASE_URL + LEXIA_TOKEN."),
    dict(name="achieve3000", description="Achieve3000 literacy platform REST. ops get/post/put/delete (writes need confirm). Auth: ACHIEVE3000_BASE_URL + ACHIEVE3000_TOKEN."),
    dict(name="slate_technolutions", description="Slate (Technolutions) admissions CRM REST. ops get/post/put/delete (writes need confirm). Auth: SLATE_TECHNOLUTIONS_BASE_URL + SLATE_TECHNOLUTIONS_TOKEN."),
    dict(name="targetx", description="TargetX (Salesforce-based admissions CRM) REST. ops get/post/put/delete (writes need confirm). Auth: TARGETX_BASE_URL + TARGETX_TOKEN."),
    dict(name="campuslogic", description="CampusLogic financial aid REST. ops get/post/put/delete (writes need confirm). Auth: CAMPUSLOGIC_BASE_URL + CAMPUSLOGIC_TOKEN."),
    dict(name="nelnet_campus_commerce", description="Nelnet Campus Commerce (tuition payments) REST. ops get/post/put/delete (writes need confirm). Auth: NELNET_CAMPUS_COMMERCE_BASE_URL + NELNET_CAMPUS_COMMERCE_TOKEN."),
    dict(name="terradotta", description="Terra Dotta study-abroad management REST. ops get/post/put/delete (writes need confirm). Auth: TERRADOTTA_BASE_URL + TERRADOTTA_TOKEN."),
    dict(name="symplicity", description="Symplicity career-services/campus-life REST. ops get/post/put/delete (writes need confirm). Auth: SYMPLICITY_BASE_URL + SYMPLICITY_TOKEN."),
    dict(name="handshake", description="Handshake career-services REST. ops get/post/put/delete (writes need confirm). Auth: HANDSHAKE_BASE_URL + HANDSHAKE_TOKEN."),
    dict(name="collegenet", description="CollegeNET Series25 scheduling REST. ops get/post/put/delete (writes need confirm). Auth: COLLEGENET_BASE_URL + COLLEGENET_TOKEN."),
    dict(name="ad_astra", description="Ad Astra academic scheduling REST. ops get/post/put/delete (writes need confirm). Auth: AD_ASTRA_BASE_URL + AD_ASTRA_TOKEN."),
    dict(name="civitas_learning", description="Civitas Learning student-success analytics REST. ops get/post/put/delete (writes need confirm). Auth: CIVITAS_LEARNING_BASE_URL + CIVITAS_LEARNING_TOKEN."),
    dict(name="eab_navigate", description="EAB Navigate student-success/advising REST. ops get/post/put/delete (writes need confirm). Auth: EAB_NAVIGATE_BASE_URL + EAB_NAVIGATE_TOKEN."),
    dict(name="starfish_retention", description="Starfish Retention Solutions (Hobsons) REST. ops get/post/put/delete (writes need confirm). Auth: STARFISH_RETENTION_BASE_URL + STARFISH_RETENTION_TOKEN."),
    dict(name="degreeworks", description="Ellucian Degree Works REST. ops get/post/put/delete (writes need confirm). Auth: DEGREEWORKS_BASE_URL + DEGREEWORKS_TOKEN."),
    dict(name="acalog", description="Acalog academic catalog management REST. ops get/post/put/delete (writes need confirm). Auth: ACALOG_BASE_URL + ACALOG_TOKEN."),
    dict(name="courseleaf", description="CourseLeaf curriculum/catalog management REST. ops get/post/put/delete (writes need confirm). Auth: COURSELEAF_BASE_URL + COURSELEAF_TOKEN."),
    dict(name="populi", description="Populi small-college SIS REST. ops get/post/put/delete (writes need confirm). Auth: POPULI_BASE_URL + POPULI_TOKEN."),
    dict(name="volunteerhub", description="VolunteerHub REST. ops get/post/put/delete (writes need confirm). Auth: VOLUNTEERHUB_BASE_URL (https://api.volunteerhub.com) + VOLUNTEERHUB_TOKEN."),
    dict(name="volunteermatch", description="VolunteerMatch REST. ops get/post/put/delete (writes need confirm). Auth: VOLUNTEERMATCH_BASE_URL + VOLUNTEERMATCH_TOKEN."),
    dict(name="volgistics", description="Volgistics volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: VOLGISTICS_BASE_URL + VOLGISTICS_TOKEN."),
    dict(name="galaxydigital", description="Galaxy Digital (Get Connected) volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: GALAXYDIGITAL_BASE_URL + GALAXYDIGITAL_TOKEN."),
    dict(name="mobileserve", description="MobileServe volunteer hour tracking REST. ops get/post/put/delete (writes need confirm). Auth: MOBILESERVE_BASE_URL + MOBILESERVE_TOKEN."),
    dict(name="signupgenius", description="SignUpGenius REST. ops get/post/put/delete (writes need confirm). Auth: SIGNUPGENIUS_BASE_URL + SIGNUPGENIUS_TOKEN."),
    dict(name="volunteerlocal", description="VolunteerLocal REST. ops get/post/put/delete (writes need confirm). Auth: VOLUNTEERLOCAL_BASE_URL + VOLUNTEERLOCAL_TOKEN."),
    dict(name="trackitforward", description="Track It Forward volunteer tracking REST. ops get/post/put/delete (writes need confirm). Auth: TRACKITFORWARD_BASE_URL + TRACKITFORWARD_TOKEN."),
    dict(name="initlive", description="InitLive volunteer/event staffing REST. ops get/post/put/delete (writes need confirm). Auth: INITLIVE_BASE_URL + INITLIVE_TOKEN."),
    dict(name="givepulse", description="GivePulse volunteer/civic engagement REST. ops get/post/put/delete (writes need confirm). Auth: GIVEPULSE_BASE_URL + GIVEPULSE_TOKEN."),
    dict(name="rosterfy", description="Rosterfy volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: ROSTERFY_BASE_URL + ROSTERFY_TOKEN."),
    dict(name="vomo", description="VOMO volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: VOMO_BASE_URL + VOMO_TOKEN."),
    dict(name="cervis", description="CERVIS volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: CERVIS_BASE_URL + CERVIS_TOKEN."),
    dict(name="timecounts", description="Timecounts volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: TIMECOUNTS_BASE_URL + TIMECOUNTS_TOKEN."),
    dict(name="betterimpact", description="Better Impact volunteer management REST. ops get/post/put/delete (writes need confirm). Auth: BETTERIMPACT_BASE_URL + BETTERIMPACT_TOKEN."),
    dict(name="pointapp", description="Point App (Points of Light) volunteer REST. ops get/post/put/delete (writes need confirm). Auth: POINTAPP_BASE_URL + POINTAPP_TOKEN."),
    dict(name="zillow", description="Zillow Group / Bridge Interactive REST. ops get/post/put/delete (writes need confirm). e.g. /api/v2/listings. Auth: ZILLOW_BASE_URL (https://api.bridgedataoutput.com) + ZILLOW_TOKEN."),
    dict(name="realtor_com", description="Realtor.com / Move Inc partner REST. ops get/post/put/delete (writes need confirm). Auth: REALTOR_COM_BASE_URL + REALTOR_COM_TOKEN."),
    dict(name="corelogic", description="CoreLogic property data REST. ops get/post/put/delete (writes need confirm). Auth: CORELOGIC_BASE_URL (https://api.corelogic.com) + CORELOGIC_TOKEN."),
    dict(name="skyslope", description="SkySlope real estate transaction management REST. ops get/post/put/delete (writes need confirm). Auth: SKYSLOPE_BASE_URL (https://api.skyslope.com) + SKYSLOPE_TOKEN."),
    dict(name="innago", description="Innago property management REST. ops get/post/put/delete (writes need confirm). Auth: INNAGO_BASE_URL + INNAGO_TOKEN."),
    dict(name="turbotenant", description="TurboTenant property management REST. ops get/post/put/delete (writes need confirm). Auth: TURBOTENANT_BASE_URL + TURBOTENANT_TOKEN."),
    dict(name="avail", description="Avail (Realtor.com) landlord/property management REST. ops get/post/put/delete (writes need confirm). Auth: AVAIL_BASE_URL + AVAIL_TOKEN."),
    dict(name="resman", description="ResMan property management REST. ops get/post/put/delete (writes need confirm). Auth: RESMAN_BASE_URL + RESMAN_TOKEN."),
    dict(name="rentvine", description="Rentvine property management REST. ops get/post/put/delete (writes need confirm). Auth: RENTVINE_BASE_URL + RENTVINE_TOKEN."),
    dict(name="propertymatrix", description="Property Matrix property management REST. ops get/post/put/delete (writes need confirm). Auth: PROPERTYMATRIX_BASE_URL + PROPERTYMATRIX_TOKEN."),
    dict(name="simplifyem", description="SimplifyEm property management REST. ops get/post/put/delete (writes need confirm). Auth: SIMPLIFYEM_BASE_URL + SIMPLIFYEM_TOKEN."),
    dict(name="rentecdirect", description="Rentec Direct property management REST. ops get/post/put/delete (writes need confirm). Auth: RENTECDIRECT_BASE_URL (https://app.rentecdirect.com) + RENTECDIRECT_TOKEN."),
    dict(name="reonomy", description="Reonomy commercial property data REST. ops get/post/put/delete (writes need confirm). Auth: REONOMY_BASE_URL (https://api.reonomy.com) + REONOMY_TOKEN."),
    dict(name="placester", description="Placester real estate website/IDX REST. ops get/post/put/delete (writes need confirm). Auth: PLACESTER_BASE_URL + PLACESTER_TOKEN."),
    dict(name="boomtown", description="BoomTown real estate CRM REST. ops get/post/put/delete (writes need confirm). Auth: BOOMTOWN_BASE_URL + BOOMTOWN_TOKEN."),
    dict(name="kvcore", description="kvCORE (Inside Real Estate) CRM REST. ops get/post/put/delete (writes need confirm). Auth: KVCORE_BASE_URL + KVCORE_TOKEN."),
    dict(name="wiseagent", description="Wise Agent real estate CRM REST. ops get/post/put/delete (writes need confirm). e.g. /WiseAgent/webconnect.asp. Auth: WISEAGENT_BASE_URL (https://sync.thewiseagent.com) + WISEAGENT_TOKEN."),
    dict(name="realgeeks", description="Real Geeks real estate CRM REST. ops get/post/put/delete (writes need confirm). Auth: REALGEEKS_BASE_URL + REALGEEKS_TOKEN."),
    dict(name="topproducer", description="Top Producer real estate CRM REST. ops get/post/put/delete (writes need confirm). Auth: TOPPRODUCER_BASE_URL + TOPPRODUCER_TOKEN."),
    dict(name="realtyna", description="Realtyna MLS/IDX REST. ops get/post/put/delete (writes need confirm). Auth: REALTYNA_BASE_URL + REALTYNA_TOKEN."),
    dict(name="idxbroker", description="IDX Broker MLS listing REST. ops get/post/put/delete (writes need confirm). Auth: IDXBROKER_BASE_URL (https://api.idxbroker.com) + IDXBROKER_TOKEN (accesskey)."),
    dict(name="rentspree", description="RentSpree rental application/screening REST. ops get/post/put/delete (writes need confirm). Auth: RENTSPREE_BASE_URL + RENTSPREE_TOKEN."),
    dict(name="buildout", description="Buildout commercial real estate marketing REST. ops get/post/put/delete (writes need confirm). Auth: BUILDOUT_BASE_URL (https://api.buildout.com) + BUILDOUT_TOKEN."),
    dict(name="crexi", description="CREXi commercial real estate marketplace REST. ops get/post/put/delete (writes need confirm). Auth: CREXI_BASE_URL + CREXI_TOKEN."),
    dict(name="tenx", description="Ten-X commercial real estate auction platform REST. ops get/post/put/delete (writes need confirm). Auth: TENX_BASE_URL + TENX_TOKEN."),
    dict(name="housecanary", description="HouseCanary property valuation/analytics REST. ops get/post/put/delete (writes need confirm). Auth: HOUSECANARY_BASE_URL (https://api.housecanary.com) + HOUSECANARY_TOKEN."),
    dict(name="attom", description="ATTOM Data property data REST. ops get/post/put/delete (writes need confirm). Auth: ATTOM_BASE_URL (https://api.gateway.attomdata.com) + ATTOM_TOKEN (apikey header)."),
    dict(name="mlsgrid", description="MLS Grid (RESO Web API aggregator) REST/OData. ops get/post/put/delete (writes need confirm). Auth: MLSGRID_BASE_URL (https://api.mlsgrid.com) + MLSGRID_TOKEN."),
    dict(name="sparkapi", description="Spark API (FBS) MLS data platform REST. ops get/post/put/delete (writes need confirm). Auth: SPARKAPI_BASE_URL (https://sparkapi.com) + SPARKAPI_TOKEN."),
    dict(name="flexmls", description="Flexmls (FBS) MLS system REST. ops get/post/put/delete (writes need confirm). Auth: FLEXMLS_BASE_URL + FLEXMLS_TOKEN."),
    dict(name="rentberry", description="Rentberry rental platform REST. ops get/post/put/delete (writes need confirm). Auth: RENTBERRY_BASE_URL + RENTBERRY_TOKEN."),
    dict(name="butterflymx", description="ButterflyMX property access control REST. ops get/post/put/delete (writes need confirm). Auth: BUTTERFLYMX_BASE_URL + BUTTERFLYMX_TOKEN."),
    dict(name="latchel", description="Latchel property maintenance coordination REST. ops get/post/put/delete (writes need confirm). Auth: LATCHEL_BASE_URL + LATCHEL_TOKEN."),
    dict(name="happyco", description="HappyCo property inspection/operations REST. ops get/post/put/delete (writes need confirm). Auth: HAPPYCO_BASE_URL + HAPPYCO_TOKEN."),
    dict(name="clickpay", description="ClickPay property/HOA payments REST. ops get/post/put/delete (writes need confirm). Auth: CLICKPAY_BASE_URL + CLICKPAY_TOKEN."),
    dict(name="payhoa", description="PayHOA community association management REST. ops get/post/put/delete (writes need confirm). Auth: PAYHOA_BASE_URL + PAYHOA_TOKEN."),
    dict(name="vantaca", description="Vantaca HOA/community management REST. ops get/post/put/delete (writes need confirm). Auth: VANTACA_BASE_URL + VANTACA_TOKEN."),
    dict(name="frontsteps", description="FRONTSTEPS community/HOA management REST. ops get/post/put/delete (writes need confirm). Auth: FRONTSTEPS_BASE_URL + FRONTSTEPS_TOKEN."),
    dict(name="cinc", description="CINC real estate CRM/lead generation REST. ops get/post/put/delete (writes need confirm). Auth: CINC_BASE_URL + CINC_TOKEN."),
    dict(name="realvolve", description="Realvolve real estate CRM REST. ops get/post/put/delete (writes need confirm). Auth: REALVOLVE_BASE_URL (https://api.realvolve.com) + REALVOLVE_TOKEN."),
    dict(name="cosmolex", description="CosmoLex legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: COSMOLEX_BASE_URL + COSMOLEX_TOKEN."),
    dict(name="rocketmatter", description="Rocket Matter legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: ROCKETMATTER_BASE_URL + ROCKETMATTER_TOKEN."),
    dict(name="zolasuite", description="Zola Suite legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: ZOLASUITE_BASE_URL + ZOLASUITE_TOKEN."),
    dict(name="lawmatics", description="Lawmatics legal CRM/intake automation REST. ops get/post/put/delete (writes need confirm). Auth: LAWMATICS_BASE_URL (https://api.lawmatics.com) + LAWMATICS_TOKEN."),
    dict(name="actionstep", description="Actionstep legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: ACTIONSTEP_BASE_URL + ACTIONSTEP_TOKEN."),
    dict(name="litify", description="Litify (Salesforce-based) legal operations platform REST. ops get/post/put/delete (writes need confirm). Auth: LITIFY_BASE_URL + LITIFY_TOKEN."),
    dict(name="leap_legal", description="LEAP legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: LEAP_LEGAL_BASE_URL + LEAP_LEGAL_TOKEN."),
    dict(name="abacuslaw", description="AbacusLaw (AbacusNext) legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: ABACUSLAW_BASE_URL + ABACUSLAW_TOKEN."),
    dict(name="tabs3", description="Tabs3 legal billing/practice management REST. ops get/post/put/delete (writes need confirm). Auth: TABS3_BASE_URL + TABS3_TOKEN."),
    dict(name="prolaw", description="ProLaw (Thomson Reuters) legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: PROLAW_BASE_URL + PROLAW_TOKEN."),
    dict(name="nuix", description="Nuix e-discovery/investigation REST. ops get/post/put/delete (writes need confirm). Auth: NUIX_BASE_URL + NUIX_TOKEN."),
    dict(name="reveal_ediscovery", description="Reveal e-discovery platform REST. ops get/post/put/delete (writes need confirm). Auth: REVEAL_EDISCOVERY_BASE_URL + REVEAL_EDISCOVERY_TOKEN."),
    dict(name="exterro", description="Exterro e-discovery/legal GRC REST. ops get/post/put/delete (writes need confirm). Auth: EXTERRO_BASE_URL + EXTERRO_TOKEN."),
    dict(name="onna", description="Onna e-discovery/knowledge integration REST. ops get/post/put/delete (writes need confirm). Auth: ONNA_BASE_URL (https://api.onna.com) + ONNA_TOKEN."),
    dict(name="casepoint", description="Casepoint e-discovery/legal hold REST. ops get/post/put/delete (writes need confirm). Auth: CASEPOINT_BASE_URL + CASEPOINT_TOKEN."),
    dict(name="zapproved", description="Zapproved (Reveal) legal hold/e-discovery REST. ops get/post/put/delete (writes need confirm). Auth: ZAPPROVED_BASE_URL + ZAPPROVED_TOKEN."),
    dict(name="concord_clm", description="Concord contract lifecycle management REST. ops get/post/put/delete (writes need confirm). Auth: CONCORD_CLM_BASE_URL (https://api.concordnow.com) + CONCORD_CLM_TOKEN."),
    dict(name="juro", description="Juro contract lifecycle management REST. ops get/post/put/delete (writes need confirm). Auth: JURO_BASE_URL (https://api.juro.com) + JURO_TOKEN."),
    dict(name="highq", description="HighQ (Thomson Reuters) legal collaboration platform REST. ops get/post/put/delete (writes need confirm). Auth: HIGHQ_BASE_URL + HIGHQ_TOKEN."),
    dict(name="kirasystems", description="Kira Systems contract analysis REST. ops get/post/put/delete (writes need confirm). Auth: KIRASYSTEMS_BASE_URL + KIRASYSTEMS_TOKEN."),
    dict(name="luminance", description="Luminance legal AI document review REST. ops get/post/put/delete (writes need confirm). Auth: LUMINANCE_BASE_URL + LUMINANCE_TOKEN."),
    dict(name="simplelegal", description="SimpleLegal (Onit) legal operations/e-billing REST. ops get/post/put/delete (writes need confirm). Auth: SIMPLELEGAL_BASE_URL + SIMPLELEGAL_TOKEN."),
    dict(name="mitratech", description="Mitratech legal operations (TAP) REST. ops get/post/put/delete (writes need confirm). Auth: MITRATECH_BASE_URL + MITRATECH_TOKEN."),
    dict(name="bill4time", description="Bill4Time legal time/billing REST. ops get/post/put/delete (writes need confirm). Auth: BILL4TIME_BASE_URL (https://www.bill4time.com/api) + BILL4TIME_TOKEN."),
    dict(name="timesolv", description="TimeSolv legal time/billing REST. ops get/post/put/delete (writes need confirm). Auth: TIMESOLV_BASE_URL + TIMESOLV_TOKEN."),
    dict(name="chrometa", description="Chrometa automatic legal time-tracking REST. ops get/post/put/delete (writes need confirm). Auth: CHROMETA_BASE_URL + CHROMETA_TOKEN."),
    dict(name="lawcus", description="Lawcus legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: LAWCUS_BASE_URL + LAWCUS_TOKEN."),
    dict(name="docketwise", description="Docketwise immigration case management REST. ops get/post/put/delete (writes need confirm). Auth: DOCKETWISE_BASE_URL + DOCKETWISE_TOKEN."),
    dict(name="inszoom", description="INSZoom immigration case management REST. ops get/post/put/delete (writes need confirm). Auth: INSZOOM_BASE_URL + INSZOOM_TOKEN."),
    dict(name="amicus_attorney", description="Amicus Attorney legal practice management REST. ops get/post/put/delete (writes need confirm). Auth: AMICUS_ATTORNEY_BASE_URL + AMICUS_ATTORNEY_TOKEN."),
    dict(name="hotdocs", description="HotDocs legal document automation REST. ops get/post/put/delete (writes need confirm). Auth: HOTDOCS_BASE_URL + HOTDOCS_TOKEN."),
    dict(name="vlex", description="vLex legal research REST. ops get/post/put/delete (writes need confirm). Auth: VLEX_BASE_URL (https://vlex.com/api) + VLEX_TOKEN."),
    dict(name="docket_alarm", description="Docket Alarm litigation analytics/docket REST. ops get/post/put/delete (writes need confirm). Auth: DOCKET_ALARM_BASE_URL (https://www.docketalarm.com/api) + DOCKET_ALARM_TOKEN."),
    dict(name="unicourt", description="UniCourt court records/litigation data REST. ops get/post/put/delete (writes need confirm). Auth: UNICOURT_BASE_URL (https://unicourt.com/api) + UNICOURT_TOKEN."),
    dict(name="trellis_law", description="Trellis litigation analytics REST. ops get/post/put/delete (writes need confirm). Auth: TRELLIS_LAW_BASE_URL + TRELLIS_LAW_TOKEN."),
    dict(name="boldpenguin", description="Bold Penguin commercial insurance quoting REST. ops get/post/put/delete (writes need confirm). Auth: BOLDPENGUIN_BASE_URL + BOLDPENGUIN_TOKEN."),
    dict(name="hawksoft", description="HawkSoft insurance agency management REST. ops get/post/put/delete (writes need confirm). Auth: HAWKSOFT_BASE_URL + HAWKSOFT_TOKEN."),
    dict(name="ebix", description="Ebix insurance exchange/agency platform REST. ops get/post/put/delete (writes need confirm). Auth: EBIX_BASE_URL + EBIX_TOKEN."),
    dict(name="one_inc", description="One Inc insurance payments REST. ops get/post/put/delete (writes need confirm). Auth: ONE_INC_BASE_URL + ONE_INC_TOKEN."),
    dict(name="sure_app", description="Sure (Sure App) embedded insurance REST. ops get/post/put/delete (writes need confirm). Auth: SURE_APP_BASE_URL (https://api.sureapp.com) + SURE_APP_TOKEN."),
    dict(name="cover_genius", description="Cover Genius embedded insurance REST. ops get/post/put/delete (writes need confirm). Auth: COVER_GENIUS_BASE_URL + COVER_GENIUS_TOKEN."),
    dict(name="ccc_intelligent", description="CCC Intelligent Solutions auto claims REST. ops get/post/put/delete (writes need confirm). Auth: CCC_INTELLIGENT_BASE_URL + CCC_INTELLIGENT_TOKEN."),
    dict(name="mitchell_international", description="Mitchell International claims management REST. ops get/post/put/delete (writes need confirm). Auth: MITCHELL_INTERNATIONAL_BASE_URL + MITCHELL_INTERNATIONAL_TOKEN."),
    dict(name="xactimate", description="Xactware / Xactimate claims estimating REST. ops get/post/put/delete (writes need confirm). Auth: XACTIMATE_BASE_URL + XACTIMATE_TOKEN."),
    dict(name="verisk", description="Verisk insurance data/analytics REST. ops get/post/put/delete (writes need confirm). Auth: VERISK_BASE_URL (https://api.verisk.com) + VERISK_TOKEN."),
    dict(name="insureio", description="Insureio insurance agency CRM REST. ops get/post/put/delete (writes need confirm). Auth: INSUREIO_BASE_URL + INSUREIO_TOKEN."),
    dict(name="ipipeline", description="iPipeline life insurance/annuity distribution REST. ops get/post/put/delete (writes need confirm). Auth: IPIPELINE_BASE_URL + IPIPELINE_TOKEN."),
    dict(name="fineos", description="FINEOS life/group/absence insurance core REST. ops get/post/put/delete (writes need confirm). Auth: FINEOS_BASE_URL + FINEOS_TOKEN."),
    dict(name="insly", description="Insly insurance policy administration REST. ops get/post/put/delete (writes need confirm). Auth: INSLY_BASE_URL (https://api.insly.com) + INSLY_TOKEN."),
    dict(name="novidea", description="Novidea insurance distribution management REST. ops get/post/put/delete (writes need confirm). Auth: NOVIDEA_BASE_URL + NOVIDEA_TOKEN."),
    dict(name="unqork", description="Unqork no-code insurance/enterprise platform REST. ops get/post/put/delete (writes need confirm). Auth: UNQORK_BASE_URL + UNQORK_TOKEN."),
    dict(name="bindable", description="Bindable insurance marketplace platform REST. ops get/post/put/delete (writes need confirm). Auth: BINDABLE_BASE_URL + BINDABLE_TOKEN."),
    dict(name="betterview", description="Betterview property risk intelligence (insurance) REST. ops get/post/put/delete (writes need confirm). Auth: BETTERVIEW_BASE_URL + BETTERVIEW_TOKEN."),
    dict(name="attune_insurance", description="Attune commercial insurance platform REST. ops get/post/put/delete (writes need confirm). Auth: ATTUNE_INSURANCE_BASE_URL + ATTUNE_INSURANCE_TOKEN."),
    dict(name="coalition_insurance", description="Coalition cyber insurance REST. ops get/post/put/delete (writes need confirm). Auth: COALITION_INSURANCE_BASE_URL + COALITION_INSURANCE_TOKEN."),
    dict(name="at_bay", description="At-Bay cyber insurance REST. ops get/post/put/delete (writes need confirm). Auth: AT_BAY_BASE_URL + AT_BAY_TOKEN."),
    dict(name="corvus_insurance", description="Corvus Insurance (cyber) REST. ops get/post/put/delete (writes need confirm). Auth: CORVUS_INSURANCE_BASE_URL + CORVUS_INSURANCE_TOKEN."),
    dict(name="vouch_insurance", description="Vouch Insurance (startup-focused) REST. ops get/post/put/delete (writes need confirm). Auth: VOUCH_INSURANCE_BASE_URL + VOUCH_INSURANCE_TOKEN."),
    dict(name="employee_navigator", description="Employee Navigator benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: EMPLOYEE_NAVIGATOR_BASE_URL + EMPLOYEE_NAVIGATOR_TOKEN."),
    dict(name="ease_benefits", description="Ease benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: EASE_BENEFITS_BASE_URL (https://api.ease.com) + EASE_BENEFITS_TOKEN."),
    dict(name="businessolver", description="Businessolver benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: BUSINESSOLVER_BASE_URL + BUSINESSOLVER_TOKEN."),
    dict(name="bswift", description="bswift (Aon) benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: BSWIFT_BASE_URL + BSWIFT_TOKEN."),
    dict(name="plansource", description="PlanSource benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: PLANSOURCE_BASE_URL + PLANSOURCE_TOKEN."),
    dict(name="benefitfocus", description="Benefitfocus benefits administration REST. ops get/post/put/delete (writes need confirm). Auth: BENEFITFOCUS_BASE_URL + BENEFITFOCUS_TOKEN."),
    dict(name="health_sherpa", description="HealthSherpa ACA health plan enrollment REST. ops get/post/put/delete (writes need confirm). Auth: HEALTH_SHERPA_BASE_URL + HEALTH_SHERPA_TOKEN."),
    dict(name="getinsured", description="GetInsured health exchange/enrollment REST. ops get/post/put/delete (writes need confirm). Auth: GETINSURED_BASE_URL + GETINSURED_TOKEN."),
    dict(name="connecture", description="Connecture insurance distribution/quoting REST. ops get/post/put/delete (writes need confirm). Auth: CONNECTURE_BASE_URL + CONNECTURE_TOKEN."),
    dict(name="ideon_benefits", description="Ideon (formerly Vericred) benefits data REST. ops get/post/put/delete (writes need confirm). Auth: IDEON_BENEFITS_BASE_URL (https://api.ideonapi.com) + IDEON_BENEFITS_TOKEN."),
    dict(name="limelight_health", description="Limelight Health benefits underwriting/quoting REST. ops get/post/put/delete (writes need confirm). Auth: LIMELIGHT_HEALTH_BASE_URL + LIMELIGHT_HEALTH_TOKEN."),
    dict(name="noyo", description="Noyo benefits data connectivity REST. ops get/post/put/delete (writes need confirm). Auth: NOYO_BASE_URL (https://api.noyo.com) + NOYO_TOKEN."),
    dict(name="procore", description="Procore construction project management REST. ops get/post/put/delete (writes need confirm). e.g. /rest/v1.0/projects. Auth: PROCORE_BASE_URL (https://api.procore.com) + PROCORE_TOKEN."),
    dict(name="autodesk_construction", description="Autodesk Construction Cloud (BIM 360) REST. ops get/post/put/delete (writes need confirm). Auth: AUTODESK_CONSTRUCTION_BASE_URL (https://developer.api.autodesk.com) + AUTODESK_CONSTRUCTION_TOKEN."),
    dict(name="plangrid", description="PlanGrid (Autodesk) construction field collaboration REST. ops get/post/put/delete (writes need confirm). Auth: PLANGRID_BASE_URL (https://io.plangrid.com) + PLANGRID_TOKEN."),
    dict(name="buildertrend", description="Buildertrend construction project management REST. ops get/post/put/delete (writes need confirm). Auth: BUILDERTREND_BASE_URL + BUILDERTREND_TOKEN."),
    dict(name="coconstruct", description="CoConstruct residential construction management REST. ops get/post/put/delete (writes need confirm). Auth: COCONSTRUCT_BASE_URL + COCONSTRUCT_TOKEN."),
    dict(name="buildops", description="BuildOps commercial contractor management REST. ops get/post/put/delete (writes need confirm). Auth: BUILDOPS_BASE_URL + BUILDOPS_TOKEN."),
    dict(name="bluebeam", description="Bluebeam construction document/markup REST. ops get/post/put/delete (writes need confirm). Auth: BLUEBEAM_BASE_URL (https://studioapi.bluebeam.com) + BLUEBEAM_TOKEN."),
    dict(name="newforma", description="Newforma project information management (AEC) REST. ops get/post/put/delete (writes need confirm). Auth: NEWFORMA_BASE_URL + NEWFORMA_TOKEN."),
    dict(name="deltek", description="Deltek project management (A/E firms) REST. ops get/post/put/delete (writes need confirm). Auth: DELTEK_BASE_URL + DELTEK_TOKEN."),
    dict(name="sage_construction", description="Sage 300 Construction and Real Estate REST. ops get/post/put/delete (writes need confirm). Auth: SAGE_CONSTRUCTION_BASE_URL + SAGE_CONSTRUCTION_TOKEN."),
    dict(name="viewpoint_construction", description="Viewpoint (Trimble Construction One) REST. ops get/post/put/delete (writes need confirm). Auth: VIEWPOINT_CONSTRUCTION_BASE_URL + VIEWPOINT_CONSTRUCTION_TOKEN."),
    dict(name="cmic", description="CMiC construction ERP/project management REST. ops get/post/put/delete (writes need confirm). Auth: CMIC_BASE_URL + CMIC_TOKEN."),
    dict(name="ebuilder", description="e-Builder (Trimble) capital program management REST. ops get/post/put/delete (writes need confirm). Auth: EBUILDER_BASE_URL + EBUILDER_TOKEN."),
    dict(name="aconex", description="Oracle Aconex construction collaboration REST. ops get/post/put/delete (writes need confirm). Auth: ACONEX_BASE_URL (https://api.aconex.com) + ACONEX_TOKEN."),
    dict(name="primavera_p6", description="Oracle Primavera P6 project scheduling REST. ops get/post/put/delete (writes need confirm). Auth: PRIMAVERA_P6_BASE_URL + PRIMAVERA_P6_TOKEN."),
    dict(name="textura", description="Textura (Oracle) construction payment management REST. ops get/post/put/delete (writes need confirm). Auth: TEXTURA_BASE_URL + TEXTURA_TOKEN."),
    dict(name="redteam", description="RedTeam construction project management REST. ops get/post/put/delete (writes need confirm). Auth: REDTEAM_BASE_URL + REDTEAM_TOKEN."),
    dict(name="contractor_foreman", description="Contractor Foreman construction management REST. ops get/post/put/delete (writes need confirm). Auth: CONTRACTOR_FOREMAN_BASE_URL + CONTRACTOR_FOREMAN_TOKEN."),
    dict(name="knowify", description="Knowify job costing/project management REST. ops get/post/put/delete (writes need confirm). Auth: KNOWIFY_BASE_URL (https://api.knowify.com) + KNOWIFY_TOKEN."),
    dict(name="jobtread", description="JobTread construction project management REST. ops get/post/put/delete (writes need confirm). Auth: JOBTREAD_BASE_URL + JOBTREAD_TOKEN."),
    dict(name="stack_construction", description="STACK Construction Technologies takeoff/estimating REST. ops get/post/put/delete (writes need confirm). Auth: STACK_CONSTRUCTION_BASE_URL + STACK_CONSTRUCTION_TOKEN."),
    dict(name="bridgit_bench", description="Bridgit Bench construction workforce planning REST. ops get/post/put/delete (writes need confirm). Auth: BRIDGIT_BENCH_BASE_URL + BRIDGIT_BENCH_TOKEN."),
    dict(name="rhumbix", description="Rhumbix construction field data collection REST. ops get/post/put/delete (writes need confirm). Auth: RHUMBIX_BASE_URL + RHUMBIX_TOKEN."),
    dict(name="raken", description="Raken construction daily reporting REST. ops get/post/put/delete (writes need confirm). Auth: RAKEN_BASE_URL (https://api.rakenapp.com) + RAKEN_TOKEN."),
    dict(name="hcss", description="HCSS heavy civil construction management REST. ops get/post/put/delete (writes need confirm). Auth: HCSS_BASE_URL + HCSS_TOKEN."),
    dict(name="b2w_software", description="B2W Software heavy construction operations REST. ops get/post/put/delete (writes need confirm). Auth: B2W_SOFTWARE_BASE_URL + B2W_SOFTWARE_TOKEN."),
    dict(name="esub", description="eSUB subcontractor project management REST. ops get/post/put/delete (writes need confirm). Auth: ESUB_BASE_URL + ESUB_TOKEN."),
    dict(name="buildxact", description="Buildxact construction estimating/project management REST. ops get/post/put/delete (writes need confirm). Auth: BUILDXACT_BASE_URL + BUILDXACT_TOKEN."),
    dict(name="houzz_pro", description="Houzz Pro contractor/design project management REST. ops get/post/put/delete (writes need confirm). Auth: HOUZZ_PRO_BASE_URL + HOUZZ_PRO_TOKEN."),
    dict(name="archdesk", description="Archdesk construction management platform REST. ops get/post/put/delete (writes need confirm). Auth: ARCHDESK_BASE_URL + ARCHDESK_TOKEN."),
    dict(name="archisnapper", description="ArchiSnapper site visit/punch list reporting REST. ops get/post/put/delete (writes need confirm). Auth: ARCHISNAPPER_BASE_URL + ARCHISNAPPER_TOKEN."),
    dict(name="monograph", description="Monograph architecture firm project management REST. ops get/post/put/delete (writes need confirm). Auth: MONOGRAPH_BASE_URL (https://api.monograph.com) + MONOGRAPH_TOKEN."),
    dict(name="bqe_core", description="BQE Core professional services/AEC management REST. ops get/post/put/delete (writes need confirm). Auth: BQE_CORE_BASE_URL + BQE_CORE_TOKEN."),
    dict(name="revizto", description="Revizto BIM collaboration/issue tracking REST. ops get/post/put/delete (writes need confirm). Auth: REVIZTO_BASE_URL + REVIZTO_TOKEN."),
    dict(name="kahua", description="Kahua construction/capital program management REST. ops get/post/put/delete (writes need confirm). Auth: KAHUA_BASE_URL + KAHUA_TOKEN."),
    dict(name="ineight", description="InEight capital project management REST. ops get/post/put/delete (writes need confirm). Auth: INEIGHT_BASE_URL + INEIGHT_TOKEN."),
    dict(name="gcpay", description="GCPay (Procore) construction payment applications REST. ops get/post/put/delete (writes need confirm). Auth: GCPAY_BASE_URL + GCPAY_TOKEN."),
    dict(name="levelset", description="Levelset (Procore Payments) construction lien/payment REST. ops get/post/put/delete (writes need confirm). Auth: LEVELSET_BASE_URL + LEVELSET_TOKEN."),
    dict(name="smartbid", description="SmartBid construction bid management REST. ops get/post/put/delete (writes need confirm). Auth: SMARTBID_BASE_URL + SMARTBID_TOKEN."),
    dict(name="assignar", description="Assignar construction workforce/asset management REST. ops get/post/put/delete (writes need confirm). Auth: ASSIGNAR_BASE_URL + ASSIGNAR_TOKEN."),
    dict(name="workmax", description="WorkMax (AboutTime Technologies) construction time/data collection REST. ops get/post/put/delete (writes need confirm). Auth: WORKMAX_BASE_URL + WORKMAX_TOKEN."),
    dict(name="busybusy", description="busybusy construction time tracking/GPS REST. ops get/post/put/delete (writes need confirm). Auth: BUSYBUSY_BASE_URL (https://api.busybusy.io) + BUSYBUSY_TOKEN."),
    dict(name="exaktime", description="ExakTime construction time/attendance tracking REST. ops get/post/put/delete (writes need confirm). Auth: EXAKTIME_BASE_URL + EXAKTIME_TOKEN."),
    dict(name="datev", description="DATEV accounting/tax REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: DATEV_BASE_URL (https://api.datev.de) + DATEV_TOKEN."),
    dict(name="lexoffice", description="lexoffice accounting REST API (Germany, Haufe-Lexware). ops get/post/put/delete (writes need confirm). Auth: LEXOFFICE_BASE_URL (https://api.lexoffice.io) + LEXOFFICE_TOKEN."),
    dict(name="sevdesk", description="sevDesk accounting REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: SEVDESK_BASE_URL (https://my.sevdesk.de/api) + SEVDESK_TOKEN."),
    dict(name="weclapp", token_header="AuthenticationToken", scheme="", description="weclapp ERP/CRM REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: WECLAPP_BASE_URL (https://{tenant}.weclapp.com/webapp/api/v1) + WECLAPP_TOKEN."),
    dict(name="billomat", token_header="X-BillomatApiKey", scheme="", description="Billomat invoicing REST API (Germany, sevDesk group). ops get/post/put/delete (writes need confirm). Auth: BILLOMAT_BASE_URL (https://{id}.billomat.net/api) + BILLOMAT_TOKEN."),
    dict(name="jtl_wawi", description="JTL-Wawi ERP/warehouse REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: JTL_WAWI_BASE_URL + JTL_WAWI_TOKEN."),
    dict(name="billbee", basic=True, description="Billbee multichannel ecommerce REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: BILLBEE_BASE_URL (https://api.billbee.io) + BILLBEE_TOKEN (API user/key)."),
    dict(name="moneybird", description="Moneybird accounting REST API (Netherlands). ops get/post/put/delete (writes need confirm). Auth: MONEYBIRD_BASE_URL (https://moneybird.com/api) + MONEYBIRD_TOKEN."),
    dict(name="twinfield", description="Twinfield accounting REST API (Netherlands, Wolters Kluwer). ops get/post/put/delete (writes need confirm). Auth: TWINFIELD_BASE_URL + TWINFIELD_TOKEN."),
    dict(name="yuki", description="Yuki accounting REST API (Netherlands). ops get/post/put/delete (writes need confirm). Auth: YUKI_BASE_URL + YUKI_TOKEN."),
    dict(name="exact_online", description="Exact Online ERP/accounting REST API (Netherlands/Belgium). ops get/post/put/delete (writes need confirm). Auth: EXACT_ONLINE_BASE_URL (https://start.exactonline.nl/api) + EXACT_ONLINE_TOKEN."),
    dict(name="afas", token_header="AfasToken", scheme="", description="AFAS Software ERP REST API (Netherlands). ops get/post/put/delete (writes need confirm). Auth: AFAS_BASE_URL (https://{env}.rest.afas.online/profitrestservices) + AFAS_TOKEN."),
    dict(name="silverfin", description="Silverfin accounting collaboration REST API (Belgium/UK). ops get/post/put/delete (writes need confirm). Auth: SILVERFIN_BASE_URL (https://live.getsilverfin.com/api) + SILVERFIN_TOKEN."),
    dict(name="teamleader", description="Teamleader CRM/invoicing REST API (Belgium). ops get/post/put/delete (writes need confirm). Auth: TEAMLEADER_BASE_URL (https://api.focus.teamleader.eu) + TEAMLEADER_TOKEN."),
    dict(name="cegid", description="Cegid ERP/retail/HR REST API (France). ops get/post/put/delete (writes need confirm). Auth: CEGID_BASE_URL + CEGID_TOKEN."),
    dict(name="pennylane", description="Pennylane accounting REST API (France). ops get/post/put/delete (writes need confirm). Auth: PENNYLANE_BASE_URL (https://app.pennylane.com/api) + PENNYLANE_TOKEN."),
    dict(name="qonto", description="Qonto business banking REST API (France). ops get/post/put/delete (writes need confirm). Auth: QONTO_BASE_URL (https://thirdparty.qonto.com/v2) + QONTO_TOKEN."),
    dict(name="lucca", description="Lucca HR suite REST API (France). ops get/post/put/delete (writes need confirm). Auth: LUCCA_BASE_URL (https://{sub}.ilucca.net/api) + LUCCA_TOKEN."),
    dict(name="spendesk", description="Spendesk expense management REST API (France). ops get/post/put/delete (writes need confirm). Auth: SPENDESK_BASE_URL (https://api.spendesk.com) + SPENDESK_TOKEN."),
    dict(name="sellsy", description="Sellsy CRM/invoicing REST API (France). ops get/post/put/delete (writes need confirm). Auth: SELLSY_BASE_URL (https://api.sellsy.com/v2) + SELLSY_TOKEN."),
    dict(name="axonaut", description="Axonaut CRM/invoicing REST API (France). ops get/post/put/delete (writes need confirm). Auth: AXONAUT_BASE_URL (https://axonaut.com/api/v2) + AXONAUT_TOKEN."),
    dict(name="efficy_crm", description="Efficy CRM REST API (Belgium/France). ops get/post/put/delete (writes need confirm). Auth: EFFICY_CRM_BASE_URL + EFFICY_CRM_TOKEN."),
    dict(name="tide", description="Tide business banking REST API (UK). ops get/post/put/delete (writes need confirm). Auth: TIDE_BASE_URL + TIDE_TOKEN."),
    dict(name="trustly", description="Trustly open banking payments REST API (Sweden/EU). ops get/post/put/delete (writes need confirm). Auth: TRUSTLY_BASE_URL + TRUSTLY_TOKEN."),
    dict(name="visma", description="Visma ERP/accounting REST API (Nordics). ops get/post/put/delete (writes need confirm). Auth: VISMA_BASE_URL (https://eaccountingapi.vismaonline.com) + VISMA_TOKEN."),
    dict(name="fortnox", token_header="Access-Token", scheme="", description="Fortnox accounting REST API (Sweden). ops get/post/put/delete (writes need confirm). Auth: FORTNOX_BASE_URL (https://api.fortnox.se/3) + FORTNOX_TOKEN."),
    dict(name="tripletex", description="Tripletex accounting/ERP REST API (Norway). ops get/post/put/delete (writes need confirm). Auth: TRIPLETEX_BASE_URL (https://tripletex.no/v2) + TRIPLETEX_TOKEN."),
    dict(name="reckon", description="Reckon accounting REST API (Australia). ops get/post/put/delete (writes need confirm). Auth: RECKON_BASE_URL + RECKON_TOKEN."),
    dict(name="otto", description="OTTO Market ecommerce REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: OTTO_BASE_URL (https://api.otto.market) + OTTO_TOKEN."),
    dict(name="inpost", description="InPost parcel locker logistics REST API (Poland). ops get/post/put/delete (writes need confirm). Auth: INPOST_BASE_URL (https://api-shipx-pl.easypack24.net) + INPOST_TOKEN."),
    dict(name="przelewy24", basic=True, description="Przelewy24 (P24) payments REST API (Poland). ops get/post/put/delete (writes need confirm). Auth: PRZELEWY24_BASE_URL (https://secure.przelewy24.pl/api) + PRZELEWY24_TOKEN."),
    dict(name="bring", token_header="X-Mybring-API-Key", scheme="", description="Bring (Posten Norge) logistics REST API (Norway). ops get/post/put/delete (writes need confirm). Auth: BRING_BASE_URL (https://api.bring.com) + BRING_TOKEN."),
    dict(name="postnord", description="PostNord logistics REST API (Nordics). ops get/post/put/delete (writes need confirm). Auth: POSTNORD_BASE_URL (https://api2.postnord.com) + POSTNORD_TOKEN."),
    dict(name="safetyculture", description="SafetyCulture (iAuditor) inspections REST API (Australia). ops get/post/put/delete (writes need confirm). Auth: SAFETYCULTURE_BASE_URL (https://api.safetyculture.io) + SAFETYCULTURE_TOKEN."),
    dict(name="servicem8", basic=True, description="ServiceM8 field service management REST API (Australia). ops get/post/put/delete (writes need confirm). Auth: SERVICEM8_BASE_URL (https://api.servicem8.com/api_1.0) + SERVICEM8_TOKEN."),
    dict(name="interswitch", description="Interswitch payments REST API (Nigeria). ops get/post/put/delete (writes need confirm). Auth: INTERSWITCH_BASE_URL + INTERSWITCH_TOKEN."),
    dict(name="yoco", token_header="X-Auth-Secret-Key", scheme="", description="Yoco payments REST API (South Africa). ops get/post/put/delete (writes need confirm). Auth: YOCO_BASE_URL (https://payments.yoco.com/api) + YOCO_TOKEN."),
    dict(name="fawry", description="Fawry payments REST API (Egypt). ops get/post/put/delete (writes need confirm). Auth: FAWRY_BASE_URL + FAWRY_TOKEN."),
    dict(name="geidea", description="Geidea payments REST API (Saudi Arabia). ops get/post/put/delete (writes need confirm). Auth: GEIDEA_BASE_URL + GEIDEA_TOKEN."),
    dict(name="network_international", description="Network International payments REST API (MENA). ops get/post/put/delete (writes need confirm). Auth: NETWORK_INTERNATIONAL_BASE_URL + NETWORK_INTERNATIONAL_TOKEN."),
    dict(name="telr", description="Telr payment gateway REST API (MENA). ops get/post/put/delete (writes need confirm). Auth: TELR_BASE_URL (https://secure.telr.com/gateway) + TELR_TOKEN."),
    dict(name="clip", description="Clip payments REST API (Mexico). ops get/post/put/delete (writes need confirm). Auth: CLIP_BASE_URL (https://api.payclip.com) + CLIP_TOKEN."),
    dict(name="conekta", basic=True, description="Conekta payments REST API (Mexico). ops get/post/put/delete (writes need confirm). Auth: CONEKTA_BASE_URL (https://api.conekta.io) + CONEKTA_TOKEN."),
    dict(name="kushki", description="Kushki payments REST API (Latin America). ops get/post/put/delete (writes need confirm). Auth: KUSHKI_BASE_URL (https://api.kushkipagos.com) + KUSHKI_TOKEN."),
    dict(name="alegra", basic=True, description="Alegra accounting REST API (Latin America). ops get/post/put/delete (writes need confirm). Auth: ALEGRA_BASE_URL (https://api.alegra.com/api/v1) + ALEGRA_TOKEN."),
    dict(name="siigo", description="Siigo accounting REST API (Colombia). ops get/post/put/delete (writes need confirm). Auth: SIIGO_BASE_URL (https://api.siigo.com) + SIIGO_TOKEN."),
    dict(name="facturama", basic=True, description="Facturama e-invoicing REST API (Mexico). ops get/post/put/delete (writes need confirm). Auth: FACTURAMA_BASE_URL (https://api.facturama.mx) + FACTURAMA_TOKEN."),
    dict(name="conta_azul", description="Conta Azul accounting REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: CONTA_AZUL_BASE_URL (https://api.contaazul.com) + CONTA_AZUL_TOKEN."),
    dict(name="bling", description="Bling ERP REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: BLING_BASE_URL (https://api.bling.com.br) + BLING_TOKEN."),
    dict(name="omie", description="Omie ERP REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: OMIE_BASE_URL (https://app.omie.com.br/api) + OMIE_TOKEN."),
    dict(name="totvs", description="TOTVS ERP REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: TOTVS_BASE_URL + TOTVS_TOKEN."),
    dict(name="tiny_erp", description="Tiny ERP REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: TINY_ERP_BASE_URL (https://api.tiny.com.br/api2) + TINY_ERP_TOKEN."),
    dict(name="vindi", basic=True, description="Vindi subscription billing REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: VINDI_BASE_URL (https://app.vindi.com.br/api/v1) + VINDI_TOKEN."),
    dict(name="iugu", basic=True, description="Iugu payments/billing REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: IUGU_BASE_URL (https://api.iugu.com) + IUGU_TOKEN."),
    dict(name="rd_station", description="RD Station marketing automation REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: RD_STATION_BASE_URL (https://api.rd.services) + RD_STATION_TOKEN."),
    dict(name="pagseguro", description="PagSeguro payments REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: PAGSEGURO_BASE_URL (https://api.pagseguro.com) + PAGSEGURO_TOKEN."),
    dict(name="stone_pagamentos", description="Stone payments REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: STONE_PAGAMENTOS_BASE_URL + STONE_PAGAMENTOS_TOKEN."),
    dict(name="nubank", description="Nubank open finance REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: NUBANK_BASE_URL + NUBANK_TOKEN."),
    dict(name="ifood", description="iFood delivery partner REST API (Brazil). ops get/post/put/delete (writes need confirm). Auth: IFOOD_BASE_URL (https://merchant-api.ifood.com.br) + IFOOD_TOKEN."),
    dict(name="pine_labs", description="Pine Labs payments REST API (India). ops get/post/put/delete (writes need confirm). Auth: PINE_LABS_BASE_URL + PINE_LABS_TOKEN."),
    dict(name="billdesk", description="BillDesk payment gateway REST API (India). ops get/post/put/delete (writes need confirm). Auth: BILLDESK_BASE_URL + BILLDESK_TOKEN."),
    dict(name="ccavenue", description="CCAvenue payment gateway REST API (India). ops get/post/put/delete (writes need confirm). Auth: CCAVENUE_BASE_URL + CCAVENUE_TOKEN."),
    dict(name="phonepe", description="PhonePe business payments REST API (India). ops get/post/put/delete (writes need confirm). Auth: PHONEPE_BASE_URL (https://api.phonepe.com/apis/hermes) + PHONEPE_TOKEN."),
    dict(name="kaleyra", token_header="api-key", scheme="", description="Kaleyra CPaaS REST API (India). ops get/post/put/delete (writes need confirm). Auth: KALEYRA_BASE_URL (https://api.kaleyra.io) + KALEYRA_TOKEN."),
    dict(name="gupshup", token_header="apikey", scheme="", description="Gupshup messaging CPaaS REST API (India). ops get/post/put/delete (writes need confirm). Auth: GUPSHUP_BASE_URL (https://api.gupshup.io) + GUPSHUP_TOKEN."),
    dict(name="msg91", token_header="authkey", scheme="", description="MSG91 SMS/communication REST API (India). ops get/post/put/delete (writes need confirm). Auth: MSG91_BASE_URL (https://api.msg91.com) + MSG91_TOKEN."),
    dict(name="textlocal", description="Textlocal SMS REST API (India/UK). ops get/post/put/delete (writes need confirm). Auth: TEXTLOCAL_BASE_URL (https://api.textlocal.in) + TEXTLOCAL_TOKEN."),
    dict(name="webengage", description="WebEngage marketing automation REST API (India). ops get/post/put/delete (writes need confirm). Auth: WEBENGAGE_BASE_URL + WEBENGAGE_TOKEN."),
    dict(name="capillary_tech", description="Capillary Technologies loyalty/CRM REST API (India). ops get/post/put/delete (writes need confirm). Auth: CAPILLARY_TECH_BASE_URL + CAPILLARY_TECH_TOKEN."),
    dict(name="delhivery", scheme="Token", description="Delhivery logistics REST API (India). ops get/post/put/delete (writes need confirm). Auth: DELHIVERY_BASE_URL (https://track.delhivery.com) + DELHIVERY_TOKEN."),
    dict(name="shiprocket", description="Shiprocket logistics aggregator REST API (India). ops get/post/put/delete (writes need confirm). Auth: SHIPROCKET_BASE_URL (https://apiv2.shiprocket.in/v1/external) + SHIPROCKET_TOKEN."),
    dict(name="greythr", description="greytHR HR/payroll REST API (India). ops get/post/put/delete (writes need confirm). Auth: GREYTHR_BASE_URL + GREYTHR_TOKEN."),
    dict(name="darwinbox", description="Darwinbox HR REST API (India). ops get/post/put/delete (writes need confirm). Auth: DARWINBOX_BASE_URL + DARWINBOX_TOKEN."),
    dict(name="keka", description="Keka HR/payroll REST API (India). ops get/post/put/delete (writes need confirm). Auth: KEKA_BASE_URL (https://{company}.keka.com/api) + KEKA_TOKEN."),
    dict(name="kintone", token_header="X-Cybozu-API-Token", scheme="", description="kintone work platform REST API (Japan, Cybozu). ops get/post/put/delete (writes need confirm). Auth: KINTONE_BASE_URL (https://{sub}.cybozu.com/k/v1) + KINTONE_TOKEN."),
    dict(name="sansan", description="Sansan business card/CRM REST API (Japan). ops get/post/put/delete (writes need confirm). Auth: SANSAN_BASE_URL + SANSAN_TOKEN."),
    dict(name="freee", description="freee accounting REST API (Japan). ops get/post/put/delete (writes need confirm). Auth: FREEE_BASE_URL (https://api.freee.co.jp) + FREEE_TOKEN."),
    dict(name="moneyforward", description="Money Forward accounting/finance REST API (Japan). ops get/post/put/delete (writes need confirm). Auth: MONEYFORWARD_BASE_URL + MONEYFORWARD_TOKEN."),
    dict(name="smaregi", description="Smaregi POS REST API (Japan). ops get/post/put/delete (writes need confirm). Auth: SMAREGI_BASE_URL (https://api.smaregi.jp) + SMAREGI_TOKEN."),
    dict(name="grab", description="Grab superapp partner REST API (Singapore/SE Asia). ops get/post/put/delete (writes need confirm). Auth: GRAB_BASE_URL (https://partner-api.grab.com) + GRAB_TOKEN."),
    dict(name="xendit", basic=True, description="Xendit payments REST API (Indonesia). ops get/post/put/delete (writes need confirm). Auth: XENDIT_BASE_URL (https://api.xendit.co) + XENDIT_TOKEN."),
    dict(name="midtrans", basic=True, description="Midtrans payment gateway REST API (Indonesia). ops get/post/put/delete (writes need confirm). Auth: MIDTRANS_BASE_URL (https://api.midtrans.com) + MIDTRANS_TOKEN."),
    dict(name="doku", description="DOKU payment gateway REST API (Indonesia). ops get/post/put/delete (writes need confirm). Auth: DOKU_BASE_URL (https://api.doku.com) + DOKU_TOKEN."),
    dict(name="ipay88", description="iPay88 payment gateway REST API (Malaysia). ops get/post/put/delete (writes need confirm). Auth: IPAY88_BASE_URL + IPAY88_TOKEN."),
    dict(name="nium", description="Nium cross-border payments REST API (Singapore). ops get/post/put/delete (writes need confirm). Auth: NIUM_BASE_URL (https://gateway.nium.com) + NIUM_TOKEN."),
    dict(name="toss", basic=True, description="Toss Payments REST API (South Korea). ops get/post/put/delete (writes need confirm). Auth: TOSS_BASE_URL (https://api.tosspayments.com) + TOSS_TOKEN."),
    dict(name="kakaopay", scheme="SECRET_KEY", description="Kakao Pay REST API (South Korea). ops get/post/put/delete (writes need confirm). Auth: KAKAOPAY_BASE_URL (https://open-api.kakaopay.com) + KAKAOPAY_TOKEN."),
    dict(name="naver", description="Naver Cloud Platform REST API (South Korea). ops get/post/put/delete (writes need confirm). Auth: NAVER_BASE_URL (https://ncloud.apigw.ntruss.com) + NAVER_TOKEN."),
    dict(name="baidu", description="Baidu AI Cloud REST API (China). ops get/post/put/delete (writes need confirm). Auth: BAIDU_BASE_URL (https://aip.baidubce.com) + BAIDU_TOKEN."),
    dict(name="tencent_cloud", description="Tencent Cloud REST API (China). ops get/post/put/delete (writes need confirm). Auth: TENCENT_CLOUD_BASE_URL + TENCENT_CLOUD_TOKEN."),
    dict(name="huawei_cloud", description="Huawei Cloud REST API (China). ops get/post/put/delete (writes need confirm). Auth: HUAWEI_CLOUD_BASE_URL + HUAWEI_CLOUD_TOKEN."),
    dict(name="dingtalk", description="DingTalk enterprise messaging REST API (China, Alibaba). ops get/post/put/delete (writes need confirm). Auth: DINGTALK_BASE_URL (https://oapi.dingtalk.com) + DINGTALK_TOKEN."),
    dict(name="feishu", description="Feishu/Lark collaboration REST API (China, ByteDance). ops get/post/put/delete (writes need confirm). Auth: FEISHU_BASE_URL (https://open.feishu.cn/open-apis) + FEISHU_TOKEN."),
    dict(name="trip_com", description="Trip.com travel partner REST API (China). ops get/post/put/delete (writes need confirm). Auth: TRIP_COM_BASE_URL + TRIP_COM_TOKEN."),
    dict(name="delivery_hero", description="Delivery Hero partner REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: DELIVERY_HERO_BASE_URL + DELIVERY_HERO_TOKEN."),
    dict(name="trivago", description="Trivago hotel search partner REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: TRIVAGO_BASE_URL + TRIVAGO_TOKEN."),
    dict(name="getyourguide", description="GetYourGuide activities partner REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: GETYOURGUIDE_BASE_URL (https://supply.getyourguide.com) + GETYOURGUIDE_TOKEN."),
    dict(name="flixbus", description="FlixBus travel partner REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: FLIXBUS_BASE_URL + FLIXBUS_TOKEN."),
    dict(name="justeat_takeaway", description="Just Eat Takeaway.com delivery partner REST API (Netherlands). ops get/post/put/delete (writes need confirm). Auth: JUSTEAT_TAKEAWAY_BASE_URL + JUSTEAT_TAKEAWAY_TOKEN."),
    dict(name="blablacar", description="BlaBlaCar carpooling partner REST API (France). ops get/post/put/delete (writes need confirm). Auth: BLABLACAR_BASE_URL + BLABLACAR_TOKEN."),
    dict(name="sixt", description="Sixt car rental partner REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: SIXT_BASE_URL + SIXT_TOKEN."),
    dict(name="takealot", scheme="Key", description="Takealot marketplace seller REST API (South Africa). ops get/post/put/delete (writes need confirm). Auth: TAKEALOT_BASE_URL (https://seller-api.takealot.com) + TAKEALOT_TOKEN."),
    dict(name="jumia", description="Jumia marketplace seller REST API (Africa). ops get/post/put/delete (writes need confirm). Auth: JUMIA_BASE_URL + JUMIA_TOKEN."),
    dict(name="daraz", description="Daraz marketplace seller REST API (South Asia, Alibaba). ops get/post/put/delete (writes need confirm). Auth: DARAZ_BASE_URL (https://api.daraz.com) + DARAZ_TOKEN."),
    dict(name="openpay_mx", basic=True, description="Openpay payments REST API (Mexico, BBVA). ops get/post/put/delete (writes need confirm). Auth: OPENPAY_MX_BASE_URL (https://api.openpay.mx) + OPENPAY_MX_TOKEN."),
    dict(name="billplz", basic=True, description="Billplz payment gateway REST API (Malaysia). ops get/post/put/delete (writes need confirm). Auth: BILLPLZ_BASE_URL (https://www.billplz.com/api) + BILLPLZ_TOKEN."),
    dict(name="computop", description="Computop payment gateway REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: COMPUTOP_BASE_URL + COMPUTOP_TOKEN."),
    dict(name="unzer", basic=True, description="Unzer payment gateway REST API (Germany, formerly Heidelpay). ops get/post/put/delete (writes need confirm). Auth: UNZER_BASE_URL (https://api.unzer.com) + UNZER_TOKEN."),
    dict(name="modulr", description="Modulr embedded banking REST API (UK). ops get/post/put/delete (writes need confirm). Auth: MODULR_BASE_URL (https://api.modulrfinance.com) + MODULR_TOKEN."),
    dict(name="awork", description="awork project management REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: AWORK_BASE_URL (https://api.awork.io) + AWORK_TOKEN."),
    dict(name="clockodo", token_header="X-Clockodo-Api-Key", scheme="", description="Clockodo time tracking REST API (Germany). ops get/post/put/delete (writes need confirm). Auth: CLOCKODO_BASE_URL (https://my.clockodo.com/api) + CLOCKODO_TOKEN."),
    dict(name="sage_hr", token_header="X-Auth-Token", scheme="", description="Sage HR REST API (UK, formerly CakeHR). ops get/post/put/delete (writes need confirm). Auth: SAGE_HR_BASE_URL (https://api.sage.hr) + SAGE_HR_TOKEN."),
    dict(name="plivo", basic=True, description="Plivo REST API. ops get/post/put/delete (writes need confirm). Auth: PLIVO_BASE_URL (https://api.plivo.com) + PLIVO_TOKEN (Auth ID as user, Auth Token as password)."),
    dict(name="telnyx", description="Telnyx REST API (SMS/voice/numbers). ops get/post/put/delete (writes need confirm). Auth: TELNYX_BASE_URL (https://api.telnyx.com) + TELNYX_TOKEN."),
    dict(name="bandwidth", basic=True, description="Bandwidth messaging/voice REST API. ops get/post/put/delete (writes need confirm). Auth: BANDWIDTH_BASE_URL (https://messaging.bandwidth.com) + BANDWIDTH_TOKEN."),
    dict(name="messagebird", scheme="AccessKey", description="MessageBird (Bird) messaging REST API. ops get/post/put/delete (writes need confirm). Auth: MESSAGEBIRD_BASE_URL (https://rest.messagebird.com) + MESSAGEBIRD_TOKEN."),
    dict(name="infobip", scheme="App", description="Infobip omnichannel messaging REST API. ops get/post/put/delete (writes need confirm). Auth: INFOBIP_BASE_URL (https://{base}.api.infobip.com) + INFOBIP_TOKEN."),
    dict(name="sinch", description="Sinch SMS/voice/conversation REST API. ops get/post/put/delete (writes need confirm). Auth: SINCH_BASE_URL (https://sms.api.sinch.com) + SINCH_TOKEN."),
    dict(name="africastalking", token_header="apiKey", scheme="", description="Africa's Talking SMS/voice REST API. ops get/post/put/delete (writes need confirm). Auth: AFRICASTALKING_BASE_URL (https://api.africastalking.com) + AFRICASTALKING_TOKEN."),
    dict(name="clickatell", description="Clickatell messaging REST API. ops get/post/put/delete (writes need confirm). Auth: CLICKATELL_BASE_URL (https://platform.clickatell.com) + CLICKATELL_TOKEN."),
    dict(name="burstsms", basic=True, description="Burst SMS REST API. ops get/post/put/delete (writes need confirm). Auth: BURSTSMS_BASE_URL (https://api.burstsms.com) + BURSTSMS_TOKEN."),
    dict(name="soprano_design", description="Soprano Design enterprise messaging REST API. ops get/post/put/delete (writes need confirm). Auth: SOPRANO_DESIGN_BASE_URL + SOPRANO_DESIGN_TOKEN."),
    dict(name="tyntec", token_header="apikey", scheme="", description="tyntec communications REST API. ops get/post/put/delete (writes need confirm). Auth: TYNTEC_BASE_URL (https://api.tyntec.com) + TYNTEC_TOKEN."),
    dict(name="smsglobal", description="SMSGlobal messaging REST API. ops get/post/put/delete (writes need confirm). Auth: SMSGLOBAL_BASE_URL (https://api.smsglobal.com) + SMSGLOBAL_TOKEN."),
    dict(name="messagemedia", basic=True, description="MessageMedia messaging REST API. ops get/post/put/delete (writes need confirm). Auth: MESSAGEMEDIA_BASE_URL (https://api.messagemedia.com) + MESSAGEMEDIA_TOKEN."),
    dict(name="cm_com", token_header="X-CM-PRODUCTTOKEN", scheme="", description="CM.com Communications Platform REST API. ops get/post/put/delete (writes need confirm). Auth: CM_COM_BASE_URL (https://gateway.cmtelecom.com) + CM_COM_TOKEN."),
    dict(name="link_mobility", description="LINK Mobility messaging REST API. ops get/post/put/delete (writes need confirm). Auth: LINK_MOBILITY_BASE_URL + LINK_MOBILITY_TOKEN."),
    dict(name="esendex", basic=True, description="Esendex messaging REST API. ops get/post/put/delete (writes need confirm). Auth: ESENDEX_BASE_URL (https://api.esendex.com) + ESENDEX_TOKEN."),
    dict(name="didww", description="DIDWW telecom numbers/voice REST API (JSON:API). ops get/post/put/delete (writes need confirm). Auth: DIDWW_BASE_URL (https://api.didww.com) + DIDWW_TOKEN."),
    dict(name="flowroute", basic=True, description="Flowroute SMS/voice REST API. ops get/post/put/delete (writes need confirm). Auth: FLOWROUTE_BASE_URL (https://api.flowroute.com) + FLOWROUTE_TOKEN."),
    dict(name="voipms", description="VoIP.ms REST API. ops get/post/put/delete (writes need confirm). Auth: VOIPMS_BASE_URL (https://voip.ms/api/v1) + VOIPMS_TOKEN."),
    dict(name="karix", description="Karix (Route Mobile) messaging REST API. ops get/post/put/delete (writes need confirm). Auth: KARIX_BASE_URL + KARIX_TOKEN."),
    dict(name="route_mobile", description="Route Mobile SMS/voice REST API. ops get/post/put/delete (writes need confirm). Auth: ROUTE_MOBILE_BASE_URL + ROUTE_MOBILE_TOKEN."),
    dict(name="telesign", basic=True, description="Telesign phone verification/messaging REST API. ops get/post/put/delete (writes need confirm). Auth: TELESIGN_BASE_URL (https://rest-api.telesign.com) + TELESIGN_TOKEN."),
    dict(name="plum_voice", description="Plum Voice IVR/voice REST API. ops get/post/put/delete (writes need confirm). Auth: PLUM_VOICE_BASE_URL + PLUM_VOICE_TOKEN."),
    dict(name="avaya", description="Avaya OneCloud/Aura REST API. ops get/post/put/delete (writes need confirm). Auth: AVAYA_BASE_URL + AVAYA_TOKEN."),
    dict(name="mitel", description="Mitel CloudLink REST API. ops get/post/put/delete (writes need confirm). Auth: MITEL_BASE_URL (https://api.mitel.io) + MITEL_TOKEN."),
    dict(name="catapush", description="Catapush secure push messaging REST API. ops get/post/put/delete (writes need confirm). Auth: CATAPUSH_BASE_URL + CATAPUSH_TOKEN."),
    dict(name="kumulos", description="Kumulos push notification REST API. ops get/post/put/delete (writes need confirm). Auth: KUMULOS_BASE_URL (https://api.kumulos.com) + KUMULOS_TOKEN."),
    dict(name="batch_push", token_header="X-Authorization", scheme="", description="Batch.com push notification REST API. ops get/post/put/delete (writes need confirm). Auth: BATCH_PUSH_BASE_URL (https://api.batch.com) + BATCH_PUSH_TOKEN."),
    dict(name="sendbird", token_header="Api-Token", scheme="", description="Sendbird chat platform REST API. ops get/post/put/delete (writes need confirm). Auth: SENDBIRD_BASE_URL (https://api-{app_id}.sendbird.com) + SENDBIRD_TOKEN."),
    dict(name="pubnub", description="PubNub realtime pub/sub REST API. ops get/post/put/delete (writes need confirm). Auth: PUBNUB_BASE_URL (https://ps.pndsn.com) + PUBNUB_TOKEN (publish/subscribe key)."),
    dict(name="pusher", description="Pusher Channels REST API. ops get/post/put/delete (writes need confirm). Auth: PUSHER_BASE_URL (https://api-{cluster}.pusher.com) + PUSHER_TOKEN."),
    dict(name="dialogflow", description="Google Dialogflow conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: DIALOGFLOW_BASE_URL (https://dialogflow.googleapis.com) + DIALOGFLOW_TOKEN."),
    dict(name="watson_assistant", basic=True, description="IBM Watson Assistant REST API. ops get/post/put/delete (writes need confirm). Auth: WATSON_ASSISTANT_BASE_URL (https://api.<region>.assistant.watson.cloud.ibm.com) + WATSON_ASSISTANT_TOKEN (apikey)."),
    dict(name="amazon_lex", description="Amazon Lex V2 conversational bot runtime REST API. ops get/post/put/delete (writes need confirm). Auth: AMAZON_LEX_BASE_URL + AMAZON_LEX_TOKEN."),
    dict(name="rasa", description="Rasa Open Source assistant REST API. ops get/post/put/delete (writes need confirm). Auth: RASA_BASE_URL (http://localhost:5005) + RASA_TOKEN."),
    dict(name="mobilemonkey", description="MobileMonkey chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: MOBILEMONKEY_BASE_URL + MOBILEMONKEY_TOKEN."),
    dict(name="kore_ai", description="Kore.ai conversational AI platform REST API. ops get/post/put/delete (writes need confirm). Auth: KORE_AI_BASE_URL (https://platform.kore.ai) + KORE_AI_TOKEN."),
    dict(name="haptik", description="Haptik conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: HAPTIK_BASE_URL + HAPTIK_TOKEN."),
    dict(name="verloop", description="Verloop.io chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: VERLOOP_BASE_URL + VERLOOP_TOKEN."),
    dict(name="cognigy", description="Cognigy.AI conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: COGNIGY_BASE_URL (https://api.cognigy.ai) + COGNIGY_TOKEN."),
    dict(name="inbenta", description="Inbenta conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: INBENTA_BASE_URL (https://api.inbenta.io) + INBENTA_TOKEN."),
    dict(name="botsify", description="Botsify chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: BOTSIFY_BASE_URL (https://botsify.com/api) + BOTSIFY_TOKEN."),
    dict(name="aivo_chatbot", description="Aivo conversational AI chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: AIVO_CHATBOT_BASE_URL + AIVO_CHATBOT_TOKEN."),
    dict(name="kommunicate", description="Kommunicate chatbot/live-chat REST API. ops get/post/put/delete (writes need confirm). Auth: KOMMUNICATE_BASE_URL (https://api.kommunicate.io) + KOMMUNICATE_TOKEN."),
    dict(name="revechat", description="REVE Chat live-chat/chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: REVECHAT_BASE_URL + REVECHAT_TOKEN."),
    dict(name="acquire_io", description="Acquire.io chatbot/live-chat REST API. ops get/post/put/delete (writes need confirm). Auth: ACQUIRE_IO_BASE_URL + ACQUIRE_IO_TOKEN."),
    dict(name="smartsupp", description="Smartsupp live-chat REST API. ops get/post/put/delete (writes need confirm). Auth: SMARTSUPP_BASE_URL (https://api.smartsupp.com) + SMARTSUPP_TOKEN."),
    dict(name="pandorabots", description="Pandorabots AIML chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: PANDORABOTS_BASE_URL (https://api.pandorabots.com) + PANDORABOTS_TOKEN."),
    dict(name="freshchat", description="Freshchat (Freshworks) messaging REST API. ops get/post/put/delete (writes need confirm). Auth: FRESHCHAT_BASE_URL (https://{domain}.freshchat.com) + FRESHCHAT_TOKEN."),
    dict(name="flow_xo", description="Flow XO chatbot automation REST API. ops get/post/put/delete (writes need confirm). Auth: FLOW_XO_BASE_URL (https://api.flowxo.com) + FLOW_XO_TOKEN."),
    dict(name="chatbot_com", description="Chatbot.com builder REST API. ops get/post/put/delete (writes need confirm). Auth: CHATBOT_COM_BASE_URL (https://api.chatbot.com) + CHATBOT_COM_TOKEN."),
    dict(name="wit_ai", description="Meta Wit.ai natural language understanding REST API. ops get/post/put/delete (writes need confirm). Auth: WIT_AI_BASE_URL (https://api.wit.ai) + WIT_AI_TOKEN."),
    dict(name="amazon_connect", description="Amazon Connect contact center REST API. ops get/post/put/delete (writes need confirm). Auth: AMAZON_CONNECT_BASE_URL + AMAZON_CONNECT_TOKEN."),
    dict(name="liveperson", description="LivePerson Conversational Cloud REST API. ops get/post/put/delete (writes need confirm). Auth: LIVEPERSON_BASE_URL + LIVEPERSON_TOKEN."),
    dict(name="netomi", description="Netomi conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: NETOMI_BASE_URL + NETOMI_TOKEN."),
    dict(name="ultimate_ai", description="Ultimate.ai conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: ULTIMATE_AI_BASE_URL + ULTIMATE_AI_TOKEN."),
    dict(name="kasisto", description="Kasisto KAI conversational banking AI REST API. ops get/post/put/delete (writes need confirm). Auth: KASISTO_BASE_URL + KASISTO_TOKEN."),
    dict(name="soul_machines", description="Soul Machines digital human REST API. ops get/post/put/delete (writes need confirm). Auth: SOUL_MACHINES_BASE_URL + SOUL_MACHINES_TOKEN."),
    dict(name="smooch", description="Smooch / Sunshine Conversations REST API. ops get/post/put/delete (writes need confirm). Auth: SMOOCH_BASE_URL (https://api.smooch.io) + SMOOCH_TOKEN."),
    dict(name="numverify", description="numverify phone number validation REST API. ops get/post/put/delete (writes need confirm). Auth: NUMVERIFY_BASE_URL (https://apilayer.net) + NUMVERIFY_TOKEN."),
    dict(name="prove_identity", description="Prove Identity phone verification REST API. ops get/post/put/delete (writes need confirm). Auth: PROVE_IDENTITY_BASE_URL + PROVE_IDENTITY_TOKEN."),
    dict(name="voximplant", description="Voximplant cloud communications platform REST API. ops get/post/put/delete (writes need confirm). Auth: VOXIMPLANT_BASE_URL (https://api.voximplant.com) + VOXIMPLANT_TOKEN."),
    dict(name="callfire", basic=True, description="CallFire voice/SMS REST API. ops get/post/put/delete (writes need confirm). Auth: CALLFIRE_BASE_URL (https://api.callfire.com) + CALLFIRE_TOKEN."),
    dict(name="callhippo", description="CallHippo cloud telephony REST API. ops get/post/put/delete (writes need confirm). Auth: CALLHIPPO_BASE_URL + CALLHIPPO_TOKEN."),
    dict(name="toky", description="Toky VoIP calling REST API. ops get/post/put/delete (writes need confirm). Auth: TOKY_BASE_URL (https://api.toky.co) + TOKY_TOKEN."),
    dict(name="ringover", description="Ringover cloud phone system REST API. ops get/post/put/delete (writes need confirm). Auth: RINGOVER_BASE_URL (https://public-api.ringover.com) + RINGOVER_TOKEN."),
    dict(name="zadarma", description="Zadarma VoIP REST API. ops get/post/put/delete (writes need confirm). Auth: ZADARMA_BASE_URL (https://api.zadarma.com) + ZADARMA_TOKEN."),
    dict(name="nextiva", description="Nextiva business phone/UCaaS REST API. ops get/post/put/delete (writes need confirm). Auth: NEXTIVA_BASE_URL + NEXTIVA_TOKEN."),
    dict(name="ooma", description="Ooma Office business phone REST API. ops get/post/put/delete (writes need confirm). Auth: OOMA_BASE_URL + OOMA_TOKEN."),
    dict(name="three_cx", description="3CX PBX/UCaaS REST API. ops get/post/put/delete (writes need confirm). Auth: THREE_CX_BASE_URL + THREE_CX_TOKEN."),
    dict(name="trumpia", description="Trumpia SMS marketing/automation REST API. ops get/post/put/delete (writes need confirm). Auth: TRUMPIA_BASE_URL (https://api.trumpia.com) + TRUMPIA_TOKEN."),
    dict(name="syniverse", description="Syniverse CPaaS/messaging REST API. ops get/post/put/delete (writes need confirm). Auth: SYNIVERSE_BASE_URL + SYNIVERSE_TOKEN."),
    dict(name="hologram", description="Hologram IoT SIM connectivity REST API. ops get/post/put/delete (writes need confirm). Auth: HOLOGRAM_BASE_URL (https://dashboard.hologram.io/api) + HOLOGRAM_TOKEN."),
    dict(name="onomondo", description="Onomondo IoT SIM connectivity REST API. ops get/post/put/delete (writes need confirm). Auth: ONOMONDO_BASE_URL (https://api.onomondo.com) + ONOMONDO_TOKEN."),
    dict(name="emnify", description="EMnify IoT SIM connectivity REST API. ops get/post/put/delete (writes need confirm). Auth: EMNIFY_BASE_URL (https://cdp.emnify.net) + EMNIFY_TOKEN."),
    dict(name="kore_wireless", description="Kore Wireless IoT connectivity REST API. ops get/post/put/delete (writes need confirm). Auth: KORE_WIRELESS_BASE_URL + KORE_WIRELESS_TOKEN."),
    dict(name="courier_api", description="Courier.com notification infrastructure REST API. ops get/post/put/delete (writes need confirm). Auth: COURIER_API_BASE_URL (https://api.courier.com) + COURIER_API_TOKEN."),
    dict(name="knock_app", description="Knock.app notification infrastructure REST API. ops get/post/put/delete (writes need confirm). Auth: KNOCK_APP_BASE_URL (https://api.knock.app) + KNOCK_APP_TOKEN."),
    dict(name="novu", description="Novu open-source notification infrastructure REST API. ops get/post/put/delete (writes need confirm). Auth: NOVU_BASE_URL (https://api.novu.co) + NOVU_TOKEN."),
    dict(name="magicbell", description="MagicBell notification inbox REST API. ops get/post/put/delete (writes need confirm). Auth: MAGICBELL_BASE_URL (https://api.magicbell.com) + MAGICBELL_TOKEN."),
    dict(name="tanla_platforms", description="Tanla Platforms CPaaS messaging REST API. ops get/post/put/delete (writes need confirm). Auth: TANLA_PLATFORMS_BASE_URL + TANLA_PLATFORMS_TOKEN."),
    dict(name="valuefirst", description="ValueFirst messaging REST API. ops get/post/put/delete (writes need confirm). Auth: VALUEFIRST_BASE_URL + VALUEFIRST_TOKEN."),
    dict(name="netcore_cloud", description="Netcore Cloud customer engagement/push REST API. ops get/post/put/delete (writes need confirm). Auth: NETCORE_CLOUD_BASE_URL + NETCORE_CLOUD_TOKEN."),
    dict(name="seven_io", description="seven.io SMS/voice gateway REST API. ops get/post/put/delete (writes need confirm). Auth: SEVEN_IO_BASE_URL (https://gateway.seven.io) + SEVEN_IO_TOKEN."),
    dict(name="dexatel", description="Dexatel CPaaS messaging REST API. ops get/post/put/delete (writes need confirm). Auth: DEXATEL_BASE_URL + DEXATEL_TOKEN."),
    dict(name="termii", description="Termii African messaging/OTP REST API. ops get/post/put/delete (writes need confirm). Auth: TERMII_BASE_URL (https://api.ng.termii.com) + TERMII_TOKEN."),
    dict(name="smsapi_com", description="SMSAPI.com SMS gateway REST API. ops get/post/put/delete (writes need confirm). Auth: SMSAPI_COM_BASE_URL (https://api.smsapi.com) + SMSAPI_COM_TOKEN."),
    dict(name="bulksms", basic=True, description="BulkSMS messaging REST API. ops get/post/put/delete (writes need confirm). Auth: BULKSMS_BASE_URL (https://api.bulksms.com) + BULKSMS_TOKEN."),
    dict(name="respond_io", description="respond.io omnichannel messaging/chatbot REST API. ops get/post/put/delete (writes need confirm). Auth: RESPOND_IO_BASE_URL (https://api.respond.io) + RESPOND_IO_TOKEN."),
    dict(name="wati", description="Wati WhatsApp Business API platform REST API. ops get/post/put/delete (writes need confirm). Auth: WATI_BASE_URL (https://live-server.wati.io) + WATI_TOKEN."),
    dict(name="interakt", description="Interakt WhatsApp Business API platform REST API. ops get/post/put/delete (writes need confirm). Auth: INTERAKT_BASE_URL (https://api.interakt.ai) + INTERAKT_TOKEN."),
    dict(name="zoko", description="Zoko WhatsApp commerce messaging REST API. ops get/post/put/delete (writes need confirm). Auth: ZOKO_BASE_URL + ZOKO_TOKEN."),
    dict(name="engati", description="Engati chatbot/conversational AI REST API. ops get/post/put/delete (writes need confirm). Auth: ENGATI_BASE_URL (https://api.engati.com) + ENGATI_TOKEN."),
    dict(name="vzaar", description="Vzaar video hosting REST. ops get/post/put/delete (writes need confirm). Auth: VZAAR_BASE_URL (https://api.vzaar.com) + VZAAR_TOKEN."),
    dict(name="dacast", description="Dacast video streaming platform REST. ops get/post/put/delete (writes need confirm). Auth: DACAST_BASE_URL (https://api.dacast.com) + DACAST_TOKEN."),
    dict(name="castr", description="Castr live streaming REST. ops get/post/put/delete (writes need confirm). Auth: CASTR_BASE_URL (https://api.castr.com) + CASTR_TOKEN."),
    dict(name="cloudflare_stream", description="Cloudflare Stream video REST. ops get/post/put/delete (writes need confirm). Auth: CLOUDFLARE_STREAM_BASE_URL (https://api.cloudflare.com/client/v4) + CLOUDFLARE_STREAM_TOKEN."),
    dict(name="bunny_stream", description="Bunny.net Stream video hosting REST. ops get/post/put/delete (writes need confirm). Auth: BUNNY_STREAM_BASE_URL (https://video.bunnycdn.com) + BUNNY_STREAM_TOKEN."),
    dict(name="amazon_ivs", description="Amazon Interactive Video Service (IVS) REST/AWS API. ops get/post/put/delete (writes need confirm). Auth: AMAZON_IVS_BASE_URL + AMAZON_IVS_TOKEN."),
    dict(name="limelight_networks", description="Limelight Networks (Edgio) video delivery REST. ops get/post/put/delete (writes need confirm). Auth: LIMELIGHT_NETWORKS_BASE_URL + LIMELIGHT_NETWORKS_TOKEN."),
    dict(name="gumlet", description="Gumlet video hosting/transcoding REST. ops get/post/put/delete (writes need confirm). Auth: GUMLET_BASE_URL (https://api.gumlet.com) + GUMLET_TOKEN."),
    dict(name="streamyard", description="StreamYard live streaming REST. ops get/post/put/delete (writes need confirm). Auth: STREAMYARD_BASE_URL + STREAMYARD_TOKEN."),
    dict(name="restream", description="Restream multistreaming REST. ops get/post/put/delete (writes need confirm). Auth: RESTREAM_BASE_URL (https://api.restream.io) + RESTREAM_TOKEN."),
    dict(name="webinarjam", description="WebinarJam REST. ops get/post/put/delete (writes need confirm). Auth: WEBINARJAM_BASE_URL + WEBINARJAM_TOKEN."),
    dict(name="everwebinar", description="EverWebinar automated webinar REST. ops get/post/put/delete (writes need confirm). Auth: EVERWEBINAR_BASE_URL + EVERWEBINAR_TOKEN."),
    dict(name="crowdcast", description="Crowdcast live event platform REST. ops get/post/put/delete (writes need confirm). Auth: CROWDCAST_BASE_URL + CROWDCAST_TOKEN."),
    dict(name="hopin", description="Hopin virtual events REST. ops get/post/put/delete (writes need confirm). Auth: HOPIN_BASE_URL + HOPIN_TOKEN."),
    dict(name="vidyo", description="Vidyo video conferencing REST. ops get/post/put/delete (writes need confirm). Auth: VIDYO_BASE_URL + VIDYO_TOKEN."),
    dict(name="switcherstudio", description="Switcher Studio live production REST. ops get/post/put/delete (writes need confirm). Auth: SWITCHERSTUDIO_BASE_URL + SWITCHERSTUDIO_TOKEN."),
    dict(name="haivision", description="Haivision video streaming platform REST. ops get/post/put/delete (writes need confirm). Auth: HAIVISION_BASE_URL + HAIVISION_TOKEN."),
    dict(name="vbrick", description="Vbrick enterprise video REST. ops get/post/put/delete (writes need confirm). Auth: VBRICK_BASE_URL + VBRICK_TOKEN."),
    dict(name="daily_co", description="Daily.co video call REST. ops get/post/put/delete (writes need confirm). Auth: DAILY_CO_BASE_URL (https://api.daily.co) + DAILY_CO_TOKEN."),
    dict(name="agora_io", description="Agora.io real-time video/voice REST. ops get/post/put/delete (writes need confirm). Auth: AGORA_IO_BASE_URL (https://api.agora.io) + AGORA_IO_TOKEN."),
    dict(name="heygen", description="HeyGen AI video generation REST. ops get/post/put/delete (writes need confirm). Auth: HEYGEN_BASE_URL (https://api.heygen.com) + HEYGEN_TOKEN."),
    dict(name="synthesia", description="Synthesia AI video generation REST. ops get/post/put/delete (writes need confirm). Auth: SYNTHESIA_BASE_URL (https://api.synthesia.io) + SYNTHESIA_TOKEN."),
    dict(name="veed", description="VEED.IO video editing REST. ops get/post/put/delete (writes need confirm). Auth: VEED_BASE_URL (https://api.veed.io) + VEED_TOKEN."),
    dict(name="kapwing", description="Kapwing video editing REST. ops get/post/put/delete (writes need confirm). Auth: KAPWING_BASE_URL + KAPWING_TOKEN."),
    dict(name="invideo", description="InVideo video creation REST. ops get/post/put/delete (writes need confirm). Auth: INVIDEO_BASE_URL + INVIDEO_TOKEN."),
    dict(name="pictory", description="Pictory AI video creation REST. ops get/post/put/delete (writes need confirm). Auth: PICTORY_BASE_URL (https://api.pictory.ai) + PICTORY_TOKEN."),
    dict(name="opus_clip", description="Opus Clip AI video clipping REST. ops get/post/put/delete (writes need confirm). Auth: OPUS_CLIP_BASE_URL + OPUS_CLIP_TOKEN."),
    dict(name="tubebuddy", description="TubeBuddy YouTube channel tools REST. ops get/post/put/delete (writes need confirm). Auth: TUBEBUDDY_BASE_URL + TUBEBUDDY_TOKEN."),
    dict(name="vidiq", description="vidIQ YouTube analytics REST. ops get/post/put/delete (writes need confirm). Auth: VIDIQ_BASE_URL + VIDIQ_TOKEN."),
    dict(name="odysee", description="Odysee (LBRY) video platform REST. ops get/post/put/delete (writes need confirm). Auth: ODYSEE_BASE_URL + ODYSEE_TOKEN."),
    dict(name="kick", description="Kick.com live streaming REST. ops get/post/put/delete (writes need confirm). Auth: KICK_BASE_URL (https://api.kick.com) + KICK_TOKEN."),
    dict(name="trovo", description="Trovo live streaming REST. ops get/post/put/delete (writes need confirm). Auth: TROVO_BASE_URL (https://open-api.trovo.live) + TROVO_TOKEN."),
    dict(name="muvi", description="Muvi OTT/streaming platform builder REST. ops get/post/put/delete (writes need confirm). Auth: MUVI_BASE_URL + MUVI_TOKEN."),
    dict(name="uscreen", description="Uscreen video monetization platform REST. ops get/post/put/delete (writes need confirm). Auth: USCREEN_BASE_URL (https://api.uscreen.io) + USCREEN_TOKEN."),
    dict(name="kajabi", description="Kajabi creator/course platform REST. ops get/post/put/delete (writes need confirm). Auth: KAJABI_BASE_URL (https://api.kajabi.com) + KAJABI_TOKEN."),
    dict(name="streamlabs", description="Streamlabs streaming tools REST. ops get/post/put/delete (writes need confirm). Auth: STREAMLABS_BASE_URL (https://streamlabs.com/api) + STREAMLABS_TOKEN."),
    dict(name="remo_co", description="Remo virtual events REST. ops get/post/put/delete (writes need confirm). Auth: REMO_CO_BASE_URL + REMO_CO_TOKEN."),
    dict(name="gathertown", description="Gather (gather.town) virtual space REST. ops get/post/put/delete (writes need confirm). Auth: GATHERTOWN_BASE_URL + GATHERTOWN_TOKEN."),
    dict(name="bombbomb", description="BombBomb video email REST. ops get/post/put/delete (writes need confirm). Auth: BOMBBOMB_BASE_URL (https://api.bombbomb.com) + BOMBBOMB_TOKEN."),
    dict(name="covideo", description="Covideo video messaging REST. ops get/post/put/delete (writes need confirm). Auth: COVIDEO_BASE_URL + COVIDEO_TOKEN."),
    dict(name="buzzsprout", description="Buzzsprout podcast hosting REST. ops get/post/put/delete (writes need confirm). e.g. /episodes.json. Auth: BUZZSPROUT_BASE_URL (https://www.buzzsprout.com/api) + BUZZSPROUT_TOKEN."),
    dict(name="libsyn", description="Libsyn podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: LIBSYN_BASE_URL + LIBSYN_TOKEN."),
    dict(name="transistor_fm", description="Transistor.fm podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: TRANSISTOR_FM_BASE_URL (https://api.transistor.fm) + TRANSISTOR_FM_TOKEN."),
    dict(name="podbean", description="Podbean podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: PODBEAN_BASE_URL (https://api.podbean.com) + PODBEAN_TOKEN."),
    dict(name="captivate_fm", description="Captivate.fm podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: CAPTIVATE_FM_BASE_URL (https://api.captivate.fm) + CAPTIVATE_FM_TOKEN."),
    dict(name="simplecast", description="Simplecast podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: SIMPLECAST_BASE_URL (https://api.simplecast.com) + SIMPLECAST_TOKEN."),
    dict(name="anchor_fm", description="Anchor (Spotify for Podcasters) REST. ops get/post/put/delete (writes need confirm). Auth: ANCHOR_FM_BASE_URL + ANCHOR_FM_TOKEN."),
    dict(name="megaphone", description="Megaphone podcast hosting/ads REST. ops get/post/put/delete (writes need confirm). Auth: MEGAPHONE_BASE_URL (https://cms.megaphone.fm/api) + MEGAPHONE_TOKEN."),
    dict(name="acast", description="Acast podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: ACAST_BASE_URL + ACAST_TOKEN."),
    dict(name="spreaker", description="Spreaker podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: SPREAKER_BASE_URL (https://api.spreaker.com) + SPREAKER_TOKEN."),
    dict(name="podigee", description="Podigee podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: PODIGEE_BASE_URL + PODIGEE_TOKEN."),
    dict(name="redcircle", description="RedCircle podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: REDCIRCLE_BASE_URL + REDCIRCLE_TOKEN."),
    dict(name="fireside_fm", description="Fireside.fm podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: FIRESIDE_FM_BASE_URL + FIRESIDE_FM_TOKEN."),
    dict(name="pinecast", description="Pinecast podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: PINECAST_BASE_URL (https://api.pinecast.com) + PINECAST_TOKEN."),
    dict(name="rss_com", description="RSS.com podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: RSS_COM_BASE_URL + RSS_COM_TOKEN."),
    dict(name="blubrry", description="Blubrry podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: BLUBRRY_BASE_URL + BLUBRRY_TOKEN."),
    dict(name="audioboom", description="Audioboom podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: AUDIOBOOM_BASE_URL + AUDIOBOOM_TOKEN."),
    dict(name="omny_studio", description="Omny Studio podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: OMNY_STUDIO_BASE_URL + OMNY_STUDIO_TOKEN."),
    dict(name="podchaser", description="Podchaser podcast database REST. ops get/post/put/delete (writes need confirm). Auth: PODCHASER_BASE_URL (https://api.podchaser.com/graphql) + PODCHASER_TOKEN."),
    dict(name="castos", description="Castos podcast hosting REST. ops get/post/put/delete (writes need confirm). Auth: CASTOS_BASE_URL + CASTOS_TOKEN."),
    dict(name="podia", description="Podia creator platform (courses, podcasts, digital products) REST. ops get/post/put/delete (writes need confirm). Auth: PODIA_BASE_URL (https://api.podia.com) + PODIA_TOKEN."),
    dict(name="adobe_stock", description="Adobe Stock media REST. ops get/post/put/delete (writes need confirm). Auth: ADOBE_STOCK_BASE_URL (https://stock.adobe.io) + ADOBE_STOCK_TOKEN."),
    dict(name="envato_elements", description="Envato Elements creative assets REST. ops get/post/put/delete (writes need confirm). Auth: ENVATO_ELEMENTS_BASE_URL (https://api.envato.com) + ENVATO_ELEMENTS_TOKEN."),
    dict(name="envato_market", description="Envato Market (GraphicRiver/VideoHive/AudioJungle) REST. ops get/post/put/delete (writes need confirm). Auth: ENVATO_MARKET_BASE_URL (https://api.envato.com) + ENVATO_MARKET_TOKEN."),
    dict(name="stocksy", description="Stocksy United stock imagery REST. ops get/post/put/delete (writes need confirm). Auth: STOCKSY_BASE_URL + STOCKSY_TOKEN."),
    dict(name="alamy", description="Alamy stock photo REST. ops get/post/put/delete (writes need confirm). Auth: ALAMY_BASE_URL (https://www.alamy.com/api) + ALAMY_TOKEN."),
    dict(name="px500", description="500px photo community REST. ops get/post/put/delete (writes need confirm). Auth: PX500_BASE_URL (https://api.500px.com) + PX500_TOKEN."),
    dict(name="smugmug", description="SmugMug photo hosting REST. ops get/post/put/delete (writes need confirm). Auth: SMUGMUG_BASE_URL (https://api.smugmug.com) + SMUGMUG_TOKEN."),
    dict(name="pixlr", description="Pixlr photo editing REST. ops get/post/put/delete (writes need confirm). Auth: PIXLR_BASE_URL + PIXLR_TOKEN."),
    dict(name="photobucket", description="Photobucket image hosting REST. ops get/post/put/delete (writes need confirm). Auth: PHOTOBUCKET_BASE_URL + PHOTOBUCKET_TOKEN."),
    dict(name="deviantart", description="DeviantArt REST. ops get/post/put/delete (writes need confirm). Auth: DEVIANTART_BASE_URL (https://www.deviantart.com/api) + DEVIANTART_TOKEN."),
    dict(name="fotor", description="Fotor photo editing REST. ops get/post/put/delete (writes need confirm). Auth: FOTOR_BASE_URL + FOTOR_TOKEN."),
    dict(name="remove_bg", token_header="X-Api-Key", scheme="", description="remove.bg image background removal REST. ops get/post/put/delete (writes need confirm). Auth: REMOVE_BG_BASE_URL (https://api.remove.bg/v1.0) + REMOVE_BG_TOKEN (X-Api-Key header)."),
    dict(name="clipdrop", description="Clipdrop (Stability AI) image API REST. ops get/post/put/delete (writes need confirm). Auth: CLIPDROP_BASE_URL (https://clipdrop-api.co) + CLIPDROP_TOKEN."),
    dict(name="imagga", description="Imagga image recognition/tagging REST. ops get/post/put/delete (writes need confirm). Auth: IMAGGA_BASE_URL (https://api.imagga.com) + IMAGGA_TOKEN."),
    dict(name="filestack", description="Filestack file/image processing REST. ops get/post/put/delete (writes need confirm). Auth: FILESTACK_BASE_URL (https://www.filestackapi.com) + FILESTACK_TOKEN."),
    dict(name="uploadcare", description="Uploadcare file/image CDN REST. ops get/post/put/delete (writes need confirm). Auth: UPLOADCARE_BASE_URL (https://api.uploadcare.com) + UPLOADCARE_TOKEN."),
    dict(name="tinypng", description="TinyPNG image compression REST. ops get/post/put/delete (writes need confirm). Auth: TINYPNG_BASE_URL (https://api.tinify.com) + TINYPNG_TOKEN."),
    dict(name="imagekit_io", description="ImageKit.io image optimization/CDN REST. ops get/post/put/delete (writes need confirm). Auth: IMAGEKIT_IO_BASE_URL (https://api.imagekit.io) + IMAGEKIT_IO_TOKEN."),
    dict(name="pixieset", description="Pixieset photography gallery/CRM REST. ops get/post/put/delete (writes need confirm). Auth: PIXIESET_BASE_URL + PIXIESET_TOKEN."),
    dict(name="zenfolio", description="Zenfolio photography portfolio REST. ops get/post/put/delete (writes need confirm). Auth: ZENFOLIO_BASE_URL + ZENFOLIO_TOKEN."),
    dict(name="shootproof", description="ShootProof photography client galleries REST. ops get/post/put/delete (writes need confirm). Auth: SHOOTPROOF_BASE_URL (https://api.shootproof.com) + SHOOTPROOF_TOKEN."),
    dict(name="freepik", description="Freepik stock graphics REST. ops get/post/put/delete (writes need confirm). Auth: FREEPIK_BASE_URL (https://api.freepik.com) + FREEPIK_TOKEN."),
    dict(name="vecteezy", description="Vecteezy stock vectors REST. ops get/post/put/delete (writes need confirm). Auth: VECTEEZY_BASE_URL + VECTEEZY_TOKEN."),
    dict(name="rawpixel", description="Rawpixel stock media REST. ops get/post/put/delete (writes need confirm). Auth: RAWPIXEL_BASE_URL + RAWPIXEL_TOKEN."),
    dict(name="depositphotos", description="Depositphotos stock media REST. ops get/post/put/delete (writes need confirm). Auth: DEPOSITPHOTOS_BASE_URL (https://api.depositphotos.com) + DEPOSITPHOTOS_TOKEN."),
    dict(name="bigstockphoto", description="Bigstock (Bigstockphoto) stock media REST. ops get/post/put/delete (writes need confirm). Auth: BIGSTOCKPHOTO_BASE_URL + BIGSTOCKPHOTO_TOKEN."),
    dict(name="dreamstime", description="Dreamstime stock media REST. ops get/post/put/delete (writes need confirm). Auth: DREAMSTIME_BASE_URL + DREAMSTIME_TOKEN."),
    dict(name="stock123rf", description="123RF stock media REST. ops get/post/put/delete (writes need confirm). Auth: STOCK123RF_BASE_URL + STOCK123RF_TOKEN."),
    dict(name="pond5", description="Pond5 stock video/audio REST. ops get/post/put/delete (writes need confirm). Auth: POND5_BASE_URL (https://www.pond5.com/api) + POND5_TOKEN."),
    dict(name="storyblocks", description="Storyblocks stock media REST. ops get/post/put/delete (writes need confirm). Auth: STORYBLOCKS_BASE_URL (https://api.storyblocks.com) + STORYBLOCKS_TOKEN."),
    dict(name="artgrid", description="Artgrid stock footage REST. ops get/post/put/delete (writes need confirm). Auth: ARTGRID_BASE_URL + ARTGRID_TOKEN."),
    dict(name="motion_array", description="Motion Array stock templates/media REST. ops get/post/put/delete (writes need confirm). Auth: MOTION_ARRAY_BASE_URL + MOTION_ARRAY_TOKEN."),
    dict(name="epidemic_sound", description="Epidemic Sound music licensing REST. ops get/post/put/delete (writes need confirm). Auth: EPIDEMIC_SOUND_BASE_URL + EPIDEMIC_SOUND_TOKEN."),
    dict(name="artlist", description="Artlist music/footage licensing REST. ops get/post/put/delete (writes need confirm). Auth: ARTLIST_BASE_URL + ARTLIST_TOKEN."),
    dict(name="soundstripe", description="Soundstripe music licensing REST. ops get/post/put/delete (writes need confirm). Auth: SOUNDSTRIPE_BASE_URL + SOUNDSTRIPE_TOKEN."),
    dict(name="musicbed", description="Musicbed music licensing REST. ops get/post/put/delete (writes need confirm). Auth: MUSICBED_BASE_URL + MUSICBED_TOKEN."),
    dict(name="istock", description="iStock (Getty) stock media REST. ops get/post/put/delete (writes need confirm). Auth: ISTOCK_BASE_URL + ISTOCK_TOKEN."),
    dict(name="picmonkey", description="PicMonkey photo/graphic editor REST. ops get/post/put/delete (writes need confirm). Auth: PICMONKEY_BASE_URL + PICMONKEY_TOKEN."),
    dict(name="befunky", description="BeFunky photo editing/design REST. ops get/post/put/delete (writes need confirm). Auth: BEFUNKY_BASE_URL + BEFUNKY_TOKEN."),
    dict(name="canstockphoto", description="Can Stock Photo stock media REST. ops get/post/put/delete (writes need confirm). Auth: CANSTOCKPHOTO_BASE_URL + CANSTOCKPHOTO_TOKEN."),
    dict(name="vistacreate", description="VistaCreate (Crello) design/media REST. ops get/post/put/delete (writes need confirm). Auth: VISTACREATE_BASE_URL + VISTACREATE_TOKEN."),
    dict(name="snappa", description="Snappa graphic design REST. ops get/post/put/delete (writes need confirm). Auth: SNAPPA_BASE_URL + SNAPPA_TOKEN."),
    dict(name="motionelements", description="MotionElements stock video/motion graphics REST. ops get/post/put/delete (writes need confirm). Auth: MOTIONELEMENTS_BASE_URL + MOTIONELEMENTS_TOKEN."),
    dict(name="adobe_lightroom", description="Adobe Lightroom photo editing REST. ops get/post/put/delete (writes need confirm). Auth: ADOBE_LIGHTROOM_BASE_URL (https://lr.adobe.io) + ADOBE_LIGHTROOM_TOKEN."),
    dict(name="wetransfer", description="WeTransfer file/media transfer REST. ops get/post/put/delete (writes need confirm). Auth: WETRANSFER_BASE_URL (https://dev.wetransfer.com) + WETRANSFER_TOKEN."),
    dict(name="substack", description="Substack newsletter publishing REST. ops get/post/put/delete (writes need confirm). Auth: SUBSTACK_BASE_URL + SUBSTACK_TOKEN."),
    dict(name="prezi", description="Prezi presentation platform REST. ops get/post/put/delete (writes need confirm). Auth: PREZI_BASE_URL + PREZI_TOKEN."),
    dict(name="issuu", description="Issuu digital publishing REST. ops get/post/put/delete (writes need confirm). Auth: ISSUU_BASE_URL (https://api.issuu.com) + ISSUU_TOKEN."),
    dict(name="flipsnack", description="Flipsnack digital flipbook publishing REST. ops get/post/put/delete (writes need confirm). Auth: FLIPSNACK_BASE_URL (https://api.flipsnack.com) + FLIPSNACK_TOKEN."),
    dict(name="calameo", description="Calameo digital publishing REST. ops get/post/put/delete (writes need confirm). Auth: CALAMEO_BASE_URL + CALAMEO_TOKEN."),
    dict(name="readymag", description="Readymag web publishing REST. ops get/post/put/delete (writes need confirm). Auth: READYMAG_BASE_URL + READYMAG_TOKEN."),
    dict(name="publitas", description="Publitas digital publishing (catalogs) REST. ops get/post/put/delete (writes need confirm). Auth: PUBLITAS_BASE_URL + PUBLITAS_TOKEN."),
    dict(name="yumpu", description="Yumpu digital publishing REST. ops get/post/put/delete (writes need confirm). Auth: YUMPU_BASE_URL (https://api.yumpu.com) + YUMPU_TOKEN."),
    dict(name="newspack", description="Newspack (WordPress VIP publishing) REST. ops get/post/put/delete (writes need confirm). Auth: NEWSPACK_BASE_URL + NEWSPACK_TOKEN."),
    dict(name="adobe_express", description="Adobe Express creative design REST. ops get/post/put/delete (writes need confirm). Auth: ADOBE_EXPRESS_BASE_URL + ADOBE_EXPRESS_TOKEN."),
    dict(name="marq", description="Marq (Lucidpress) brand templating REST. ops get/post/put/delete (writes need confirm). Auth: MARQ_BASE_URL + MARQ_TOKEN."),
    dict(name="visme", description="Visme visual content design REST. ops get/post/put/delete (writes need confirm). Auth: VISME_BASE_URL (https://api.visme.co) + VISME_TOKEN."),
    dict(name="genially", description="Genially interactive content design REST. ops get/post/put/delete (writes need confirm). Auth: GENIALLY_BASE_URL + GENIALLY_TOKEN."),
    dict(name="venngage", description="Venngage infographic/design REST. ops get/post/put/delete (writes need confirm). Auth: VENNGAGE_BASE_URL + VENNGAGE_TOKEN."),
    dict(name="beautiful_ai", description="Beautiful.ai presentation design REST. ops get/post/put/delete (writes need confirm). Auth: BEAUTIFUL_AI_BASE_URL + BEAUTIFUL_AI_TOKEN."),
    dict(name="patreon", description="Patreon creator membership REST. ops get/post/put/delete (writes need confirm). Auth: PATREON_BASE_URL (https://www.patreon.com/api) + PATREON_TOKEN."),
    dict(name="buymeacoffee", description="Buy Me a Coffee creator support REST. ops get/post/put/delete (writes need confirm). Auth: BUYMEACOFFEE_BASE_URL (https://developers.buymeacoffee.com) + BUYMEACOFFEE_TOKEN."),
    dict(name="rev_com", description="Rev.com transcription/captioning REST. ops get/post/put/delete (writes need confirm). Auth: REV_COM_BASE_URL (https://www.rev.com/api) + REV_COM_TOKEN."),
    dict(name="descript", description="Descript audio/video editing REST. ops get/post/put/delete (writes need confirm). Auth: DESCRIPT_BASE_URL + DESCRIPT_TOKEN."),
    dict(name="trint", description="Trint transcription for media REST. ops get/post/put/delete (writes need confirm). Auth: TRINT_BASE_URL (https://api.trint.com) + TRINT_TOKEN."),
    dict(name="happyscribe", description="Happy Scribe transcription/subtitling REST. ops get/post/put/delete (writes need confirm). Auth: HAPPYSCRIBE_BASE_URL (https://api.happyscribe.com) + HAPPYSCRIBE_TOKEN."),
    dict(name="threeplay_media", description="3Play Media captioning/subtitling REST. ops get/post/put/delete (writes need confirm). Auth: THREEPLAY_MEDIA_BASE_URL (https://api.3playmedia.com) + THREEPLAY_MEDIA_TOKEN."),
]

# GraphQL services (single POST endpoint; mutations confirm-gated).
_GRAPHQL_SPECS: list[dict] = [
    dict(name="monday",
         scheme="", description="monday.com GraphQL. op query (queries run; mutations "
         "need confirm). Auth: MONDAY_BASE_URL (https://api.monday.com/v2) + "
         "MONDAY_TOKEN (raw Authorization)."),
    dict(name="wiz",
         description="Wiz CNAPP GraphQL. op query (queries run; mutations need "
         "confirm). Auth: WIZ_BASE_URL (your Wiz API endpoint) + WIZ_TOKEN (bearer)."),
]
